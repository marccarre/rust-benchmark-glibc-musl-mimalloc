---
plan_id: 260523-885
type: quick
status: complete
date: 2026-05-23
description: Update all GitHub Actions to latest major versions
commits:
  - 51d1f39  # ci(quick-260523-885): bump GitHub Actions to latest major versions
  - 07c1b39  # docs(quick-260523-885): refresh CLAUDE.md GHA pinning examples
---

# Quick Task 260523-885: Summary

## What changed

Bumped 5 third-party GitHub Actions to their latest major versions across 9 `uses:` lines in `.github/workflows/bench.yml`, fixed up 2 inline comments to match the new pins, and refreshed 4 example pins in `CLAUDE.md`'s "GHA action pinning" convention so the convention text stays byte-consistent with what the workflow actually uses.

| Action | Before | After | Occurrences in `bench.yml` |
|---|---|---|---|
| `actions/checkout` | `@v4` | `@v6` | 3 |
| `extractions/setup-just` | `@v2` | `@v4` | 3 |
| `docker/setup-buildx-action` | `@v3` | `@v4` | 1 |
| `docker/build-push-action` | `@v6` | `@v7` | 1 |
| `actions/upload-artifact` | `@v4` | `@v7` | 2 |
| `actions/download-artifact` | `@v4` | `@v8` | 1 |
| `Swatinem/rust-cache` | `@v2` | (unchanged) | 3 |
| `dtolnay/rust-toolchain` | `@1.91.0` | (unchanged — rustc pin) | 2 |

Inline comment fixups in `bench.yml`:
- Line 22 (now part of the larger D-05 reference block): `actions/upload-artifact@v4` → `@v7`, picked up by `replace_all` since the comment text needed to match the new pin too.
- Line 197 prose: `"v4 is required for the matrix unique-name-per-job model"` → `"v4+ supports the matrix unique-name-per-job model"` so the comment doesn't claim a specific minor version is the only supported one.
- Line 27 left intact: `actions/cache@v4` is documenting prior history (the redundant cache layer was removed per Phase-5 WR-01); changing it would erase a deliberate audit trail.

`CLAUDE.md` line 91 (the "GHA action pinning" convention bullet) updated four example pins: `actions/checkout@v6`, `actions/upload-artifact@v7`, `Swatinem/rust-cache@v2` (unchanged), `docker/build-push-action@v7`. The `dtolnay/rust-toolchain@1.91.0` reference and the surrounding convention text are unchanged.

## Files modified

- `.github/workflows/bench.yml` — 15 insertions, 15 deletions across 9 `uses:` lines + 2 comment fixups (commit `51d1f39`).
- `CLAUDE.md` — 1 insertion, 1 deletion on the "GHA action pinning" bullet (commit `07c1b39`).

## Files explicitly NOT modified

Per plan §"Files explicitly NOT modified":

- `rust-toolchain.toml`, `Cargo.toml`, `docker/*.Dockerfile`, `justfile` — rustc-pin tier; out of scope for this task.
- `.planning/RETROSPECTIVE.md`, `.planning/milestones/v1.0-phases/05-ci-image-size-gate-public-polish/05-REVIEW.md`, `README.md` — historical / unrelated documents that don't carry the live action-pin convention.

Inside `bench.yml` itself the following `uses:` lines were intentionally left alone (verified via grep):

- Lines 88, 224 — `dtolnay/rust-toolchain@1.91.0` (rustc patch-pin, not an action release).
- Lines 92, 147, 226 — `Swatinem/rust-cache@v2` (already at latest major).

The historical comment references at lines 27 and 152 (mentioning the removed `actions/cache@v4` layer) were also left intact — they document prior architecture decisions, not live pins.

## Verification performed

### `bench.yml` — every `uses:` line is on the new pin

```
$ grep -nE 'uses: ' .github/workflows/bench.yml
86:      - uses: actions/checkout@v6
88:      - uses: dtolnay/rust-toolchain@1.91.0
92:      - uses: Swatinem/rust-cache@v2
96:      - uses: extractions/setup-just@v4
138:      - uses: actions/checkout@v6
140:      - uses: docker/setup-buildx-action@v4
142:      - uses: extractions/setup-just@v4
147:        uses: Swatinem/rust-cache@v2
164:        uses: docker/build-push-action@v7
200:        uses: actions/upload-artifact@v7
222:      - uses: actions/checkout@v6
224:      - uses: dtolnay/rust-toolchain@1.91.0
226:      - uses: Swatinem/rust-cache@v2
230:      - uses: extractions/setup-just@v4
235:        uses: actions/download-artifact@v8
256:        uses: actions/upload-artifact@v7
```

Counts: 16 `uses:` lines — `actions/checkout@v6` ×3, `dtolnay/rust-toolchain@1.91.0` ×2, `Swatinem/rust-cache@v2` ×3, `extractions/setup-just@v4` ×3, `docker/setup-buildx-action@v4` ×1, `docker/build-push-action@v7` ×1, `actions/upload-artifact@v7` ×2, `actions/download-artifact@v8` ×1. Matches the plan's expected distribution exactly.

### `bench.yml` — only intentional `@v3/@v4/@v6` references remain (none on `uses:` lines)

```
$ grep -n '@v4\|@v3\|@v6' .github/workflows/bench.yml
27:#         actions/cache@v4 layer was removed per Phase-5 WR-01) +
86:      - uses: actions/checkout@v6
96:      - uses: extractions/setup-just@v4
138:      - uses: actions/checkout@v6
140:      - uses: docker/setup-buildx-action@v4
142:      - uses: extractions/setup-just@v4
152:          # cargo-cache layer here. An earlier `actions/cache@v4` block
222:      - uses: actions/checkout@v6
230:      - uses: extractions/setup-just@v4
```

Lines 27 and 152 are the historical `actions/cache@v4` comment references (deliberately preserved). Every other hit is the new `@v6` pin for `actions/checkout` or the new `@v4` pin for `docker/setup-buildx-action` and `extractions/setup-just` — none are leftover old majors on `uses:` lines.

### YAML still parses

```
$ python3 -c 'import yaml,sys; yaml.safe_load(open(".github/workflows/bench.yml"))' && echo OK
OK
```

### `CLAUDE.md` — convention bullet updated

```
$ grep -n 'GHA action pinning' CLAUDE.md
91:- **GHA action pinning:** every action pinned to a specific major version (e.g., `actions/checkout@v6`, `actions/upload-artifact@v7`, `Swatinem/rust-cache@v2`, `docker/build-push-action@v7`); `dtolnay/rust-toolchain@1.91.0` is patch-pinned. No `@latest` or `@main` references.
```

The four example pins (`actions/checkout@v6`, `actions/upload-artifact@v7`, `Swatinem/rust-cache@v2`, `docker/build-push-action@v7`) match the workflow file. The `dtolnay/rust-toolchain@1.91.0` patch-pin reference and the convention prose ("major-version pinning, patch-pinning for `dtolnay/rust-toolchain`", "No `@latest` or `@main` references") are unchanged.

## Self-Check: PASSED

- `.github/workflows/bench.yml` modified, committed at `51d1f39` — confirmed via `git log --oneline -5`.
- `CLAUDE.md` modified, committed at `07c1b39` — confirmed via `git log --oneline -5`.
- No file deletions in either commit (`git diff --diff-filter=D` empty).
- YAML round-trips through `yaml.safe_load`.
- All grep counts match the plan's expected distribution.

## Risks acknowledged

Per plan §"Risks":
- `actions/download-artifact@v8` introduces fatal hash-mismatch on artifact corruption — user-confirmed acceptable (security improvement).
- `docker/build-push-action@v7` removed environment variables we don't set in this workflow.
- `extractions/setup-just@v2 → @v4` is a 2-major skip; the action is a thin composite wrapper and we pass no inputs, so it's a drop-in.

The canonical end-to-end test (a real CI run on push of a feature branch — pre-bench → bench-matrix → aggregate) is the user's responsibility post-merge; the executor's contract was static byte-correct edits + the local checks above.
