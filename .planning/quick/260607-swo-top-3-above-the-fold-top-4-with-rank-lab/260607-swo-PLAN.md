---
phase: 260607-swo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/alloc-bench-aggregator/src/recommend.rs
  - crates/alloc-bench-aggregator/src/html.rs
  - crates/alloc-bench-aggregator/src/main.rs
  - crates/alloc-bench-aggregator/templates/index.html.tmpl
  - crates/alloc-bench-aggregator/tests/smoke.rs
autonomous: true
requirements:
  - QUICK-260607-SWO

must_haves:
  truths:
    - "TOP_N_SPIDER constant equals 4 (single source of truth for spider rendering)"
    - "Rendered HTML emits exactly 4 <div class=\"spider-cell\"> elements with stable ids spider-cell-1..spider-cell-4"
    - "Each spider chart Plotly title.text starts with #N where N is the rank (#1, #2, #3, #4) followed by two spaces and {alloc}/{env}"
    - "Spider grid renders as a 2x2 layout at desktop widths (>=768px) and reflows to a single column at <768px"
    - "Section heading reads 'Top-4 Above the Fold' and caption references 'top-4 cells across 8 normalized axes'"
    - "All in-tree tests pass (`cargo test -p alloc-bench-aggregator`); pinning tests now expect 4 instead of 3"
    - "No occurrences of `top-3` / `Top-3` / `TOP_N_SPIDER (=3)` remain anywhere in `crates/alloc-bench-aggregator/`"
  artifacts:
    - path: "crates/alloc-bench-aggregator/src/recommend.rs"
      provides: "TOP_N_SPIDER = 4 constant + updated pinning test"
      contains: "pub const TOP_N_SPIDER: usize = 4"
    - path: "crates/alloc-bench-aggregator/src/html.rs"
      provides: "build_spider_context emits #N rank-prefixed Plotly title.text"
      contains: "format!(\"#{}  {}/{}\", idx + 1, score.alloc, score.env)"
    - path: "crates/alloc-bench-aggregator/templates/index.html.tmpl"
      provides: "Top-4 section heading + caption + 2x2 CSS grid + mobile reflow"
      contains: "<h2>Top-4 Above the Fold</h2>"
    - path: "crates/alloc-bench-aggregator/tests/smoke.rs"
      provides: "End-to-end test pinning spider-cell count to 4"
      contains: "cell_count, 4"
  key_links:
    - from: "crates/alloc-bench-aggregator/src/recommend.rs"
      to: "crates/alloc-bench-aggregator/src/html.rs"
      via: "TOP_N_SPIDER constant import"
      pattern: "use crate::recommend::TOP_N_SPIDER"
    - from: "crates/alloc-bench-aggregator/src/html.rs"
      to: "crates/alloc-bench-aggregator/templates/index.html.tmpl"
      via: "{{ for cell in spider_cells }} loop populated by build_spider_context"
      pattern: "spider_cells"
    - from: "crates/alloc-bench-aggregator/templates/index.html.tmpl"
      to: "crates/alloc-bench-aggregator/tests/smoke.rs"
      via: "rendered HTML matched by class=\"spider-cell\" + spider-cell-{N} id assertions"
      pattern: "spider-cell-"
---

<objective>
Promote the "Top-3 Above the Fold" spider section to "Top-4" and add explicit `#1`..`#4` rank labels prefixed onto each per-chart Plotly title. Restructure the CSS grid from a 3-column flex row to a 2x2 grid (with mobile single-column reflow) so the four charts render in a clean grid layout.

Purpose: Improve at-a-glance ranking comprehension in the HTML report. Top-4 with explicit rank markers (#1/#2/#3/#4) and a 2x2 grid is more legible than 3 unmarked side-by-side charts.

Output: Five files updated in lock-step (one constant flip, one format-string edit, one main.rs comment, one template heading+CSS rewrite, one smoke-test count update). Rendered `report/index.html` will show 4 ranked spider charts in a 2x2 grid (regenerated by aggregator on next run; not hand-edited).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

@crates/alloc-bench-aggregator/src/recommend.rs
@crates/alloc-bench-aggregator/src/html.rs
@crates/alloc-bench-aggregator/src/main.rs
@crates/alloc-bench-aggregator/templates/index.html.tmpl
@crates/alloc-bench-aggregator/tests/smoke.rs

<interfaces>
<!-- Key contracts the executor needs. Verified against the codebase before plan emission. -->

From crates/alloc-bench-aggregator/src/recommend.rs (lines 38-50):
```rust
/// Top-3 cells overlaid on the Phase 9 spider chart (small-multiples
/// grid above the fold). Phase 9 polar.rs uses
/// `score::top_n(scores, TOP_N_SPIDER)` to skip prose computation on
/// the chart hot path.
pub const TOP_N_SPIDER: usize = 3;

/// Top-5 cells in the above-the-fold REPORT.md table (Phase 8).
pub const TOP_N_TABLE: usize = 5;

/// Total cards / fragments emitted by Phase 8 templates
/// (`recommend-cell.{md,html}.tmpl`). Also pins the body length of
/// `top_n_cells`: `min(TOP_N_TOTAL, scores.len())`.
pub const TOP_N_TOTAL: usize = 10;
```

From crates/alloc-bench-aggregator/src/recommend.rs (lines 995-1003):
```rust
/// REC-02: the three top-N constants are pinned to the locked values
/// `3 / 5 / 10` (Phase 9 polar.rs spider; Phase 8 above-the-fold table;
/// Phase 8 total cards/fragments). Any drift breaks downstream consumers.
#[test]
fn top_n_constants_match_locked_values() {
    assert_eq!(TOP_N_SPIDER, 3);
    assert_eq!(TOP_N_TABLE, 5);
    assert_eq!(TOP_N_TOTAL, 10);
}
```

From crates/alloc-bench-aggregator/src/html.rs (lines 595-640) — current build_spider_context loop:
```rust
let mut cells: Vec<SpiderCellContext> = Vec::with_capacity(head.len());
for (idx, score) in head.iter().enumerate() {
    let trace_pair = serde_json::Value::Array(vec![
        reference_trace.clone(),
        polar::build_trace(score),
    ]);
    let layout = serde_json::json!({
        // ... polar/showlegend/margin omitted ...
        "title": {
            "text": format!("{}/{}", score.alloc, score.env),  // <-- current
            "font": { "size": 14, "color": "#1F2328" }
        }
    });
    cells.push(SpiderCellContext {
        traces_json: to_script_safe_json(&trace_pair)?,
        layout_json: to_script_safe_json(&layout)?,
        index: idx + 1,  // already 1-based
    });
}
```

The `idx + 1` value (already in scope, already used for the `index` field) is what we prefix onto the title.

From crates/alloc-bench-aggregator/templates/index.html.tmpl (lines 249-262) — current CSS to replace:
```css
.spider-grid {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-md);
}
.spider-cell {
  background: var(--color-dominant);
  border: 1px solid var(--color-border);
  border-radius: 6px;
  padding: var(--space-md);
  flex: 1 1 calc((100% - 2 * var(--space-md)) / 3);
  aspect-ratio: 1 / 1;
  min-width: 280px;
}
```

From crates/alloc-bench-aggregator/templates/index.html.tmpl (lines 314-316) — current section heading + caption:
```html
{{ endif }}{{ if has_spider }}  <section class="spider-chart">
    <h2>Top-3 Above the Fold</h2>
    <p>Spider charts of the top-3 cells across 8 normalized axes (0-1). Grey reference polygon = mean across all 18 cells.</p>
```

From crates/alloc-bench-aggregator/tests/smoke.rs (lines 818-849) — current pinning test:
```rust
fn three_spider_cells_present_when_data_exists() {
    let (_dir, html) = run_aggregator_against_fixtures();
    let cell_count = html.matches(r#"class="spider-cell""#).count();
    assert_eq!(
        cell_count, 3,
        "expected exactly 3 `class=\"spider-cell\"` divs in rendered index.html, \
         got {cell_count} — small-multiples grid contract broken (UI-REVIEW BLOCKER)"
    );
    for n in 1..=3usize {
        let needle = format!(r#"id="spider-cell-{n}""#);
        assert!(html.contains(&needle), "missing `{needle}` per-cell id ...");
    }
    // ...
    for n in 1..=3usize {
        let needle = format!("Plotly.react('spider-cell-{n}'");
        assert!(html.contains(&needle), "missing `{needle}` independent Plotly.react call ...");
    }
}
```

From crates/alloc-bench-aggregator/src/html.rs (lines 1542-1573) — in-crate test pinning verbatim section heading + caption strings (line 1562, 1566):
```rust
assert!(
    html.contains("<h2>Top-3 Above the Fold</h2>"),
    "missing verbatim spider section heading in:\n{html}"
);
assert!(
    html.contains("Spider charts of the top-3 cells across 8 normalized axes (0-1). Grey reference polygon = mean across all 18 cells."),
    "missing verbatim spider section caption in:\n{html}"
);
```

These two literal-string assertions MUST be updated in the same edit-batch as the template, otherwise the `cargo test` gate fails after the template change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip TOP_N_SPIDER to 4 and update its doc-comment + pinning test</name>
  <files>crates/alloc-bench-aggregator/src/recommend.rs</files>
  <action>
In `crates/alloc-bench-aggregator/src/recommend.rs`:

(a) Line 42: change `pub const TOP_N_SPIDER: usize = 3;` to `pub const TOP_N_SPIDER: usize = 4;`.

(b) Lines 38-41 doc-comment: replace the `Top-3 cells overlaid on the Phase 9 spider chart` opening with `Top-4 cells overlaid on the Phase 9 spider chart`. Keep the rest of the comment (small-multiples grid, score::top_n reference, etc.) verbatim.

(c) Lines 995-1003 test `top_n_constants_match_locked_values`:
   - Change `assert_eq!(TOP_N_SPIDER, 3);` to `assert_eq!(TOP_N_SPIDER, 4);`.
   - Update the doc-comment line 996 from `pinned to the locked values \`3 / 5 / 10\`` to `pinned to the locked values \`4 / 5 / 10\``.
   - Leave the `TOP_N_TABLE` and `TOP_N_TOTAL` assertions untouched.

This is the single source-of-truth flip. All downstream surfaces (html.rs, main.rs, template, smoke test) honour the new value through this constant or through verbatim copy that mirrors it.
  </action>
  <verify>
    <automated>cd /Users/marc.carre/src/rust-benchmark-glibc-musl-mimalloc && cargo test -p alloc-bench-aggregator --lib top_n_constants_match_locked_values</automated>
  </verify>
  <done>`recommend.rs` constant equals 4. The pinning test passes against the new value. The doc-comment on TOP_N_SPIDER says "Top-4". `grep -n "TOP_N_SPIDER" crates/alloc-bench-aggregator/src/recommend.rs` shows `= 4` and the test expects `4`.</done>
</task>

<task type="auto">
  <name>Task 2: Add #N rank prefix to Plotly title and update html.rs comments + in-crate test literals</name>
  <files>crates/alloc-bench-aggregator/src/html.rs, crates/alloc-bench-aggregator/src/main.rs</files>
  <action>
In `crates/alloc-bench-aggregator/src/html.rs`:

(a) Around line 629 (inside `build_spider_context`, the `layout` JSON's `title.text` field): change
```
"text": format!("{}/{}", score.alloc, score.env),
```
to
```
"text": format!("#{}  {}/{}", idx + 1, score.alloc, score.env),
```
NOTE the two spaces between `#{}` and `{}/{}` — this is the user-locked title format `#1  jemalloc/debian-slim`. The `idx + 1` is already in scope (assigned to the `index` field on line 639); reusing it keeps the rank consistent with the per-cell DOM id `spider-cell-{N}`.

(b) Doc-comment on `build_spider_context` (lines 540-568): replace every reference to `top-3` / `TOP_N_SPIDER (=3)` with the corresponding `top-4` / `TOP_N_SPIDER (=4)` form. Specifically at minimum:
   - Line 543: "Each top-`TOP_N_SPIDER` cell receives" — leave constant-name reference unchanged (still parameterized).
   - Line 561: "the top-3" → "the top-4" in the matrix-mean rationale paragraph.

(c) Line 570-572 inline comment block inside the fn body:
   - "Take the first TOP_N_SPIDER (=3) cells" → "Take the first TOP_N_SPIDER (=4) cells".
   - "the slice [..TOP_N_SPIDER] gives the top-3 in rank order" → "...gives the top-4 in rank order".

(d) Line 591 inline comment: "(not the top-3 head)" → "(not the top-4 head)".

(e) Lines 1465-1468 module-level doc-block comment: "consumes the top-3 by `cell_scores[..TOP_N_SPIDER]`" → "consumes the top-4 by `cell_scores[..TOP_N_SPIDER]`".

(f) Line 1562 (in-crate `spider_section_present_when_top_n_non_empty` test): change literal
```
html.contains("<h2>Top-3 Above the Fold</h2>"),
```
to
```
html.contains("<h2>Top-4 Above the Fold</h2>"),
```

(g) Line 1566 (same test, caption assertion): change
```
html.contains("Spider charts of the top-3 cells across 8 normalized axes (0-1). Grey reference polygon = mean across all 18 cells."),
```
to
```
html.contains("Spider charts of the top-4 cells across 8 normalized axes (0-1). Grey reference polygon = mean across all 18 cells."),
```

(h) Run a final grep inside `crates/alloc-bench-aggregator/src/html.rs` for any remaining `top-3` / `Top-3` substrings; update each one consistently. This is paranoia against missed comments — there should be none after edits (a)-(g).

In `crates/alloc-bench-aggregator/src/main.rs`:

(i) Line 155 comment: "builder takes the first TOP_N_SPIDER (=3) cells in rank order" → "builder takes the first TOP_N_SPIDER (=4) cells in rank order".

This task does NOT modify the template (Task 3) or the tests/smoke.rs file (Task 4). The two in-crate-test literal updates (f, g) are bundled here because they live in `html.rs` and break atomically with the format-string edit in (a) — keeping them in one task lets the executor compile-check the whole file in one pass.
  </action>
  <verify>
    <automated>cd /Users/marc.carre/src/rust-benchmark-glibc-musl-mimalloc && cargo check -p alloc-bench-aggregator && grep -rn "top-3\|Top-3\|TOP_N_SPIDER (=3)" crates/alloc-bench-aggregator/src/ | (grep -v "^Binary file" || true) | wc -l | grep -E "^\s*0\s*$"</automated>
  </verify>
  <done>html.rs `build_spider_context` produces `title.text = "#1  alloc/env"` style strings. main.rs comment updated. All `top-3` / `Top-3` / `(=3)` references in `crates/alloc-bench-aggregator/src/` are gone (the grep returns zero non-binary matches). `cargo check -p alloc-bench-aggregator` compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Convert template to top-4 with 2x2 CSS grid and mobile reflow</name>
  <files>crates/alloc-bench-aggregator/templates/index.html.tmpl</files>
  <action>
In `crates/alloc-bench-aggregator/templates/index.html.tmpl`:

(a) Section heading (line 315): change
```
<h2>Top-3 Above the Fold</h2>
```
to
```
<h2>Top-4 Above the Fold</h2>
```

(b) Section caption (line 316): change
```
<p>Spider charts of the top-3 cells across 8 normalized axes (0-1). Grey reference polygon = mean across all 18 cells.</p>
```
to
```
<p>Spider charts of the top-4 cells across 8 normalized axes (0-1). Grey reference polygon = mean across all 18 cells.</p>
```
The trailing "Grey reference polygon ..." sentence is unchanged.

(c) `.spider-grid` rule (lines 249-253): replace the flex layout
```css
.spider-grid \{
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-md);
}
```
with a 2-column grid:
```css
.spider-grid \{
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: var(--space-md);
}
```
Preserve the `\{` escape (tinytemplate convention used elsewhere in this file) — every CSS opening brace inside this `<style>` block is escaped as `\{`. Match the surrounding style.

(d) `.spider-cell` rule (lines 254-262): remove the `flex: 1 1 calc((100% - 2 * var(--space-md)) / 3);` line. Keep `aspect-ratio: 1 / 1;` and `min-width: 280px;` exactly as-is. Final shape:
```css
.spider-cell \{
  background: var(--color-dominant);
  border: 1px solid var(--color-border);
  border-radius: 6px;
  padding: var(--space-md);
  aspect-ratio: 1 / 1;
  min-width: 280px;
}
```

(e) Add a mobile-reflow `@media` block IMMEDIATELY AFTER the `.spider-cell` rule (so layout rules stay co-located):
```css
@media (max-width: 768px) \{
  .spider-grid \{
    grid-template-columns: 1fr;
  }
}
```
Indentation: match the surrounding style block (no leading spaces on the `@media` line; 2-space indent on the nested rule, mirroring the existing CSS conventions in this template).

(f) CSS block-comment header (lines 224-231): rewrite from the existing "Top-3 ... three .spider-cell divs sit side-by-side" prose to:
```css
/* Phase 9 / Plan 09-04 (UI-REVIEW BLOCKER fix), promoted to top-4 by
   quick-260607-swo: Top-4 Above the Fold spider chart small-multiples
   grid. Mirrors `.report-mirror` framing on the outer section (same
   border/radius/spacing) for visual rhythm. `.spider-grid` is a CSS
   grid with two columns at desktop widths so the four `.spider-cell`
   divs sit in a 2x2 layout; each cell is square via `aspect-ratio:
   1 / 1` and reuses the `.chart-card`-style border framing. Mobile
   `@media` override below collapses to a single column at viewports
   under 768px. */
```
Keep the comment immediately above the `.spider-chart` rule (line 232 in current file); do not relocate it.

Do NOT touch any other rule, the `<section>` body markup (the `{{ for cell in spider_cells }}` loop, lines 317-334), or the inline `<script>` comment about three Plotly.react calls — that comment will be addressed in a follow-up if it becomes load-bearing, but the existing wording is non-blocking (the loop emits N divs / N react calls regardless of N's value, so "three" is now slightly stale-looking but not broken). Per scope-reduction prohibition this is NOT a quality reduction; it is a deliberate scope boundary because the user requested the layout/CSS/heading/title changes — the secondary comment refresh is cosmetic noise that the next quick task can sweep up if anyone notices. If the executor wants to update the script-comment opportunistically while in this file (changing "three independent Plotly.react calls" to "four independent Plotly.react calls"), that is acceptable but not required for completion.
  </action>
  <verify>
    <automated>cd /Users/marc.carre/src/rust-benchmark-glibc-musl-mimalloc && grep -c "Top-4 Above the Fold" crates/alloc-bench-aggregator/templates/index.html.tmpl | grep -E "^[1-9]" && grep -c "grid-template-columns: repeat(2, 1fr)" crates/alloc-bench-aggregator/templates/index.html.tmpl | grep -E "^[1-9]" && grep -c "@media (max-width: 768px)" crates/alloc-bench-aggregator/templates/index.html.tmpl | grep -E "^[1-9]" && (grep -v '^[[:space:]]*\*\|^[[:space:]]*//' crates/alloc-bench-aggregator/templates/index.html.tmpl | grep -c "Top-3\|top-3" || true) | grep -E "^\s*0\s*$"</automated>
  </verify>
  <done>Template heading reads "Top-4 Above the Fold". Caption references "top-4 cells". `.spider-grid` is a CSS grid with `repeat(2, 1fr)` at desktop and `1fr` under `@media (max-width: 768px)`. `.spider-cell` no longer has the `flex` property. Block-comment header describes the 2x2 grid + mobile reflow. No `Top-3` / `top-3` remains outside comments.</done>
</task>

<task type="auto">
  <name>Task 4: Pin smoke test to 4 spider cells and verify end-to-end build</name>
  <files>crates/alloc-bench-aggregator/tests/smoke.rs</files>
  <action>
In `crates/alloc-bench-aggregator/tests/smoke.rs`:

(a) Lines 815-817 doc-comment: change "the `{{ if has_spider }}` gate fires and the loop emits 3 cell divs" to "...emits 4 cell divs".

(b) Line 819 fn name: rename `three_spider_cells_present_when_data_exists` to `four_spider_cells_present_when_data_exists`. (Function names are not load-bearing for any other test; rename is safe.)

(c) Line 822-826 first assertion: change
```rust
assert_eq!(
    cell_count, 3,
    "expected exactly 3 `class=\"spider-cell\"` divs in rendered index.html, \
     got {cell_count} — small-multiples grid contract broken (UI-REVIEW BLOCKER)"
);
```
to
```rust
assert_eq!(
    cell_count, 4,
    "expected exactly 4 `class=\"spider-cell\"` divs in rendered index.html, \
     got {cell_count} — small-multiples grid contract broken (UI-REVIEW BLOCKER)"
);
```

(d) Line 827 inline comment: change "Per-cell stable ids `spider-cell-1`, `-2`, `-3`." to "Per-cell stable ids `spider-cell-1`, `-2`, `-3`, `-4`."

(e) Line 828 loop bound: change `for n in 1..=3usize {` to `for n in 1..=4usize {`.

(f) Line 842 loop bound (the second loop, asserting Plotly.react calls): change `for n in 1..=3usize {` to `for n in 1..=4usize {`.

(g) Search the rest of `tests/smoke.rs` for any other `1..=3` / `cell_count, 3` / `top-3` / `Top-3` references in spider-context tests; update consistently. Use:
```
grep -n "1..=3\|cell_count, 3\|top-3\|Top-3" crates/alloc-bench-aggregator/tests/smoke.rs
```
Expected: only the lines covered by edits (c)-(f) above. If anything else surfaces, evaluate whether it is spider-related and update accordingly; if it is unrelated (e.g., a Top-3 in some other test domain), leave it.

After all edits, run the full aggregator test suite to confirm no regressions:
```
cd /Users/marc.carre/src/rust-benchmark-glibc-musl-mimalloc
cargo test -p alloc-bench-aggregator
```
This exercises:
- `top_n_constants_match_locked_values` (Task 1 pinning test, expects 4)
- `spider_section_present_when_top_n_non_empty` (in-crate test with the literal "Top-4 Above the Fold" + "top-4 cells" caption from Task 2 edits f, g)
- `four_spider_cells_present_when_data_exists` (this task's smoke test)
- All other unrelated aggregator tests (must continue to pass — defense against accidental breakage)

The committed fixtures used by `run_aggregator_against_fixtures` (per the comment at lines 815-816) have only 3 (alloc, env) cells. After flipping TOP_N_SPIDER to 4, `cell_scores.len() <= TOP_N_SPIDER` is true, so `build_spider_context` falls through to the `cell_scores` branch (full slice) and emits 3 cells, NOT 4. This will FAIL the new assertion.

**Mitigation:** the executor MUST extend the smoke-test fixtures to include a 4th synthetic (alloc, env) cell. Inspect the fixture-loading helper `run_aggregator_against_fixtures` in `tests/smoke.rs` and the fixture files it loads (typically under `tests/fixtures/` or `tests/data/`). Add one more JSON fixture file mimicking the existing pattern (a Run with a unique `(alloc, env)` pair, healthy stats so it scores). Use the existing fixtures as templates — same scenario coverage, same numeric shape, just a different `(alloc, env)` label. If extending fixtures expands beyond a trivial 50-line addition (e.g., requires fabricating 11 scenarios × 3 seeds of synthetic data), STOP and surface this to the user — the original task summary did not anticipate fixture authoring as scope, and we should ask whether to (i) author a minimal 4th-cell fixture, (ii) skip this test by gating it on `cell_scores.len() >= 4`, or (iii) defer this one assertion update to a follow-up. The executor's judgment call here.

In all other respects, the smoke test scaffold itself stays unchanged.
  </action>
  <verify>
    <automated>cd /Users/marc.carre/src/rust-benchmark-glibc-musl-mimalloc && cargo test -p alloc-bench-aggregator</automated>
  </verify>
  <done>`cargo test -p alloc-bench-aggregator` is green. The smoke test pins the spider count to 4 and the per-cell-id loop iterates 1..=4. `grep -rn "top-3\|Top-3" crates/alloc-bench-aggregator/` returns zero matches. If fixture authoring was required, a 4th (alloc, env) fixture file is committed and `run_aggregator_against_fixtures` picks it up.</done>
</task>

</tasks>

<verification>
After all four tasks:

1. **Compile + lib tests:** `cargo check -p alloc-bench-aggregator && cargo test -p alloc-bench-aggregator` is green.

2. **Constant flip verified:** `grep -n "TOP_N_SPIDER" crates/alloc-bench-aggregator/src/recommend.rs` shows `usize = 4` and the test expects `4`.

3. **No top-3 ghost references:** `grep -rn "top-3\|Top-3\|TOP_N_SPIDER (=3)" crates/alloc-bench-aggregator/` returns no source-code or template matches (block-comment prose included).

4. **Title format verified:** Run `grep -A1 "title\":" crates/alloc-bench-aggregator/src/html.rs | grep -E '#\{\}'` and confirm the `format!` arm includes the `#{}  {}/{}` rank-prefix shape.

5. **Template grid verified:** Open `crates/alloc-bench-aggregator/templates/index.html.tmpl` and confirm `.spider-grid` uses `display: grid; grid-template-columns: repeat(2, 1fr)` plus a `@media (max-width: 768px)` override collapsing to `1fr`.

6. **End-to-end render (only if results/*.json exists locally):**
   ```
   cargo run -p alloc-bench-aggregator -- --input "results/*.json" --output report/
   ```
   then visually inspect `report/index.html`:
   - Section heading reads "Top-4 Above the Fold".
   - 4 spider charts visible in a 2x2 grid at desktop widths.
   - Each chart's title begins with `#1`, `#2`, `#3`, or `#4` followed by `{alloc}/{env}`.
   - Resize browser <768px: charts stack into a single column.
   - If `results/*.json` is absent, skip this step — Task 4's `cargo test` already exercises the full render path against fixtures.
</verification>

<success_criteria>
- TOP_N_SPIDER constant equals 4; pinning test enforces it.
- `build_spider_context` produces Plotly title strings of the form `"#1  jemalloc/debian-slim"` (with two spaces between rank and identifier).
- Template renders `<h2>Top-4 Above the Fold</h2>` and a 2x2 CSS grid that reflows to one column under 768px.
- Smoke test pins spider-cell count to 4 (with fixtures extended if necessary, or surfaced to user if fixture authoring is non-trivial).
- `cargo test -p alloc-bench-aggregator` is green.
- Zero `top-3` / `Top-3` / `TOP_N_SPIDER (=3)` references remain anywhere in `crates/alloc-bench-aggregator/`.
- Conventional-commit messages scoped to `quick-260607-swo` (executor's discretion: one combined commit or per-task commits per the suggestions in the task summary).
- Byte-identical-output discipline preserved: REPORT.md and per-scenario tables unchanged. The HTML output IS expected to change — this is the deliverable.
- No schema changes to `crates/alloc-bench-core/src/output.rs`. Decorate-not-rewrite respected.
</success_criteria>

<output>
Create `.planning/quick/260607-swo-top-3-above-the-fold-top-4-with-rank-lab/260607-swo-SUMMARY.md` when done.
</output>
