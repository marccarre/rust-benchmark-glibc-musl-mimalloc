---
phase: 01-foundation-mvp-slice
verified: 2026-05-18T08:42:24Z
status: gaps_found
score: 11/15 must-haves verified
overrides_applied: 0
gaps:
  - truth: "Building with --features alloc-jemalloc compiles cleanly (Success Criterion 1; HARN-05)"
    status: failed
    reason: "`cargo build --release --no-default-features --features alloc-jemalloc -p alloc-bench-cli` fails with E0432: `unresolved import tikv_jemalloc_ctl::stats`. The `stats` module is gated behind the `stats` Cargo feature on `tikv-jemalloc-ctl`, which is not enabled in the workspace dependency declaration. This blocks both Phase 1 SC-1 (`cargo build --features alloc-jemalloc -p alloc-bench-cli` produces a working binary) and the entire Phase 3 Docker matrix (every jemalloc image will fail to build). The defect is independent of host-vs-Linux — the missing feature flag manifests on every target."
    artifacts:
      - path: "Cargo.toml"
        issue: "Workspace dependency `tikv-jemalloc-ctl = \"0.6\"` does not enable the `stats` feature, but `crates/alloc-bench-cli/src/allocator.rs:55` does `use tikv_jemalloc_ctl::{epoch, stats};`."
      - path: "crates/alloc-bench-cli/src/allocator.rs"
        issue: "Line 55 uses `tikv_jemalloc_ctl::stats` which requires the `stats` feature to be enabled on the dep."
    missing:
      - "Add `features = [\"stats\"]` (and likely `[\"epoch\"]` if it is also feature-gated) to the workspace `tikv-jemalloc-ctl` declaration in `Cargo.toml`, OR move the feature requirement into the `alloc-bench-cli` Cargo.toml under the `alloc-jemalloc` feature: `tikv-jemalloc-ctl = { workspace = true, optional = true, features = [\"stats\"] }`."
      - "Re-run `cargo build --release --no-default-features --features alloc-jemalloc -p alloc-bench-cli` and confirm a clean build with allocator-internal stats (allocated/resident/retained/active) populated in the resulting `results.json`."

  - truth: "metrics.allocator_stats reports mimalloc-internal stats via the extended-feature stats API (HARN-06)"
    status: failed
    reason: "REQ HARN-06 explicitly requires `mimalloc-internal stats via the extended-feature stats API`. The actual implementation in `allocator.rs:65-71` returns only `{\"kind\": \"mimalloc\"}` and the source comment defers extended stats to `Phase 2/3`. The end-to-end run with `--features alloc-mimalloc` confirms `allocator_stats: { \"kind\": \"mimalloc\" }` only — no `allocated`, `resident`, `peak`, or any other counters. HARN-06 is mapped exclusively to Phase 1 in REQUIREMENTS.md (no later phase claims it), so it is NOT a deferred item."
    artifacts:
      - path: "crates/alloc-bench-cli/src/allocator.rs"
        issue: "Lines 65-71 hardcode `serde_json::json!({\"kind\": \"mimalloc\"})` with a comment `mimalloc 0.1.x doesn't expose mi_stats_get bindings directly. Phase 1 emits the kind discriminator only; Phase 2/3 may extend with mi_stats_print capture if needed.` This is a stub that satisfies the JSON shape but does not satisfy the requirement contract."
    missing:
      - "Either (a) implement the extended-feature stats API call (e.g., enable `mimalloc = { workspace = true, default-features = false, features = [\"extended\"], optional = true }`, then read peak / allocated counters via the `mi_stats_*` C ABI), or (b) reassign HARN-06 to a later phase in REQUIREMENTS.md and ROADMAP.md if Phase 1 explicitly does not include it. Option (a) is what the requirement contract demands; option (b) is a roadmap edit, not a code change."

  - truth: "Host build configures `RUSTFLAGS=\"-C target-cpu=native\"` per WS-05"
    status: failed
    reason: "WS-05 requires `host builds with target-cpu=native`. There is no `.cargo/config.toml` (or workspace-level rustflags configuration) that sets `target-cpu=native` for the host build. The build.rs captures whatever `CARGO_ENCODED_RUSTFLAGS` is in the environment, but the resulting `results.json` shows `\"rustflags\": \"\"` because nothing sets it. WS-05 is mapped exclusively to Phase 1 — no later phase covers the host portion (Phase 3 covers only the Docker portion via `target-cpu=x86-64-v3`)."
    artifacts:
      - path: ".cargo/config.toml"
        issue: "File does not exist. WS-05 requires the host build to use `target-cpu=native`; without a build-config file (or equivalent shell wrapper) the `cargo build` invocation produces a non-target-tuned binary."
      - path: "Cargo.toml"
        issue: "No `[build]` section sets rustflags. (Note: workspace-level rustflags via Cargo.toml is not a stable Cargo feature; the canonical fix is `.cargo/config.toml`.)"
    missing:
      - "Create `.cargo/config.toml` with `[build] rustflags = [\"-C\", \"target-cpu=native\"]` (or a target-specific section) so host builds tune to the running CPU."
      - "Update Phase 1 plans / roadmap to record WS-05's coverage explicitly (host portion in Phase 1, Docker portion in Phase 3) so verification knows where to look."

  - truth: "metrics.rusage.voluntary_csw is populated with a positive value on representative runs"
    status: partial
    reason: "Smoke run on macOS host emits `voluntary_csw: 0` (see `/tmp/phase1-verify-fresh.json:60`). The plan's must-have list says `metrics.rusage.{...,voluntary_csw,involuntary_csw}` populated; `0` is technically populated but consistently zero across runs is suspicious. macOS `getrusage(RUSAGE_SELF)` historically does not populate `ru_nvcsw` (it is a documented divergence vs. Linux). The `involuntary_csw` field is non-zero (~195k), so the structure works, but the macOS code path silently emits `0` for the voluntary count. This is acceptable for the host smoke per the plan's documented Linux-vs-macOS divergence, but the verification cannot confirm the Linux path produces a positive value without a Linux test run."
    artifacts:
      - path: "crates/alloc-bench-core/src/metrics/rusage.rs"
        issue: "Lines 32 / `ru.ru_nvcsw as u64` blindly reads the field; on macOS the kernel does not populate it. No code-level guard or comment notes this divergence."
    missing:
      - "Document the macOS-vs-Linux divergence in `metrics/rusage.rs` (a doc comment is sufficient) so consumers don't read `voluntary_csw: 0` as a code bug."
      - "Optionally: run the multithread scenario on a Linux machine (or in Docker) and confirm `voluntary_csw > 0` in the resulting JSON. Phase 3 will surface this naturally; for Phase 1 it is sufficient to document the macOS limitation."
---

# Phase 1: Foundation MVP Slice — Verification Report

**Phase Goal:** User can build the workspace for one allocator combo, run the multi-thread allocation scenario, and get a fully-populated `results.json` proving the harness loop, the metrics pipeline, and the build-metadata injection all work end-to-end.

**Verified:** 2026-05-18T08:42:24Z
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria + PLAN Must-Haves)

| #   | Truth                                                                                                                                                                                                                                                                                                                                                                                              | Status     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | SC-1: `cargo build --release --no-default-features --features alloc-jemalloc -p alloc-bench-cli` works                                                                                                                                                                                                                                                                                              | FAILED   | Build fails with `E0432: unresolved import tikv_jemalloc_ctl::stats`. `stats` module is gated behind a feature flag the workspace dep does not enable. See gap above.                                                                                                                                                                                                                                                                                                  |
| 1b  | macOS host libmalloc default-features build is the practical smoke (per the user's softening note)                                                                                                                                                                                                                                                                                                  | VERIFIED | `cargo build --workspace --release` succeeds in 5.40s; `target/release/alloc-bench-cli` runs and prints the contract banner with `allocator=libmalloc`. Prior commit `33680ca` and the current commit on `main` both produce a working binary on macOS host.                                                                                                                                                                                                            |
| 1c  | mimalloc allocator build works on macOS host (defense-in-depth alternative to jemalloc)                                                                                                                                                                                                                                                                                                              | VERIFIED | `cargo build --release --no-default-features --features alloc-mimalloc -p alloc-bench-cli` succeeds in 5.07s. Resulting binary emits `allocator=mimalloc` and produces a valid v1 results.json.                                                                                                                                                                                                                                                                          |
| 2   | SC-2: `multithread` subcommand executes warm-up + measurement with all values black_boxed                                                                                                                                                                                                                                                                                                          | VERIFIED | `harness.rs:50` (warm-up) and `harness.rs:71` (measurement) both wrap `scenario.tick()` in `std::hint::black_box`. `scenarios/multithread.rs:138` and `:153` add scenario-level `black_box` discipline (boxed slices and the per-thread `Vec<Vec<Box<[u8]>>>`). End-to-end smoke shows 32k+ ticks completed in 1s.                                                                                                                                                       |
| 3   | SC-3: `results.json` has all required fields (env, build, scenario, harness, metrics with throughput, latency, peak_rss, rss_growth_samples, rusage, allocator_stats), accepting the post-review `ticks_per_s`/`tick_latency_ns`/`allocations_per_tick` rename as equivalent                                                                                                                       | VERIFIED | `/tmp/phase1-verify-fresh.json` has every field: `schema_version: 1`, `run_id`, `env.{os, os_version, cpu_model, cpu_count, memory_total_kb}` (docker_image is correctly omitted on host), `build.{allocator, rustc_version, target_triple, host_triple, profile, git_sha, git_dirty, build_timestamp, rustflags}`, `scenario.{name, config}`, `harness.{warmup_duration_s, measurement_duration_s, samples_count}`, `metrics.{ticks_per_s, allocations_per_tick, tick_latency_ns.{p50,p95,p99,p999,max}, peak_rss_kb, rss_growth_samples, rusage.{user_time_s, sys_time_s, minor_faults, major_faults, voluntary_csw, involuntary_csw, peak_rss_kb}, allocator_stats.kind}`. |
| 4   | SC-4: Mutual exclusion enforced (compile_error! in allocator.rs)                                                                                                                                                                                                                                                                                                                                   | VERIFIED | `cargo build -p alloc-bench-cli --features "alloc-jemalloc,alloc-mimalloc" --release` fails at compile time with: `error: cargo features alloc-jemalloc and alloc-mimalloc are mutually exclusive. Build with at most one allocator feature.` Source: `crates/alloc-bench-cli/src/allocator.rs:6-10`.                                                                                                                                                                  |
| 5   | Plan 02 must-have #4: Harness panics if `--warmup` < 1s with message containing "warm-up must be >= 1s"                                                                                                                                                                                                                                                                                            | VERIFIED | `harness.rs:41-43` does `if cfg.warmup < Duration::from_secs(1) { bail!(...) }` and the unit test `warmup_too_short_returns_error` (harness.rs:148) confirms the error message contains "warm-up must be >= 1s". Test passes.                                                                                                                                                                                                                                          |
| 6   | Plan 02 must-have #5: Every measured tick wrapped in `std::hint::black_box(scenario.tick())` (verified by source grep)                                                                                                                                                                                                                                                                              | VERIFIED | `grep -c 'std::hint::black_box' harness.rs` returns 2 (one in warm-up loop at :50, one in measurement at :71). Multithread scenario adds `black_box` on each per-thread allocated buffer (`scenarios/multithread.rs:138`) and on the all-threads bag (`:153`).                                                                                                                                                                                                          |
| 7   | Plan 01 must-have #4: `[profile.release]` has `lto = "fat", codegen-units = 1, opt-level = 3, strip = "symbols", debug = false, panic = "abort"`                                                                                                                                                                                                                                                    | VERIFIED | `Cargo.toml:26-33` exact match; `overflow-checks = false` is also set per the plan.                                                                                                                                                                                                                                                                                                                                                                                       |
| 8   | Plan 01 must-have #2: Banner format matches `alloc-bench v0.1.0 (allocator=<name>, rustc=<X.Y.Z>, target=<triple>, host=<triple>, profile=release, git=<sha8>[-dirty], built=<rfc3339>)`                                                                                                                                                                                                            | VERIFIED | Captured stderr: `alloc-bench v0.1.0 (allocator=libmalloc, rustc=1.91.1, target=aarch64-apple-darwin, host=aarch64-apple-darwin, profile=release, git=5196a947-dirty, built=2026-05-18T08:38:27Z)` — every field present, format matches.                                                                                                                                                                                                                              |
| 9   | Plan 01 must-have #5: Default-feature build runs on macOS host and prints `allocator=libmalloc`                                                                                                                                                                                                                                                                                                    | VERIFIED | Banner shows `allocator=libmalloc`; allocator.rs:34-35 returns `"libmalloc"` on `target_os = "macos"` when neither feature is enabled.                                                                                                                                                                                                                                                                                                                                  |
| 10  | Plan 02 must-have #3: Building with `--features alloc-jemalloc` and running the multithread scenario emits `metrics.allocator_stats.kind == "jemalloc"`                                                                                                                                                                                                                                              | FAILED   | Cannot satisfy because the alloc-jemalloc build does not compile (see truth #1 / SC-1 gap).                                                                                                                                                                                                                                                                                                                                                                              |
| 11  | Plan 02 must-have #1: `alloc-bench-cli multithread --threads 8 --objects 100000 --size-dist uniform --warmup 5s --duration 30s --output run.json` runs to completion                                                                                                                                                                                                                                | VERIFIED | Smaller smoke (`--threads 2 --objects 100 --warmup 1s --duration 1s`) ran to completion in ~3s and emitted a valid v1 JSON. The CLI accepts all listed flags (verified via `--help` discovery in the integration test). The `--threads 8 --objects 100000 --warmup 5s --duration 30s` invocation was not run (would take 35s+), but every code path it exercises is covered by the smaller run plus unit tests.                                                          |
| 12  | HARN-05: Run with jemalloc emits jemalloc-internal stats (allocated, resident, retained, active) via `tikv_jemalloc_ctl`                                                                                                                                                                                                                                                                            | FAILED   | The code in `allocator.rs:53-64` is shaped correctly and would emit the four counters, but the build does not compile (truth #1). The full requirement contract — that an end-user can BUILD and RUN to obtain those stats — is not satisfied.                                                                                                                                                                                                                          |
| 13  | HARN-06: Run with mimalloc emits mimalloc-internal stats via the extended-feature stats API                                                                                                                                                                                                                                                                                                          | FAILED   | The mimalloc path emits only `{"kind": "mimalloc"}` (allocator.rs:65-71). No allocated / resident / peak counters. Source comment explicitly defers to `Phase 2/3 may extend`, but neither Phase 2 nor Phase 3 success criteria mention this requirement. HARN-06 is in the Phase 1 contract (REQUIREMENTS.md line 23, traceability line 127).                                                                                                                          |
| 14  | WS-05: Docker `RUSTFLAGS=-C target-cpu=x86-64-v3`, host `target-cpu=native`                                                                                                                                                                                                                                                                                                                          | FAILED   | The host portion is not configured — there is no `.cargo/config.toml` and no rustflags in `Cargo.toml`. JSON output shows `"rustflags": ""`. The Docker portion is Phase 3's responsibility (per the requirement assignment) but the host portion of WS-05 is a Phase 1 gap.                                                                                                                                                                                            |

**Score:** 11/15 truths verified (4 truths failed: #1/SC-1, #10, #12, #13, #14 — but #10 and #12 are downstream consequences of #1, so structurally there are 4 distinct root-cause failures: jemalloc build, mimalloc-stats stub, WS-05 host RUSTFLAGS, and the optional partial on `voluntary_csw`).

### Required Artifacts (PLAN Must-Have Artifacts)

| Artifact                                                | Expected                                                                                  | Status     | Details                                                                                                                                                            |
| ------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `Cargo.toml`                                            | Workspace root with profile.release LTO=fat                                               | VERIFIED | Lines 1-40: workspace, members, deps, `[profile.release]` with `lto="fat", codegen-units=1, opt-level=3, strip="symbols", debug=false, panic="abort"`             |
| `rust-toolchain.toml`                                   | Channel pinning                                                                            | VERIFIED | Channel is 1.91 (deviated from plan's 1.83 but consistent with host rustc and all builds succeed). +rustfmt +clippy components present.                            |
| `crates/alloc-bench-core/Cargo.toml`                    | Core lib manifest                                                                         | VERIFIED | Workspace deps wired correctly. `cargo check -p alloc-bench-core` passes.                                                                                          |
| `crates/alloc-bench-core/src/lib.rs`                    | Module declarations                                                                       | VERIFIED | Real declarations: `pub mod harness; pub mod metrics; pub mod output; pub mod scenarios;` plus `pub use` re-exports.                                              |
| `crates/alloc-bench-core/src/output.rs`                 | Schema v1 structs                                                                         | VERIFIED | `Run, Env, Build, ScenarioInfo, HarnessInfo, LatencyNs, RssGrowthSample, Rusage, Metrics` all present with `Serialize` derive. `SCHEMA_VERSION = 1`.              |
| `crates/alloc-bench-core/src/harness.rs`                | Scenario trait + run() loop                                                                | VERIFIED | Trait, blanket SinkValue impl, `run()` with warm-up guard, HDR histogram, 1Hz RSS sampling, end-of-run getrusage. `cargo test` passes.                            |
| `crates/alloc-bench-core/src/metrics/rusage.rs`         | getrusage wrapper                                                                          | VERIFIED | `read_rusage()` returns Rusage with macOS / Linux unit divergence handled. Unit test asserts `peak_rss_kb > 0`.                                                   |
| `crates/alloc-bench-core/src/metrics/statm.rs`          | /proc/self/statm reader (Linux-only)                                                      | VERIFIED | Linux branch reads field 2 of statm + sysconf page size; non-Linux returns 0. Test asserts platform expectation.                                                 |
| `crates/alloc-bench-core/src/metrics/env.rs`            | Env reader                                                                                 | VERIFIED | os, os_version, cpu_model (with x86/aarch64 fallback chain), cpu_count, memory_total_kb, docker_image. macOS host reports `Apple M4 Max`, 16 cores, 64GB.        |
| `crates/alloc-bench-core/src/scenarios/multithread.rs`  | Multithread scenario                                                                       | VERIFIED | `thread::scope`, mid-buffer write, `black_box`, `SizeDist::{Uniform,Bimodal,Pareto}` with `FromStr`, validated config. 7 unit tests pass.                         |
| `crates/alloc-bench-cli/Cargo.toml`                     | CLI manifest with allocator features                                                      | VERIFIED | `alloc-jemalloc` and `alloc-mimalloc` features wired to optional deps. dev-deps `assert_cmd`, `tempfile`. **WARNING:** `tikv-jemalloc-ctl` lacks `stats` feature. |
| `crates/alloc-bench-cli/src/allocator.rs`               | Feature-gated allocator selection + mutual exclusion + `name()` + `stats()`                | PARTIAL  | All five required substrings present (`compile_error!`, `#[global_allocator]` (×2), `pub const fn name()`, `pub fn assert_mutual_exclusion()`, `pub fn stats() -> serde_json::Value`). However, `stats()` for mimalloc is a hardcoded stub that does not satisfy HARN-06. |
| `crates/alloc-bench-cli/src/build_info.rs`              | Compile-time constants via env!()                                                          | VERIFIED | All 9 expected constants exposed; build.rs sets the matching env vars.                                                                                            |
| `crates/alloc-bench-cli/build.rs`                       | Hand-rolled build metadata injection                                                       | VERIFIED | Handles RUSTC_VERSION, TARGET_TRIPLE, HOST_TRIPLE, PROFILE, GIT_SHA, GIT_DIRTY, TIMESTAMP, RUSTFLAGS. Includes CR-03 fix: `rerun-if-changed` for `.git/HEAD`, `.git/index`, `NULL`. |
| `crates/alloc-bench-cli/src/main.rs`                    | Walking Skeleton CLI with Clap                                                            | VERIFIED | `Cli`, `Cmd::{Version, Multithread}`, every flag from SCEN-01 (`--threads`, `--objects`, `--size-dist`, `--size-min`, `--size-max`, `--warmup`, `--duration`, `--seed`, `--output`). WR-07 fix: banner deferred until after clap parse.       |
| `crates/alloc-bench-cli/src/run.rs`                     | parse_duration + run_multithread                                                          | VERIFIED | Hand-rolled `parse_duration` with WR-09 ms-suffix-precedence test and overflow check. `run_multithread` builds the full Run record and writes pretty JSON.       |
| `crates/alloc-bench-cli/tests/multithread_smoke.rs`     | Integration test                                                                          | VERIFIED | End-to-end run, asserts schema v1, scenario name, ticks_per_s, allocations_per_tick=200, tick_latency_ns.p50, warmup_duration_s, allocator_stats.kind.            |
| `crates/alloc-bench-aggregator/Cargo.toml`              | Aggregator placeholder manifest                                                            | VERIFIED | Minimal binary crate. Compiles.                                                                                                                                   |
| `crates/alloc-bench-aggregator/src/main.rs`             | Placeholder main                                                                          | VERIFIED | Prints "not yet implemented" message; exits 0 (info-level WR / IN-01 in code review noted this as low-severity — phase 4 will replace this).                     |

### Key Link Verification

| From                                  | To                                       | Via                                        | Status   | Details                                                                                                                                                                                                                            |
| ------------------------------------- | ---------------------------------------- | ------------------------------------------ | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `alloc-bench-cli/main.rs`             | `alloc-bench-core::run`                  | `run::run_multithread` → `run(&mut scenario, &cfg, allocator::stats)` | WIRED   | `crates/alloc-bench-cli/src/run.rs:84` calls `alloc_bench_core::run(...)`, passing the CLI's allocator-feature-gated `stats()` closure.                                                                                            |
| `harness::run`                        | `metrics::rusage`                        | `read_rusage()`                            | WIRED   | `harness.rs:95` invokes `read_rusage()` once at end of measurement.                                                                                                                                                                |
| `harness::run`                        | `metrics::statm`                         | `read_rss_kb()`                            | WIRED   | `harness.rs:79` calls `read_rss_kb()` in the 1Hz sampling loop. Returns 0 on macOS (documented), positive on Linux.                                                                                                                |
| `harness::run`                        | `output::Metrics`                        | direct construction                        | WIRED   | `harness.rs:99-113` builds `Metrics { ticks_per_s, allocations_per_tick, tick_latency_ns, peak_rss_kb, rss_growth_samples, rusage, allocator_stats }`. All fields populated.                                                       |
| `harness::run`                        | HDR histogram percentiles                 | `hist.value_at_quantile`                   | WIRED   | `harness.rs:103-107` reads p50/p95/p99/p999/max from the histogram.                                                                                                                                                                |
| `cli::run::run_multithread`           | `output::Run` schema                     | direct struct construction                  | WIRED   | `run.rs:108-116` builds the top-level `Run` and serializes via `serde_json::to_string_pretty`. End-to-end JSON observed in `/tmp/phase1-verify-fresh.json`.                                                                       |
| `multithread::tick`                   | thread workers                            | `std::thread::scope`                      | WIRED   | `scenarios/multithread.rs:125-151`: scope, spawn, mid-buffer write, panic-propagating join (CR-02 fix).                                                                                                                            |
| `cli::main`                           | `allocator::name()` and `allocator::stats()` | direct call                                | WIRED   | `main.rs:63` calls `allocator::name()`; `run.rs:84` calls `allocator::stats` as the third argument to `run`.                                                                                                                       |
| build.rs env vars                     | `build_info` consts                       | `env!()` macro                             | WIRED   | All 8 `BUILD_*` env vars set in `build.rs` are consumed by `build_info.rs`.                                                                                                                                                       |
| `--features alloc-jemalloc`           | `tikv_jemalloc_ctl::stats`                | `use ...::stats` in allocator.rs          | NOT_WIRED | The required Cargo feature `stats` on `tikv-jemalloc-ctl` is NOT enabled in workspace deps. This is the root cause of truth #1 / SC-1 / HARN-05 failures.                                                                       |

### Data-Flow Trace (Level 4)

| Artifact                                  | Data Variable           | Source                                                | Produces Real Data | Status         |
| ----------------------------------------- | ----------------------- | ----------------------------------------------------- | ------------------ | -------------- |
| `output.rs::Run`                          | `metrics`               | `harness::run` outcome (real HDR histogram + getrusage) | Yes              | FLOWING       |
| `output.rs::Run`                          | `env`                   | `metrics::env::read_env()` (sysctl / /proc parsers)   | Yes              | FLOWING       |
| `output.rs::Run`                          | `build`                 | `build_info::*` constants (build.rs env injection)    | Yes              | FLOWING       |
| `metrics::Metrics`                        | `tick_latency_ns`       | HDR histogram quantiles                               | Yes              | FLOWING       |
| `metrics::Metrics`                        | `rusage`                | `getrusage(RUSAGE_SELF)`                              | Yes              | FLOWING       |
| `metrics::Metrics`                        | `peak_rss_kb`           | `rusage.peak_rss_kb`                                  | Yes              | FLOWING       |
| `metrics::Metrics`                        | `rss_growth_samples`    | 1Hz `read_rss_kb()` loop                              | Linux: yes / macOS: 0 | FLOWING (Linux), STATIC (macOS — documented divergence) |
| `metrics::Metrics`                        | `allocator_stats`       | `allocator::stats()` closure                           | jemalloc: blocked by build / mimalloc: stubbed / system: real | DISCONNECTED for jemalloc, HOLLOW_PROP for mimalloc, FLOWING for system |

### Behavioral Spot-Checks

| Behavior                                                                  | Command                                                                                                              | Result                                                              | Status |
| ------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | ------ |
| Workspace builds in release                                                | `cargo build --workspace --release`                                                                                   | Finished `release` profile [optimized] target(s) in 5.40s            | PASS  |
| `--version` (clap) returns clean output                                    | `target/release/alloc-bench-cli --version`                                                                            | `alloc-bench-cli 0.1.0`                                              | PASS  |
| `version` subcommand prints contract banner                                | `target/release/alloc-bench-cli version 2>&1`                                                                          | full banner per Plan 01 must-have #2                                 | PASS  |
| `multithread` subcommand produces v1 JSON                                  | `target/release/alloc-bench-cli multithread --threads 2 --objects 100 --warmup 1s --duration 1s --output /tmp/x.json` | `schema_version: 1` + every field populated                          | PASS  |
| `multithread` 32k+ samples in 1s                                           | (same)                                                                                                                | `samples_count: 32611`, `ticks_per_s: 32610.3`                       | PASS  |
| Mutual-exclusion compile_error                                              | `cargo build -p alloc-bench-cli --features "alloc-jemalloc,alloc-mimalloc"`                                            | `error: cargo features alloc-jemalloc and alloc-mimalloc are mutually exclusive...` | PASS  |
| jemalloc-only build                                                        | `cargo build --release --no-default-features --features alloc-jemalloc -p alloc-bench-cli`                            | `error[E0432]: unresolved import tikv_jemalloc_ctl::stats`           | FAIL  |
| mimalloc-only build + run                                                  | `cargo build --release --no-default-features --features alloc-mimalloc -p alloc-bench-cli && ./target/release/alloc-bench-cli multithread ...` | banner shows `allocator=mimalloc`, JSON `kind: "mimalloc"`           | PASS  |
| `cargo test --workspace`                                                   | `cargo test --workspace`                                                                                              | 15 tests pass (3 cli + 1 integration + 11 core); 0 failures          | PASS  |
| `cargo clippy --workspace --all-targets -- -D warnings`                    | (same)                                                                                                                | 0 errors, 0 warnings                                                 | PASS  |
| `cargo fmt --all --check`                                                  | (same)                                                                                                                | clean diff                                                           | PASS  |

### Probe Execution

No project probes (`scripts/*/tests/probe-*.sh`) declared in the PLAN or in the conventional location. Step 7c skipped — Phase 1 uses `cargo test` + ad-hoc smoke as its verification mechanism.

### Requirements Coverage

| Requirement | Source Plan | Description                                                                                              | Status   | Evidence                                                                                                                                                                                                                                                  |
| ----------- | ----------- | -------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WS-01       | 01-PLAN     | Cargo workspace with `alloc-bench-cli`, `alloc-bench-core`, `alloc-bench-aggregator`                       | SATISFIED | Three crates exist under `crates/`, `cargo build --workspace` passes.                                                                                                                                                                                |
| WS-02       | 01-PLAN     | Compile-time allocator selection via Cargo features, mutually exclusive                                  | SATISFIED | `alloc-jemalloc`, `alloc-mimalloc` features defined in `crates/alloc-bench-cli/Cargo.toml:13-15`. `compile_error!` in `allocator.rs:6-10` enforces exclusion (verified: build fails as expected).                                                       |
| WS-03       | 01-PLAN     | Binary prints rustc version, target triple, host triple, profile, git SHA, build timestamp at startup     | SATISFIED | Banner format verified: `alloc-bench v0.1.0 (allocator=libmalloc, rustc=1.91.1, target=aarch64-apple-darwin, host=aarch64-apple-darwin, profile=release, git=5196a947-dirty, built=2026-05-18T08:38:27Z)`.                                              |
| WS-04       | 01-PLAN     | `[profile.release]` lto=fat, codegen-units=1, opt-level=3, strip=symbols, debug=false                       | SATISFIED | `Cargo.toml:26-33` has every field plus `panic="abort"` and `overflow-checks=false`.                                                                                                                                                                  |
| WS-05       | 01-PLAN     | Docker `target-cpu=x86-64-v3`, host `target-cpu=native`                                                    | BLOCKED   | Host portion not configured: no `.cargo/config.toml`, JSON shows `rustflags: ""`. Docker portion is Phase 3's responsibility but the host portion is missing here. See gap above.                                                                  |
| HARN-01     | 02-PLAN     | Configurable warm-up (default 5s, minimum 1s) before measurement                                          | SATISFIED | `harness.rs:41-43` enforces `warmup >= 1s`; CLI default in `main.rs:35` is `"5s"`. Unit test `warmup_too_short_returns_error` confirms the guard.                                                                                                       |
| HARN-02     | 02-PLAN     | Per-tick latency to HDR histogram, p50/p95/p99/p999/max in ns                                              | SATISFIED | `harness.rs:62, 76, 100-108`. `tick_latency_ns.{p50, p95, p99, p999, max}` populated in JSON. Schema renamed from `latency_ns` to `tick_latency_ns` per the user's note (post-review correction).                                                       |
| HARN-03     | 02-PLAN     | Sample `/proc/self/statm` every 1s producing RSS-over-time array                                            | SATISFIED | `harness.rs:78-83`: 1Hz `read_rss_kb()` loop. On macOS the array is `[{t_s: 0, rss_kb: 0}]` per the documented Linux-only divergence; Linux behavior conforms.                                                                                       |
| HARN-04     | 02-PLAN     | `getrusage(RUSAGE_SELF)` at end-of-run for peak RSS, user/sys CPU time, page faults, context switches      | SATISFIED | `metrics/rusage.rs::read_rusage()` populates all six fields. JSON shows `user_time_s: 1.12, sys_time_s: 1.66, minor_faults: 262067, major_faults: 0, voluntary_csw: 0 (macOS), involuntary_csw: 198327, peak_rss_kb: 9152`.                            |
| HARN-05     | 02-PLAN     | jemalloc-internal stats (allocated, resident, retained, active) via `tikv_jemalloc_ctl`                     | BLOCKED   | Code structure correct (allocator.rs:53-64) but `cargo build --features alloc-jemalloc` does not compile. Dependent on the `stats` feature flag fix.                                                                                                  |
| HARN-06     | 02-PLAN     | mimalloc-internal stats via the extended-feature stats API                                                  | BLOCKED   | `allocator.rs:65-71` returns only `{"kind": "mimalloc"}` — no actual stats. Source comment defers to Phase 2/3 but no later phase claims this requirement.                                                                                            |
| HARN-07     | 02-PLAN     | Every measured tick wrapped in `std::hint::black_box` to prevent dead-code elimination                      | SATISFIED | `harness.rs:50, 71` plus scenario-level black_box at `multithread.rs:138, 153`. Mid-buffer write `b[size/2] = 0xAB` defeats DCE per PITFALLS.md §1.2.                                                                                              |
| HARN-08     | 02-PLAN     | Single results.json record matching the schema (env + build + scenario + harness + metrics)                | SATISFIED | `output.rs::Run` struct + `serde_json::to_string_pretty` in `run.rs:118-125`. Smoke verification confirms all five blocks populated and JSON-parses.                                                                                                  |
| SCEN-01     | 02-PLAN     | `multithread` with `--threads N --objects M --size-dist <uniform\|bimodal\|pareto>`                          | SATISFIED | CLI flags defined in `main.rs:24-43`. `SizeDist` enum supports all three variants (`scenarios/multithread.rs:11-15`).                                                                                                                              |
| REPR-02     | 02-PLAN     | results.json includes cpu_model, cpu_count, kernel_version (os_version), docker_image, rustc_version, target_triple, git_sha, rustflags | SATISFIED | All eight fields present in the JSON output. `docker_image` is correctly omitted when env var unset (skip_serializing_if). `rustflags` is empty in the host build per the WS-05 gap, but the *field* is present.                                  |

**Coverage:** All 15 declared requirement IDs (WS-01..05, HARN-01..08, SCEN-01, REPR-02) are accounted for. 12 SATISFIED, 3 BLOCKED (WS-05 host portion, HARN-05, HARN-06).

**Orphans:** None. REQUIREMENTS.md maps WS-01..05, HARN-01..08, SCEN-01, REPR-02 (15 IDs) to Phase 1, and PLAN 01 + PLAN 02 frontmatter together cover all 15.

### Anti-Patterns Found

| File                                              | Line          | Pattern                                                                                                          | Severity   | Impact                                                                                                                                                                                                  |
| ------------------------------------------------- | ------------- | ---------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `crates/alloc-bench-cli/src/allocator.rs`         | 65-71          | Hardcoded stub: `return serde_json::json!({ "kind": "mimalloc" });` — flows to `metrics.allocator_stats` in JSON   | Blocker  | This stub satisfies the schema shape but not HARN-06. The source comment explicitly defers to "Phase 2/3 may extend" — but neither later phase claims this work.                                  |
| `crates/alloc-bench-cli/src/allocator.rs`         | 65-71          | Comment "Phase 2/3 may extend with mi_stats_print capture if needed."                                            | Warning  | Comment language ("may extend ... if needed") signals soft commitment, not a tracked deferral. No `#123` issue reference; the project relies on REQUIREMENTS.md for tracking, which still lists HARN-06 as Phase 1 work. |
| `crates/alloc-bench-aggregator/src/main.rs`       | 1-3           | `eprintln!("not yet implemented (Phase 4...)"); std::process::exit(0);`                                          | Info     | Documented in 01-REVIEW.md IN-01: phase 4 placeholder exits with status 0; should arguably exit 2 to make absence visible to scripts. Acceptable for Phase 1.                                       |

No `TBD` / `FIXME` / `XXX` markers found in modified files. The "deferred to Phase 2/3" comment in `allocator.rs` is a soft marker rather than a formal debt marker, but combined with the missing requirement coverage, it manifests as an actual gap.

### Human Verification Required

Phase 1 is technical (build + run + JSON shape). All success criteria are programmatically verifiable (and most are verified). No items require human judgement on visual / UX behavior.

The only candidates for human consideration are **roadmap-shaping decisions**, not test items — these are surfaced as override suggestions below rather than verification items.

### Override Suggestions (For User Decision)

Two of the failed truths look intentional given the user's softening note. If the user accepts these as deliberate scope decisions, they can add overrides to the VERIFICATION.md frontmatter:

```yaml
overrides:
  - must_have: "Building with --features alloc-jemalloc compiles cleanly (Success Criterion 1; HARN-05)"
    reason: "User's softening note explicitly defers jemalloc cross-compile to Phase 3 Docker. However, the build error is NOT a cross-compile issue — it is a missing `features = [\"stats\"]` on the workspace `tikv-jemalloc-ctl` dep that will block Phase 3 too. Recommend FIXING this in Phase 1 rather than overriding."
    accepted_by: ""
    accepted_at: ""
  - must_have: "metrics.allocator_stats reports mimalloc-internal stats via the extended-feature stats API (HARN-06)"
    reason: "Source comment defers to Phase 2/3, but neither later phase claims this requirement. Either implement it here or update REQUIREMENTS.md + ROADMAP.md to reassign HARN-06 to a later phase."
    accepted_by: ""
    accepted_at: ""
```

Recommend NOT applying these overrides — both gaps are actionable and the jemalloc fix is a one-line change to `Cargo.toml`. They should be closed in Phase 1 to unblock Phase 3.

### Gaps Summary

Phase 1 lands a strong technical foundation. The walking skeleton, harness, metrics layer, multithread scenario, schema v1, and end-to-end macOS-host smoke all work cleanly. 11 of the 14 distinct must-have truths verify against the codebase, and every code review finding from `01-REVIEW.md` (CR-01..03, WR-01..09) has been fixed in commits `d2f2b11..5196a94`.

However, three structural gaps remain that violate the phase contract:

1. **`cargo build --features alloc-jemalloc` does not compile** (Success Criterion 1 + HARN-05). The fix is a one-line addition: enable the `stats` feature on the workspace `tikv-jemalloc-ctl` dependency. This blocks the explicit Phase 1 success criterion AND the entire Phase 3 Docker matrix (every jemalloc image will fail).

2. **mimalloc allocator_stats is a hardcoded stub** (HARN-06). The current implementation emits `{"kind": "mimalloc"}` only. The requirement contract demands extended-feature stats. Either implement using the mimalloc `extended` feature + FFI, or move HARN-06 to a later phase in REQUIREMENTS.md / ROADMAP.md.

3. **WS-05 host RUSTFLAGS not configured**. There is no `.cargo/config.toml` setting `target-cpu=native`. JSON shows `rustflags: ""`. Add `.cargo/config.toml` with the host build flags.

A fourth, lower-severity item (`voluntary_csw: 0` on macOS) is partially blocked by the documented Linux/macOS divergence and warrants a code comment but not a structural change.

The roadmap currently has Phase 1 marked as `[x] completed` (line 15 of ROADMAP.md). Verification disagrees — Phase 1 cannot be considered complete until at least gaps #1 and #3 are closed and gap #2 is either fixed or formally re-scoped.

---

_Verified: 2026-05-18T08:42:24Z_
_Verifier: Claude (gsd-verifier)_
