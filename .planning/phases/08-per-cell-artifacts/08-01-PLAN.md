---
phase: 08-per-cell-artifacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/alloc-bench-aggregator/templates/recommend-cell.md.tmpl
  - crates/alloc-bench-aggregator/templates/recommend-cell.html.tmpl
  - crates/alloc-bench-aggregator/src/html.rs
autonomous: true
requirements:
  - CELL-01
  - CELL-02
  - CELL-05
tags: [phase-08, templates, tinytemplate, drift-defense, recommend-cell, rust]
must_haves:
  truths:
    - "Two new template files exist driven by the same CellRecommendation"
    - "A CellTemplateContext serde struct adds rank_padded for {:02d} formatting; reuses cell.env (already short-form per Phase 7)"
    - "Both templates compile via tinytemplate at cargo test time"
    - "WR-01 sentinel test renders both templates and asserts every CellRecommendation field's sentinel substring appears in BOTH outputs (drift defense for CELL-02)"
    - "Both surfaces emit the literal six bytes (suspect) between asterisks when suspect_flag = true (no <em>, no badge)"
    - "Card structure matches CELL-05: TL;DR -> Strengths -> Weaknesses -> Recommended for -> Avoid for"
    - "RECOMMEND_CELL_MD const lives in html.rs (canonical home — colocated with WR-01 sentinel test that registers both templates). markdown.rs imports via `use crate::html::{build_cell_template_context, RECOMMEND_CELL_MD};`. Acknowledged trade-off: cross-module template-string visibility, accepted for test colocation."
  artifacts:
    - path: "crates/alloc-bench-aggregator/templates/recommend-cell.md.tmpl"
      provides: "Markdown card body — single-card fragment driven by CellTemplateContext"
      contains: "{{ for s in strengths }}"
    - path: "crates/alloc-bench-aggregator/templates/recommend-cell.html.tmpl"
      provides: "HTML <article class='recommend-card'> body — same field set as Markdown card"
      contains: "<article class=\"recommend-card\""
    - path: "crates/alloc-bench-aggregator/src/html.rs"
      provides: "RECOMMEND_CELL_MD / RECOMMEND_CELL_HTML constants, CellTemplateContext serde struct, build_cell_template_context helper, two new tests"
      contains: "fn cell_templates_both_reference_all_fields"
  key_links:
    - from: "recommend-cell.md.tmpl + recommend-cell.html.tmpl"
      to: "CellTemplateContext"
      via: "tinytemplate field substitution by name"
      pattern: "\\{(rank|alloc|env|tldr|rank_padded)\\}"
    - from: "html::tests::cell_templates_both_reference_all_fields"
      to: "both templates"
      via: "sentinel-render + cross-surface substring assertion"
      pattern: "_SENTINEL_"
---

<objective>
Land the foundation surfaces for Phase 8: two new tinytemplate files driven by the same CellRecommendation struct, a CellTemplateContext wrapper that adds the pre-formatted rank_padded field, and the WR-01-pattern drift-defense test that pre-empts template/struct drift before any wiring exists.

Purpose: Establish the byte-identical-output contract between the Markdown card and the HTML panel BEFORE any aggregator wiring depends on them. The WR-01 sentinel test fails loudly if a future contributor adds a field to one template without the other — pre-empting the WR-01 winner-tiebreak drift the v1.0 fix already exposed once. CELL-01, CELL-02, and the structural shape of CELL-05 (section order TL;DR → Strengths → Weaknesses → Recommended-for → Avoid-for; *(suspect)* annotation literal six bytes) all land here.
Output: Two new .tmpl files, three new tests in `html.rs::tests` (compile gate + sentinel drift test + a struct-derives-Serialize sanity check), one new `pub(crate)` builder fn, one new `pub(crate)` serde::Serialize struct, two new `const` template strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-per-cell-artifacts/08-CONTEXT.md
@.planning/phases/08-per-cell-artifacts/08-RESEARCH.md
@.planning/phases/08-per-cell-artifacts/08-PATTERNS.md
@.planning/phases/08-per-cell-artifacts/08-UI-SPEC.md
@.planning/phases/07-scoring-top-n/07-02-SUMMARY.md
@crates/alloc-bench-aggregator/src/recommend.rs
@crates/alloc-bench-aggregator/src/html.rs
@crates/alloc-bench-aggregator/templates/index.html.tmpl
@CLAUDE.md

<interfaces>
<!-- Phase 7 deliverables Phase 8 templates consume. Verbatim from recommend.rs. -->

From crates/alloc-bench-aggregator/src/recommend.rs (Phase 7 — already shipped):

```rust
pub const TOP_N_SPIDER: usize = 3;
pub const TOP_N_TABLE:  usize = 5;
pub const TOP_N_TOTAL:  usize = 10;

#[derive(Debug, Clone, PartialEq)]
pub struct CellRecommendation {
    pub rank: usize,
    pub alloc: String,
    pub env: String,                                 // already short-form (e.g. "alpine", "debian-slim", "host")
    pub composite_score: f64,
    pub axes: BTreeMap<&'static str, f64>,
    pub tldr: String,
    pub strengths: Vec<&'static str>,
    pub weaknesses: Vec<&'static str>,
    pub recommended_for: Vec<&'static str>,
    pub avoid_for: Vec<&'static str>,
    pub suspect_flag: bool,
}

pub fn top_n_cells(scores: Vec<CellScore>, runs: &[Run]) -> Vec<CellRecommendation>;
```

Tinytemplate registration precedent (html.rs:34, 228-230, 339-343):
- `const TEMPLATE: &str = include_str!("../templates/index.html.tmpl");`
- `tt.add_template("index", TEMPLATE).context("compiling index.html.tmpl")?;`
- existing compile-time test `fn tinytemplate_compiles_index_template`
</interfaces>

<canonical_excerpts>
<!-- Verbatim from 08-RESEARCH.md §2 + 08-PATTERNS.md — the exact template body text. -->

recommend-cell.md.tmpl (canonical):
- First line: `### {rank}. {alloc}/{env}{{ if suspect_flag }} *(suspect)*{{ endif }}`
- Then blank line.
- Then `{tldr}` line.
- Then blank line.
- Then `**Strengths**` line, blank, then `{{ for s in strengths }}- {s}\n{{ endfor }}` compact form.
- Same pattern for Weaknesses (`{{ for w in weaknesses }}- {w}\n{{ endfor }}`).
- Same pattern for `**Recommended for**` (`{{ for c in recommended_for }}- {c}\n{{ endfor }}`).
- Same pattern for `**Avoid for**` (`{{ for c in avoid_for }}- {c}\n{{ endfor }}`).
- NO trailing horizontal rule inside the template (the section emitter inserts `---` between cards).

recommend-cell.html.tmpl (canonical):
- Outer: `<article class="recommend-card" id="recommend-{rank_padded}-{alloc}-{env}">` ... `</article>`.
- `<h3>{rank}. {alloc}/{env}{{ if suspect_flag }} *(suspect)*{{ endif }}</h3>`
- `<p>{tldr}</p>`
- `<p><strong>Strengths</strong></p>` then `<ul>{{ for s in strengths }}<li>{s}</li>{{ endfor }}</ul>`
- Same `<p><strong>...</strong></p>` + `<ul>...{{ for ... }}<li>{...}</li>{{ endfor }}...</ul>` pattern for Weaknesses, Recommended for, Avoid for.

CellTemplateContext (canonical, lives in html.rs near HtmlContext at line 59-96):

```rust
#[derive(serde::Serialize)]
pub(crate) struct CellTemplateContext {
    pub rank: usize,
    pub rank_padded: String,                     // format!("{:02}", cell.rank)
    pub alloc: String,
    pub env: String,                             // cell.env.clone() — already short-form
    pub tldr: String,
    pub strengths: Vec<&'static str>,
    pub weaknesses: Vec<&'static str>,
    pub recommended_for: Vec<&'static str>,
    pub avoid_for: Vec<&'static str>,
    pub suspect_flag: bool,
}

pub(crate) fn build_cell_template_context(cell: &CellRecommendation) -> CellTemplateContext;
```

Note 1: `composite_score` and `axes` are intentionally NOT on CellTemplateContext — per CONTEXT.md the cards do not render score numbers or per-axis values. The leading `| Rank | Cell | Score |` summary table (Plan 02) handles composite_score; per-axis values are the Phase 9 spider chart's job.

Note 2: `cell.env` is already short-form (verified Phase 7 plumbing: `score::env_short_name → CellScore.env → CellRecommendation.env` at recommend.rs:659). Templates use `{env}` directly for both the title and the HTML id; the CONTEXT.md "env_short" naming refers to this same value.

Note 3: RECOMMEND_CELL_MD lives in html.rs (canonical home — colocated with the WR-01 sentinel test that registers both templates). Plan 02's markdown.rs imports it via `use crate::html::{build_cell_template_context, RECOMMEND_CELL_MD};`. Acknowledged trade-off: cross-module template-string visibility, accepted for test colocation.
</canonical_excerpts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author both .tmpl files and the CellTemplateContext + builder + RECOMMEND_CELL_* consts</name>
  <files>crates/alloc-bench-aggregator/templates/recommend-cell.md.tmpl, crates/alloc-bench-aggregator/templates/recommend-cell.html.tmpl, crates/alloc-bench-aggregator/src/html.rs</files>
  <read_first>
    - .planning/phases/08-per-cell-artifacts/08-CONTEXT.md (sections "Card Structure & Prose Format", "Template Engineering & Drift Defense", "Claude's Discretion")
    - .planning/phases/08-per-cell-artifacts/08-PATTERNS.md (sections recommend-cell.md.tmpl, recommend-cell.html.tmpl, html.rs MODIFY)
    - .planning/phases/08-per-cell-artifacts/08-UI-SPEC.md (Layout & Interaction; Cross-Surface Byte-Identity Contract)
    - .planning/phases/08-per-cell-artifacts/08-RESEARCH.md §2 (Library Surface — tinytemplate syntax) and §5 Pitfall 2 (whitespace inside {{for}})
    - crates/alloc-bench-aggregator/src/html.rs (lines 22-96 imports + HtmlContext; lines 227-248 render fn; lines 339-343 compile-time test precedent)
    - crates/alloc-bench-aggregator/src/recommend.rs (lines 138-151 CellRecommendation struct, line 42-50 TOP_N_* constants)
    - crates/alloc-bench-aggregator/templates/index.html.tmpl (lines 22-30 `\{` escape precedent; lines 253-256 sibling section)
  </read_first>
  <action>
    Create `crates/alloc-bench-aggregator/templates/recommend-cell.md.tmpl` with the canonical body sketched in 08-PATTERNS.md "recommend-cell.md.tmpl" and 08-RESEARCH.md §2. First line is the h3 title with the conditional suspect suffix; section order inside the body is TL;DR (paragraph), then `**Strengths**`/`**Weaknesses**`/`**Recommended for**`/`**Avoid for**` blocks, each as a bold label line followed by a blank line then the compact `{{ for s in strengths }}- {s}\n{{ endfor }}` loop body — mirroring the field iteration order in CellTemplateContext. Use the compact `{{ for s in strengths }}- {s}\n{{ endfor }}` form per RESEARCH §Pitfall 2 (no leading whitespace before `{{ for }}`, single newline at the end of the bullet line). The `*(suspect)*` annotation uses the conditional `{{ if suspect_flag }} *(suspect)*{{ endif }}` with the leading space per UI-SPEC line 130. NO trailing `---` separator inside the template (the section emitter in Plan 02 inserts those between cards). NO leading file-level title or frontmatter — the file IS the card body fragment.

    Create `crates/alloc-bench-aggregator/templates/recommend-cell.html.tmpl` with the canonical body sketched in 08-PATTERNS.md "recommend-cell.html.tmpl" and 08-RESEARCH.md §2. Outer element is `<article class="recommend-card" id="recommend-{rank_padded}-{alloc}-{env}">`...`</article>`. Inside: `<h3>{rank}. {alloc}/{env}{{ if suspect_flag }} *(suspect)*{{ endif }}</h3>`, then `<p>{tldr}</p>`, then for each of the four bullet sections a `<p><strong>Label</strong></p>` followed by `<ul>{{ for x in field }}<li>{x}</li>{{ endfor }}</ul>` (no leading whitespace inside the loop body so tinytemplate doesn't emit stray indentation). Suspect annotation emits the literal six bytes `*(suspect)*` between asterisks — NO `<em>(suspect)</em>` wrapper, NO `<span class="suspect">` badge, NO `⚠` glyph (per CONTEXT.md "Suspect annotation byte-identity" + UI-SPEC line 113). Field-name set MUST be exactly: `rank`, `rank_padded`, `alloc`, `env`, `tldr`, `strengths`, `weaknesses`, `recommended_for`, `avoid_for`, `suspect_flag` — no other identifiers, so the WR-01 sentinel test substring assertions hold and tinytemplate compile errors fire on missing fields.

    Modify `crates/alloc-bench-aggregator/src/html.rs` to:
    1. Add two new `const` declarations near line 34 (parallel to the existing `const TEMPLATE: &str = include_str!("../templates/index.html.tmpl");`):
       `const RECOMMEND_CELL_MD: &str = include_str!("../templates/recommend-cell.md.tmpl");`
       `const RECOMMEND_CELL_HTML: &str = include_str!("../templates/recommend-cell.html.tmpl");`
       Both MUST be `pub(crate)` from the start so Plan 02's `markdown.rs` can `use crate::html::RECOMMEND_CELL_MD` without a Plan-02 visibility tweak. RECOMMEND_CELL_MD lives in html.rs (canonical home — colocated with the WR-01 sentinel test that registers both templates). Acknowledged trade-off: cross-module template-string visibility, accepted for test colocation.
    2. Add the `pub(crate) struct CellTemplateContext` definition after `HtmlContext` (around line 96) with `#[derive(serde::Serialize)]`, fields exactly as specified in `<canonical_excerpts>` (rank, rank_padded, alloc, env, tldr, strengths, weaknesses, recommended_for, avoid_for, suspect_flag — NO composite_score, NO axes per CONTEXT.md decision).
    3. Add `pub(crate) fn build_cell_template_context(cell: &CellRecommendation) -> CellTemplateContext` immediately after the struct. Body clones cell.alloc / cell.env / cell.tldr; clones the four `Vec<&'static str>` slices; pre-formats `rank_padded = format!("{:02}", cell.rank)`; copies `rank` and `suspect_flag` by value. Add `use crate::recommend::CellRecommendation;` to the imports block (line 30-32 cluster).
    4. NO changes to `HtmlContext`, `render`, or `write` in this plan — Plan 02 extends those after the foundation lands.

    Discretion notes per CONTEXT.md:
    - "whether the bold label `**Strengths**` is followed by a colon then list, or by a newline then list — pick whichever renders cleanest in GitHub Markdown previewer" → use the newline form (no colon) per UI-SPEC "Copywriting Contract" Strengths label entry. Bold label paragraph, blank line, bullet list.
    - "Whether the standalone fragment files include a leading byte-order-mark / trailing newline — recommend trailing `\n` for POSIX file convention" → trailing newline is enforced by Plan 02's per-cell render fn; the .tmpl file itself ends with whatever the last `{{ endfor }}` emits.

    Do NOT register the new templates with `tt.add_template` in this plan — that wiring lives in Plan 02 alongside the section-emit + standalone-fragment-write logic. Plan 01's compile-time tests (Task 2) construct their own `TinyTemplate::new()` instances locally to gate the new .tmpl files in isolation.
  </action>
  <acceptance_criteria>
    - File `crates/alloc-bench-aggregator/templates/recommend-cell.md.tmpl` exists with first line matching `^### \{rank\}\. \{alloc\}/\{env\}\{\{ if suspect_flag \}\} \*\(suspect\)\*\{\{ endif \}\}$`
    - File `crates/alloc-bench-aggregator/templates/recommend-cell.html.tmpl` exists and contains the substring `<article class="recommend-card" id="recommend-{rank_padded}-{alloc}-{env}">`
    - Running `grep -v '^#' crates/alloc-bench-aggregator/templates/recommend-cell.md.tmpl | grep -c '{{ for'` returns 4 (one loop per bullet section: strengths, weaknesses, recommended_for, avoid_for)
    - Running `grep -v '^#' crates/alloc-bench-aggregator/templates/recommend-cell.html.tmpl | grep -c '{{ for'` returns 4
    - Running `grep -c '{{ if suspect_flag }}' crates/alloc-bench-aggregator/templates/recommend-cell.md.tmpl` returns 1
    - Running `grep -c '{{ if suspect_flag }}' crates/alloc-bench-aggregator/templates/recommend-cell.html.tmpl` returns 1
    - Both .tmpl files reference exactly this set of identifiers (and no others) when stripped of `\n` and matched against `\{[a-z_]+(?:\s*\|\s*\w+)?\}`: `rank`, `alloc`, `env`, `tldr`, `rank_padded`, `s`, `w`, `c` (the `s`/`w`/`c` are loop variables); HTML-only adds `rank_padded` in the `id="recommend-{rank_padded}-{alloc}-{env}"` attribute
    - `crates/alloc-bench-aggregator/src/html.rs` contains the substring `pub(crate) const RECOMMEND_CELL_MD: &str = include_str!`
    - `crates/alloc-bench-aggregator/src/html.rs` contains the substring `pub(crate) const RECOMMEND_CELL_HTML: &str = include_str!`
    - `crates/alloc-bench-aggregator/src/html.rs` contains the substring `pub(crate) struct CellTemplateContext`
    - `crates/alloc-bench-aggregator/src/html.rs` contains the substring `pub(crate) fn build_cell_template_context`
    - `crates/alloc-bench-aggregator/src/html.rs` contains the substring `format!("{:02}", cell.rank)`
    - `crates/alloc-bench-aggregator/src/html.rs` does NOT contain `composite_score` inside the new struct (verified by `grep -A 12 'pub(crate) struct CellTemplateContext' crates/alloc-bench-aggregator/src/html.rs | grep -c composite_score` returning 0)
    - The new `CellTemplateContext` struct has the `#[derive(serde::Serialize)]` attribute on the line immediately above its declaration
  </acceptance_criteria>
  <verify>
    <automated>cargo build -p alloc-bench-aggregator --lib 2>&1 | tail -20</automated>
  </verify>
  <done>Two .tmpl files compile when included via `include_str!`; RECOMMEND_CELL_MD / RECOMMEND_CELL_HTML constants exist; CellTemplateContext + build_cell_template_context land in html.rs without registering with TinyTemplate yet (deferred to Plan 02). Library builds cleanly with the new code path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land the WR-01 drift-defense tests + tinytemplate compile gate in html.rs::tests</name>
  <files>crates/alloc-bench-aggregator/src/html.rs</files>
  <behavior>
    - Test 1 `tinytemplate_compiles_recommend_cell_templates`: constructs `TinyTemplate::new()`, calls `tt.add_template("recommend-cell-md", RECOMMEND_CELL_MD)` AND `tt.add_template("recommend-cell-html", RECOMMEND_CELL_HTML)`, both must `expect` cleanly. Mirrors `tinytemplate_compiles_index_template` at line 339 — catches missed `\{` escapes and unbalanced `{{for}}` / `{{if}}` blocks.
    - Test 2 `cell_templates_both_reference_all_fields` (REQUIREMENTS-pinned name; CELL-02): constructs a synthetic `CellRecommendation` whose every renderable scalar field is a unique sentinel substring (`_SENTINEL_ALLOC_`, `_SENTINEL_ENV_`, `_SENTINEL_TLDR_`, `_SENTINEL_STRENGTH_A_`, `_SENTINEL_STRENGTH_B_`, `_SENTINEL_WEAKNESS_A_`, `_SENTINEL_WEAKNESS_B_`, `_SENTINEL_RECCLASS_`, `_SENTINEL_AVOIDCLASS_`), with `suspect_flag = true`, `rank = 7`, `composite_score = 42.42`, and `axes` populated with one or more axis keys (values irrelevant — `axes` is NOT in the per-cell template). Builds a `CellTemplateContext` via `build_cell_template_context`, registers BOTH templates in one `TinyTemplate`, calls `tt.render("recommend-cell-md", &ctx)` and `tt.render("recommend-cell-html", &ctx)`. Asserts every sentinel substring appears in BOTH outputs (drift defense — if md template forgets `{tldr}`, the test fails) AND asserts the literal `(suspect)` substring appears in BOTH outputs (suspect-byte-identity gate). Asserts the rendered rank prefix `7. ` appears in both (rank substitution).
    - Test 3 `cell_template_context_excludes_score_and_axes`: a unit test that verifies `CellTemplateContext` does NOT carry `composite_score` or `axes` — guards the CONTEXT.md decision that cards don't render score numbers. Uses serde serialization to JSON and asserts the JSON keys are exactly `["rank", "rank_padded", "alloc", "env", "tldr", "strengths", "weaknesses", "recommended_for", "avoid_for", "suspect_flag"]` (alphabetical sort of the JSON object keys, any order in the struct).
  </behavior>
  <action>
    Add the three tests inside the existing `#[cfg(test)] mod tests` block at the bottom of `crates/alloc-bench-aggregator/src/html.rs` (the block that already contains `make_test_run` and `tinytemplate_compiles_index_template` — see html.rs:258 onward).

    For Test 1 (`tinytemplate_compiles_recommend_cell_templates`): place it directly below the existing `tinytemplate_compiles_index_template` test (around line 343) so the precedent is contextually obvious. Body is three lines: `let mut tt = TinyTemplate::new();` then two `tt.add_template(name, CONST).expect("...should compile — missed `\\{` escape or `{{endfor}}`?");` calls.

    For Test 2 (`cell_templates_both_reference_all_fields`): name MUST be exactly `cell_templates_both_reference_all_fields` per REQUIREMENTS CELL-02. Construct `CellRecommendation` directly via struct-literal syntax importing `crate::recommend::CellRecommendation`. Build the `axes: BTreeMap<&'static str, f64>` with at least one entry (any axis key is fine — the test never inspects axes; this just satisfies the type). Use `vec!["_SENTINEL_STRENGTH_A_", "_SENTINEL_STRENGTH_B_"]` for `strengths`, etc., for `weaknesses`/`recommended_for`/`avoid_for` (one or two sentinels each). Call `let ctx = build_cell_template_context(&cell);` to convert to template context. Register both templates via `tt.add_template("recommend-cell-md", RECOMMEND_CELL_MD).unwrap();` and `tt.add_template("recommend-cell-html", RECOMMEND_CELL_HTML).unwrap();`. Render both: `let md = tt.render("recommend-cell-md", &ctx).expect("md render");` and `let html = tt.render("recommend-cell-html", &ctx).expect("html render");`. Iterate the array of expected substrings (the 9 sentinel strings + the literal `(suspect)`) and assert `md.contains(s)` AND `html.contains(s)` for each, with debug message `"MD missing {sentinel}: {md}"` / `"HTML missing {sentinel}: {html}"` so a failure prints the full rendered output. Add the rank-prefix assertion `assert!(md.contains("7. ") && html.contains("7. "));` per RESEARCH §Section 4 sketch.

    For Test 3 (`cell_template_context_excludes_score_and_axes`): import `serde_json::to_value` (it's already a workspace dep — verify via `cargo metadata`, falls back to constructing a minimal serde::Value sequence if needed). Build a synthetic `CellRecommendation` via struct-literal, call `build_cell_template_context`, serialize to a `serde_json::Value`. Extract `value.as_object().unwrap().keys().cloned().collect::<Vec<_>>()`, sort the resulting Vec, and assert the sorted Vec equals exactly `["alloc", "avoid_for", "env", "rank", "rank_padded", "recommended_for", "strengths", "suspect_flag", "tldr", "weaknesses"]` (alphabetical, no `composite_score`, no `axes`).

    All three tests live in the same `mod tests` block. Add `use crate::recommend::CellRecommendation;` and `use std::collections::BTreeMap;` at the top of `mod tests` if not already imported. Reuse the existing imports.
  </action>
  <acceptance_criteria>
    - `cargo test -p alloc-bench-aggregator --lib html::tests::tinytemplate_compiles_recommend_cell_templates` exits 0
    - `cargo test -p alloc-bench-aggregator --lib html::tests::cell_templates_both_reference_all_fields` exits 0
    - `cargo test -p alloc-bench-aggregator --lib html::tests::cell_template_context_excludes_score_and_axes` exits 0
    - `cargo test -p alloc-bench-aggregator --lib html::tests::tinytemplate_compiles_index_template` still exits 0 (no regression on the existing precedent)
    - `cargo test -p alloc-bench-aggregator --lib` exits 0 (full lib test suite green; existing 100+ tests preserved)
    - The test module file `crates/alloc-bench-aggregator/src/html.rs` contains the verbatim test name `cell_templates_both_reference_all_fields` (REQUIREMENTS-pinned)
    - Mutation guard: temporarily remove `{tldr}` from `recommend-cell.md.tmpl` and re-run the sentinel test — the test MUST fail with a message containing `_SENTINEL_TLDR_` (this is a manual sanity check; not asserted in CI but proves the test catches drift). Restore the template before committing.
    - The sentinel array in `cell_templates_both_reference_all_fields` contains the literal substring `"(suspect)"` (with parentheses) — verifiable via `grep -c '"(suspect)"' crates/alloc-bench-aggregator/src/html.rs` returning at least 1
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p alloc-bench-aggregator --lib html::tests::tinytemplate_compiles_recommend_cell_templates html::tests::cell_templates_both_reference_all_fields html::tests::cell_template_context_excludes_score_and_axes -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <done>Three new tests added to `html.rs::tests`; all three exit 0 individually; full lib test suite (`cargo test -p alloc-bench-aggregator --lib`) exits 0. The WR-01 sentinel test name matches REQUIREMENTS CELL-02 verbatim. Templates are gated by both the compile-time tinytemplate test and the cross-surface field-presence sentinel test before any wiring depends on them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tinytemplate template files (.tmpl) → rendered output | Template files are checked-in source code; field substitution operates on `CellTemplateContext` data already validated upstream by Phase 7's `top_n_cells` (which produces only ASCII kebab-case allocator/env strings + `&'static str` axis labels + Phase-7-derived TL;DR prose). No untrusted input crosses this boundary at template-render time. |
| Template field-set drift (struct ↔ tmpl) | Compile-time + test-time gate. `tinytemplate_compiles_recommend_cell_templates` catches missing fields at `cargo test`; `cell_templates_both_reference_all_fields` catches single-side field omissions. |
| `*(suspect)*` byte-identity | Tampering with one template's annotation form (e.g., `<em>(suspect)</em>` in HTML) without the other → drift test fails on the `(suspect)` substring assertion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | recommend-cell.{md,html}.tmpl single-template-side edit (a contributor adds {composite_score} only to HTML) | mitigate | `cell_templates_both_reference_all_fields` sentinel test fails on the omitted side; CI gate is `cargo test --workspace`. |
| T-08-02 | Information disclosure | XSS via tldr / strengths label injection in HTML render | accept | `CellRecommendation.tldr` is generated by Phase 7's `format_tldr` from internal axis labels and ASCII-validated allocator/env strings; `strengths` etc. are `&'static str` axis labels from `MEASUREMENT_AXES`. Tinytemplate's DEFAULT formatter HTML-escapes `<`/`>`/`&`/`"`. No untrusted input source. |
| T-08-03 | Tampering | Tinytemplate compile failure shipped via missed `\{` escape | mitigate | `tinytemplate_compiles_recommend_cell_templates` test catches at `cargo test` time per RESEARCH §Pitfall 1. |
| T-08-SC | Tampering | npm/pip/cargo install of new dependencies | accept | NO new runtime dependencies in Phase 8 (CONTEXT.md "Out of Scope" + 08-RESEARCH.md §Constraint). `tinytemplate = "1"` is already in workspace deps; `serde`/`serde_json` already present. Plan 01 adds zero `[dependencies]` entries to any Cargo.toml. |
</threat_model>

<verification>
1. Build gate: `cargo build -p alloc-bench-aggregator --lib` exits 0
2. Test gate (Plan 01 specific): `cargo test -p alloc-bench-aggregator --lib html::tests::tinytemplate_compiles_recommend_cell_templates html::tests::cell_templates_both_reference_all_fields html::tests::cell_template_context_excludes_score_and_axes` all pass
3. Regression gate: `cargo test -p alloc-bench-aggregator --lib` exits 0 (no existing test broken)
4. Workspace gate: `cargo test --workspace` exits 0 (full v1.0 byte-identical golden-output suite still green — Plan 01 adds NO bytes to REPORT.md or index.html)
5. Source assertion: both `.tmpl` files reference exactly the field-name set from `CellTemplateContext` (no spurious identifiers, no missing fields)
</verification>

<success_criteria>
- `crates/alloc-bench-aggregator/templates/recommend-cell.md.tmpl` exists with the canonical body shape (header + 4 bullet sections + suspect conditional)
- `crates/alloc-bench-aggregator/templates/recommend-cell.html.tmpl` exists with `<article class="recommend-card" id="recommend-{rank_padded}-{alloc}-{env}">`
- `crates/alloc-bench-aggregator/src/html.rs` exports `pub(crate) const RECOMMEND_CELL_MD`, `pub(crate) const RECOMMEND_CELL_HTML`, `pub(crate) struct CellTemplateContext`, `pub(crate) fn build_cell_template_context`
- The CellTemplateContext struct has fields: rank, rank_padded, alloc, env, tldr, strengths, weaknesses, recommended_for, avoid_for, suspect_flag — and NO composite_score, NO axes
- Three tests pass in `html::tests`: `tinytemplate_compiles_recommend_cell_templates`, `cell_templates_both_reference_all_fields`, `cell_template_context_excludes_score_and_axes`
- The test name `cell_templates_both_reference_all_fields` matches REQUIREMENTS CELL-02 verbatim
- Both templates emit the literal six bytes `(suspect)` (between asterisks) when `suspect_flag = true` — verified by sentinel test
- No mutation of `crates/alloc-bench-core/src/output.rs` (decorate-not-rewrite preserved)
- No new entries in any `Cargo.toml` (no new runtime deps; tinytemplate + serde already present)
- v1.0 golden-output tests still pass (`cargo test --workspace` exits 0) — Plan 01 adds NO bytes to existing REPORT.md or index.html outputs (templates are not yet wired in)
</success_criteria>

<output>
Create `.planning/phases/08-per-cell-artifacts/08-01-SUMMARY.md` when done. Include:
- Field-set of CellTemplateContext (verbatim) and rationale (no composite_score / axes)
- Decision to colocate RECOMMEND_CELL_MD in html.rs (visible to markdown.rs via pub(crate)) for Plan 02 to use
- Whether the sentinel test caught the WR-01-pattern drift in pre-merge mutation testing (manual sanity-check as described in Task 2 acceptance)
- Any tinytemplate compile-time errors hit during template authoring (and how resolved — typically `\{` escape or `{{endfor}}` mismatch per RESEARCH §Pitfall 1, 2)
- Confirmation that no new dependencies were added to any Cargo.toml
</output>
</content>
