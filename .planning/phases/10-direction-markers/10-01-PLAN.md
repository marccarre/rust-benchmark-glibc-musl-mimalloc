---
phase: 10-direction-markers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/alloc-bench-aggregator/src/axes.rs
  - crates/alloc-bench-aggregator/src/markdown.rs
autonomous: true
requirements:
  - DIR-01
  - DIR-02
  - DIR-05
must_haves:
  truths:
    - "User reads any per-scenario allocator-comparison table in REPORT.md and finds the arrow glyph (↑ or ↓) at the end of every measurement column header (throughput ↑, p50 ↓, p95 ↓, p99 ↓, p999 ↓, peak RSS ↓). The 'allocator' column stays plain — it's a label, not a measurement."
    - "User reads REPORT.md and finds a one-line legend exactly equal to '↑ higher is better · ↓ lower is better · ⚠ suspect run' above every per-scenario table — placed one blank line below the '## {scenario}' heading and one blank line above the table."
    - "User reads REPORT.md data cells and finds them byte-identical to v1.0 numeric formatting ({:.0} medians, {:.1} throughputs, {} ns latencies). No '↑' or '↓' character appears in any data row across the entire emitted REPORT.md."
  artifacts:
    - path: "crates/alloc-bench-aggregator/src/axes.rs"
      provides: "pub fn column_header_with_arrow(label: &str, dir: Direction) -> String — single-source-of-truth helper threading Direction::arrow() onto a measurement-column label. Lives next to MEASUREMENT_AXES and Direction so markdown.rs and html.rs both consume the same function (WR-01 cross-surface drift defense)."
      contains: "pub fn column_header_with_arrow"
    - path: "crates/alloc-bench-aggregator/src/markdown.rs"
      provides: "emit_per_scenario_tables emits arrow-decorated column headers and the verbatim legend row above every per-scenario table. Data-row format string at lines 226-236 is unchanged (DIR-05)."
      contains: "↑ higher is better · ↓ lower is better · ⚠ suspect run"
  key_links:
    - from: "crates/alloc-bench-aggregator/src/markdown.rs"
      to: "crates/alloc-bench-aggregator/src/axes.rs::column_header_with_arrow"
      via: "use crate::axes::{column_header_with_arrow, Direction}"
      pattern: "column_header_with_arrow\\("
    - from: "crates/alloc-bench-aggregator/src/markdown.rs (legend emitter)"
      to: "crates/alloc-bench-aggregator/src/markdown.rs (per-scenario table emitter)"
      via: "blank-line-legend-blank-line-table sequence inside emit_per_scenario_tables"
      pattern: "↑ higher is better · ↓ lower is better · ⚠ suspect run"
---

<objective>
Add the `column_header_with_arrow` helper to `axes.rs` (Phase 6 SSoT module) and use it in `markdown.rs::emit_per_scenario_tables` to (a) decorate every measurement column header with the appropriate `↑`/`↓` glyph drawn from `Direction::arrow()` and (b) emit a one-line legend above every per-scenario table. Cells stay byte-identical to v1.0 — markers live in headers only.

Purpose: Surface direction semantics for the human reader on the Markdown surface. The helper centralizes the format pattern so the HTML surface (Plan 10-02) consumes the same function — no parallel implementations, drift defended by tests.

Output: 1 new helper in `axes.rs`, modified `emit_per_scenario_tables` in `markdown.rs`, 4 new tests gating DIR-01/DIR-02/DIR-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-direction-markers/10-CONTEXT.md
@.planning/phases/10-direction-markers/10-UI-SPEC.md
@crates/alloc-bench-aggregator/src/axes.rs
@crates/alloc-bench-aggregator/src/markdown.rs

<interfaces>
<!-- Key types and helpers from axes.rs (Phase 6 SSoT). Use these directly — do not redefine. -->

From crates/alloc-bench-aggregator/src/axes.rs:
```rust
pub enum Direction { Higher, Lower }

impl Direction {
    pub const fn arrow(self) -> char {
        // Returns '\u{2191}' (↑) for Higher, '\u{2193}' (↓) for Lower
    }
}

pub struct AxisSpec { /* … */ }
pub const MEASUREMENT_AXES: [AxisSpec; 8] = [ /* … */ ];
```

This plan ADDS:
```rust
pub fn column_header_with_arrow(label: &str, dir: Direction) -> String;
// Returns format!("{label} {arrow}", arrow = dir.arrow())
// e.g., column_header_with_arrow("throughput", Direction::Higher) == "throughput ↑"
//       column_header_with_arrow("p99", Direction::Lower)         == "p99 ↓"
```

The helper returns `String` (not `Cow<'static, str>`) per CONTEXT.md decision: call sites are sparse
(6 markdown columns + 4 HTML axis labels) — heap allocation is not a concern.
</interfaces>

<conventions>
- Aggregator decorate-not-rewrite — v1 input schema in `alloc-bench-core/src/output.rs` is LOCKED. This plan touches only the aggregator emit path.
- Byte-identical-output discipline — alphabetical iteration via `BTreeMap`/`BTreeSet`; data-row format string at `markdown.rs` lines 226-236 stays unchanged.
- Conventional-commit prefix: `feat(10-01)` for the helper + table changes, `test(10-01)` for the test commits, or combined `feat(10-01): add column_header_with_arrow + decorate per-scenario tables`.
</conventions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add column_header_with_arrow helper to axes.rs</name>
  <files>crates/alloc-bench-aggregator/src/axes.rs</files>
  <read_first>
    - crates/alloc-bench-aggregator/src/axes.rs (full file — Phase 6 SSoT; understand `Direction` enum + `arrow()` const fn placement and the existing `#[cfg(test)] mod tests` block at the bottom)
  </read_first>
  <behavior>
    - Test 1: column_header_with_arrow("throughput", Direction::Higher) returns "throughput ↑" (literal U+2191)
    - Test 2: column_header_with_arrow("p99", Direction::Lower) returns "p99 ↓" (literal U+2193)
    - Test 3: helper threads exactly one ASCII space between label and glyph (no double-space, no leading/trailing whitespace, no NBSP)
  </behavior>
  <action>
    Add the helper to `axes.rs` (per D-01 helper-home decision in CONTEXT.md):

    ```
    pub fn column_header_with_arrow(label: &str, dir: Direction) -> String {
        format!("{label} {arrow}", arrow = dir.arrow())
    }
    ```

    Place it directly below `impl Direction { pub const fn arrow(...) }` and above
    `pub const MEASUREMENT_AXES`. Use `String` return type per CONTEXT decision (D-claude-discretion-2):
    call sites are sparse and `Cow<'static, str>` adds discipline cost without a profiler signal.

    Add a single combined unit test inside the existing `#[cfg(test)] mod tests` block:
    `column_header_with_arrow_threads_glyph_after_label` — asserts all three behaviors above
    (Higher case, Lower case, single-ASCII-space delimiter). The test uses literal `\u{2191}` /
    `\u{2193}` in expected strings to guard against accidental copy-paste of look-alike glyphs.

    Do NOT modify any existing function in `axes.rs`. Do NOT introduce a `Cow` return — that
    pattern (`polar.rs::axis_label_for_chart`) is reserved for axis-label helpers under WR-04
    borrow pressure; this helper has no such pressure.
  </action>
  <verify>
    <automated>cargo test -p alloc-bench-aggregator axes::tests::column_header_with_arrow_threads_glyph_after_label -- --exact</automated>
    <automated>cargo test -p alloc-bench-aggregator axes:: -- --quiet</automated>
  </verify>
  <acceptance_criteria>
    - Source: `axes.rs` exports `pub fn column_header_with_arrow(label: &str, dir: Direction) -> String`
    - Source: helper body is exactly `format!("{label} {arrow}", arrow = dir.arrow())` (or equivalent — no extra whitespace handling, no allocation reduction tricks)
    - Test: `axes::tests::column_header_with_arrow_threads_glyph_after_label` passes asserting Higher → "throughput ↑", Lower → "p99 ↓", single-space delimiter
    - Test: existing 5 axes::tests still pass (Phase 6 invariants preserved)
    - Byte-level: helper output uses U+2191 / U+2193 (validated by literal-byte expected strings in the test)
  </acceptance_criteria>
  <done>
    `cargo test -p alloc-bench-aggregator axes::` passes with one additional test (total = 6 axes:: tests). The helper is callable from `markdown.rs` (Task 2) and `html.rs` (Plan 10-02) — single source of truth established.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Decorate per-scenario tables in markdown.rs with arrows + legend</name>
  <files>crates/alloc-bench-aggregator/src/markdown.rs</files>
  <read_first>
    - crates/alloc-bench-aggregator/src/markdown.rs (lines 130-250 — full `emit_per_scenario_tables` function: header line at 162-165, separator at line 166, data-row loop at 226-236)
    - crates/alloc-bench-aggregator/src/axes.rs (verify `column_header_with_arrow` and `Direction::{Higher, Lower}` are exported and importable as `use crate::axes::{column_header_with_arrow, Direction};`)
  </read_first>
  <behavior>
    - Test 1 (`scenario_table_headers_carry_direction_markers`): rendered REPORT.md per-scenario table headers end with the expected glyph for each measurement column — `throughput ↑`, `p50 ↓`, `p95 ↓`, `p99 ↓`, `p999 ↓`, `peak RSS ↓`. The `allocator` column stays plain (no arrow).
    - Test 2 (`legend_row_above_each_scenario_table`): the literal string `↑ higher is better · ↓ lower is better · ⚠ suspect run` appears once per scenario in the rendered REPORT.md, placed one blank line below the `## {scenario}` heading and one blank line above the table header. Uses interpunct U+00B7 — NOT bullet U+2022, NOT full stop U+002E.
    - Test 3 (`data_cells_contain_no_direction_markers`): the rendered REPORT.md contains zero `↑` or `↓` characters in any data row (DIR-05). Implementation: strip the legend lines and the header lines, scan the remainder for the two glyphs, assert count == 0.
  </behavior>
  <action>
    Modify `emit_per_scenario_tables` in `markdown.rs` (existing function spanning lines 136-240):

    1. **Add import** at the top of `markdown.rs`: `use crate::axes::{column_header_with_arrow, Direction};` (extending the existing `use crate::axes::*` if present; keep alphabetical use order per project convention).

    2. **Emit the legend row** between the scenario heading and the table header. The current code at lines 160-161 emits `## {scenario}\n` followed by a blank line. After that blank line, emit:

       `↑ higher is better · ↓ lower is better · ⚠ suspect run\n\n`

       (Verbatim from CONTEXT D-02 / ROADMAP DIR-02. Interpunct is U+00B7 — encode as a Rust string literal `"\u{00b7}"` or paste the character directly; test fixture validates the byte.) The trailing `\n\n` produces one blank line above the table header.

    3. **Replace the header line** at lines 162-165 from the current literal `| allocator | throughput | p50 | p95 | p99 | p999 | peak RSS |` to a dynamically-built string using `column_header_with_arrow`:

       ```
       writeln!(
           out,
           "| allocator | {} | {} | {} | {} | {} | {} |",
           column_header_with_arrow("throughput", Direction::Higher),
           column_header_with_arrow("p50",        Direction::Lower),
           column_header_with_arrow("p95",        Direction::Lower),
           column_header_with_arrow("p99",        Direction::Lower),
           column_header_with_arrow("p999",       Direction::Lower),
           column_header_with_arrow("peak RSS",   Direction::Lower),
       )?;
       ```

       The `allocator` column stays plain (it's a label, not a measurement — per CONTEXT D-01).

    4. **Do NOT modify** the separator line at line 166 (`|---|---|---|---|---|---|---|` is byte-stable — column count is unchanged).

    5. **Do NOT modify** the data-row format string at lines 226-236 — DIR-05 mandates cells stay numeric. Existing `{:.0}` / `{:.1}` / `{}` discipline is preserved.

    Add three tests inside `#[cfg(test)] mod tests` in `markdown.rs`:

    - `scenario_table_headers_carry_direction_markers` — build a minimal `RunSummary` set with one scenario, render via the existing test helper, parse the rendered output, locate the per-scenario table header line, assert it equals (literally) `| allocator | throughput ↑ | p50 ↓ | p95 ↓ | p99 ↓ | p999 ↓ | peak RSS ↓ |`. Use `\u{2191}` / `\u{2193}` literals in the expected string to guard against look-alike glyphs.

    - `legend_row_above_each_scenario_table` — same fixture; assert the rendered output contains `↑ higher is better · ↓ lower is better · ⚠ suspect run` exactly once per scenario, on its own line, with one blank line on each side. Use a simple line-by-line walk: find `## {scenario}`, assert next non-blank non-heading line is the legend, assert the line after is blank, assert the line after that is the table header. Interpunct check: assert the legend bytes contain U+00B7 (`'\u{00b7}'`) at the expected positions, NOT U+2022 (`'•'`) and NOT U+002E (`'.'`).

    - `data_cells_contain_no_direction_markers` — render a multi-scenario fixture, strip every line containing `## ` (scenario headings) AND every line containing the legend literal AND every line containing `↑` or `↓` in a header context (any line between `|---|` separator and the next `## `). For the remaining data rows, count `↑` and `↓` occurrences — must be zero. (Practical implementation: scan all lines that match `^|.*|$` AND are NOT the header line — assert no glyph in any.)
  </action>
  <verify>
    <automated>cargo test -p alloc-bench-aggregator markdown::tests::scenario_table_headers_carry_direction_markers -- --exact</automated>
    <automated>cargo test -p alloc-bench-aggregator markdown::tests::legend_row_above_each_scenario_table -- --exact</automated>
    <automated>cargo test -p alloc-bench-aggregator markdown::tests::data_cells_contain_no_direction_markers -- --exact</automated>
    <automated>cargo test -p alloc-bench-aggregator markdown:: -- --quiet</automated>
    <automated>cargo build -p alloc-bench-aggregator --release</automated>
  </verify>
  <acceptance_criteria>
    - Source: `markdown.rs` imports `column_header_with_arrow` and `Direction` from `crate::axes`
    - Source: `emit_per_scenario_tables` emits the legend row literal `↑ higher is better · ↓ lower is better · ⚠ suspect run` between the scenario heading and the table header
    - Source: header line is built via 6 calls to `column_header_with_arrow` (one per measurement column); `allocator` column stays plain
    - Source: data-row format string at lines 226-236 is byte-identical to its v1.0 form (DIR-05)
    - Test: `markdown::tests::scenario_table_headers_carry_direction_markers` passes — header line equals exactly `| allocator | throughput ↑ | p50 ↓ | p95 ↓ | p99 ↓ | p999 ↓ | peak RSS ↓ |`
    - Test: `markdown::tests::legend_row_above_each_scenario_table` passes — legend appears once per scenario with U+00B7 interpunct
    - Test: `markdown::tests::data_cells_contain_no_direction_markers` passes — zero `↑`/`↓` glyphs in data rows
    - Test: all existing `markdown::tests` continue to pass (no regression of v1.0 invariants beyond the intentional header + legend changes)
    - Byte-level: separator line `|---|---|---|---|---|---|---|` unchanged; column count = 7 unchanged
  </acceptance_criteria>
  <done>
    `cargo test -p alloc-bench-aggregator markdown::` passes with three additional tests. `cargo build --release` succeeds. The Markdown surface now satisfies DIR-01 (column-header glyphs), DIR-02 (legend), and DIR-05 (cells unchanged). Plan 10-02 can consume the same `column_header_with_arrow` helper for the HTML surface.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → CI | Source-code changes flow through PR; CI runs `cargo test` to gate merges |
| CI → committed REPORT.md fixtures | Phase 11 will pin the byte-identical surface; Phase 10 only changes the surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `column_header_with_arrow` glyph drift (e.g., a future contributor uses `→` U+2192 instead of `↑` U+2191) | mitigate | Test `axes::tests::column_header_with_arrow_threads_glyph_after_label` pins literal U+2191 / U+2193 bytes via `\u{2191}` / `\u{2193}` escapes in expected strings. |
| T-10-02 | Tampering | Legend interpunct drift (U+002E or U+2022 substituted for U+00B7) | mitigate | Test `markdown::tests::legend_row_above_each_scenario_table` asserts the legend bytes contain U+00B7 at the expected positions, fails on U+002E or U+2022. |
| T-10-03 | Tampering | Cell-content drift (arrow accidentally injected into data rows) | mitigate | Test `markdown::tests::data_cells_contain_no_direction_markers` asserts zero `↑`/`↓` glyphs in data rows; fires loudly if anyone adds direction markers to cells in a future change. |
| T-10-04 | Tampering | Cross-surface drift (Markdown surface and HTML surface use different format strings) | mitigate | Both surfaces consume `axes::column_header_with_arrow` — the only place where the format pattern lives. Plan 10-02 imports the same helper; helper-callsite test in 10-02 closes the loop. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies in this plan. Only `format!`, `String`, and existing aggregator imports. No package-legitimacy gate triggered. |
</threat_model>

<verification>
- `cargo test -p alloc-bench-aggregator` passes (no regressions in axes::, markdown::, or downstream html::/score::/recommend:: modules — all cells continue to render byte-correctly modulo the intentional header + legend changes).
- `cargo clippy -p alloc-bench-aggregator -- -D warnings` passes (no new warnings introduced).
- `cargo build -p alloc-bench-aggregator --release` succeeds.
- Manual byte-check (no human required for plan completion — confirmation step): `grep -c '↑\|↓' .planning/phases/10-direction-markers/sample-output.md` returns the expected count (6 arrows per per-scenario table × N scenarios + 1 arrow each in the legend rows × N scenarios). The exact count is computable from the fixture used in test 1.
</verification>

<success_criteria>
- DIR-01: Every per-scenario allocator-comparison table in REPORT.md has `↑` or `↓` in every measurement column header. The `allocator` column stays plain.
- DIR-02: A one-line legend `↑ higher is better · ↓ lower is better · ⚠ suspect run` appears above every per-scenario table, between the `## {scenario}` heading (with one blank line on each side).
- DIR-05: All data cells are byte-identical to v1.0 — `{:.0}` / `{:.1}` / `{}` discipline preserved; no `↑`/`↓` glyphs anywhere in data rows.
- Single source of truth: `axes::column_header_with_arrow` is the only function that knows the format. `markdown.rs` and `html.rs` (Plan 10-02) both call it — no parallel implementations.
- Test gate: 4 new tests guarantee the invariants above hold under future refactors.
</success_criteria>

<output>
Create `.planning/phases/10-direction-markers/10-01-SUMMARY.md` when done, capturing:
- Helper signature: `pub fn column_header_with_arrow(label: &str, dir: Direction) -> String`
- Modified function: `markdown::emit_per_scenario_tables` (lines 136-240, before/after diff at the header + legend region)
- Tests added: 1 in `axes::tests`, 3 in `markdown::tests`
- Bytes changed in REPORT.md (compare a sample fixture before/after — informs Phase 11's regen accounting)
- Confirmation that DIR-01, DIR-02, DIR-05 are now satisfied on the Markdown surface
</output>
