---
context: phase
phase: 09-spider-chart
task: null
total_tasks: null
status: in_progress
last_updated: 2026-05-27T19:46:08.420Z
---

<current_state>
Phase 9 (Spider Chart) is paused after **discuss-phase** and **ui-phase** completed cleanly.
Both artifacts are committed on `main`:

- `ee7934f docs(09): smart discuss context` → `09-CONTEXT.md`
- `46748d6 docs(09): UI design contract — spider chart + Pareto column` → `09-UI-SPEC.md`

The next step is **plan-phase** (gsd-phase-researcher → gsd-planner → gsd-plan-checker), which is
the heaviest agent chain in the GSD lifecycle. The pause was triggered by a context-budget warning
at 79% during plan-phase initialization — running the chain would have exceeded the safe envelope.
No code has been written for any v1.1 phase beyond the Phase 7/8 work that is already shipped.
</current_state>

<completed_work>

Earlier in this session (before pausing):

- Phase 7 (Scoring & Top-N): resumed from prior pause, plans applied, checker PASSED, executed both waves, verified.
- Phase 8 (Per-cell artifacts): verifier PASS, 5/5 must-haves. Top-N wired into REPORT + per-cell `.html` fragments.
- Phase 9 smart discuss: 26 decisions captured across 5 grey areas (3 user-facing + 2 inferred). User accepted all recommended answers.
- Phase 9 UI design contract: gsd-ui-researcher produced UI-SPEC.md; gsd-ui-checker scored 5/6 PASS, 1 FLAG (non-blocking, justified), 1 SKIPPED (ui_safety_gate=false).
</completed_work>

<remaining_work>

- Phase 9 plan-phase: research → planner → plan-checker → revise to PASS
- Phase 9 execute-phase: build `crates/alloc-bench-aggregator/src/polar.rs` + chart wiring + Pareto column in top-N table
- Phase 9 code-review + fix chain
- Phase 9 verify-phase (gsd-verifier, goal-backward analysis)
- Phase 10 (Direction Markers, DIR-01..05): full discuss → plan → execute → review → verify cycle
- Phase 11 (Golden-fixture Regen, TEST-01..02): release gate, standalone PR with no production code
- Milestone v1.1 lifecycle: audit → complete → cleanup
</remaining_work>

<decisions_made>

Locked in `09-CONTEXT.md` (committed) — do NOT re-ask:

- **Chart layout**: 1×3 horizontal grid, 340×340 px square, autosize+responsive
- **Heuristic axis distinction**: `(heuristic)` label suffix + `#666` tickfont (vs `#222` measured); WCAG AA at 11px+. Per-tick gridcolor is a Plotly scatterpolar limitation — dual-signal (label suffix + muted tickfont) is the agreed mitigation.
- **Pareto-front overlay**: POLAR-05 **IN SCOPE this phase**. ★ U+2605 glyph in table. macOS host excluded (no Docker image). Includes unit tests for Pareto-frontier calculation over (composite_score↑, image_size_mb↓).
- **polar.rs module shape**: returns `serde_json::Value` (NOT a typed struct). Pre-serialize to `String` for tinytemplate context — tinytemplate cannot render `serde_json::Value` directly.
- **Plotly version**: v2.35.3 pinned with SHA-384 SRI. Safety gate via `html::tests::plotly_sri_hash_unchanged` sentinel test. No JS dependencies beyond the pinned CDN (honors D-02 static `file://` discipline).
- **Axes source-of-truth**: `MEASUREMENT_AXES` const in `crates/alloc-bench-aggregator/src/axes.rs` (`[AxisSpec; 8]`, frozen, alphabetical, with `is_heuristic` flag).
- **Top-N for spider**: `TOP_N_SPIDER = 3` from Phase 7's `recommend.rs`.

Locked in `09-UI-SPEC.md` (committed) — design tokens & section pattern:

- Inherits v1.0 design tokens from `:root` (8-point scale, 4 sizes / 2 weights typography, 60/30/10 chrome).
- Section pattern: `<section class="spider-chart">` mirrors Phase 8's `.top-n-recommendations`.
- Plotly internal margins `{l:30,r:30,t:40,b:30}` are not 8-point multiples — flagged but justified by Plotly defaults (non-blocking).
</decisions_made>

<blockers>

None.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable state.
1. `.planning/phases/09-spider-chart/09-CONTEXT.md` — locked decisions (do not re-ask).
1. `.planning/phases/09-spider-chart/09-UI-SPEC.md` — design contract (8 dimensions, sentinel test).
1. `.planning/STATE.md` — milestone progress.
1. `.planning/ROADMAP.md` — phase order + acceptance criteria for POLAR-01..05 and PARETO-01..03.
1. `crates/alloc-bench-aggregator/src/axes.rs` — `MEASUREMENT_AXES` source-of-truth (read before plan-phase researcher runs).
1. `crates/alloc-bench-aggregator/src/score.rs` — `score_cells`, `top_n`, `CellScore` (Phase 7 outputs polar consumes).
1. `crates/alloc-bench-aggregator/src/recommend.rs` — `TOP_N_SPIDER=3`, `TOP_N_TABLE=5`, `TOP_N_TOTAL=10`.
1. `crates/alloc-bench-aggregator/src/html.rs` (lines 58-66, 28-34, 228-230) — Plotly CDN URL, SRI hash, tinytemplate registration pattern.
1. `crates/alloc-bench-aggregator/templates/index.html.tmpl` (lines 7-19) — existing Plotly CDN tag + SRI integrity + CSP.

## Critical Anti-Patterns (do NOT repeat these)

- **Mutating the v1 input schema**: `crates/alloc-bench-core/src/output.rs` is locked (Phase 1 D-11; sha256 `1bcfb91252eddc2710222abd46b031b85d91267d97a0874fa78d042c15f99a84`). New fields ride sidecar files; the aggregator merges at emit time. Decorate, never rewrite.
- **Non-deterministic iteration**: use `BTreeMap`/`BTreeSet`, never `HashMap`/`HashSet`. Byte-identical output discipline applies to spider-chart trace JSON too.
- **Designing a typed Plotly trace struct**: `polar.rs` returns `serde_json::Value`. Pre-serialize to `String` for tinytemplate. Don't fight the templating engine.
- **Adding JS dependencies**: only the pinned Plotly v2.35.3 CDN (with SHA-384 SRI) is allowed. Anything else breaks D-02 static `file://` discipline.
- **Treating macOS host as a Pareto candidate**: macOS host has no Docker image; excluding it from Pareto consideration is cleaner than treating `image_size_mb` as INFINITY.

## Infrastructure State

- Branch: `main`
- HEAD: `46748d6 docs(09): UI design contract — spider chart + Pareto column`
- Workspace tests: passing as of Phase 8 verification.
- Pre-existing uncommitted state (NOT created by Phase 9 work):
  - `.planning/STATE.md` modified (last touched at Phase 8 close — review before Phase 9 commits).
  - `.planning/phases/08-per-cell-artifacts/VERIFICATION.md` untracked (Phase 8 verifier output that didn't get added to commit `4c17372`).

## Pre-Execution Critique Required

Not applicable — pause is between two ORCHESTRATED steps (ui-phase done, plan-phase next), not between design and execution. The plan-checker provides the equivalent gate before execute-phase runs.

<context>
The autonomous workflow is mid-flight on milestone v1.1, currently at Phase 9 of 11. Phases 7 and 8
shipped earlier this session. Phase 9 design surface is now locked: layout decisions, heuristic-axis
treatment, Pareto-front behaviour, module shape, and Plotly pinning are all in CONTEXT.md and
UI-SPEC.md, both committed. The next chain (researcher → planner → plan-checker) is the heaviest
remaining agent sequence in the milestone — pausing before it preserves a clean restart.
</context>

<next_action>
Start with: `/gsd:autonomous --from 9` in a fresh session.

The autonomous skill detects the existing `09-CONTEXT.md` and `09-UI-SPEC.md` and skips directly to
`plan-phase`. After plan-phase produces a PASS verdict, autonomous continues through execute-phase,
code-review, verify-phase, then advances to Phase 10 and Phase 11, then runs the milestone audit →
complete → cleanup lifecycle.
</next_action>
