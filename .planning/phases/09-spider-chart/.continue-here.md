---
context: phase
phase: 09-spider-chart
task: phase-09-closeout-blocked-on-ui-decision
total_tasks: null
status: paused
last_updated: 2026-05-28T17:00:00Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Auto-completing phase 9 despite UI-REVIEW BLOCKER | Workflow says UI review is "advisory, non-blocking" but the BLOCKER reveals a real UI-SPEC contract violation (single overlay chart vs three small-multiples). | advisory | Pause and ask user before phase.complete; document the divergence. |
| Treating verifier-passed as final word | The verifier passed POLAR-02 reading "small-multiples grid" loosely; the UI-SPEC's explicit `<div class="spider-cell" id="spider-cell-N">` triplet contract was missed. | advisory | When verifier and UI-auditor disagree, surface the conflict to the user. |

<current_state>
Phase 9 close-out is **blocked on a user decision** about a UI-SPEC contract violation. All other Phase 9 close-out steps completed:

- ✅ Code-review fixes applied: 5 commits (CR-01 + WR-01..04). Tree clean.
  - `06bd85f` CR-01 align inline-JS suspect threshold with Rust `< 1_000`
  - `b2ab7a9` WR-01 interpolate `scores.len()` in Matrix-mean reference legend
  - `70cdc3a` WR-02 deterministic + NaN-safe `build_image_sizes` reduction
  - `c4d15c1` WR-03 compute Pareto front on full scores (not truncated top-N)
  - `1cb51a5` WR-04 `axis_label_for_chart` returns `Cow<'static, str>`
  - `794710d` docs(09): add code review fix report
- ✅ Regression gate: `cargo test --workspace` → 248/248 pass (131 aggregator unit + 30 smoke + 81 core + others).
- ✅ Verifier: 5/5 must-haves passed. `09-VERIFICATION.md` written but NOT YET COMMITTED.
- ✅ UI review: `09-UI-REVIEW.md` committed at `1e0727f`. Score: 16/24. **1 BLOCKER, 6 WARNINGs**.

**The conflict:**
- Verifier: POLAR-02 PASSED — `<div id="chart-spider">` exists, reference polygon + 3 cell polygons all rendered.
- UI-auditor: BLOCKER — UI-SPEC required three `<div class="spider-cell" id="spider-cell-N">` containers each receiving its own `Plotly.react` call (small-multiples grid). What shipped is a single `<div id="chart-spider">` with all 4 traces overlaid in one polar plot. The 6 WARNINGs (missing flex CSS on `.spider-grid` and `.spider-cell`, scalar `#666` tickfont instead of per-tick array `['#222','#222','#666','#222','#222','#222','#666','#222']`, missing per-chart Plotly title, etc.) all follow from the single-chart implementation choice.

`09-03-SUMMARY.md` describes the as-built single-chart implementation as if it were correct, masking the divergence from CONTEXT.md and UI-SPEC.md.

**Tree state:**
- Main at `1e0727f`. Tree mostly clean: `09-VERIFICATION.md` is uncommitted (verifier wrote it; orchestrator was about to commit at phase.complete).
- 49 commits ahead of origin/main from this milestone (8 from this session: 5 fixes + 3 docs).

**Pause trigger:** Context at 66% mid-Phase-9-close-out, after UI review BLOCKER surfaced. Remaining work (the user-decision question + `phase.complete` + Phase 10 full cycle + Phase 11 + milestone lifecycle) is too much for 34% remaining budget.
</current_state>

<remaining_work>

### Decision required from user (before phase.complete):

**Q:** UI review found a BLOCKER — implementation shipped a single overlay polar chart instead of the three small-multiples grid required by UI-SPEC.md and CONTEXT.md. How to proceed?

**Options:**
1. **Accept advisory** — UI review is non-blocking per workflow contract. Mark phase 9 complete, defer the small-multiples refactor to a follow-up quick task or v1.2.
2. **Gap closure** — write `09-04-PLAN.md` for a small refactor (3 spider-cell divs + per-cell `Plotly.react` loop + flex CSS + per-tick tickfont array). Estimate: 30-45 min execution. Then re-run verifier+UI-review.
3. **Pivot** — accept advisory now, plan the small-multiples refactor as a standalone phase 11.5 quick-task between phases.

### Phase 9 close-out remaining (after decision):

1. If gap closure chosen: re-plan + execute + re-verify + re-UI-review.
2. **Commit `09-VERIFICATION.md`:** `gsd-sdk query commit "docs(09): add verification report" --files .planning/phases/09-spider-chart/09-VERIFICATION.md`
3. **Mark phase complete:** `gsd-sdk query phase.complete 09` — updates ROADMAP.md, STATE.md, REQUIREMENTS.md.
4. **Commit:** `gsd-sdk query commit "docs(09): mark phase complete" --files .planning/ROADMAP.md .planning/STATE.md .planning/REQUIREMENTS.md`

### Phase 10 (Direction Markers) — full cycle:
- Discuss → Plan → Execute (DIR-01..05) — column-header arrows, axis-label arrows, legend, a11y aria-labels
- `axes::arrow()` already exists (Phase 6); consumers in `markdown.rs`, `html.rs`, and templates need wiring.

### Phase 11 (Golden-fixture Regen) — release gate:
- Standalone PR with no production code. Regenerates fixtures byte-by-byte. Requires Phase 10's direction markers + spider trace JSON to be byte-stable. PR description must list fixture byte counts.

### Milestone v1.1 lifecycle:
- `Skill(skill="gsd-audit-milestone")` — produces v1.1-MILESTONE-AUDIT.md
- `Skill(skill="gsd-complete-milestone", args="v1.1")` — archives to `.planning/milestones/v1.1-ROADMAP.md`
- `Skill(skill="gsd-cleanup")` — interactive dry-run; user approves file deletions

**Resume command:** `/clear` then `/gsd:autonomous --from 9` — SDK will re-discover Phase 9 still incomplete (no SUMMARY for the gap-closure plan yet, or VERIFICATION not committed). After answering the BLOCKER question, the orchestrator continues through phase.complete → Phase 10 → Phase 11 → lifecycle.

</remaining_work>

<decisions_made>

- **Code-review --fix --auto chained successfully:** Single fixer agent applied all 5 in-scope findings (1 Critical + 4 Warning) in one pass. No iteration needed. Each commit was individually verified by `cargo test`.
- **WR-03 fix took option 2 (compute Pareto on full scores):** Eliminates the user-facing ambiguity of "Top-10 Pareto" vs global Pareto — recommendations now carry global Pareto-front membership semantics. Test sentinel survives.
- **Verifier passed POLAR-02 on the single-chart implementation:** Reading the requirement loosely as "small-multiples grid" — the `<div>` with `class="spider-grid"` was deemed sufficient. UI-auditor disagreed because UI-SPEC contained a much more specific triplet contract. This is a real divergence in expectations that the user should resolve.
- **UI review committed despite BLOCKER:** Workflow contract says UI review is advisory and non-blocking. The BLOCKER is documented in `09-UI-REVIEW.md` for the user's review.
- **Pause at 66% context:** before phase.complete to give user time to decide on the BLOCKER + leave budget for Phase 10 / 11 / lifecycle work.

</decisions_made>

<blockers>

**User decision required: how to handle the UI-REVIEW BLOCKER (single chart vs three small-multiples)?**

Until decided, phase 9 cannot be marked complete cleanly. Options 1/2/3 above.

Note: there are 49 unpushed commits ahead of origin/main from this milestone; user has not requested push. Don't push without explicit approval.

</blockers>

## Required Reading (in order)

1. `.planning/phases/09-spider-chart/09-UI-REVIEW.md` — the BLOCKER finding (read first)
2. `.planning/phases/09-spider-chart/09-VERIFICATION.md` — the verifier's PASS for context
3. `.planning/phases/09-spider-chart/09-UI-SPEC.md` — the original spec contract (Layout section)
4. `.planning/phases/09-spider-chart/09-CONTEXT.md` — locked decisions including "Small-multiples grid"
5. `crates/alloc-bench-aggregator/templates/index.html.tmpl` lines 297-313 — what was actually built
6. `~/.claude/get-shit-done/workflows/autonomous.md` §3d (Post-Execution Routing — gaps_found path)
7. `~/.claude/get-shit-done/workflows/execute-phase.md` §9 (update_roadmap)

## Infrastructure State

- Git: `main` at `1e0727f`. Tree has one uncommitted file: `.planning/phases/09-spider-chart/09-VERIFICATION.md` (verifier output, ready to commit at phase.complete).
- 49 commits ahead of origin/main from this milestone.
- All 248 workspace tests pass.
- `just aggregate` produces:
  - `report/index.html` — `<div id="chart-spider">` + 4 scatterpolar traces overlaid (currently single-chart; would become 3 small-multiples after gap closure)
  - `report/REPORT.md` — `## Top 10 cells` table with `| Pareto |` column with `★` (U+2605) for global Pareto-front cells (post-WR-03 fix)

## Critical Anti-Patterns (do NOT repeat)

- **Marking phase complete with an unresolved BLOCKER:** the workflow's "advisory" framing for UI review is true *in general*, but a BLOCKER finding indicates a real spec-contract violation that should at least be surfaced to the user before phase.complete.
- **Treating verifier-PASS as the only signal:** the verifier reads requirements semi-literally; the UI-auditor reads the UI-SPEC literally. When they disagree, surface the conflict.
- **Re-running fixer with `--auto`:** the initial fixer pass succeeded on iteration 1; running `--auto` would just re-review and confirm clean. Skip if status is `all_fixed`.

<context>
This is the FIFTH pause for Phase 9. Prior pauses: plan-phase boundary (75%), execute-phase boundary (66%), mid-Wave-1-merge (65%), post-Wave-2-merge (67%), and now mid-close-out at 66%. Phase 9 has consistently been the budget-hungriest phase of the milestone — UI-SPEC + 3 plans + 2 waves + verifier + code-review chain + UI-review chain is more than 2x the budget of the 2-plan phases (6, 7, 8) that came before. Each pause has been clean: no orphan commits, no half-merged worktrees, all decisions documented in `.continue-here.md` for the next session.

The UI-REVIEW BLOCKER is the first instance in this milestone where verifier and UI-auditor have disagreed. The conflict is real: UI-SPEC.md is explicit about small-multiples; the implementation shipped a single overlay; `09-03-SUMMARY.md` describes the as-built without flagging the divergence. This is salvageable in a 30-45 min gap-closure plan but requires user decision first.
</context>

<next_action>
**User chose Option 2 (gap closure). 09-04-PLAN.md committed at `c6cd3ad`. Plan is ready to execute.**

1. `cd /Users/marc.carre/src/rust-benchmark-glibc-musl-mimalloc`
2. `git status --short` — only `09-VERIFICATION.md` is uncommitted (DO NOT delete; will be regenerated by re-verifier after gap closure).
3. **Execute 09-04-PLAN.md:**
   - Option A: `Skill(skill="gsd-execute-phase", args="9 --no-transition")` — SDK picks up the unexecuted plan automatically.
   - Option B: Dispatch a single `gsd-executor` agent in worktree isolation with the plan path. ~30-45 min, 6 tasks (refactor html.rs, refactor template, update CSS, add tests, verify just aggregate, write SUMMARY).
4. **After gap closure executes:**
   - Re-run `gsd-verifier` (POLAR-01..05) — should still PASS.
   - Re-run `gsd-ui-auditor` — BLOCKER + 6 WARNINGs should close; expect ≥20/24.
   - Commit fresh `09-VERIFICATION.md` and (overwritten) `09-UI-REVIEW.md`.
   - `gsd-sdk query phase.complete 09` → commit ROADMAP/STATE/REQUIREMENTS update.
5. Continue per `<remaining_work>`: Phase 10 (full cycle) → Phase 11 → milestone lifecycle.
6. **Resume command:** `/clear` then `/gsd:autonomous --from 9` — SDK will see 09-04-PLAN.md exists with no SUMMARY, recognize phase 9 still incomplete, run executor, then verifier/UI-review/complete, then advance to Phase 10.
</next_action>
