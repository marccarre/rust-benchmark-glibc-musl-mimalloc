---
phase: 03-docker-matrix-local-orchestration
plan: "03"
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - justfile
autonomous: true
requirements: [DOCK-09, ORCH-01, ORCH-02]
tags: [justfile, orchestration, docker-run, numa, cgroup, macos-host, bench-all, dive-check]

must_haves:
  truths:
    - "User runs `just build alpine jemalloc` and the recipe builds `alloc-bench:jemalloc-alpine` with `--platform linux/amd64` and OCI annotations populated from --build-arg."
    - "User runs `just run debian-slim ptmalloc` and the recipe runs the image with `--cpus=4 --memory=4g --cpuset-cpus=0-3 --platform linux/amd64`, mounts ./results, and writes `results/ptmalloc-debian-slim.json`."
    - "User runs `just bench-cell wolfi mimalloc` and the recipe builds + runs the cell sequentially."
    - "User runs `just bench-all` and the recipe iterates all 18 valid (env, alloc) tuples sequentially with per-cell error capture, writes per-cell JSON to results/, and ends with the D-12 stdout summary table (alloc/env/status/ticks_per_s_p50)."
    - "User runs `just bench-all-smoke` and the recipe runs the matrix with --warmup 1s --duration 5s per scenario for fast dev iteration."
    - "User runs `just bench-host` on macOS and the recipe builds + runs natively (no Docker), writes `results/host-system.json`."
    - "User runs `just dive-check alpine jemalloc` and the recipe runs `dive --ci` against alloc-bench:jemalloc-alpine using .dive-ci."
    - "User runs `just dive-check-all` and the recipe runs dive against every image in the matrix."
    - "User runs `just clean-images` and the recipe removes all `alloc-bench:*` tags."
    - "User runs `just build debian-slim mallocng` and the recipe HARD-REJECTS with a clear error before any docker invocation (D-04 cross-libc rejection)."
  artifacts:
    - path: "justfile"
      provides: "All Phase 3 recipes: build, run, bench-cell, bench-all, bench-all-smoke, bench-host, dive-check, dive-check-all, clean-images"
      contains: "bench-all:"
  key_links:
    - from: "justfile build recipe"
      to: "docker buildx build --platform linux/amd64 -f docker/{env}.Dockerfile"
      via: "Justfile shebang recipe with case statement mapping env → target triple"
      pattern: "docker buildx build"
    - from: "justfile run recipe"
      to: "docker run --cpus=4 --memory=4g --cpuset-cpus=0-3 --platform linux/amd64"
      via: "Justfile shebang recipe with mkdir + chmod 0777 results/ before docker run (Pitfall §4)"
      pattern: "--cpus="
    - from: "justfile bench-all recipe"
      to: "_matrix_cells hard-coded list of 18 valid (env, alloc) tuples"
      via: "while-read loop over the heredoc; per-cell error capture"
      pattern: "_matrix_cells"
    - from: "justfile build recipe"
      to: "D-04 cross-libc rejection case statement"
      via: "case '{{env}}-{{alloc}}' before any docker call"
      pattern: "mallocng is the musl|ptmalloc is the glibc"
    - from: "justfile bench-host recipe"
      to: "cargo run --release -p alloc-bench-cli (no Docker)"
      via: ".cargo/config.toml's target-cpu=native is honored automatically"
      pattern: "results/host-system.json"
---

<objective>
Extend `justfile` with the full Phase-3 recipe surface (build, run, bench-cell, bench-all, bench-all-smoke, bench-host, dive-check, dive-check-all, clean-images) so the entire allocator-runtime matrix runs reproducibly via `just <recipe>` calls. The Justfile is the single user-facing entrypoint for Phase 3 — all `docker buildx build` and `docker run` invocations live here, parameterized by positional args (D-10) and overridable env vars (D-17).

Purpose: The Dockerfiles from Plans 01 and 02 are inert without orchestration. This plan wires them into a sequential 18-cell matrix loop with per-cell error capture (Discretion: bench-all keeps going past a failed cell, recording status), Apple Silicon platform pinning (RESEARCH §Pitfall 3), distroless nonroot mount fix (RESEARCH §Pitfall 4 — host-side `mkdir -p results && chmod 0777 results`), and the D-04 cross-libc rejection.

Output: One file (`justfile`) extended with ~9 new recipes plus the `_matrix_cells` private heredoc. The existing `dce-check` and `run-all-smoke` recipes from Phase 2 must remain functional. No Cargo changes (Phase 3 adds zero new Rust crates per RESEARCH).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-docker-matrix-local-orchestration/03-CONTEXT.md
@.planning/phases/03-docker-matrix-local-orchestration/03-RESEARCH.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@justfile
@docker/alpine.Dockerfile
@docker/debian-slim.Dockerfile
@docker/distroless-cc.Dockerfile
@docker/distroless-static.Dockerfile
@docker/scratch.Dockerfile
@docker/wolfi.Dockerfile
@.dive-ci
@.dockerignore

<interfaces>
<!-- Existing justfile contract (Phase 2 — to be extended, NOT replaced).
     Verified live 2026-05-19. -->

From justfile (current — DO NOT REMOVE):
  default: → @just --list
  dce-check ALLOCATOR='system': → bash scripts/dce_check.sh {{ALLOCATOR}}
  run-all-smoke OUTPUT='/tmp/alloc-bench-runall.json': → cargo build --release + run-all + jq summary

From CONTEXT.md D-10 — positional-arg recipes (≤ 2 positional args):
  just build {env} {alloc}
  just run {env} {alloc}
  just bench-cell {env} {alloc}
  just dive-check {env} {alloc}

From CONTEXT.md D-04 — cross-libc rejections (HARD ERROR, not silent skip):
  Glibc envs (debian-slim, distroless-cc, wolfi) accept ALLOC ∈ {ptmalloc, jemalloc, mimalloc}.
  Musl envs (alpine, distroless-static, scratch) accept ALLOC ∈ {mallocng, jemalloc, mimalloc}.
  Cross combos (mallocng on glibc, ptmalloc on musl) — print [ERR] message + exit 1.

From CONTEXT.md D-15 + RESEARCH.md §Pitfall 3 (Apple Silicon):
  Every docker buildx build AND docker run MUST pass --platform linux/amd64.
  Default flags (overridable via D-17 env vars): --cpus=4 --memory=4g --cpuset-cpus=0-3.

From CONTEXT.md D-17 — override knobs (env vars consumed by `just run`):
  BENCH_CPUS (default 4)
  BENCH_MEMORY (default 4g)
  BENCH_CPUSET (default 0-3)

From CONTEXT.md D-11 + RESEARCH.md §"Pattern 2" — bench-all sequencing:
  Sequential, NOT parallel (PITFALLS §1.3 spirit — kernel page cache, NUMA, thermal pollution).
  Per-cell logs streamed with [{alloc}-{env}] prefix.
  Per-cell JSON written immediately to results/{alloc}-{env}.json.
  Per-cell error capture (Discretion): one failed cell does not abort the rest.
  End-of-run stdout summary table (D-12): "alloc env status ticks_per_s_p50" — jq the multithread scenario's metrics.ticks_per_s.

From CONTEXT.md D-13 — bench-all-smoke:
  Runs the matrix with reduced warmup/duration (1s + 5s per scenario) for fast dev iteration.
  Implementation choice: pass overrides through to `run-all` via env vars or CLI flags. Note: existing CLI run-all does NOT accept --warmup/--duration overrides (per `crates/alloc-bench-cli/src/main.rs` Cmd::RunAll definition — only --output and --seed). Per RESEARCH §"Pattern 2" line "If run-all does NOT yet accept warmup/duration overrides, plan-phase replaces this with per-scenario overrides at Justfile level."
  Decision in this plan: Implement bench-all-smoke as a sibling loop that invokes individual scenario subcommands (multithread, spmc, mpsc, ...) directly with --warmup 1s --duration 5s and aggregates per-cell into results/{alloc}-{env}.json by overwriting with the LAST scenario's JSON — this is the lowest-impact implementation that doesn't require modifying the CLI. Document this trade-off in 03-03-SUMMARY.md.
  ALTERNATIVE (cleaner but cross-plan): introduce `--warmup` and `--duration` flags on the `run-all` subcommand. NOT done in this plan (would touch crates/* and trigger a re-test cycle that's out of scope for the orchestration plan).
  CHOSEN: Use the simplest implementation — bench-all-smoke calls `bench-all` with a BENCH_SMOKE=1 env var that the bench-all recipe reads to pass `--seed 7` only (and accepts that smoke vs full is the "duration of run-all" — currently 60s default). The smoke recipe instead just runs run-all with the existing defaults; "smoke" semantics in Phase 3 mean "we don't care about full 60s warmup; just confirm the matrix end-to-end works". The Phase-2 `run-all` already uses warmup=1s + duration=5s per scenario per CONTEXT 02 D — see crates/alloc-bench-cli/src/run.rs default_scenarios. So `bench-all-smoke` is `bench-all` invoked the same way as `bench-all`; the SMOKE distinction is captured via reduced full-matrix duration (run-all is already ~60s per cell), and a flag in the SUMMARY reporting mode. THIS IS THE FINAL CHOICE.

From CONTEXT.md D-18, D-19 — bench-host:
  macOS-native, libmalloc only (no jemalloc/mimalloc on macOS).
  Output: results/host-system.json (literal filename, success criterion 4).
  Build flags: target-cpu=native (already set by .cargo/config.toml; no override needed).
  Detect host triple via `rustc -vV | awk '/^host:/ {print $2}'` and emit it in stdout for traceability.
  The bench's metrics::env::read_env automatically populates os="macos", docker_image=null (env var unset), target_triple=<host>.

From RESEARCH.md §Pitfall 4 (distroless nonroot mount):
  Before `docker run -v $(pwd)/results:/out`, the host MUST `mkdir -p results && chmod 0777 results` (or chown to UID 65532). The simplest cross-platform approach is `chmod 0777` — harmless on macOS where Docker Desktop mounts as host user anyway, mandatory on Linux runners where distroless nonroot binary needs write access.

From RESEARCH.md §"Pattern 2" — _matrix_cells hard-coded list (18 entries):
  glibc envs first (alphabetical by env, alphabetical by alloc), then musl envs.
  Format: "<env> <alloc>" per line, one per heredoc line.
  Cross-libc combos are ABSENT from the list (structural skip, not conditional).

From CONTEXT.md D-14 — clean-images, dive-check, dive-check-all:
  clean-images: docker images --filter "reference=alloc-bench:*" → docker rmi -f
  dive-check {env} {alloc}: dive --ci alloc-bench:{alloc}-{env} --ci-config .dive-ci
  dive-check-all: loop over _matrix_cells, run dive-check per cell.

From CONTEXT.md D-08 + Plan 01/02 Dockerfile ARGs — build-arg values the build recipe must compute:
  OCI_VERSION = $(grep -m1 '^version' crates/alloc-bench-cli/Cargo.toml | cut -d'"' -f2)  →  "0.1.0"
  OCI_REVISION = $(git rev-parse HEAD)
  OCI_CREATED = $(date -u +%Y-%m-%dT%H:%M:%SZ)
  RUST_VERSION = 1.91 (matches rust-toolchain.toml)
  ALLOC = positional arg
  TARGET = derived from env (glibc → x86_64-unknown-linux-gnu, musl → x86_64-unknown-linux-musl)

From RESEARCH.md §"Environment Availability":
  jq is required for D-12 summary table (verify presence in build-host recipe; macOS dev box has it).
  dive is OPTIONAL on host: fall back to `docker run --rm wagoodman/dive:latest` per RESEARCH §Code Examples §8.

From CONTEXT.md "code_context" — DOCKER_IMAGE env: ALREADY SET in each Dockerfile's runtime stage (Plans 01/02). Justfile does NOT need to pass --env DOCKER_IMAGE.
</interfaces>

<reference_recipe_shape>
<!-- The shape the executor should produce. Concrete identifiers below; full
     implementation reasoned via the inline comments in each recipe body.
     Total file is ~150-180 lines including the existing 24 lines from Phase 2. -->

# justfile (Phase 3 additions — KEEP existing default/dce-check/run-all-smoke recipes)

# ──────────────────────────────────────────────────────────────────────
# Phase 3: Docker matrix recipes
# ──────────────────────────────────────────────────────────────────────

# Existing (from Phase 2):
#   default: → @just --list
#   dce-check ALLOCATOR='system': → bash scripts/dce_check.sh
#   run-all-smoke OUTPUT='/tmp/alloc-bench-runall.json': → cargo build + run-all + jq

# Build one cell. Validates (env, alloc) and hard-rejects cross-libc combos.
# Sets --platform linux/amd64 (Apple Silicon dev box defaults to arm64 — Pitfall 3).
build env alloc:
    #!/usr/bin/env bash
    set -euo pipefail
    # D-04 cross-libc rejection — HARD ERROR.
    case "{{env}}-{{alloc}}" in
        debian-slim-mallocng|distroless-cc-mallocng|wolfi-mallocng)
            echo "[ERR] mallocng is the musl libc allocator; cannot run on glibc env '{{env}}'" >&2
            exit 1 ;;
        alpine-ptmalloc|distroless-static-ptmalloc|scratch-ptmalloc)
            echo "[ERR] ptmalloc is the glibc libc allocator; cannot run on musl env '{{env}}'" >&2
            exit 1 ;;
    esac
    # Map env → target triple.
    case "{{env}}" in
        debian-slim|distroless-cc|wolfi)            TARGET="x86_64-unknown-linux-gnu" ;;
        alpine|distroless-static|scratch)           TARGET="x86_64-unknown-linux-musl" ;;
        *) echo "[ERR] unknown env '{{env}}'" >&2; exit 1 ;;
    esac
    # Compute OCI annotation values.
    OCI_VERSION=$(grep -m1 '^version' crates/alloc-bench-cli/Cargo.toml | cut -d'"' -f2)
    OCI_REVISION=$(git rev-parse HEAD)
    OCI_CREATED=$(date -u +%Y-%m-%dT%H:%M:%SZ)
    docker buildx build \
        --platform linux/amd64 \
        -f docker/{{env}}.Dockerfile \
        --build-arg ALLOC={{alloc}} \
        --build-arg TARGET="$TARGET" \
        --build-arg RUST_VERSION=1.91 \
        --build-arg OCI_VERSION="$OCI_VERSION" \
        --build-arg OCI_REVISION="$OCI_REVISION" \
        --build-arg OCI_CREATED="$OCI_CREATED" \
        --tag alloc-bench:{{alloc}}-{{env}} \
        --load .

# Run one cell. Pre-creates ./results with permissive perms (Pitfall §4
# — distroless nonroot UID 65532 needs write access).
run env alloc:
    #!/usr/bin/env bash
    set -euo pipefail
    mkdir -p results
    chmod 0777 results
    : "${BENCH_CPUS:=4}"
    : "${BENCH_MEMORY:=4g}"
    : "${BENCH_CPUSET:=0-3}"
    docker run --rm \
        --platform linux/amd64 \
        --cpus="${BENCH_CPUS}" --memory="${BENCH_MEMORY}" --cpuset-cpus="${BENCH_CPUSET}" \
        -v "$(pwd)/results:/out" \
        alloc-bench:{{alloc}}-{{env}} \
        run-all --output /out/{{alloc}}-{{env}}.json --seed 7

# Build + run one cell.
bench-cell env alloc:
    just build {{env}} {{alloc}}
    just run {{env}} {{alloc}}

# The 18-cell hard-coded valid (env, alloc) tuple list. Cross-libc combos
# are ABSENT (structural skip — D-04 + Pattern 2). Order: all glibc first
# (cargo-chef BuildKit cache benefits from grouping by libc family).
_matrix_cells := '''
debian-slim ptmalloc
debian-slim jemalloc
debian-slim mimalloc
distroless-cc ptmalloc
distroless-cc jemalloc
distroless-cc mimalloc
wolfi ptmalloc
wolfi jemalloc
wolfi mimalloc
alpine mallocng
alpine jemalloc
alpine mimalloc
distroless-static mallocng
distroless-static jemalloc
distroless-static mimalloc
scratch mallocng
scratch jemalloc
scratch mimalloc
'''

# Build + run the full 18-cell matrix sequentially with per-cell error capture.
# D-11 (sequential), D-12 (stdout summary table at end).
bench-all:
    #!/usr/bin/env bash
    set -uo pipefail   # NOT -e — we want to continue past per-cell failures.
    declare -a results=()
    while IFS= read -r line; do
        [[ -z "$line" ]] && continue
        env="${line%% *}"
        alloc="${line##* }"
        echo
        echo "════════════════════════════════════════════════════════"
        echo "[${alloc}-${env}] starting"
        echo "════════════════════════════════════════════════════════"
        if just bench-cell "$env" "$alloc" 2>&1 | sed "s/^/[${alloc}-${env}] /"; then
            results+=("OK   ${alloc}-${env}")
        else
            results+=("FAIL ${alloc}-${env}")
        fi
    done <<< '{{_matrix_cells}}'
    echo
    echo "════════════════════════════════════════════════════════"
    echo "Matrix summary"
    echo "════════════════════════════════════════════════════════"
    printf '%s\n' "${results[@]}"
    echo
    echo "alloc env status ticks_per_s_p50"
    while IFS= read -r line; do
        [[ -z "$line" ]] && continue
        env="${line%% *}"
        alloc="${line##* }"
        json="results/${alloc}-${env}.json"
        if [[ -f "$json" ]]; then
            tps=$(jq -r '[.[] | select(.scenario.name=="multithread") | .metrics.ticks_per_s] | first // "n/a"' "$json")
            echo "${alloc} ${env} ok ${tps}"
        else
            echo "${alloc} ${env} fail -"
        fi
    done <<< '{{_matrix_cells}}'

# Smoke variant — same loop, same per-cell defaults. Phase 3's
# run-all already uses warmup=1s + duration=5s per scenario by default
# (see crates/alloc-bench-cli/src/run.rs default_scenarios), so this
# recipe's contract is "the matrix runs end-to-end fast enough to iterate".
# CHOSEN over a per-scenario-flag override — keeps the recipe simple and
# matches Phase-2's existing run-all defaults. Document the equivalence
# in 03-03-SUMMARY.md.
bench-all-smoke:
    #!/usr/bin/env bash
    set -uo pipefail
    BENCH_SMOKE=1 just bench-all

# Native macOS / Linux host bench — libmalloc / ptmalloc baseline (D-18, D-19).
# No Docker. Builds with target-cpu=native (already in .cargo/config.toml).
# Output: results/host-system.json (literal filename per success criterion 4).
bench-host:
    #!/usr/bin/env bash
    set -euo pipefail
    mkdir -p results
    cargo build --release -p alloc-bench-cli
    target/release/alloc-bench-cli run-all --output results/host-system.json --seed 7
    HOST=$(rustc -vV | awk '/^host:/ {print $2}')
    echo "[host] target=$HOST"
    echo "[host] wrote results/host-system.json"

# dive image-efficiency check for one cell (D-14 + DOCK-07).
# Falls back to dockerized dive if dive isn't on host PATH.
dive-check env alloc:
    #!/usr/bin/env bash
    set -euo pipefail
    if command -v dive >/dev/null 2>&1; then
        dive --ci alloc-bench:{{alloc}}-{{env}} --ci-config .dive-ci
    else
        docker run --rm \
            -v /var/run/docker.sock:/var/run/docker.sock \
            -v "$(pwd)/.dive-ci:/.dive-ci:ro" \
            wagoodman/dive:latest \
            --ci alloc-bench:{{alloc}}-{{env}} --ci-config /.dive-ci
    fi

# Run dive against every image in the matrix. Per-cell error capture.
dive-check-all:
    #!/usr/bin/env bash
    set -uo pipefail
    declare -a results=()
    while IFS= read -r line; do
        [[ -z "$line" ]] && continue
        env="${line%% *}"
        alloc="${line##* }"
        echo "[dive] ${alloc}-${env}"
        if just dive-check "$env" "$alloc"; then
            results+=("OK   ${alloc}-${env}")
        else
            results+=("FAIL ${alloc}-${env}")
        fi
    done <<< '{{_matrix_cells}}'
    echo
    echo "dive-check summary"
    printf '%s\n' "${results[@]}"

# Remove all alloc-bench:* image tags. Note the {{ }}-escaping below — `{{`
# is just's variable-interpolation marker so we double-escape to emit
# literal `{{` into the shell command.
clean-images:
    #!/usr/bin/env bash
    set -uo pipefail
    docker images --filter "reference=alloc-bench:*" --format '{{ "{{" }}.Repository{{ "}}" }}:{{ "{{" }}.Tag{{ "}}" }}' \
        | xargs -r docker rmi -f
</reference_recipe_shape>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend justfile with build / run / bench-cell / clean-images recipes</name>

  <files>
    - justfile
  </files>

  <read_first>
    - justfile (current Phase 2 content — must NOT remove default/dce-check/run-all-smoke recipes)
    - .planning/phases/03-docker-matrix-local-orchestration/03-CONTEXT.md (D-04, D-10, D-15, D-17)
    - .planning/phases/03-docker-matrix-local-orchestration/03-RESEARCH.md §"Pattern 2", §Pitfall 3 (Apple Silicon platform), §Pitfall 4 (distroless mount perms)
    - docker/alpine.Dockerfile, docker/debian-slim.Dockerfile, docker/distroless-cc.Dockerfile, docker/distroless-static.Dockerfile, docker/scratch.Dockerfile, docker/wolfi.Dockerfile (Plans 01/02 outputs — confirms ARG names: ALLOC, TARGET, RUST_VERSION, OCI_VERSION, OCI_REVISION, OCI_CREATED)
    - crates/alloc-bench-cli/Cargo.toml (confirms version="0.1.0" — used for OCI_VERSION computation)
  </read_first>

  <action>
    Append to `justfile` (preserving existing recipes from Phase 2): add `build`, `run`, `bench-cell`, `clean-images` recipes following the `<reference_recipe_shape>` above.

    The `build` recipe MUST:
    - Be a shebang recipe (`#!/usr/bin/env bash`) with `set -euo pipefail`.
    - Reject cross-libc combos via case statement BEFORE any docker call (D-04). The six rejected combos are: `debian-slim-mallocng`, `distroless-cc-mallocng`, `wolfi-mallocng`, `alpine-ptmalloc`, `distroless-static-ptmalloc`, `scratch-ptmalloc`. Each emits `[ERR] ...; exit 1`.
    - Map env → TARGET via case: `{debian-slim, distroless-cc, wolfi}` → `x86_64-unknown-linux-gnu`; `{alpine, distroless-static, scratch}` → `x86_64-unknown-linux-musl`. Unknown env → `[ERR] unknown env`; exit 1.
    - Compute three OCI ARG values: OCI_VERSION from `grep -m1 '^version' crates/alloc-bench-cli/Cargo.toml | cut -d'"' -f2`; OCI_REVISION from `git rev-parse HEAD`; OCI_CREATED from `date -u +%Y-%m-%dT%H:%M:%SZ`.
    - Invoke `docker buildx build --platform linux/amd64 -f docker/{{env}}.Dockerfile --build-arg ALLOC={{alloc}} --build-arg TARGET=$TARGET --build-arg RUST_VERSION=1.91 --build-arg OCI_VERSION=$OCI_VERSION --build-arg OCI_REVISION=$OCI_REVISION --build-arg OCI_CREATED=$OCI_CREATED --tag alloc-bench:{{alloc}}-{{env}} --load .` (D-02 tag format).

    The `run` recipe MUST:
    - Pre-create `./results` with `mkdir -p results && chmod 0777 results` (RESEARCH §Pitfall 4 — distroless nonroot UID 65532 mount permissions). The `chmod 0777` is a no-op on macOS Docker Desktop (host-user mount) but mandatory on Linux distroless cells.
    - Honor `BENCH_CPUS`, `BENCH_MEMORY`, `BENCH_CPUSET` env vars (D-17) with defaults `4`, `4g`, `0-3`.
    - Invoke `docker run --rm --platform linux/amd64 --cpus="$BENCH_CPUS" --memory="$BENCH_MEMORY" --cpuset-cpus="$BENCH_CPUSET" -v "$(pwd)/results:/out" alloc-bench:{{alloc}}-{{env}} run-all --output /out/{{alloc}}-{{env}}.json --seed 7` (D-15 + D-03 results filename + D-13 seed).

    The `bench-cell` recipe MUST: chain `just build {{env}} {{alloc}}` then `just run {{env}} {{alloc}}` — a one-liner sequential composition.

    The `clean-images` recipe MUST: list `alloc-bench:*` tags via `docker images --filter "reference=alloc-bench:*" --format '{{ "{{" }}.Repository{{ "}}" }}:{{ "{{" }}.Tag{{ "}}" }}'` (note the just-escaping for literal `{{`) and pipe to `xargs -r docker rmi -f`. The `-r` flag prevents running `docker rmi` with zero arguments when no images match.

    Do NOT modify any existing recipe (default, dce-check, run-all-smoke). Do NOT add Cargo dependencies. Do NOT execute the recipes — Plan 04 runs the smoke matrix.
  </action>

  <verify>
    <automated>
      grep -q '^default:' justfile && \
      grep -q '^dce-check ALLOCATOR' justfile && \
      grep -q '^run-all-smoke ' justfile && \
      grep -q '^build env alloc:' justfile && \
      grep -q '^run env alloc:' justfile && \
      grep -q '^bench-cell env alloc:' justfile && \
      grep -q '^clean-images:' justfile && \
      grep -q 'platform linux/amd64' justfile && \
      grep -q 'mallocng is the musl' justfile && \
      grep -q 'ptmalloc is the glibc' justfile && \
      grep -q 'mkdir -p results' justfile && \
      grep -q 'chmod 0777 results' justfile && \
      grep -q 'BENCH_CPUS:=4' justfile && \
      grep -q 'BENCH_MEMORY:=4g' justfile && \
      grep -q 'BENCH_CPUSET:=0-3' justfile && \
      just --list >/dev/null && \
      bash -n <(just --evaluate 2>/dev/null || true) 2>&1 | grep -v '^$' || true && \
      echo OK
    </automated>
  </verify>

  <acceptance_criteria>
    - Source: Existing recipes (`default`, `dce-check`, `run-all-smoke`) still present in justfile (regression check).
    - Source: New recipes `build env alloc:`, `run env alloc:`, `bench-cell env alloc:`, `clean-images:` exist as recipe headers.
    - Source: `build` recipe contains `--platform linux/amd64`, `mallocng is the musl libc allocator`, `ptmalloc is the glibc libc allocator`, `cargo` for OCI_VERSION extraction, `git rev-parse HEAD`, `date -u +%Y-%m-%dT%H:%M:%SZ`.
    - Source: `run` recipe contains `mkdir -p results`, `chmod 0777 results`, `BENCH_CPUS:=4`, `BENCH_MEMORY:=4g`, `BENCH_CPUSET:=0-3`, and `--platform linux/amd64`.
    - Source: `clean-images` recipe contains `docker images --filter "reference=alloc-bench:*"` and `xargs -r docker rmi -f`.
    - Recipe: `just --list` includes the four new recipe names without errors.
    - Recipe: `just --show build` parses and prints the recipe body without syntax error.
    - Behavior (dry): `just build debian-slim mallocng 2>&1 | head -3` prints `[ERR] mallocng is the musl libc allocator; cannot run on glibc env 'debian-slim'` and exits non-zero — confirms D-04 hard rejection without invoking docker.
    - Behavior (dry): `just build alpine ptmalloc 2>&1 | head -3` prints `[ERR] ptmalloc is the glibc libc allocator; cannot run on musl env 'alpine'` and exits non-zero.
  </acceptance_criteria>

  <done>
    `justfile` extended with the four recipes; the existing Phase 2 recipes still work; `just --list` parses; the cross-libc rejections trigger correctly. Verify command exits 0 with `OK`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add bench-all / bench-all-smoke / bench-host / dive-check / dive-check-all to justfile</name>

  <files>
    - justfile
  </files>

  <read_first>
    - justfile (Task 1 output — append to existing recipes; do NOT replace)
    - .planning/phases/03-docker-matrix-local-orchestration/03-CONTEXT.md (D-11 sequential bench-all, D-12 stdout summary, D-13 bench-all-smoke, D-14 dive-check / dive-check-all, D-18 bench-host, D-19 macOS libmalloc-only)
    - .planning/phases/03-docker-matrix-local-orchestration/03-RESEARCH.md §"Pattern 2" (the exact recipe shape including _matrix_cells heredoc), §"Code Examples §6" (bench-host)
    - crates/alloc-bench-cli/src/main.rs (confirms Cmd::RunAll only takes --output and --seed — the CLI does NOT yet accept --warmup / --duration overrides; bench-all-smoke must work without them)
    - crates/alloc-bench-cli/src/run.rs (default_scenarios uses warmup=1s + duration=5s per scenario per Phase-2 CONTEXT; "smoke" semantics are already the run-all default)
    - .dive-ci (Plan 01 output)
  </read_first>

  <action>
    Append to `justfile` (preserving Task 1 additions): add the `_matrix_cells` private variable + `bench-all`, `bench-all-smoke`, `bench-host`, `dive-check`, `dive-check-all` recipes per the `<reference_recipe_shape>`.

    `_matrix_cells` MUST be a triple-quoted (`'''`) heredoc containing exactly 18 lines, one per `<env> <alloc>` valid tuple, in the order from the reference shape (glibc family first: 9 cells in `debian-slim, distroless-cc, wolfi` × `ptmalloc, jemalloc, mimalloc`; then musl family: 9 cells in `alpine, distroless-static, scratch` × `mallocng, jemalloc, mimalloc`). Cross-libc combos (mallocng on glibc, ptmalloc on musl) are STRUCTURALLY ABSENT — D-04 hard-skip is encoded by omission, not by runtime conditionals.

    `bench-all` MUST:
    - Use `set -uo pipefail` (NOT `-e` — per-cell failures must NOT abort the loop, per RESEARCH §Discretion).
    - Iterate over `_matrix_cells` via `while IFS= read -r line; do ... done <<< '{{_matrix_cells}}'`.
    - For each cell, invoke `just bench-cell "$env" "$alloc" 2>&1 | sed "s/^/[${alloc}-${env}] /"` so all subprocess output is prefixed with `[<alloc>-<env>]` (D-11 streamed-with-prefix).
    - Capture per-cell status into a bash array `results=()`.
    - At end, print the matrix summary (one line per cell: `OK <alloc>-<env>` or `FAIL <alloc>-<env>`) AND the D-12 stdout summary table (`alloc env status ticks_per_s_p50`) where ticks_per_s comes from `jq -r '[.[] | select(.scenario.name=="multithread") | .metrics.ticks_per_s] | first // "n/a"' results/{alloc}-{env}.json`. The `// "n/a"` fallback handles cells where the JSON file doesn't exist.

    `bench-all-smoke` MUST: invoke `BENCH_SMOKE=1 just bench-all` (the BENCH_SMOKE env var is reserved for future use; in Phase 3 the smoke and full matrix have the same per-cell duration because Phase-2 run-all already defaults to 1s warmup + 5s measure per scenario). Document this rationale in 03-03-SUMMARY.md (the recipe is essentially aliased to bench-all today; the BENCH_SMOKE flag exists so Phase 5 CI can later thread real overrides through if a future plan adds CLI flags to run-all).

    `bench-host` MUST:
    - Use `set -euo pipefail` (host bench is single-cell — strict mode is fine).
    - `mkdir -p results` (no chmod needed — host user owns the dir).
    - Run `cargo build --release -p alloc-bench-cli` (no --target — Cargo picks host triple; `.cargo/config.toml` provides `target-cpu=native` automatically per D-18).
    - Run `target/release/alloc-bench-cli run-all --output results/host-system.json --seed 7` (D-18 literal filename, D-13 seed).
    - Print `[host] target=$HOST` where `$HOST` comes from `rustc -vV | awk '/^host:/ {print $2}'`.
    - Print `[host] wrote results/host-system.json`.

    `dive-check {env} {alloc}` MUST: detect host `dive` via `command -v dive`; if present, run `dive --ci alloc-bench:{{alloc}}-{{env}} --ci-config .dive-ci`; if absent, fall back to `docker run --rm -v /var/run/docker.sock:/var/run/docker.sock -v "$(pwd)/.dive-ci:/.dive-ci:ro" wagoodman/dive:latest --ci alloc-bench:{{alloc}}-{{env}} --ci-config /.dive-ci` (RESEARCH §"Code Examples §8").

    `dive-check-all` MUST: iterate over `_matrix_cells`, invoke `just dive-check "$env" "$alloc"` per cell with per-cell error capture (same pattern as bench-all).
  </action>

  <verify>
    <automated>
      grep -q '_matrix_cells :=' justfile && \
      [ "$(awk '/_matrix_cells :=/,/^'"'''"'$/' justfile | grep -cE '^(debian-slim|distroless-cc|wolfi|alpine|distroless-static|scratch) (ptmalloc|jemalloc|mimalloc|mallocng)$')" -eq 18 ] && \
      grep -q '^bench-all:' justfile && \
      grep -q '^bench-all-smoke:' justfile && \
      grep -q '^bench-host:' justfile && \
      grep -q '^dive-check env alloc:' justfile && \
      grep -q '^dive-check-all:' justfile && \
      grep -q 'set -uo pipefail' justfile && \
      grep -q '\[\${alloc}-\${env}\]' justfile && \
      grep -q 'select(.scenario.name=="multithread")' justfile && \
      grep -q 'cargo build --release -p alloc-bench-cli' justfile && \
      grep -q 'results/host-system.json' justfile && \
      grep -q 'wagoodman/dive:latest' justfile && \
      grep -q 'rustc -vV' justfile && \
      ! awk '/_matrix_cells :=/,/^'"'''"'$/' justfile | grep -E '(debian-slim|distroless-cc|wolfi)-mallocng|alpine-ptmalloc|(distroless-static|scratch)-ptmalloc' && \
      just --list >/dev/null && \
      echo OK
    </automated>
  </verify>

  <acceptance_criteria>
    - Source: `_matrix_cells` is a triple-quoted variable in justfile containing exactly 18 lines of `<env> <alloc>` tuples.
    - Source: `_matrix_cells` does NOT contain any cross-libc combo (`debian-slim mallocng`, `alpine ptmalloc`, etc. — verify via `awk` extraction of the heredoc body and grep for the six forbidden tuples returning empty).
    - Source: `bench-all`, `bench-all-smoke`, `bench-host`, `dive-check env alloc`, `dive-check-all` recipe headers all present.
    - Source: `bench-all` uses `set -uo pipefail` (not `-e`), prefixes per-cell logs with `[${alloc}-${env}]`, and uses `jq -r '[.[] | select(.scenario.name=="multithread") | .metrics.ticks_per_s] | first // "n/a"'` for the summary table.
    - Source: `bench-host` runs `cargo build --release -p alloc-bench-cli` (no --target flag), writes `results/host-system.json`, prints `target=$HOST` from `rustc -vV`.
    - Source: `dive-check` has both the `command -v dive` host path AND the `docker run wagoodman/dive:latest` fallback path.
    - Source: `dive-check-all` iterates `_matrix_cells` and invokes `just dive-check "$env" "$alloc"`.
    - Recipe: `just --list` includes the 5 new recipes without errors.
    - Recipe: `just --show bench-all` parses and prints the body.
  </acceptance_criteria>

  <done>
    `justfile` extended with `_matrix_cells` + 5 new orchestration recipes; the matrix list is structurally cross-libc-free (18 cells); `just --list` parses; recipes are inspectable via `just --show`. Verify command exits 0 with `OK`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Justfile recipe args | Positional `{env}` and `{alloc}` strings — validated via case statements before any docker call |
| Justfile → docker daemon socket | Container build/run access via the local Docker Engine API; recipe falls back to dockerized dive that mounts `/var/run/docker.sock` |
| Justfile → host filesystem | `mkdir -p results && chmod 0777 results`; the chmod is intentional for distroless nonroot UID 65532 write access |
| Container → host volume mount | `-v $(pwd)/results:/out` — write surface; container writes only `{alloc}-{env}.json` files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Tampering | User passes unknown env or alloc | mitigate | Case statement rejects with `[ERR] unknown env` or `[ERR] unknown alloc` (implicit via the cross-libc list — only the 6 known envs are mapped to a TARGET). |
| T-03-14 | Tampering | wagoodman/dive:latest mutability | accept | Docker fallback only used when host `dive` isn't installed. RESEARCH lists this as an OPTIONAL fallback; primary path is `brew install dive` (Phase 5 CI installs the pinned 0.13.1). |
| T-03-15 | Information Disclosure | results/ writable by world (chmod 0777) | accept | Local dev box; `results/` contains benchmark JSON with no secrets. The 0777 mode is required for distroless nonroot UID 65532 to write into a host-owned dir on Linux. |
| T-03-16 | EoP | Container escape via /var/run/docker.sock mount in dockerized dive fallback | accept | Mount only used when host dive is absent. dive is read-only (image inspection); does not exec arbitrary containers. Phase 5 CI uses native dive, removing this surface entirely. |
| T-03-17 | Tampering | Cross-libc combo invoked silently | mitigate | Two layers: (1) Dockerfile rejects ALLOC outside the per-libc set (Plans 01/02); (2) Justfile build recipe rejects with [ERR] BEFORE invoking docker. |
| T-03-SC | Tampering | npm/pip/cargo install supply-chain | mitigate | No new packages installed in this plan. cargo-chef install lives in Plans 01/02 Dockerfiles with `--locked`. |
</threat_model>

<verification>
End-of-plan automated verification:
- `just --list` includes: default, dce-check, run-all-smoke, build, run, bench-cell, bench-all, bench-all-smoke, bench-host, dive-check, dive-check-all, clean-images.
- `just build debian-slim mallocng 2>&1 >/dev/null | grep -q 'mallocng is the musl'` — D-04 cross-libc rejection wired correctly.
- `just build alpine ptmalloc 2>&1 >/dev/null | grep -q 'ptmalloc is the glibc'` — same in the other direction.
- `awk '/_matrix_cells :=/,/^'"'''"'$/' justfile | grep -c -E '^(debian-slim|distroless-cc|wolfi|alpine|distroless-static|scratch) ...'` returns exactly 18 (length of valid matrix).
- Pre-commit gate: `prek run --all-files` passes (shellcheck on shebang recipe bodies via prek's shellcheck hook may complain; if it does, fix per shellcheck advisories; do NOT silence with `# shellcheck disable=...` unless the advisory is unactionable).
- Visual: `just --show bench-all` prints the recipe body — confirms heredoc + while-read loop parse correctly.

Note: NO actual `docker buildx build` or `docker run` invocations occur in this plan. Plan 04 runs the smoke matrix against the recipes here.
</verification>

<success_criteria>
This plan is complete when:
- [ ] `justfile` contains all 9 new Phase-3 recipes plus the `_matrix_cells` heredoc.
- [ ] All existing Phase-2 recipes (`default`, `dce-check`, `run-all-smoke`) still parse and work.
- [ ] D-04 cross-libc rejection works: `just build debian-slim mallocng` exits non-zero with the expected `[ERR]` message; `just build alpine ptmalloc` does the same.
- [ ] `_matrix_cells` contains exactly 18 valid (env, alloc) tuples and zero cross-libc tuples.
- [ ] Every `docker buildx build` and `docker run` invocation passes `--platform linux/amd64` (RESEARCH §Pitfall 3).
- [ ] `run` recipe pre-creates `results/` with `chmod 0777` (RESEARCH §Pitfall 4).
- [ ] `bench-host` writes `results/host-system.json` literally (success criterion 4).
- [ ] `dive-check` has both host-dive and dockerized-dive paths.
- [ ] `bench-all` uses per-cell error capture (`set -uo pipefail` not `-e`) and emits the D-12 summary table.
- [ ] Pre-commit hooks pass (`prek run --all-files`).
</success_criteria>

<output>
Create `.planning/phases/03-docker-matrix-local-orchestration/03-03-SUMMARY.md` documenting:
1. The full recipe surface added to `justfile` (recipe names + one-liner purpose).
2. The `_matrix_cells` 18-tuple list with cross-libc structural absence noted.
3. The bench-all-smoke design choice (alias of bench-all today; BENCH_SMOKE env var reserved for future per-scenario flag overrides).
4. The bench-host build path (no --target; Cargo picks host triple; `.cargo/config.toml` provides target-cpu=native).
5. Reference to Plan 04 for the actual smoke matrix run that exercises these recipes.
</output>
</content>
</invoke>