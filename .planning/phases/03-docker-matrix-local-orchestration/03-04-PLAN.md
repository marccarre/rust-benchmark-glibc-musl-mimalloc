---
phase: 03-docker-matrix-local-orchestration
plan: "04"
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - results/.gitkeep
  - .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md
autonomous: true
requirements: [DOCK-01, DOCK-02, DOCK-03, DOCK-04, DOCK-05, DOCK-06, DOCK-07, DOCK-08, DOCK-09, ORCH-01, ORCH-02]
tags: [smoke, anchor-cells, full-matrix, dive, bench-host, image-inspection, escape-hatch]

must_haves:
  truths:
    - "User runs the 6 anchor cells from CONTEXT D-01 (alpine+jemalloc, debian-slim+ptmalloc, distroless-cc+mimalloc, distroless-static+mimalloc, scratch+mallocng, wolfi+jemalloc) and EVERY ONE produces a tagged image whose `docker inspect` shows all 8 OCI annotations populated (success criterion 1)."
    - "User runs `docker run --rm --cpus=4 --memory=4g --cpuset-cpus=0-3 --platform linux/amd64 -v $(pwd)/results:/out alloc-bench:jemalloc-alpine run-all --output /out/jemalloc-alpine.json` and gets a complete results.json with no OOM-kill, env block reporting `docker_image: \"alpine:3.20\"` and `target_triple: \"x86_64-unknown-linux-musl\"` (success criterion 2)."
    - "User runs `just bench-all-smoke` (or equivalently `just bench-all`) and the recipe runs the full meaningful matrix (12-18 cells, depending on which musl-static + alt-allocator combos drop), emits one results.json per surviving cell, and ends with the D-12 summary table (success criterion 3)."
    - "User runs `just bench-host` on macOS and gets `results/host-system.json` with `os: \"macos\"`, `docker_image: null`, `target_triple` matching the host (success criterion 4)."
    - "User runs `just dive-check-all` and the dive thresholds (lowestEfficiency 0.95, highestUserWastedPercent 0.05, highestWastedBytes 50MB) PASS for every image in the matrix (success criterion 5)."
    - "If any musl-static + alt-allocator cell fails to link (D-01 escape hatch), it is documented with reason in 03-04-SMOKE-RESULTS.md and dropped from subsequent recipes."
  artifacts:
    - path: "results/.gitkeep"
      provides: "Ensures results/ directory exists in git for downstream phases (Phase 4 aggregator globs results/*.json)"
      contains: ""
    - path: ".planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md"
      provides: "Per-cell smoke build + run results, image sizes, dive efficiency scores, any escape-hatch drops"
      contains: "## Smoke build matrix"
  key_links:
    - from: "Anchor cell builds (Task 1)"
      to: "Plan 01 + Plan 02 Dockerfiles"
      via: "just build {env} {alloc}"
      pattern: "alloc-bench:.*-(alpine|debian-slim|distroless-cc|distroless-static|scratch|wolfi)"
    - from: "OCI annotation verification (Task 1)"
      to: "Plan 01 + Plan 02 LABEL injection"
      via: "docker inspect | jq '.Config.Labels'"
      pattern: "org.opencontainers.image.title"
    - from: "Anchor cell run (Task 2)"
      to: "Plan 03 just run recipe"
      via: "just run {env} {alloc}"
      pattern: "results/.+\\.json"
    - from: "Full-matrix run (Task 3)"
      to: "Plan 03 just bench-all recipe"
      via: "just bench-all-smoke (or just bench-all)"
      pattern: "_matrix_cells"
    - from: "Bench-host (Task 4)"
      to: "Plan 03 just bench-host recipe"
      via: "cargo build --release -p alloc-bench-cli"
      pattern: "results/host-system.json"
    - from: "Image-size gate (Task 5)"
      to: ".dive-ci config + Plan 03 dive-check-all recipe"
      via: "just dive-check-all"
      pattern: "lowestEfficiency"
---

<objective>
Exercise the entire Phase 3 stack end-to-end: build the 6 anchor cells (D-01 escape-hatch sentinels), smoke-run the matrix, run the macOS host bench, and run dive image-size gate against every image. Capture sizes, dive scores, and any cells that hit the D-01 escape hatch into a smoke-results document.

Purpose: This is where Phase 3's success criteria 1-5 actually become demonstrably true. Plans 01/02/03 ship file artifacts; Plan 04 verifies they compose. The anchor-cell-first approach (D-01 + RESEARCH §Pitfall 1) catches musl-static + jemalloc/mimalloc link failures before committing to the full 18-cell run, and produces an evidence trail in `03-04-SMOKE-RESULTS.md`.

Output: 6 successfully-built anchor images + ~12-18 full-matrix images + per-cell results.json + host-system.json + a structured smoke-results markdown documenting sizes, OCI labels, and any drops. NO new code.

Note on duration: Each cell takes ~30-90s for `bench-cell` (build is dominated by the workspace's first cargo-chef cook ~5min uncached; subsequent cells reuse the chef base + dep cache). First-time full matrix: ~25-40 minutes wall-clock on a fast machine. With cargo-chef caching, repeat runs are ~10-15 minutes. Phase-3 success criterion 3's "full matrix in 2.4h at default --duration 60s" is informational; this plan exercises the smoke variant where each cell is ~1m wall-clock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-docker-matrix-local-orchestration/03-CONTEXT.md
@.planning/phases/03-docker-matrix-local-orchestration/03-RESEARCH.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@justfile
@docker/alpine.Dockerfile
@docker/debian-slim.Dockerfile
@docker/distroless-cc.Dockerfile
@docker/distroless-static.Dockerfile
@docker/scratch.Dockerfile
@docker/wolfi.Dockerfile
@.dive-ci
@.dockerignore

<interfaces>
<!-- Anchor cells per CONTEXT D-01 (success criterion 1 verbatim). These six
     are the structural-soundness sentinels: one per env, mixing libc
     families, mixing alt-allocators in both static and dynamic envs.
     If any of these fails, the matrix is broken at the structural level. -->

CONTEXT D-01 anchor cells (6):
  alpine + jemalloc          → musl dynamic + alt-alloc (RESEARCH §Pitfall 1 candidate)
  debian-slim + ptmalloc     → glibc dynamic + libc default
  distroless-cc + mimalloc   → glibc minimal + alt-alloc
  distroless-static + mimalloc → musl static + alt-alloc (RESEARCH §Pitfall 1 high-risk)
  scratch + mallocng         → musl static + libc default (smallest image)
  wolfi + jemalloc           → glibc Chainguard + alt-alloc

CONTEXT D-01 escape-hatch candidates (likely-to-fail static-link cells):
  jemalloc-distroless-static (Pitfall 1 high-risk; smoke first, drop if fail)
  mimalloc-scratch           (Pitfall 1 high-risk; smoke first, drop if fail)
  mimalloc-distroless-static (additional risk; verify in smoke)
  jemalloc-scratch           (additional risk; verify in smoke)

CONTEXT D-22 image-size budgets (informational; record actuals in SMOKE-RESULTS):
  scratch ≤ 15MB
  distroless-static ≤ 25MB
  alpine ≤ 30MB
  wolfi ≤ 35MB
  distroless-cc ≤ 50MB
  debian-slim ≤ 100MB

CONTEXT success criterion 2 (verbatim runtime invocation):
  docker run --rm --cpus=4 --memory=4g --cpuset-cpus=0-3 -v $(pwd)/results:/out \
      alloc-bench:jemalloc-alpine run-all --output /out/jemalloc-alpine.json

  Expected:
    Exit 0; results/jemalloc-alpine.json exists; jq '.[0].env.docker_image' returns "alpine:3.20";
    jq '.[0].build.target_triple' returns "x86_64-unknown-linux-musl"; no OOM-kill in dmesg.

CONTEXT D-08 + Plan 01/02 LABEL injection — eight OCI keys to verify per cell:
  org.opencontainers.image.title       = "alloc-bench"
  org.opencontainers.image.description = "<env-specific>"
  org.opencontainers.image.source      = "https://github.com/marccarre/rust-benchmark-glibc-musl-mimalloc"
  org.opencontainers.image.version     = "0.1.0" (Cargo crate version)
  org.opencontainers.image.revision    = $(git rev-parse HEAD) — full SHA
  org.opencontainers.image.licenses    = "MIT OR Apache-2.0"
  org.opencontainers.image.created     = ISO-8601 timestamp at build time
  org.opencontainers.image.authors     = "Marc Carré"

  Verify command per cell:
    docker inspect alloc-bench:{alloc}-{env} --format '{{json .Config.Labels}}' | jq .
    Expected: object with all 8 keys present and non-empty.

CONTEXT D-21 — .dive-ci thresholds:
  lowestEfficiency: 0.95
  highestUserWastedPercent: 0.05
  highestWastedBytes: 50MB

  Verify per cell: `just dive-check {env} {alloc}` exits 0.

RESEARCH §Pitfall 1 — D-01 escape hatch protocol:
  If `cargo build --release --target x86_64-unknown-linux-musl --no-default-features --features alloc-jemalloc -p alloc-bench-cli` (inside the distroless-static or scratch builder) fails with link errors like "undefined reference to __tls_get_addr", "linking against libc.a not supported", or similar:
    1. Capture the link-error stderr to SMOKE-RESULTS.md.
    2. Drop the cell from the matrix going forward (do NOT modify _matrix_cells in this plan — Plan 03 already shipped it; Plan 05 documents the drop).
    3. Continue with remaining cells.
  
  This is structural — bench-all uses per-cell error capture, so a failed cell becomes `FAIL <alloc>-<env>` in the summary; the surviving cells are the matrix.

RESEARCH §"Environment Availability" — required tooling:
  Docker daemon (✓ OrbStack 29.4.0); buildx (✓ 0.33.0); just (✓ 1.51.0); cargo (✓); jq (assumed present); git (✓); rustc (✓); date (✓).
  dive: NOT installed; recipe falls back to dockerized wagoodman/dive:latest.

CONTEXT "code_context" + RESEARCH §"Runtime State Inventory":
  Each Dockerfile sets ENV DOCKER_IMAGE; metrics::env::read_env populates env.docker_image automatically.
  bench-host does NOT set DOCKER_IMAGE → JSON has docker_image: null (success criterion 4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke-build the 6 anchor cells and verify OCI annotations</name>

  <files>
    - results/.gitkeep
    - .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md
  </files>

  <read_first>
    - justfile (Plan 03 output — confirms `just build` recipe shape)
    - docker/alpine.Dockerfile, docker/debian-slim.Dockerfile, docker/distroless-cc.Dockerfile, docker/distroless-static.Dockerfile, docker/scratch.Dockerfile, docker/wolfi.Dockerfile (Plan 01/02 outputs)
    - .planning/phases/03-docker-matrix-local-orchestration/03-CONTEXT.md (D-01 anchor cells, D-08 OCI keys, D-22 size budgets)
    - .planning/phases/03-docker-matrix-local-orchestration/03-RESEARCH.md §Pitfall 1 (escape hatch), §"Code Examples §4" (OCI verification jq command)
  </read_first>

  <action>
    First create `results/.gitkeep` as an empty file (so the directory exists in git for Phase 4 aggregator).

    Initialise `.planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md` with section headers: `## Anchor cells (6)`, `## Full matrix (18 cells nominal)`, `## Image sizes`, `## OCI annotations`, `## Dive --ci scores`, `## Bench-host`, `## D-01 escape-hatch drops`.

    Build the 6 anchor cells per CONTEXT D-01 in order (glibc family first to maximize cargo-chef cache reuse):
      1. `just build debian-slim ptmalloc`
      2. `just build distroless-cc mimalloc`
      3. `just build wolfi jemalloc`
      4. `just build alpine jemalloc`
      5. `just build distroless-static mimalloc`
      6. `just build scratch mallocng`

    For EACH anchor cell:
    - Capture build success/failure into SMOKE-RESULTS.md.
    - On failure: capture the last 50 lines of `docker buildx build` stderr into SMOKE-RESULTS.md under `## D-01 escape-hatch drops` with the link-error excerpt highlighted. If the failure is a static-link issue (Pitfall 1: `undefined reference to __tls_get_addr`, `linking against libc.a not supported`, `cannot find -lc`, `relocation R_X86_64_TLSGD`), mark the cell as DROPPED and continue. If the failure is unrelated (e.g., Dockerfile syntax error, network timeout), STOP the plan and surface the error.
    - On success: capture image size via `docker images alloc-bench:{alloc}-{env} --format '{{.Size}}'` into SMOKE-RESULTS.md `## Image sizes` table.
    - On success: verify all 8 OCI labels via `docker inspect alloc-bench:{alloc}-{env} --format '{{json .Config.Labels}}' | jq .`. Confirm every one of the 8 keys (`org.opencontainers.image.{title,description,source,version,revision,licenses,created,authors}`) is present AND non-empty. Capture the JSON object into SMOKE-RESULTS.md `## OCI annotations`.

    SMOKE-RESULTS.md format for `## OCI annotations`:
    ```
    | Cell | title | description | source | version | revision | licenses | created | authors |
    |------|-------|-------------|--------|---------|----------|----------|---------|---------|
    | jemalloc-alpine | alloc-bench | alpine (musl dynamic; ...) | github.com/... | 0.1.0 | <sha> | MIT OR Apache-2.0 | 2026-... | Marc Carré |
    ```

    Do NOT proceed to Task 2 unless ALL 6 anchor cells either build successfully OR fail with documented Pitfall-1 link errors AND ≥ 4 of the 6 anchors built successfully (4 = enough to declare the matrix structurally sound). If < 4 anchors build, STOP and report.
  </action>

  <verify>
    <automated>
      test -f results/.gitkeep && \
      test -f .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md && \
      grep -q '## Anchor cells' .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md && \
      grep -q '## OCI annotations' .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md && \
      grep -q '## Image sizes' .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md && \
      docker images --filter "reference=alloc-bench:ptmalloc-debian-slim" --format '{{.Repository}}:{{.Tag}}' | grep -q '^alloc-bench:ptmalloc-debian-slim$' && \
      docker inspect alloc-bench:ptmalloc-debian-slim --format '{{json .Config.Labels}}' | jq -e '."org.opencontainers.image.title" == "alloc-bench"' >/dev/null && \
      docker inspect alloc-bench:ptmalloc-debian-slim --format '{{json .Config.Labels}}' | jq -e '."org.opencontainers.image.revision" | length == 40' >/dev/null && \
      echo OK
    </automated>
  </verify>

  <acceptance_criteria>
    - Source: `results/.gitkeep` exists.
    - Source: `.planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md` exists with all 7 section headers.
    - Build: At minimum 4 of the 6 anchor cells exist as docker images (`docker images alloc-bench:*` returns ≥ 4 anchor cells). The two musl-static + alt-allocator anchors (`mimalloc-distroless-static`, `jemalloc-alpine` is dynamic so it should always pass) may legitimately fail per D-01.
    - Build: `alloc-bench:ptmalloc-debian-slim` MUST exist (this anchor uses the most-mature path; failure = phase-blocking).
    - Inspect: For every successfully-built anchor cell, `docker inspect alloc-bench:{alloc}-{env}` shows all 8 OCI labels populated and non-empty.
    - Inspect: `org.opencontainers.image.revision` is exactly 40 hex chars (full SHA from `git rev-parse HEAD`).
    - Inspect: `org.opencontainers.image.title` is `"alloc-bench"`.
    - Inspect: `org.opencontainers.image.licenses` is `"MIT OR Apache-2.0"`.
    - Document: SMOKE-RESULTS.md `## Anchor cells` has 6 rows; failed cells are recorded in `## D-01 escape-hatch drops` with link-error excerpts.
    - Document: SMOKE-RESULTS.md `## Image sizes` has actual sizes for every successfully-built cell, comparable against D-22 budgets (informational).
  </acceptance_criteria>

  <done>
    Six anchor builds attempted; ≥ 4 succeeded (including ptmalloc-debian-slim which is mandatory); SMOKE-RESULTS.md documents builds, sizes, OCI labels, and any D-01 escape-hatch drops with link errors. Verify command exits 0 with `OK`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run success criterion 2's verbatim invocation (jemalloc-alpine)</name>

  <files>
    - results/jemalloc-alpine.json
    - .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md
  </files>

  <read_first>
    - .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md (Task 1 output — must reflect successful jemalloc-alpine build)
    - .planning/phases/03-docker-matrix-local-orchestration/03-CONTEXT.md (success criterion 2 verbatim)
    - justfile (Plan 03 — `just run` recipe)
    - crates/alloc-bench-core/src/output.rs (Run schema — env.docker_image, build.target_triple)
  </read_first>

  <action>
    Execute the literal CONTEXT success criterion 2 invocation. Use `just run alpine jemalloc` since the Justfile recipe encodes the exact same docker run flags (D-15) — this proves the recipe works AND the success criterion is satisfied verbatim:

    1. `just run alpine jemalloc`  (mounts ./results, applies cgroup defaults, writes results/jemalloc-alpine.json)
    2. Capture the run wall-clock and exit code into SMOKE-RESULTS.md.
    3. Verify the produced JSON file:
       - `jq '.[0].env.docker_image' results/jemalloc-alpine.json` returns `"alpine:3.20"` (literal — success criterion 2).
       - `jq '.[0].build.target_triple' results/jemalloc-alpine.json` returns `"x86_64-unknown-linux-musl"` (literal — success criterion 2).
       - `jq 'length' results/jemalloc-alpine.json` returns 10 (run-all emits 10 records per Phase-2 default_scenarios).
       - `jq '[.[] | .status // "success"] | unique' results/jemalloc-alpine.json` returns `["success"]` or `["failed", "success"]` — at least one scenario succeeded.
       - `jq '[.[] | select(.scenario.name == "multithread") | .metrics.ticks_per_s] | first' results/jemalloc-alpine.json` returns a positive number.
    4. Append the verification output to SMOKE-RESULTS.md `## Anchor cells` under jemalloc-alpine row.

    If `just run alpine jemalloc` fails with `OOM-kill`, the recipe's `--memory=4g` was not honored — capture the error and STOP. This is structurally important because PITFALLS §3.1 and CONTEXT D-15 explicitly lock 4 GiB to avoid mimalloc segment OOM.

    If `jq` returns the wrong `docker_image` value (e.g., empty or different from `"alpine:3.20"`), the Dockerfile's `ENV DOCKER_IMAGE` is misconfigured — STOP. This is structurally important because Phase 4 aggregator depends on this field for matrix labelling.
  </action>

  <verify>
    <automated>
      test -f results/jemalloc-alpine.json && \
      [ "$(jq -r '.[0].env.docker_image' results/jemalloc-alpine.json)" = "alpine:3.20" ] && \
      [ "$(jq -r '.[0].build.target_triple' results/jemalloc-alpine.json)" = "x86_64-unknown-linux-musl" ] && \
      [ "$(jq 'length' results/jemalloc-alpine.json)" -eq 10 ] && \
      [ "$(jq -r '[.[] | select(.scenario.name == "multithread") | .metrics.ticks_per_s] | first' results/jemalloc-alpine.json)" != "null" ] && \
      grep -q 'jemalloc-alpine' .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md && \
      echo OK
    </automated>
  </verify>

  <acceptance_criteria>
    - Run: `just run alpine jemalloc` exits 0 (no OOM-kill).
    - Source: `results/jemalloc-alpine.json` exists and is valid JSON (jq parses it).
    - JSON: `.[0].env.docker_image` is exactly the string `"alpine:3.20"` (success criterion 2 literal).
    - JSON: `.[0].build.target_triple` is exactly `"x86_64-unknown-linux-musl"` (success criterion 2 literal).
    - JSON: array length is 10 (run-all emits 10 records).
    - JSON: at least one scenario has positive ticks_per_s (proves the binary actually ran).
    - JSON: `.[0].env.os` is `"linux"` (not `"macos"`).
    - JSON: `.[0].build.rustc_version` matches the rust-toolchain.toml channel (1.91.x).
    - Document: SMOKE-RESULTS.md `## Anchor cells` jemalloc-alpine row reflects success criterion 2 verification.
  </acceptance_criteria>

  <done>
    `results/jemalloc-alpine.json` exists; `docker_image: "alpine:3.20"` and `target_triple: "x86_64-unknown-linux-musl"` literal; SMOKE-RESULTS.md updated. Verify command exits 0 with `OK`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run the full matrix via just bench-all-smoke + verify per-cell JSON output</name>

  <files>
    - results/*.json (12-18 files, one per surviving cell)
    - .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md
  </files>

  <read_first>
    - justfile (Plan 03 — bench-all + bench-all-smoke + _matrix_cells heredoc)
    - .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md (Tasks 1+2 output — anchor + jemalloc-alpine documented)
    - .planning/phases/03-docker-matrix-local-orchestration/03-CONTEXT.md (D-11, D-12 success criterion 3)
    - crates/alloc-bench-core/src/output.rs (Run schema invariants)
  </read_first>

  <action>
    Run `just bench-all-smoke` (which invokes the full 18-cell matrix loop with per-cell error capture). Capture stdout and stderr.

    `just bench-all-smoke` does the following per `_matrix_cells` (Plan 03):
    - For each of the 18 valid (env, alloc) tuples: `just bench-cell {env} {alloc}` (build + run).
    - Per-cell error capture means a failed cell prints `FAIL <alloc>-<env>` and the loop continues.
    - After all cells, prints the matrix summary (one OK/FAIL line per cell) and the D-12 stdout summary table (`alloc env status ticks_per_s_p50`).

    Expected runtime: ~25-40 minutes uncached, ~10-15 minutes with cargo-chef cache from Task 1's anchor builds. Use `run_in_background: true` if available; otherwise run synchronously (the executor must wait for completion).

    Capture into SMOKE-RESULTS.md `## Full matrix (18 cells nominal)`:
    - The full stdout `Matrix summary` block (one line per cell).
    - The full stdout D-12 summary table.
    - For every cell with `OK` status: confirm `results/{alloc}-{env}.json` exists and `jq 'length' returns 10`.
    - For every cell with `FAIL` status: capture the last 30 lines of that cell's docker buildx stderr into a per-cell sub-section under `## D-01 escape-hatch drops` so the failure mode is debuggable.

    For each successfully-built cell, also record into SMOKE-RESULTS.md `## Image sizes` the actual `docker images alloc-bench:{alloc}-{env} --format '{{.Size}}'` value. Compare against D-22 budgets and flag any cell exceeding budget (informational only — Phase 5 enforces).

    Do NOT proceed to Task 4 unless ≥ 12 of 18 cells produce a valid `results/*.json` file (12 = the minimum meaningful-matrix size from CONTEXT D-01 — 9 glibc cells, all of which should pass + 3 musl libc-default cells which should pass + at least 0 of the musl-static + alt-allocator cells which may legitimately drop). If < 12 cells succeed, STOP and surface the failure pattern.
  </action>

  <verify>
    <automated>
      [ "$(ls results/*.json 2>/dev/null | wc -l | tr -d ' ')" -ge 12 ] && \
      ls results/ptmalloc-debian-slim.json results/ptmalloc-distroless-cc.json results/ptmalloc-wolfi.json 2>/dev/null && \
      ls results/jemalloc-debian-slim.json results/jemalloc-distroless-cc.json results/jemalloc-wolfi.json 2>/dev/null && \
      ls results/mimalloc-debian-slim.json results/mimalloc-distroless-cc.json results/mimalloc-wolfi.json 2>/dev/null && \
      ls results/mallocng-alpine.json 2>/dev/null && \
      ls results/jemalloc-alpine.json 2>/dev/null && \
      ls results/mimalloc-alpine.json 2>/dev/null && \
      for f in results/*-debian-slim.json results/*-distroless-cc.json results/*-wolfi.json results/*-alpine.json; do
        [ -f "$f" ] || continue
        [ "$(jq 'length' "$f")" -eq 10 ] || { echo "FAIL: $f does not have 10 records"; exit 1; }
      done && \
      grep -q '## Full matrix' .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md && \
      echo OK
    </automated>
  </verify>

  <acceptance_criteria>
    - Run: `just bench-all-smoke` completes (with per-cell error capture, exit code may be 0 even if some cells failed — that's the point).
    - Source: `results/` contains ≥ 12 JSON files matching `*.json`.
    - Source: All 9 glibc cells (`{ptmalloc, jemalloc, mimalloc} × {debian-slim, distroless-cc, wolfi}`) MUST have produced a JSON file. These have the lowest static-link risk per RESEARCH; failure = phase-blocking.
    - Source: All 3 musl libc-default cells (`mallocng × {alpine, distroless-static, scratch}`) MUST have produced a JSON file. These use the system allocator; if they fail, the static-link itself is broken.
    - Source: At minimum the 3 alpine cells (`{mallocng, jemalloc, mimalloc} × alpine`) MUST have produced a JSON file (alpine is musl dynamic, lowest risk after glibc).
    - JSON: every `results/*.json` has `jq 'length' == 10` (10 scenarios per run-all).
    - Document: SMOKE-RESULTS.md `## Full matrix` has the bench-all output (matrix summary + D-12 summary table).
    - Document: SMOKE-RESULTS.md `## Image sizes` records actual sizes for every successfully-built cell.
    - Document: SMOKE-RESULTS.md `## D-01 escape-hatch drops` documents any failed cells with link-error excerpts.
  </acceptance_criteria>

  <done>
    Full matrix run produces ≥ 12 valid `results/*.json` files; all 9 glibc cells succeed; all 3 musl libc-default cells succeed; SMOKE-RESULTS.md documents the full matrix + drops. Verify command exits 0 with `OK`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Run just bench-host (macOS native libmalloc baseline)</name>

  <files>
    - results/host-system.json
    - .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md
  </files>

  <read_first>
    - justfile (Plan 03 — bench-host recipe)
    - .planning/phases/03-docker-matrix-local-orchestration/03-CONTEXT.md (D-18, D-19, success criterion 4)
    - .cargo/config.toml (target-cpu=native for host build)
    - crates/alloc-bench-core/src/metrics/env.rs (env.docker_image is `std::env::var("DOCKER_IMAGE").ok()` — None on host)
  </read_first>

  <action>
    Run `just bench-host`. The recipe (per Plan 03) does:
    - `mkdir -p results`
    - `cargo build --release -p alloc-bench-cli` (host triple, target-cpu=native via .cargo/config.toml)
    - `target/release/alloc-bench-cli run-all --output results/host-system.json --seed 7`
    - Prints `[host] target=$HOST` and `[host] wrote results/host-system.json`.

    Verify the produced file:
    - `jq '.[0].env.os' results/host-system.json` returns `"macos"` (the executor is on macOS per CLAUDE.md "darwin" environment).
    - `jq '.[0].env.docker_image' results/host-system.json` returns `null` (DOCKER_IMAGE env var is unset on host).
    - `jq -r '.[0].build.target_triple' results/host-system.json` returns `aarch64-apple-darwin` or `x86_64-apple-darwin` (whichever the executor is on; CLAUDE.md indicates darwin/aarch64).
    - `jq 'length' results/host-system.json` returns 10.
    - `jq -r '.[0].env.cpu_model' results/host-system.json` returns a non-empty string (e.g., "Apple M1 Pro" or similar).

    Append SMOKE-RESULTS.md `## Bench-host` with: detected target triple, cpu_model, run wall-clock, per-scenario pass/fail status from the JSON.

    If `cargo build --release -p alloc-bench-cli` fails on the host (likely cause: no target-cpu=native compatibility for some allocator crate), STOP — bench-host is success criterion 4 and is a phase-blocking gate. Note: the host bench uses NO allocator features (libmalloc default), so this should not encounter musl/jemalloc/mimalloc cross-compile pitfalls.
  </action>

  <verify>
    <automated>
      test -f results/host-system.json && \
      [ "$(jq -r '.[0].env.os' results/host-system.json)" = "macos" ] && \
      [ "$(jq -r '.[0].env.docker_image // "NULL"' results/host-system.json)" = "NULL" ] && \
      jq -r '.[0].build.target_triple' results/host-system.json | grep -qE '^(aarch64|x86_64)-apple-darwin$' && \
      [ "$(jq 'length' results/host-system.json)" -eq 10 ] && \
      [ -n "$(jq -r '.[0].env.cpu_model' results/host-system.json)" ] && \
      grep -q '## Bench-host' .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md && \
      echo OK
    </automated>
  </verify>

  <acceptance_criteria>
    - Run: `just bench-host` exits 0.
    - Source: `results/host-system.json` exists (literal filename per success criterion 4).
    - JSON: `.[0].env.os` is `"macos"` literal.
    - JSON: `.[0].env.docker_image` is `null` (the `// "NULL"` jq fallback returns `"NULL"` only when the field is absent or null — verifies it's not set).
    - JSON: `.[0].build.target_triple` matches `^(aarch64|x86_64)-apple-darwin$`.
    - JSON: `length == 10` (run-all emits 10 records).
    - JSON: `.[0].env.cpu_model` is non-empty.
    - Document: SMOKE-RESULTS.md `## Bench-host` records the target, cpu_model, and run wall-clock.
  </acceptance_criteria>

  <done>
    `results/host-system.json` exists with macos+null docker_image+darwin target triple; SMOKE-RESULTS.md updated. Verify command exits 0 with `OK`.
  </done>
</task>

<task type="auto">
  <name>Task 5: Run just dive-check-all and verify image-size thresholds pass</name>

  <files>
    - .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md
  </files>

  <read_first>
    - justfile (Plan 03 — dive-check + dive-check-all recipes; dive falls back to dockerized wagoodman/dive:latest)
    - .dive-ci (Plan 01 — thresholds: lowestEfficiency 0.95, highestUserWastedPercent 0.05, highestWastedBytes 50MB)
    - .planning/phases/03-docker-matrix-local-orchestration/03-CONTEXT.md (D-21, D-07 success criterion 5)
  </read_first>

  <action>
    Run `just dive-check-all`. The recipe iterates `_matrix_cells` and runs `just dive-check {env} {alloc}` per cell.

    `just dive-check {env} {alloc}` (Plan 03):
    - If host `dive` is on PATH: `dive --ci alloc-bench:{alloc}-{env} --ci-config .dive-ci`.
    - Otherwise: `docker run --rm -v /var/run/docker.sock:/var/run/docker.sock -v "$(pwd)/.dive-ci:/.dive-ci:ro" wagoodman/dive:latest --ci alloc-bench:{alloc}-{env} --ci-config /.dive-ci`.

    For each cell, dive returns exit 0 if all thresholds pass, exit 1 otherwise. The recipe captures per-cell pass/fail.

    Capture per-cell results into SMOKE-RESULTS.md `## Dive --ci scores`:
    - For each successful cell: capture `efficiency`, `wasted bytes`, `userWastedPercent` from dive's stdout (parse with grep/awk if needed; dive stdout format is documented in github.com/wagoodman/dive README).
    - For each failing cell: capture which threshold tripped + which layer is responsible.

    Decision criteria for proceeding:
    - SUCCESS: all cells pass dive thresholds OR ≥ 80% of cells pass and the failing cells are documented with the specific threshold breach. Phase 3 only ships `.dive-ci` config and `just dive-check-all`; Phase 5 enforces in CI. So a few cells failing dive in this smoke is informational, not phase-blocking.
    - PHASE-BLOCKING: dive recipe itself errors out (e.g., dockerized wagoodman/dive can't pull due to network) — STOP and capture for user attention.
  </action>

  <verify>
    <automated>
      grep -q '## Dive --ci scores' .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md && \
      [ "$(grep -c -E '^\| (alloc-bench:|ptmalloc-|jemalloc-|mimalloc-|mallocng-)' .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md)" -ge 12 ] && \
      echo OK
    </automated>
  </verify>

  <acceptance_criteria>
    - Run: `just dive-check-all` completes without the recipe itself erroring (per-cell pass/fail is allowed; we want the recipe to RUN, not necessarily PASS every cell).
    - Document: SMOKE-RESULTS.md `## Dive --ci scores` has at least 12 rows (one per surviving cell from Task 3).
    - Document: For every cell, the row records efficiency score (or "FAIL: <threshold>") and image size.
    - Informational: Cells exceeding D-22 budgets are noted but do not block the plan (Phase 5 enforces).
  </acceptance_criteria>

  <done>
    `just dive-check-all` runs to completion; SMOKE-RESULTS.md documents per-cell dive scores and any threshold breaches. Verify command exits 0 with `OK`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev box → Docker daemon | All 18+ cell builds + runs invoke local Docker via buildx + run |
| wagoodman/dive:latest pull | Optional fallback when host dive isn't installed; pulled at runtime |
| host filesystem ↔ container volume | `-v $(pwd)/results:/out` write surface; container only writes JSON files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | DoS | Full matrix takes 25-40 min uncached | accept | Smoke variant; first-run cost; documented in objective. |
| T-03-19 | Tampering | wagoodman/dive:latest mutability | accept | Optional fallback only when host dive is absent. Plan 05 documents brew install dive recommendation. |
| T-03-20 | Repudiation | Per-cell failure mode obscured by FAIL prefix | mitigate | SMOKE-RESULTS.md captures last-30-lines stderr per failed cell so the cause is debuggable post-hoc. |
| T-03-21 | EoP | distroless nonroot writes via /out volume | mitigate | Plan 03 `chmod 0777 results` before mount; covered in Plan 03 threat model. |
| T-03-SC | Tampering | npm/pip/cargo install supply-chain | mitigate | No new packages installed in this plan. cargo-chef already in Plans 01/02. |
</threat_model>

<verification>
End-of-plan automated verification:
- `[ -f results/jemalloc-alpine.json ] && jq -r '.[0].env.docker_image' results/jemalloc-alpine.json | grep -qx 'alpine:3.20'` — success criterion 2.
- `[ -f results/host-system.json ] && jq -r '.[0].env.os' results/host-system.json | grep -qx 'macos'` — success criterion 4.
- `[ "$(ls results/*.json | wc -l | tr -d ' ')" -ge 12 ]` — success criterion 3 (matrix produces per-cell JSON).
- `grep -q '## Image sizes' .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md` — success criterion 1 evidence captured.
- `grep -q '## Dive --ci scores' .planning/phases/03-docker-matrix-local-orchestration/03-04-SMOKE-RESULTS.md` — success criterion 5 evidence captured.
</verification>

<success_criteria>
This plan is complete when:
- [ ] 6 anchor cells attempted; ≥ 4 succeed (including the mandatory ptmalloc-debian-slim).
- [ ] `results/jemalloc-alpine.json` exists with `docker_image: "alpine:3.20"` and `target_triple: "x86_64-unknown-linux-musl"` literal (success criterion 2).
- [ ] `just bench-all-smoke` ran to completion; ≥ 12 of 18 cells produced `results/*.json` (success criterion 3).
- [ ] `results/host-system.json` exists with `os: "macos"`, `docker_image: null`, `target_triple` matching `(aarch64|x86_64)-apple-darwin` (success criterion 4).
- [ ] `just dive-check-all` ran to completion; per-cell dive scores documented (success criterion 5).
- [ ] All 8 OCI annotations verified populated on every successfully-built image (success criterion 1).
- [ ] SMOKE-RESULTS.md is the canonical evidence document with sections: Anchor cells, Full matrix, Image sizes, OCI annotations, Dive --ci scores, Bench-host, D-01 escape-hatch drops.
- [ ] Any D-01 escape-hatch drops are documented with link-error excerpts.
</success_criteria>

<output>
Create `.planning/phases/03-docker-matrix-local-orchestration/03-04-SUMMARY.md` (the standard plan SUMMARY.md, distinct from SMOKE-RESULTS.md) documenting:
1. Tasks 1-5 outcomes (bench results + image sizes + dive scores).
2. Cells dropped via D-01 escape hatch (with reason).
3. Any deviations from the success criteria (e.g., one cell failed but the matrix is still meaningful).
4. Reference to `03-04-SMOKE-RESULTS.md` for the per-cell evidence detail.
5. Phase-3 ROADMAP success criteria 1-5 status (each as ✓ or ✗ with one-line justification).
</output>
</content>
</invoke>