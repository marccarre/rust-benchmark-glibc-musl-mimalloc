---
phase: 04-aggregator-dashboard
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - crates/alloc-bench-aggregator/src/diagrams.rs
  - crates/alloc-bench-aggregator/src/recommend.rs
  - crates/alloc-bench-aggregator/src/markdown.rs
  - crates/alloc-bench-aggregator/src/main.rs
  - crates/alloc-bench-aggregator/templates/index.html.tmpl
  - crates/alloc-bench-aggregator/tests/smoke.rs
  - crates/alloc-bench-aggregator/tests/fixtures/jemalloc-alpine.json
  - README.md
autonomous: true
requirements:
  - AGG-04
  - AGG-05
  - AGG-06
  - AGG-07
  - AGG-08
must_haves:
  truths:
    - "After `just aggregate`, REPORT.md contains a per-scenario allocator comparison table (one section per scenario) where the per-row best-throughput allocator is rendered as `**✓ {name}**` (bold + ✓ prefix per D-09); columns are Throughput, p50, p95, p99, p999, peak RSS."
    - "Suspect runs (`samples_count < 10000` OR `warmup_duration_s < 5.0` per D-07) appear in REPORT.md with an italic note appended to their throughput cell — `*(⚠ suspect: low samples)*` for sample-count failures, `*(⚠ suspect: short warmup)*` for warmup failures, and `*(⚠ suspect: low samples, short warmup)*` when both fire (AGG-04)."
    - "REPORT.md contains a `## Docker runtimes` section with a table whose rows are unique env labels (alphabetical) and columns are image_size_mb, build_time_s, run_overhead_pct; missing values render as `—` (em-dash U+2014); a footnote line documents that Phase 5 CI backfills via `docker inspect` (AGG-05, D-10)."
    - "REPORT.md contains a `## Allocator architectures` section with four `### {alloc}` subsections (alphabetical: jemalloc, mallocng, mimalloc, ptmalloc) each containing a `flowchart TD` Mermaid block of 10–15 nodes (AGG-06, D-11)."
    - "REPORT.md contains a `## Recommendations by workload` section with a markdown table where each rationale string is data-derived: it cites a measured percentage delta versus a runner-up (e.g., `+12.5% throughput vs ptmalloc on multithread`) when comparative data exists, and emits `*insufficient comparative data — only {alloc} measured*` when only one allocator was tested for that workload class. Hardcoded prose without measured rationale is forbidden (AGG-07, D-12)."
    - "README.md begins with `# rust-benchmark-glibc-musl-mimalloc` followed by a section heading `## How memory allocation works on Linux`, a `flowchart TD` Mermaid block whose nodes include `Application code`, `Rust std::alloc`, `#[global_allocator]`, `libc malloc`, `Kernel mmap / brk / sbrk`, and `Physical memory`, and the locked ~80-word paragraph from UI-SPEC line 175 verbatim (AGG-08, D-13)."
    - "Smoke test asserts: REPORT.md contains `flowchart TD` at least 4 times (one per allocator architecture); contains `## Recommendations by workload`; contains `## Docker runtimes`; contains the literal `**✓ ` (winner-prefix marker); contains `⚠ suspect` (italic suspect note) when run against the jemalloc-alpine suspect fixture; README.md contains `## How memory allocation works on Linux` AND `flowchart TD`."
    - "All commits use `feat(04):` / `chore(04):` / `docs(04):` / `test(04):` conventional prefixes; `cargo fmt --all --check` and `cargo clippy --workspace --all-targets -- -D warnings` pass before each commit."
    - "Running the aggregator twice against the same fixtures produces byte-identical REPORT.md (after stripping the single top-of-file `<!-- schema_version ... -->` timestamp line) — sort everything alphabetically, BTreeMap not HashMap, explicit numeric formatting per D-09."
  artifacts:
    - path: "crates/alloc-bench-aggregator/src/diagrams.rs"
      provides: "Four `pub const &'static str` Mermaid `flowchart TD` constants — PTMALLOC_DIAGRAM, MALLOCNG_DIAGRAM, JEMALLOC_DIAGRAM, MIMALLOC_DIAGRAM — each 10-15 nodes, source-URL cited as Rust doc comment above each constant; plus pub const ALL_DIAGRAMS in alphabetical order so markdown.rs can iterate without sorting (D-11, AGG-06)."
      contains: "PTMALLOC_DIAGRAM"
    - path: "crates/alloc-bench-aggregator/src/recommend.rs"
      provides: "`pub fn recommendations(runs: &[Run]) -> Vec<Recommendation>` returning one Recommendation per WorkloadClass with data-derived rationale strings citing measured deltas; `pub struct Recommendation { class: &'static str, allocator: String, rationale: String }`; suspect-aware ranking that appends `*(suspect)*` suffix when winner or runner-up is suspect; full unit-test suite with synthetic Run builder (D-12, AGG-07)."
      contains: "fn recommendations"
    - path: "crates/alloc-bench-aggregator/src/markdown.rs"
      provides: "Extended REPORT.md emitter — adds `## {scenario}` per-scenario allocator comparison tables with bold-and-check-prefix on best-throughput row + suspect-italic-note appended to suspect cells (D-09, D-07/AGG-04); `## Docker runtimes` table emitting em-dash for absent fields with footnote (D-10/AGG-05); `## Allocator architectures` section embedding the four Mermaid constants verbatim in alphabetical order (AGG-06); `## Recommendations by workload` section calling recommend::recommendations (AGG-07). Plan-01 `## Runs` bullet list is replaced by the per-scenario tables."
      contains: "## Recommendations by workload"
    - path: "crates/alloc-bench-aggregator/src/main.rs"
      provides: "Wire two new `mod diagrams; mod recommend;` declarations alongside the existing `mod html; mod loader; mod markdown;` so markdown.rs can import `crate::diagrams::*` and `crate::recommend::recommendations`."
      contains: "mod diagrams"
    - path: "crates/alloc-bench-aggregator/templates/index.html.tmpl"
      provides: "Extends Plan 02's HTML with a new `<section class=report-mirror>` block emitting the per-scenario allocator comparison table mirrored from REPORT.md, with winner cells styled `background: var(--color-winner-bg); color: var(--color-winner-text);` per UI-SPEC §Color §Per-row winner highlight lines 110-119. Built client-side from RESULTS in a new renderReportMirrorTable() JS function so the visual contract is consistent between HTML and Markdown."
      contains: "report-mirror"
    - path: "crates/alloc-bench-aggregator/tests/smoke.rs"
      provides: "Five new integration tests covering AGG-04..08: REPORT.md contains four flowchart TD blocks; contains `## Recommendations by workload`; contains `## Docker runtimes`; contains the **✓ winner marker; contains ⚠ suspect italic note when run against the suspect fixture; README.md contains `## How memory allocation works on Linux` AND a flowchart TD block."
      contains: "flowchart TD"
    - path: "crates/alloc-bench-aggregator/tests/fixtures/jemalloc-alpine.json"
      provides: "Plan 01 already includes one suspect run (samples_count=5000); Plan 03 appends a third Run with warmup_duration_s=2.0 so REPORT.md exercises BOTH suspect-predicate branches end-to-end."
      contains: "samples_count"
    - path: "README.md"
      provides: "Section `## How memory allocation works on Linux` inserted immediately after the existing `# rust-benchmark-glibc-musl-mimalloc` H1, containing the 8-node Mermaid flowchart from RESEARCH §Code Examples §8 + the LOCKED ~80-word paragraph from UI-SPEC line 175 verbatim. Hand-edited (D-13: aggregator must NOT mutate README from Rust)."
      contains: "How memory allocation works on Linux"
  key_links:
    - from: "crates/alloc-bench-aggregator/src/markdown.rs"
      to: "crates/alloc-bench-aggregator/src/diagrams.rs"
      via: "use crate::diagrams::ALL_DIAGRAMS"
      pattern: "use crate::diagrams"
    - from: "crates/alloc-bench-aggregator/src/markdown.rs"
      to: "crates/alloc-bench-aggregator/src/recommend.rs"
      via: "use crate::recommend::recommendations"
      pattern: "recommend::recommendations"
    - from: "crates/alloc-bench-aggregator/src/markdown.rs"
      to: "crates/alloc-bench-aggregator/src/html.rs"
      via: "use crate::html::{env_label, is_suspect}"
      pattern: "html::is_suspect"
    - from: "crates/alloc-bench-aggregator/src/main.rs"
      to: "crates/alloc-bench-aggregator/src/diagrams.rs + recommend.rs"
      via: "mod diagrams; mod recommend;"
      pattern: "mod (diagrams|recommend)"
    - from: "README.md"
      to: "GitHub-flavoured-markdown Mermaid renderer"
      via: "fenced mermaid block followed by flowchart TD"
      pattern: "flowchart TD"
---

<objective>
Phase 4 vertical-slice 3: make the report informative. Plan 01 shipped the loader + skeleton report; Plan 02 made it interactive (charts, filters, A/B picker, suspect badge). Plan 03 makes it READABLE: a developer scanning REPORT.md learns which allocator wins for which workload (with a measured delta), reads the four allocator architectures via Mermaid, and sees a Docker runtime comparison table. Separately, README.md gains a top-of-file system diagram so a first-time visitor understands the layered allocator stack.

Purpose: AGG-04 (per-row winner highlight + suspect propagation into the Markdown side), AGG-05 (Docker runtime table with em-dash fallback per D-10), AGG-06 (four Mermaid allocator-architecture diagrams), AGG-07 (data-derived recommendations table), and AGG-08 (README system diagram with locked verbatim paragraph) become real readable content. After Plan 03, Phase 4 is feature-complete except for Phase-5 CI backfill (image_size_mb / build_time_s / run_overhead_pct) which Plan 03 leaves explicitly as em-dash.

Output:
- crates/alloc-bench-aggregator/src/diagrams.rs (NEW) — four &'static str Mermaid constants with cited Wikipedia / upstream README sources as Rust doc comments.
- crates/alloc-bench-aggregator/src/recommend.rs (NEW) — recommendations(&[Run]) -> Vec<Recommendation> data-derived workload→allocator picker. Comprehensive unit tests exercising single-allocator fallback, suspect-aware ranking, and the rationale-formatting contract.
- crates/alloc-bench-aggregator/src/markdown.rs (EXTENDED) — Plan 01's basic emitter is upgraded: per-scenario allocator tables with winner highlight + suspect italic notes, Docker runtime table, Mermaid diagrams section, recommendations section.
- crates/alloc-bench-aggregator/src/main.rs (EXTENDED) — two new mod declarations.
- crates/alloc-bench-aggregator/templates/index.html.tmpl (EXTENDED) — HTML mirror of the per-scenario comparison table with green winner-cell tinting per UI-SPEC §Color §Per-row winner highlight.
- crates/alloc-bench-aggregator/tests/smoke.rs (EXTENDED) — five new assertions covering all five new requirements.
- crates/alloc-bench-aggregator/tests/fixtures/jemalloc-alpine.json (EXTENDED) — append a third Run with warmup_duration_s=2.0 so the warmup-suspect branch is observable in REPORT.md.
- README.md (EXTENDED) — append the locked AGG-08 system diagram + paragraph after the existing single-line H1.

Out of scope (handled by Phase 5):
- CI matrix / Dive image-size gate / GHA artifact upload (Phase 5 ORCH-04, ORCH-05).
- docker inspect-based image_size_mb backfill (Phase 5 — Plan 03 emits em-dash per D-10).
- README "Run it yourself" walkthrough (Phase 5 REPR-01 — Plan 03 only adds the system diagram + paragraph; Phase 5 will append the walkthrough below).
- Multi-run median + min/max range aggregation (Phase 5 REPR-03 — Plan 03 handles single-run-per-cell only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-aggregator-dashboard/04-CONTEXT.md
@.planning/phases/04-aggregator-dashboard/04-RESEARCH.md
@.planning/phases/04-aggregator-dashboard/04-PATTERNS.md
@.planning/phases/04-aggregator-dashboard/04-UI-SPEC.md
@.planning/phases/04-aggregator-dashboard/04-01-PLAN.md
@.planning/phases/04-aggregator-dashboard/04-02-PLAN.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase + Plan 01/02 final state. -->

From crates/alloc-bench-core/src/output.rs (post-Plan-01 — Deserialize derives are in):

  pub const SCHEMA_VERSION: u32 = 1;
  pub struct Run { schema_version, run_id, env, build, scenario, harness, metrics, status: Option<String>, error: Option<String> }
  pub struct Env { os, os_version, docker_image: Option<String>, cpu_model, cpu_count: u32, memory_total_kb: u64 }
  pub struct Build { allocator: String, rustc_version, target_triple, host_triple, profile, git_sha, git_dirty: bool, build_timestamp, rustflags }
  pub struct ScenarioInfo { name: String, config: serde_json::Value, unit: Option<String> }
  pub struct HarnessInfo { warmup_duration_s: f64, measurement_duration_s: f64, samples_count: u64 }
  pub struct LatencyNs { p50: u64, p95: u64, p99: u64, p999: u64, max: u64 }
  pub struct Metrics { ticks_per_s: f64, allocations_per_tick: u64, tick_latency_ns: LatencyNs, peak_rss_kb: u64, rss_growth_samples: Vec<RssGrowthSample>, rusage: Rusage, allocator_stats: serde_json::Value }

From crates/alloc-bench-aggregator/src/html.rs (post-Plan-02 final state — these helpers exist and are pub(crate); markdown.rs imports them):

  pub(crate) fn env_label(env: &Env) -> &str   // returns env.docker_image.as_deref().unwrap_or("host")
  pub(crate) fn is_suspect(h: &HarnessInfo) -> bool   // returns h.samples_count < 10_000 || h.warmup_duration_s < 5.0

From crates/alloc-bench-aggregator/src/loader.rs (post-Plan-01):

  pub struct LoadOutcome { pub runs: Vec<Run>, pub skipped: Vec<SkippedFile> }
  pub struct SkippedFile { pub path: PathBuf, pub reason: String }
  pub fn discover(pattern: &str) -> Result<LoadOutcome>

From crates/alloc-bench-aggregator/src/markdown.rs (Plan-01 surface to be EXTENDED, not rewritten):

  pub fn write(outcome: &LoadOutcome, out_dir: &Path) -> Result<()>
  // internal helpers Plan 01 may have factored: emit_header, emit_skipped_inputs.
  // Plan 03 ADDS: emit_per_scenario_tables, emit_docker_runtime_table, emit_allocator_diagrams, emit_recommendations.
  // Plan 03 REPLACES: the basic `## Runs` bullet-list section from Plan 01 (tables subsume it).

Suspect predicate branches (D-07 — strict less-than thresholds):
- samples_count < 10_000     → italic note `*(⚠ suspect: low samples)*`
- warmup_duration_s < 5.0    → italic note `*(⚠ suspect: short warmup)*`
- Both true                   → italic note `*(⚠ suspect: low samples, short warmup)*` (combined; emit both reasons in a single italic note for byte-identical-output stability)

Per-scenario table format (D-09):
- Heading: `## {scenario.name}` (alphabetical scenario order)
- Columns: `| allocator | throughput | p50 | p95 | p99 | p999 | peak RSS |`
- Best-throughput row: `**✓ {alloc}**` (bold + ✓ prefix)
- Suspect cells: throughput cell becomes `{value} *(⚠ suspect: ...)*`

Docker runtimes table format (D-10, AGG-05):
- Heading: `## Docker runtimes`
- Columns: `| env | image_size_mb | build_time_s | run_overhead_pct |`
- Rows: alphabetical unique env labels (env.docker_image.as_deref().unwrap_or("host"))
- Missing fields: `—` (em-dash U+2014)
- Footnote line below table: italic line documenting that Phase 5 CI backfills these via docker inspect (REPR-03)

Recommendations table format (D-12, AGG-07):
- Heading: `## Recommendations by workload`
- Columns: `| Workload | Recommended | Rationale |`
- Workload classes (locked, alphabetical for byte-identical output): channel-heavy, contention, cpu-bound, fragmentation-prone, memory-bound, web-ser-de
- Workload→scenario mapping (locked):
  - channel-heavy: scenarios spmc, mpsc, mpmc (best across all three by mean throughput)
  - contention:    scenario contention
  - cpu-bound:     scenario cpu-bound
  - fragmentation-prone: scenario fragmentation-soak
  - memory-bound:  scenario mem-bound
  - web-ser-de:    scenario web
- Rationale string: `+{delta:.1}% throughput vs {runner_up} on {scenario}` when comparative data exists
- Single-allocator fallback: `insufficient comparative data — only {alloc} measured`

Mermaid sources (RESEARCH §Mermaid Allocator Diagram Sources lines 840-887):
- ptmalloc:  https://en.wikipedia.org/wiki/C_dynamic_memory_allocation
- mallocng:  https://github.com/richfelker/mallocng-draft
- jemalloc:  https://github.com/jemalloc/jemalloc/blob/dev/doc/jemalloc.xml.in
- mimalloc:  https://github.com/microsoft/mimalloc

README.md system-diagram paragraph (UI-SPEC line 175 — LOCKED VERBATIM):
"When a Rust program calls `Vec::new()` or `Box::new(x)`, the request travels through `std::alloc` → the configured `#[global_allocator]` (jemalloc / mimalloc / system) → libc malloc (ptmalloc on glibc, mallocng on musl) → the kernel's `mmap` / `brk` / `sbrk` → physical memory. Each layer can change the cost, fragmentation profile, and tail-latency shape of an allocation. This benchmark measures those differences across four allocators, six libc·env combinations, and eleven workload scenarios."

README.md system-diagram Mermaid block (RESEARCH §Code Examples §8 lines 826-838) — 6 nodes labeled Application code, Rust std::alloc, #[global_allocator], libc malloc, Kernel mmap/brk/sbrk, Physical memory, chained app → std → ga → libc → kernel → phys.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Mermaid allocator diagrams (diagrams.rs) + wire mod declarations in main.rs</name>
  <files>crates/alloc-bench-aggregator/src/diagrams.rs, crates/alloc-bench-aggregator/src/recommend.rs, crates/alloc-bench-aggregator/src/main.rs</files>
  <read_first>
    - crates/alloc-bench-aggregator/src/main.rs (post-Plan-02 final state — existing `mod html; mod loader; mod markdown;` declarations; need to add `mod diagrams; mod recommend;` between them in alphabetical order)
    - .planning/phases/04-aggregator-dashboard/04-RESEARCH.md §"Code Examples §7" lines 802-823 (PTMALLOC_DIAGRAM raw-string skeleton — exact node set: thread→arena selector→main arena/per-thread arenas→fastbins/smallbins/largebins/unsorted bin/mmap chunks; ~10 nodes)
    - .planning/phases/04-aggregator-dashboard/04-RESEARCH.md §"Mermaid Allocator Diagram Sources" lines 840-887 (the four upstream sources + the per-allocator key concepts; 10-15 nodes per allocator)
    - .planning/phases/04-aggregator-dashboard/04-PATTERNS.md §"crates/alloc-bench-aggregator/src/diagrams.rs (constants module, static data)" lines 459-498 (constants module shape + alphabetical emission order rationale)
    - .planning/phases/04-aggregator-dashboard/04-UI-SPEC.md §"Mermaid Theme Contract" lines 263-276 (no styling, default theme, plain-text edge labels, allocator diagrams use flowchart TD)
  </read_first>
  <behavior>
    - Behavior 1: crates/alloc-bench-aggregator/src/diagrams.rs exports four public constants: PTMALLOC_DIAGRAM, MALLOCNG_DIAGRAM, JEMALLOC_DIAGRAM, MIMALLOC_DIAGRAM, each pub const &'static str.
    - Behavior 2: Each constant is a raw-string literal containing exactly one fenced flowchart TD Mermaid block — opening fence followed by flowchart TD followed by 10–15 node/edge lines followed by a closing fence followed by a trailing newline. The result of running grep on flowchart TD against this file returns exactly 4.
    - Behavior 3: Each constant has a Rust doc comment (/// Source: <upstream-url>) above it citing the upstream URL from RESEARCH §Mermaid Allocator Diagram Sources.
    - Behavior 4: A unit test inside diagrams.rs (#[cfg(test)] mod tests) named each_diagram_has_flowchart_td_and_min_nodes asserts each constant contains the substring `flowchart TD`, contains the opening fence (mermaid info-string), the closing fence count is exactly 2, and contains at least 10 lines.
    - Behavior 5: crates/alloc-bench-aggregator/src/main.rs declares `mod diagrams;` and `mod recommend;` in addition to the existing `mod html; mod loader; mod markdown;` (alphabetical order: diagrams, html, loader, markdown, recommend). The aggregator binary continues to compile with `cargo build -p alloc-bench-aggregator --release`.
    - Behavior 6: A second unit test diagrams_in_alphabetical_emission_order documents the alphabetical-emission contract by exposing a pub const ALL_DIAGRAMS: [(&str, &str); 4] slice in alphabetical order: [("jemalloc", JEMALLOC_DIAGRAM), ("mallocng", MALLOCNG_DIAGRAM), ("mimalloc", MIMALLOC_DIAGRAM), ("ptmalloc", PTMALLOC_DIAGRAM)]. The test asserts ALL_DIAGRAMS[0].0 == "jemalloc" and ALL_DIAGRAMS[3].0 == "ptmalloc".
    - Behavior 7: Because Task 2 implements recommend.rs but Task 1 wires `mod recommend;` in main.rs, Task 1 ALSO creates a stub `crates/alloc-bench-aggregator/src/recommend.rs` containing only a single module-level doc comment (e.g., `//! Stub — Task 2 implements this module.`) so cargo build stays green between Task 1 and Task 2. Task 2 wholesale-rewrites the stub.
  </behavior>
  <action>
Create crates/alloc-bench-aggregator/src/diagrams.rs from scratch. The file is a constants module with NO function bodies beyond unit tests (PATTERNS §"diagrams.rs (constants module, static data)" lines 459-498). Module structure:

  - Module-level doc comment: explain that the file holds static Mermaid flowchart TD constants for the four allocators; sources cited per allocator; diagrams change only when our understanding of the allocator changes, not per benchmark run (D-11); emitted verbatim into REPORT.md by markdown.rs in alphabetical order (jemalloc, mallocng, mimalloc, ptmalloc) per RESEARCH §Pitfall 5.
  - Four `pub const X_DIAGRAM: &str` declarations, each preceded by a `/// Source: <upstream-url>` doc comment.
  - One `pub const ALL_DIAGRAMS: [(&str, &str); 4]` slice in alphabetical order so markdown.rs can iterate it without imposing its own ordering logic.
  - One `#[cfg(test)] mod tests` block with the two unit tests from <behavior>.

Diagram content (each ~10–15 nodes, plain-text edge labels per UI-SPEC §Mermaid Theme Contract). Mermaid syntax uses NO `{` characters in node labels — square brackets `[label]` are the node-label syntax — so no tinytemplate brace-escape concern applies in this file. The &str is plain markdown emitted into REPORT.md by markdown.rs, NOT through tinytemplate.

  - PTMALLOC_DIAGRAM (Source: https://en.wikipedia.org/wiki/C_dynamic_memory_allocation; ~10 nodes per RESEARCH §Code Examples §7 lines 802-823): thread→arena selector; arena→main arena (heap brk); arena→per-thread arenas; main→fastbins (≤80B); main→smallbins (≤504B); main→largebins (≤128KB); main→unsorted bin; main→top chunk; large→mmap chunks ≥128KB; per→per-arena fastbins; per→per-arena smallbins. ~11 nodes; safely within 10–15.

  - MALLOCNG_DIAGRAM (Source: https://github.com/richfelker/mallocng-draft; ~10 nodes): thread→size class selector; classsel→active group for class; classsel→new group from heap; active→group of N≤32 slots; newg→slots; slots→slot bitmap status; slots→out-of-band group header; slots→16-byte base alignment; classsel→large alloc 1-slot mmap group. ~10 nodes.

  - JEMALLOC_DIAGRAM (Source: https://github.com/jemalloc/jemalloc/blob/dev/doc/jemalloc.xml.in; ~13 nodes per RESEARCH §Mermaid lines 866-874): thread→per-thread tcache; tcache→arena (4× num_cpus); arena→bin small size class; arena→extent; binsmall→slab in extent; slab→slot; slot→slab bitmap; arena→large alloc dedicated extent; tcache→bin medium 16/32/64B intervals; binmedium→slab; arena→page-size quantization 2-4KiB. ~12 nodes.

  - MIMALLOC_DIAGRAM (Source: https://github.com/microsoft/mimalloc; ~13 nodes per RESEARCH §Mermaid lines 877-887): thread→per-thread heap; heap→segment 64KB OS alloc; seg→page (one size class); page→thread-local free list; page→concurrent free list; concfree→cross-thread free via CAS; heap→first-class heap; heap→arena for OS reservations; page→free-list sharding; seg→eager page purging; thread→alloc hot path; alloc→tlfree. ~13 nodes.

Each constant uses a `r#"..."#` raw string starting with a leading newline so writeln-ing it into a markdown buffer produces a clean spacer-line followed by a fenced block. Format pattern: leading newline, then a fenced opener line containing the literal triple-backtick + mermaid info-string, then `flowchart TD` on its own line, then the indented node/edge lines, then the closing fence triple-backtick on its own line, then a trailing newline.

Add `pub const ALL_DIAGRAMS: [(&str, &str); 4] = [("jemalloc", JEMALLOC_DIAGRAM), ("mallocng", MALLOCNG_DIAGRAM), ("mimalloc", MIMALLOC_DIAGRAM), ("ptmalloc", PTMALLOC_DIAGRAM)];` — alphabetical so markdown.rs can iterate without sorting.

Add #[cfg(test)] mod tests:

  - each_diagram_has_flowchart_td_and_min_nodes: iterate ALL_DIAGRAMS; for each (name, body) assert body.contains("flowchart TD"), body contains the mermaid info-string opener, the count of triple-backtick fences inside body equals 2 (one opener + one closer), body.lines().count() >= 10.
  - diagrams_in_alphabetical_emission_order: assert ALL_DIAGRAMS[0].0 == "jemalloc", ALL_DIAGRAMS[1].0 == "mallocng", ALL_DIAGRAMS[2].0 == "mimalloc", ALL_DIAGRAMS[3].0 == "ptmalloc".

Edit crates/alloc-bench-aggregator/src/main.rs. Add two new mod declarations alongside the existing `mod html; mod loader; mod markdown;` (Plan 01 wired the original three; Plan 03 adds two more in alphabetical order):

  - Final list: `mod diagrams; mod html; mod loader; mod markdown; mod recommend;` — five module declarations, alphabetical.

Create the stub crates/alloc-bench-aggregator/src/recommend.rs containing ONLY a single module-level doc comment line (e.g., a single `//! Stub — Task 2 implements this module.` line). A doc-comment-only file is a valid empty Rust module; clippy will emit no lints. Task 2 wholesale-rewrites this stub.

Apply `cargo fmt --all` and `cargo clippy --workspace --all-targets -- -D warnings` after edits. Run `cargo test -p alloc-bench-aggregator --lib -- diagrams::tests` to verify the two new unit tests pass.
  </action>
  <verify>
    <automated>cargo fmt --all --check &amp;&amp; cargo build -p alloc-bench-aggregator --release &amp;&amp; cargo test -p alloc-bench-aggregator --lib -- diagrams::tests &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - File assertion: `test -f crates/alloc-bench-aggregator/src/diagrams.rs && test -f crates/alloc-bench-aggregator/src/recommend.rs` exits 0 (both files exist after Task 1; recommend.rs is the stub).
    - Source assertion: `grep -cE "^pub const .*_DIAGRAM: &str" crates/alloc-bench-aggregator/src/diagrams.rs` returns exactly 4.
    - Source assertion: `grep -c "flowchart TD" crates/alloc-bench-aggregator/src/diagrams.rs` returns exactly 4.
    - Source assertion: `grep -F "ALL_DIAGRAMS" crates/alloc-bench-aggregator/src/diagrams.rs` returns at least 1 line.
    - Source assertion: `grep -F '"jemalloc"' crates/alloc-bench-aggregator/src/diagrams.rs` returns at least 1 line (alphabetical first entry).
    - Source assertion: `grep -F '"ptmalloc"' crates/alloc-bench-aggregator/src/diagrams.rs` returns at least 1 line (alphabetical last entry).
    - Source assertion: `grep -cE "^/// Source: " crates/alloc-bench-aggregator/src/diagrams.rs` returns at least 4 (one per constant).
    - Source assertion: `grep -F "wikipedia.org/wiki/C_dynamic_memory_allocation" crates/alloc-bench-aggregator/src/diagrams.rs` returns at least 1 line (ptmalloc source).
    - Source assertion: `grep -F "github.com/microsoft/mimalloc" crates/alloc-bench-aggregator/src/diagrams.rs` returns at least 1 line (mimalloc source).
    - Source assertion: `grep -F "github.com/richfelker/mallocng-draft" crates/alloc-bench-aggregator/src/diagrams.rs` returns at least 1 line (mallocng source).
    - Source assertion: `grep -F "github.com/jemalloc/jemalloc" crates/alloc-bench-aggregator/src/diagrams.rs` returns at least 1 line (jemalloc source).
    - Source assertion: `grep -cE "^mod (diagrams|recommend);" crates/alloc-bench-aggregator/src/main.rs` returns exactly 2.
    - Test command: `cargo test -p alloc-bench-aggregator --lib -- diagrams::tests::each_diagram_has_flowchart_td_and_min_nodes diagrams::tests::diagrams_in_alphabetical_emission_order` exits 0 with both tests green.
    - Code-style: `cargo clippy --workspace --all-targets -- -D warnings` passes with the stub recommend.rs (a single doc-comment line is valid; clippy issues no lints).
  </acceptance_criteria>
  <done>The four Mermaid allocator-architecture constants exist with cited sources; the alphabetical iteration slice ALL_DIAGRAMS is in place; main.rs wires both new modules; the recommend.rs stub keeps the build green for Task 2 to fill in. Two unit tests gate the diagram shape (flowchart TD present, fence count = 2, ≥ 10 lines).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement recommend.rs (data-derived workload→allocator picker)</name>
  <files>crates/alloc-bench-aggregator/src/recommend.rs</files>
  <read_first>
    - crates/alloc-bench-aggregator/src/recommend.rs (Task 1 stub — wholesale-rewrite this file)
    - crates/alloc-bench-aggregator/src/html.rs (post-Plan-02 — confirm pub(crate) fn is_suspect(h: &HarnessInfo) -> bool exists; recommend.rs imports it via use crate::html::is_suspect)
    - crates/alloc-bench-core/src/output.rs (post-Plan-01 — confirm Run / Build / ScenarioInfo / HarnessInfo / Metrics shapes used by recommend logic)
    - crates/alloc-bench-core/src/scenarios/multithread.rs (lines 39-69 for the validated() pattern; lines 157-231 for the unit-test cfg builder pattern — recommend.rs unit tests follow the same shape per PATTERNS §"recommend.rs" lines 372-456)
    - .planning/phases/04-aggregator-dashboard/04-CONTEXT.md §"D-12 Recommendations section" lines 52-60 (the workload classes table + the data-derived-rationale rule)
    - .planning/phases/04-aggregator-dashboard/04-RESEARCH.md §"Pitfall 7: Recommendations table emits hard-coded prose" lines 624-634 (every claim must be data-derived; emit "insufficient comparative data" when only one allocator was measured)
    - .planning/phases/04-aggregator-dashboard/04-PATTERNS.md §"crates/alloc-bench-aggregator/src/recommend.rs" lines 372-456 (full unit-test pattern with synth_run helper)
    - .planning/phases/04-aggregator-dashboard/04-UI-SPEC.md §"Copywriting Contract" line 172 (`## Recommendations by workload` heading is locked here)
  </read_first>
  <behavior>
    - Behavior 1: pub fn recommendations(runs: &[Run]) -> Vec<Recommendation> returns exactly six rows (one per workload class) in alphabetical class order: channel-heavy, contention, cpu-bound, fragmentation-prone, memory-bound, web-ser-de.
    - Behavior 2: When two or more allocators have data for a workload class, the rationale string format is `+{delta:.1}% throughput vs {runner_up} on {scenario}` — e.g., `+12.5% throughput vs ptmalloc on multithread` — where `delta = ((winner.ticks_per_s - runner_up.ticks_per_s) / runner_up.ticks_per_s) * 100.0`. Format uses `{:.1}` precision (one decimal) for byte-identical-output stability per RESEARCH §Pitfall 5.
    - Behavior 3: When only one allocator has data for a workload class, the Recommendation has `allocator = "(only X measured)"` and `rationale = "insufficient comparative data — only X measured"` (D-12, RESEARCH §Pitfall 7). The recommendation is still emitted (not skipped) so REPORT.md shows the full six rows.
    - Behavior 4: When no allocators have data for a workload class (no runs match the scenario set), the Recommendation has `allocator = "—"` (em-dash) and `rationale = "no measurements"`. REPORT.md still emits the row so a reader sees the gap explicitly.
    - Behavior 5: Suspect runs (per D-07 / crate::html::is_suspect) are STILL counted in the ranking (they have measured data) but their rationale string is suffixed with ` *(suspect)*`: e.g., `+12.5% throughput vs ptmalloc on multithread *(suspect)*` if EITHER the winner OR the runner-up is suspect. This matches D-07's "suspect runs are kept in the output but rendered with" contract — we don't drop them, we annotate.
    - Behavior 6: channel-heavy workload class is computed across the THREE channel scenarios (spmc, mpsc, mpmc). Per-allocator score = mean throughput across whichever of the three are present. If only one channel scenario is present, that's the score. If no channel scenarios are present, the class falls into the "no measurements" branch.
    - Behavior 7: Comprehensive unit-test suite (PATTERNS §recommend.rs lines 404-450): synthetic Run builder; tests for two-allocator delta, three-allocator picking the top, single-allocator fallback, zero-allocator no-measurements branch, suspect-suffix annotation, channel-heavy mean-of-three logic, alphabetical class order in the output Vec.
    - Behavior 8: All tests pass via `cargo test -p alloc-bench-aggregator --lib -- recommend::tests`.
  </behavior>
  <action>
Wholesale-rewrite crates/alloc-bench-aggregator/src/recommend.rs (the Task-1 stub is replaced). Module surface:

  - Module-level doc comment explaining: workload→allocator picker; returns one Recommendation per WorkloadClass with rationale strings derived from measured RUN data (D-12); hard-coded prose is forbidden per RESEARCH §Pitfall 7.
  - `use alloc_bench_core::output::Run;`
  - `use crate::html::is_suspect;` — the canonical D-07 predicate from Plan 02 Task 1.
  - `use std::collections::BTreeMap;` — for byte-identical-output ordering (RESEARCH §Pitfall 5).

Public types:

  - `pub struct Recommendation { pub class: &'static str, pub allocator: String, pub rationale: String }`
  - Internal `enum WorkloadClass { ChannelHeavy, Contention, CpuBound, FragmentationProne, MemoryBound, WebSerDe }` with helper `fn label(&self) -> &'static str` returning the lowercase-hyphenated form (channel-heavy, contention, cpu-bound, fragmentation-prone, memory-bound, web-ser-de) and helper `fn scenarios(&self) -> &'static [&'static str]` returning the matching scenario name set per the interfaces table (channel-heavy → ["spmc", "mpsc", "mpmc"]; others single-element).
  - `const ALL_CLASSES: [WorkloadClass; 6]` in alphabetical order: ChannelHeavy, Contention, CpuBound, FragmentationProne, MemoryBound, WebSerDe.

Public function:

  - `pub fn recommendations(runs: &[Run]) -> Vec<Recommendation>`:
    1. For each class in ALL_CLASSES.iter():
       a. Filter runs whose scenario.name is in class.scenarios().
       b. Group by build.allocator (use BTreeMap<String, Vec<&Run>>).
       c. If groups.is_empty() → push Recommendation with class label, allocator = em-dash, rationale = "no measurements"; continue.
       d. For each allocator group: compute score = mean(run.metrics.ticks_per_s for run in group). (Channel-heavy with 3 scenarios returns mean across whichever of the three the allocator measured; cpu-bound with 1 scenario returns just that scenario's value.)
       e. If groups.len() == 1 → unwrap the single allocator → push Recommendation with class label, allocator string `(only {alloc} measured)`, rationale `insufficient comparative data — only {alloc} measured`; continue.
       f. Else: sort allocator-score pairs by score descending (BTreeMap iteration is alphabetical; sorting by descending score with stable ordering means ties break alphabetically). Take top two as (winner, runner_up).
       g. Compute delta = `((winner.score - runner_up.score) / runner_up.score) * 100.0` (use `if runner_up.score > 0.0` guard to avoid division by zero; if zero, set delta = 0.0).
       h. Pick the scenario for the rationale: if class.scenarios().len() == 1, use that. If multiple (channel-heavy), use whichever scenario gave the winner the largest absolute throughput (most representative win). If still ambiguous, fall back to the alphabetically-first scenario in class.scenarios().
       i. Compute is_either_suspect = winner.runs.iter().any(|r| is_suspect(&r.harness)) || runner_up.runs.iter().any(|r| is_suspect(&r.harness)).
       j. Build rationale:
          - Base: `format!("+{:.1}% throughput vs {} on {}", delta, runner_up.allocator, scenario)`.
          - If is_either_suspect, append ` *(suspect)*`.
       k. Push Recommendation with class label, winner.allocator, rationale.
    2. Return the Vec (six entries, alphabetical class order).

Helpers:

  - `fn winner_scenario_for_channel(winner_runs: &BTreeMap<&str, f64>) -> String` — scans the per-scenario throughput map; returns the scenario name with the maximum throughput (alphabetical tiebreak via BTreeMap iteration order). Used in step h above.
  - `fn group_runs_by_allocator<'a>(runs: &'a [&'a Run]) -> BTreeMap<&'a str, Vec<&'a Run>>` — pure utility for the score-grouping step; testable in isolation.

Unit tests (#[cfg(test)] mod tests) — pattern from PATTERNS §recommend.rs lines 404-450:

  - synth_run helper signature: `fn synth_run(alloc: &str, scenario: &str, ticks_per_s: f64, samples_count: u64, warmup_s: f64) -> Run` — builds a canonical Run with the given allocator/scenario/throughput/harness fields; other fields use sentinels (cpu_model="test-cpu", memory_total_kb=1, etc.).

  Tests:
    1. winner_picker_emits_data_derived_rationale_two_allocators: Two runs (jemalloc=100.0 cpu-bound, ptmalloc=80.0 cpu-bound, both clean). Expect a recommendation for cpu-bound with allocator == "jemalloc" and rationale.starts_with("+25.0% throughput vs ptmalloc on cpu-bound"). Verify +25.0% = ((100-80)/80)*100.
    2. winner_picker_three_allocators_picks_top_with_runner_up: Three runs (jemalloc=100.0, mimalloc=110.0, ptmalloc=80.0, all cpu-bound). Expect rec with allocator == "mimalloc" and rationale.starts_with("+10.0% throughput vs jemalloc on cpu-bound") — runner-up is the second-best (jemalloc=100, NOT ptmalloc=80).
    3. winner_picker_single_allocator_fallback: One run (ptmalloc=100.0 web). Expect rec for web-ser-de with allocator == "(only ptmalloc measured)" and rationale == "insufficient comparative data — only ptmalloc measured".
    4. winner_picker_no_runs_for_class_emits_no_measurements: No runs at all. Expect six recommendations, every one having allocator == "—" (em-dash) and rationale == "no measurements".
    5. winner_picker_alphabetical_class_order: Two runs (jemalloc=100, mimalloc=110, both cpu-bound). Assert recommendations()[0].class == "channel-heavy" (first alphabetically), recommendations()[2].class == "cpu-bound", recommendations()[5].class == "web-ser-de".
    6. winner_picker_suspect_winner_appends_suspect_suffix: Two runs — jemalloc=100.0 cpu-bound with samples_count=5000 (suspect) — vs ptmalloc=80.0 cpu-bound clean. Expect rec rationale.ends_with("*(suspect)*").
    7. winner_picker_suspect_runner_up_also_appends_suffix: Two runs — jemalloc=100.0 cpu-bound clean vs ptmalloc=80.0 cpu-bound with warmup_duration_s=2.0 (warmup-suspect). Expect rec rationale.ends_with("*(suspect)*").
    8. winner_picker_channel_heavy_means_three_scenarios: Six runs — jemalloc has spmc=100, mpsc=200, mpmc=300 (mean=200); ptmalloc has spmc=50, mpsc=100, mpmc=150 (mean=100). Expect rec with class == "channel-heavy", allocator == "jemalloc", rationale.starts_with("+100.0% throughput vs ptmalloc on") (the scenario in the rationale is whichever channel scenario gave jemalloc its max throughput → mpmc).
    9. winner_picker_handles_zero_throughput_runner_up_without_div_by_zero: Two runs — jemalloc=100.0 cpu-bound vs ptmalloc=0.0 cpu-bound. Expect no panic; expect delta == 0.0 in the rationale (`+0.0% throughput vs ptmalloc on cpu-bound`).

Apply `cargo fmt --all` and `cargo clippy --workspace --all-targets -- -D warnings`. Run `cargo test -p alloc-bench-aggregator --lib -- recommend::tests` to verify all nine tests pass.
  </action>
  <verify>
    <automated>cargo fmt --all --check &amp;&amp; cargo build -p alloc-bench-aggregator --release &amp;&amp; cargo test -p alloc-bench-aggregator --lib -- recommend::tests &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "pub fn recommendations" crates/alloc-bench-aggregator/src/recommend.rs` returns exactly 1.
    - Source assertion: `grep -c "pub struct Recommendation" crates/alloc-bench-aggregator/src/recommend.rs` returns exactly 1.
    - Source assertion: `grep -F "use crate::html::is_suspect" crates/alloc-bench-aggregator/src/recommend.rs` returns 1 line (reuses Plan 02's predicate).
    - Source assertion: `grep -F "BTreeMap" crates/alloc-bench-aggregator/src/recommend.rs` returns at least 1 line (byte-identical-output ordering per RESEARCH §Pitfall 5).
    - Source assertion: `grep -F "channel-heavy" crates/alloc-bench-aggregator/src/recommend.rs` returns at least 1 line.
    - Source assertion: `grep -F "fragmentation-prone" crates/alloc-bench-aggregator/src/recommend.rs` returns at least 1 line.
    - Source assertion: `grep -F "web-ser-de" crates/alloc-bench-aggregator/src/recommend.rs` returns at least 1 line.
    - Source assertion: `grep -F "insufficient comparative data" crates/alloc-bench-aggregator/src/recommend.rs` returns at least 1 line (single-allocator fallback string).
    - Source assertion: `grep -F "no measurements" crates/alloc-bench-aggregator/src/recommend.rs` returns at least 1 line (zero-allocator branch).
    - Source assertion: `grep -F "*(suspect)*" crates/alloc-bench-aggregator/src/recommend.rs` returns at least 1 line (suspect suffix).
    - Source assertion: `grep -F "% throughput vs" crates/alloc-bench-aggregator/src/recommend.rs` returns at least 1 line (the rationale format string template).
    - Test command: `cargo test -p alloc-bench-aggregator --lib -- recommend::tests::winner_picker_emits_data_derived_rationale_two_allocators recommend::tests::winner_picker_three_allocators_picks_top_with_runner_up recommend::tests::winner_picker_single_allocator_fallback recommend::tests::winner_picker_no_runs_for_class_emits_no_measurements recommend::tests::winner_picker_alphabetical_class_order recommend::tests::winner_picker_suspect_winner_appends_suspect_suffix recommend::tests::winner_picker_suspect_runner_up_also_appends_suffix recommend::tests::winner_picker_channel_heavy_means_three_scenarios recommend::tests::winner_picker_handles_zero_throughput_runner_up_without_div_by_zero` exits 0 with all NINE tests green.
    - Code-style: `cargo clippy --workspace --all-targets -- -D warnings` passes.
  </acceptance_criteria>
  <done>recommend.rs emits exactly six recommendations (one per workload class, alphabetical) with data-derived rationale strings citing measured percentage deltas. Single-allocator fallback and zero-allocator no-measurements branches are explicit. Suspect runs propagate a *(suspect)* suffix. Nine unit tests gate the contract — each rationale claim is provably derivable from synthetic input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend markdown.rs — per-scenario tables (winner highlight + suspect notes), Docker runtimes table, Mermaid diagrams, Recommendations</name>
  <files>crates/alloc-bench-aggregator/src/markdown.rs</files>
  <read_first>
    - crates/alloc-bench-aggregator/src/markdown.rs (Plan 01 final state — confirm pub fn write(outcome, out_dir) surface, the existing header / timestamp comment / Skipped Inputs emitters; Plan 03 EXTENDS this file, does not rewrite it)
    - crates/alloc-bench-aggregator/src/diagrams.rs (Task 1 — confirm pub const ALL_DIAGRAMS plus four *_DIAGRAM constants)
    - crates/alloc-bench-aggregator/src/recommend.rs (Task 2 — confirm pub fn recommendations(&[Run]) -> Vec<Recommendation> and the Recommendation struct shape)
    - crates/alloc-bench-aggregator/src/html.rs (post-Plan-02 — confirm env_label(env) and is_suspect(harness) helpers exist and are pub(crate))
    - .planning/phases/04-aggregator-dashboard/04-CONTEXT.md §"D-09 Allocator comparison table" + "D-10 Docker runtime comparison table" + "D-11 Mermaid architecture diagrams" + "D-12 Recommendations section" lines 49-60
    - .planning/phases/04-aggregator-dashboard/04-RESEARCH.md §"Pitfall 5: Byte-identical output reproducibility" lines 584-601 (sort everything alphabetically; use BTreeMap; format integers as `{}`, latency ns as `{} ns`, throughput as `{:.1}`; explicit precision)
    - .planning/phases/04-aggregator-dashboard/04-RESEARCH.md §"Pitfall 6: image_size_mb field expectations vs. v1 schema reality" lines 603-621 (env.image_size_mb / build_time_s / run_overhead_pct are NOT in the schema; emit em-dash)
    - .planning/phases/04-aggregator-dashboard/04-PATTERNS.md §"crates/alloc-bench-aggregator/src/markdown.rs" lines 308-369 (sorted-output pattern; hand-rolled markdown table pattern with width specifiers; image_size_cell match pattern)
    - .planning/phases/04-aggregator-dashboard/04-UI-SPEC.md §"Copywriting Contract" lines 167-173 (REPORT.md heading strings: `## {scenario_name}`, `## Docker runtimes`, `## Allocator architectures`, `## Recommendations by workload`, `## Skipped inputs`)
  </read_first>
  <behavior>
    - Behavior 1: After Task 3, running `just aggregate` against the committed fixtures produces a report/REPORT.md containing — in order: the schema_version timestamp comment, `# alloc-bench REPORT`, the "**N runs across M cells.**" line from Plan 01, ONE `## {scenario}` section per unique scenario in alphabetical order, `## Docker runtimes`, `## Allocator architectures`, `## Recommendations by workload`, and (only if applicable) `## Skipped inputs`.
    - Behavior 2: Each `## {scenario}` section contains a markdown table with header `| allocator | throughput | p50 | p95 | p99 | p999 | peak RSS |` and a separator row `|---|---|---|---|---|---|---|`. Rows are alphabetical by allocator. Throughput is formatted `{:.1}` with the unit appended (`{ticks_per_s:.1} ticks/s` or use scenario.unit.as_deref().unwrap_or("ticks/s")). Latency cells are integer ns: `{p50} ns`, etc. Peak RSS as integer kB: `{peak_rss_kb} kB`. The best-throughput row's allocator cell becomes `**✓ {alloc}**` (bold + ✓ prefix per D-09).
    - Behavior 3: When a row's run is suspect, the THROUGHPUT cell gets an italic suffix appended: `{ticks_per_s:.1} ticks/s *(⚠ suspect: low samples)*` for samples_count < 10_000, OR `*(⚠ suspect: short warmup)*` for warmup_duration_s < 5.0, OR `*(⚠ suspect: low samples, short warmup)*` for both. The suffix lives in the throughput cell (not as a separate column) so the table layout stays clean and the suspect signal is co-located with the data it modifies.
    - Behavior 4: The `## Docker runtimes` section contains a markdown table with header `| env | image_size_mb | build_time_s | run_overhead_pct |` and rows for each unique env_label(env) in alphabetical order. Every value cell is em-dash U+2014 since the v1 schema has no image_size_mb / build_time_s / run_overhead_pct fields. Below the table: an italic footnote line saying that those columns are populated by Phase 5 CI via docker inspect (REPR-03).
    - Behavior 5: The `## Allocator architectures` section contains four `### {alloc}` subsections in alphabetical order (`### jemalloc`, `### mallocng`, `### mimalloc`, `### ptmalloc`) — iterate diagrams::ALL_DIAGRAMS directly (it's already alphabetical). Below each `### {alloc}` heading: a one-line pointer that the upstream source URL is cited in diagrams.rs, then the corresponding *_DIAGRAM const written verbatim. (The fenced mermaid block lives inside the constant per Task 1.)
    - Behavior 6: The `## Recommendations by workload` section contains a markdown table with header `| Workload | Recommended | Rationale |` and one row per recommendation in recommend::recommendations(&runs) output (six rows, alphabetical class order). The rationale cell is the rationale string verbatim — including the data-derived `+{delta:.1}%` substring AND the `*(suspect)*` annotation when applicable. NO hardcoded prose anywhere — every rationale comes from recommend::recommendations.
    - Behavior 7: Running the aggregator twice against the same fixtures produces byte-identical REPORT.md after stripping the single top-of-file schema_version timestamp line. (D-09 contract.)
    - Behavior 8: A unit test inside markdown.rs (#[cfg(test)] mod tests) named per_scenario_table_marks_winner_with_check_prefix builds three synthetic runs (jemalloc=100, mimalloc=110, ptmalloc=80, all cpu-bound), calls the per-scenario table emitter into a String buffer, and asserts the output contains `**✓ mimalloc**` (winner) AND does NOT contain `**✓ jemalloc**` AND does NOT contain `**✓ ptmalloc**`.
    - Behavior 9: A unit test per_scenario_table_appends_suspect_note_to_low_samples_run builds two runs (jemalloc cpu-bound with samples_count=5000, ptmalloc cpu-bound with samples_count=50000), calls the emitter, and asserts the output contains `*(⚠ suspect: low samples)*` exactly once (jemalloc row) AND does NOT contain "short warmup".
    - Behavior 10: A unit test per_scenario_table_marks_warmup_suspect builds one run with warmup_duration_s = 2.0, asserts the output contains `*(⚠ suspect: short warmup)*`.
    - Behavior 11: A unit test per_scenario_table_marks_both_suspect_predicates builds one run with both samples_count = 5000 AND warmup_duration_s = 2.0, asserts the output contains `*(⚠ suspect: low samples, short warmup)*` (single combined italic note for byte-identical-output stability).
    - Behavior 12: A unit test docker_runtimes_table_emits_em_dash_for_missing_fields builds two runs in different envs, calls the docker-runtimes emitter, and asserts the output contains exactly two non-header rows (one per unique env), every value cell is em-dash U+2014, and the footnote line about Phase 5 backfill is present.
    - Behavior 13: A unit test report_md_two_runs_byte_identical_after_timestamp_strip builds the same synthetic outcome twice, calls a `pub(crate) fn build_report(outcome) -> String` helper into two String buffers (so the test doesn't touch disk), strips the single timestamp comment line via simple string ops (first-line strip), and asserts the remaining bodies are byte-equal — gating the byte-identical-output contract.
  </behavior>
  <action>
Edit crates/alloc-bench-aggregator/src/markdown.rs to add four new emitter functions and rewrite pub fn write to call them in the right order. Do NOT delete the Plan 01 header / Skipped Inputs emitters — they continue to exist. The `## Runs` bullet-list emitter from Plan 01 is REMOVED (the per-scenario tables subsume it).

New imports at the top of markdown.rs:

  - `use std::collections::BTreeMap;`
  - `use std::fmt::Write;`
  - `use crate::diagrams::ALL_DIAGRAMS;`
  - `use crate::html::{env_label, is_suspect};`
  - `use crate::recommend::recommendations;`

Add a small enum and helper for the suspect-reason classification:

  - `enum SuspectReason { LowSamples, ShortWarmup, Both }` — only used internally; not pub.
  - `fn suspect_reason(h: &HarnessInfo) -> Option<SuspectReason>`:
    - let low = h.samples_count < 10_000;
    - let short = h.warmup_duration_s < 5.0;
    - returns Some(Both) if both, Some(LowSamples) if only low, Some(ShortWarmup) if only short, None if neither. The inputs match is_suspect exactly — keep them in sync.
  - `fn suspect_note(reason: &SuspectReason) -> &'static str` returns:
    - LowSamples   → `"*(⚠ suspect: low samples)*"`
    - ShortWarmup  → `"*(⚠ suspect: short warmup)*"`
    - Both         → `"*(⚠ suspect: low samples, short warmup)*"`

Add four new emitter functions (each takes buf: &mut String and the relevant inputs). Rust width-specifier markdown tables per PATTERNS §markdown.rs lines 350-357.

  - `fn emit_per_scenario_tables(buf: &mut String, runs: &[Run])`:
    - Build BTreeMap<&str, Vec<&Run>> keyed by r.scenario.name.as_str(). (BTreeMap → alphabetical iteration → byte-identical-output.)
    - For each (scenario_name, scenario_runs):
      - writeln the heading `## {scenario_name}` followed by a blank line.
      - Header row: writeln `| allocator | throughput | p50 | p95 | p99 | p999 | peak RSS |`.
      - Separator: writeln `|---|---|---|---|---|---|---|`.
      - Find the winner: scan scenario_runs, track the run with maximum metrics.ticks_per_s (alphabetical tiebreak via stable iteration). Track winner_run_idx (NOT winner_alloc) so multiple envs of the same allocator don't double-mark.
      - Sort scenario_runs alphabetically by (build.allocator, env_label(env)) — Vec::sort_by with a tuple-key.
      - For each run in sorted order:
        - let unit = run.scenario.unit.as_deref().unwrap_or("ticks/s");
        - alloc_cell: if this row IS the winner_run_idx row → `**✓ {alloc}**` (bold + ✓ prefix per D-09); else just the allocator name.
        - throughput_cell: start with `format!("{:.1} {}", run.metrics.ticks_per_s, unit)`. If suspect_reason(&run.harness) is Some, write the matching suspect_note literal after the throughput value with a leading space.
        - writeln the row: pipe-delimited cells `| {alloc_cell} | {throughput_cell} | {p50} ns | {p95} ns | {p99} ns | {p999} ns | {peak_rss} kB |`.
      - buf.push_str("\n");

  - `fn emit_docker_runtimes_table(buf: &mut String, runs: &[Run])`:
    - Collect unique env labels: `let envs: BTreeSet<String> = runs.iter().map(|r| env_label(&r.env).to_string()).collect();`
    - writeln the heading `## Docker runtimes` followed by a blank line.
    - Header: writeln `| env | image_size_mb | build_time_s | run_overhead_pct |`.
    - Separator: writeln `|---|---|---|---|`.
    - For each env in alphabetical order: writeln `| {env} | — | — | — |` using em-dash U+2014.
    - Footnote (single line, italic): writeln a line stating `*image_size_mb / build_time_s / run_overhead_pct populated by Phase 5 CI via docker inspect (REPR-03).*` followed by a blank line.

  - `fn emit_allocator_diagrams(buf: &mut String)`:
    - writeln the heading `## Allocator architectures` followed by a blank line.
    - For each (name, body) in ALL_DIAGRAMS.iter() (already alphabetical):
      - writeln `### {name}` followed by a blank line.
      - Optionally emit a one-line source pointer: writeln a line like `Source: see crates/alloc-bench-aggregator/src/diagrams.rs for upstream reference.` followed by a blank line — keep the link inside the diagrams.rs file (already cited per Task 1) so REPORT.md doesn't drift if upstream URLs change.
      - writeln the entire raw-string constant body — emits the leading newline + fenced mermaid opener + flowchart TD + nodes + closing fence + trailing newline that Task 1 baked into the constant.

  - `fn emit_recommendations(buf: &mut String, runs: &[Run])`:
    - let recs = recommendations(runs);
    - writeln the heading `## Recommendations by workload` followed by a blank line.
    - Header: writeln `| Workload | Recommended | Rationale |`.
    - Separator: writeln `|---|---|---|`.
    - For each r in recs: writeln `| {r.class} | {r.allocator} | {r.rationale} |`.
    - buf.push_str("\n");

Wholesale-rewrite pub fn write(outcome: &LoadOutcome, out_dir: &Path) -> Result<()>:

  - Build String buffer.
  - Plan-01 header section: timestamp comment + `# alloc-bench REPORT` + "**N runs across M cells.**" (preserve verbatim from Plan 01 — no Plan-03 changes here).
  - REMOVE the Plan-01 `## Runs` bullet-list section. The per-scenario tables subsume it.
  - Call emit_per_scenario_tables(&mut buf, &outcome.runs);
  - Call emit_docker_runtimes_table(&mut buf, &outcome.runs);
  - Call emit_allocator_diagrams(&mut buf);
  - Call emit_recommendations(&mut buf, &outcome.runs);
  - If !outcome.skipped.is_empty(): keep the Plan-01 `## Skipped inputs` section emitter — call it last.
  - Final std::fs::write(out_dir.join("REPORT.md"), &buf) with with_context.

To enable Behavior-13's two-runs-byte-identical test without disk I/O, factor a helper `pub(crate) fn build_report(outcome: &LoadOutcome) -> String` that builds the buffer end-to-end and returns it; have pub fn write call build_report then fs::write. The test calls build_report twice, strips the leading schema_version comment line, asserts byte-equality.

Add #[cfg(test)] mod tests with the six unit tests from <behavior> (per_scenario_table_marks_winner..., per_scenario_table_appends_suspect_note_to_low_samples_run, per_scenario_table_marks_warmup_suspect, per_scenario_table_marks_both_suspect_predicates, docker_runtimes_table_emits_em_dash_for_missing_fields, report_md_two_runs_byte_identical_after_timestamp_strip). Use a small make_run(alloc, scenario, ticks, samples, warmup_s) -> Run helper. For the byte-identical test, outcome includes 2-3 synthetic runs across 2 scenarios; assert lines_after_timestamp_a == lines_after_timestamp_b.

Apply `cargo fmt --all` + `cargo clippy --workspace --all-targets -- -D warnings` after edits.
  </action>
  <verify>
    <automated>cargo fmt --all --check &amp;&amp; cargo build -p alloc-bench-aggregator --release &amp;&amp; cargo test -p alloc-bench-aggregator --lib -- markdown::tests &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -F "use crate::diagrams::ALL_DIAGRAMS" crates/alloc-bench-aggregator/src/markdown.rs` returns 1 line.
    - Source assertion: `grep -F "use crate::recommend::recommendations" crates/alloc-bench-aggregator/src/markdown.rs` returns 1 line.
    - Source assertion: `grep -F "use crate::html::" crates/alloc-bench-aggregator/src/markdown.rs` returns at least 1 line (env_label + is_suspect imports).
    - Source assertion: `grep -F "## Docker runtimes" crates/alloc-bench-aggregator/src/markdown.rs` returns at least 1 line (heading literal).
    - Source assertion: `grep -F "## Allocator architectures" crates/alloc-bench-aggregator/src/markdown.rs` returns at least 1 line.
    - Source assertion: `grep -F "## Recommendations by workload" crates/alloc-bench-aggregator/src/markdown.rs` returns at least 1 line.
    - Source assertion: `grep -F "**✓ " crates/alloc-bench-aggregator/src/markdown.rs` returns at least 1 line (winner-prefix format string).
    - Source assertion: `grep -F "*(⚠ suspect: low samples)*" crates/alloc-bench-aggregator/src/markdown.rs` returns at least 1 line.
    - Source assertion: `grep -F "*(⚠ suspect: short warmup)*" crates/alloc-bench-aggregator/src/markdown.rs` returns at least 1 line.
    - Source assertion: `grep -F "*(⚠ suspect: low samples, short warmup)*" crates/alloc-bench-aggregator/src/markdown.rs` returns at least 1 line (combined branch).
    - Source assertion: `grep -F "Phase 5 CI via" crates/alloc-bench-aggregator/src/markdown.rs` returns at least 1 line (Docker footnote).
    - Source assertion: `grep -F "BTreeMap" crates/alloc-bench-aggregator/src/markdown.rs` returns at least 1 line (byte-identical-output ordering per RESEARCH §Pitfall 5).
    - Source assertion: `grep -c "fn emit_" crates/alloc-bench-aggregator/src/markdown.rs` returns at least 4 (per_scenario_tables, docker_runtimes_table, allocator_diagrams, recommendations).
    - Test command: `cargo test -p alloc-bench-aggregator --lib -- markdown::tests::per_scenario_table_marks_winner_with_check_prefix markdown::tests::per_scenario_table_appends_suspect_note_to_low_samples_run markdown::tests::per_scenario_table_marks_warmup_suspect markdown::tests::per_scenario_table_marks_both_suspect_predicates markdown::tests::docker_runtimes_table_emits_em_dash_for_missing_fields markdown::tests::report_md_two_runs_byte_identical_after_timestamp_strip` exits 0 with all SIX tests green.
    - Behavior assertion (full pipeline): `mkdir -p /tmp/p4-plan3-task3 && cargo run --release -p alloc-bench-aggregator -- --input "crates/alloc-bench-aggregator/tests/fixtures/*.json" --output /tmp/p4-plan3-task3 && grep -c "flowchart TD" /tmp/p4-plan3-task3/REPORT.md` returns at least 4 (Mermaid diagrams emitted).
    - Behavior assertion: `grep -F "## Recommendations by workload" /tmp/p4-plan3-task3/REPORT.md` returns 1 line.
    - Behavior assertion: `grep -F "## Docker runtimes" /tmp/p4-plan3-task3/REPORT.md` returns 1 line.
    - Behavior assertion: `grep -F "**✓ " /tmp/p4-plan3-task3/REPORT.md` returns at least 1 line (a winner row was rendered).
    - Behavior assertion: `grep -F "—" /tmp/p4-plan3-task3/REPORT.md` returns at least 2 lines (em-dash cells in Docker table).
    - Behavior assertion: `grep -F "% throughput vs" /tmp/p4-plan3-task3/REPORT.md` returns at least 1 line (data-derived rationale string in Recommendations table).
    - Reproducibility: `bash -c 'mkdir -p /tmp/p4-r1 /tmp/p4-r2 && cargo run --release -q -p alloc-bench-aggregator -- --input "crates/alloc-bench-aggregator/tests/fixtures/*.json" --output /tmp/p4-r1 && cargo run --release -q -p alloc-bench-aggregator -- --input "crates/alloc-bench-aggregator/tests/fixtures/*.json" --output /tmp/p4-r2 && diff <(grep -v "^<!-- schema_version" /tmp/p4-r1/REPORT.md) <(grep -v "^<!-- schema_version" /tmp/p4-r2/REPORT.md)'` exits 0 with no diff (byte-identical after timestamp strip).
    - Code-style: `cargo clippy --workspace --all-targets -- -D warnings` passes.
  </acceptance_criteria>
  <done>REPORT.md is now feature-complete: per-scenario allocator comparison tables with bold-and-check winner prefix and italic suspect notes for either or both suspect predicates; `## Docker runtimes` table with em-dash cells and a phase-5-backfill footnote; `## Allocator architectures` with four `### {name}` Mermaid blocks in alphabetical order; `## Recommendations by workload` with data-derived rationale strings citing measured deltas. Six unit tests gate the contract; full-pipeline runs prove byte-identical reproducibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Mirror per-scenario winner-highlight in HTML report-mirror table + extend suspect fixture for warmup branch</name>
  <files>crates/alloc-bench-aggregator/templates/index.html.tmpl, crates/alloc-bench-aggregator/src/html.rs, crates/alloc-bench-aggregator/tests/fixtures/jemalloc-alpine.json</files>
  <read_first>
    - crates/alloc-bench-aggregator/templates/index.html.tmpl (post-Plan-02 final state — confirm the existing structure: header, sidebar, 2×2 chart grid, A/B picker section. Plan 03 ADDS a `<section class="report-mirror">` block AFTER the diff section but BEFORE the closing `</main>` tag — or as a sibling block; pick whichever keeps the existing CSS sane.)
    - crates/alloc-bench-aggregator/src/html.rs (post-Plan-02 — confirm HtmlContext and the existing JSON-string fields; we add NO new HtmlContext fields in Task 4 — the mirror table is built client-side from RESULTS already inlined.)
    - crates/alloc-bench-aggregator/tests/fixtures/jemalloc-alpine.json (Plan 01 — confirm it has 2 runs, one with samples_count=5000; Task 4 APPENDS a third run with warmup_duration_s=2.0 so REPORT.md exercises BOTH suspect-predicate branches end-to-end)
    - .planning/phases/04-aggregator-dashboard/04-UI-SPEC.md §"Color" §"Per-row winner highlight" lines 110-119 (winner-bg `#DAFBE1`, winner-text `#1A7F37`, ✓ prefix in `#1A7F37`)
    - .planning/phases/04-aggregator-dashboard/04-UI-SPEC.md §"Layout Color Tokens" lines 313-315 (CSS variables `--color-winner-bg` and `--color-winner-text` already declared in Plan 01's :root block)
    - .planning/phases/04-aggregator-dashboard/04-RESEARCH.md §"Pitfall 1" lines 522-534 (every literal opening curly brace in inline JS body MUST be backslash-escaped in the template file)
  </read_first>
  <behavior>
    - Behavior 1: After Task 4, opening report/index.html (against the augmented fixtures) renders, BELOW the four chart cards + A/B section, a new `<section class="report-mirror">` containing one HTML table per scenario. The header row matches REPORT.md's columns (allocator | throughput | p50 | p95 | p99 | p999 | peak RSS). The best-throughput row in each table has its `<tr>` styled with `background: var(--color-winner-bg); color: var(--color-winner-text);` and the allocator cell prefixed `✓ ` (rendered with `color: var(--color-winner-text)` to match REPORT.md's bold-+-prefix semantic).
    - Behavior 2: Suspect rows in the mirror table render the throughput cell with a `<span class="suspect-note">` containing the warning glyph + reason inline (e.g., `⚠ low samples`); CSS rule `.suspect-note { color: var(--color-destructive); font-style: italic; margin-left: var(--space-xs); }` (every opening curly brace in CSS escaped per RESEARCH §Pitfall 1).
    - Behavior 3: A new JS function renderReportMirrorTable() is called from bootstrap() (or from a new explicit call in the wire-up section). It reads RESULTS, groups by r.scenario.name (alphabetical via Object.keys(...).sort()), builds the per-scenario table element, finds the winner row by max r.metrics.ticks_per_s, and applies winner styling. The function does NOT depend on chart filters — the report-mirror is a static representation of all data, matching REPORT.md.
    - Behavior 4: The new section is OUTSIDE the chart grid (so it doesn't break the 2×2 layout) and appears BELOW the diff section. CSS adds `.report-mirror`, `.report-mirror table`, `.report-mirror th`, `.report-mirror td`, `.report-mirror tr.winner`, `.report-mirror .suspect-note` rules referencing the existing CSS variables.
    - Behavior 5: The existing tinytemplate_compiles_index_template test still passes — no missed brace escape after the additions.
    - Behavior 6: The augmented jemalloc-alpine.json fixture has 3 runs total: Run 1 (multithread, samples=5000 → low-samples suspect), Run 2 (cpu-bound, samples=50000, clean), Run 3 (multithread, samples=50000, warmup=2.0 → short-warmup suspect). After running `just aggregate`, REPORT.md contains BOTH `*(⚠ suspect: low samples)*` AND `*(⚠ suspect: short warmup)*` literals — proving both predicate branches emit.
    - Behavior 7: The aggregator_emits_html_and_markdown_against_fixtures smoke test from Plan 01 still passes — Run count goes from 5 to 6 (2 ptmalloc + 3 jemalloc + 1 mimalloc), so the `aggregated 6 runs, skipped 0` summary should appear instead of `aggregated 5 runs`. Plan 03 Task 5 updates the smoke test to use a count >= 5 instead of == 5 so the assertion remains stable as fixtures grow.
    - Behavior 8: The HTML-mirror's `✓ ` prefix is the U+2713 CHECK MARK glyph, matching REPORT.md's prefix for cross-format consistency.
  </behavior>
  <action>
Edit crates/alloc-bench-aggregator/tests/fixtures/jemalloc-alpine.json. Plan 01 wrote it as a JSON array of 2 Runs. APPEND a third Run object at the end of the array (i.e., insert before the closing bracket). The new Run uses the same env/build blocks as Run 1 and Run 2 but distinct scenario + harness:

  - schema_version: 1, status: "success", no error key.
  - scenario.name: "multithread" (same as Run 1; this gives the per-scenario table 3 rows for the multithread scenario across allocators).
  - harness.warmup_duration_s: 2.0 (TRIGGERS short-warmup suspect predicate).
  - harness.samples_count: 50000 (does NOT trigger low-samples; we want ONLY short-warmup to fire so REPORT.md emits the short-warmup italic note in isolation).
  - metrics.ticks_per_s: 95.0 (between Run 1's 100.0 and ptmalloc's 80.0 from ptmalloc-debian-slim.json; ensures the multithread per-scenario table has clear winner ordering — mimalloc=110.0 from mimalloc-distroless-cc-single.json still wins multithread, with `**✓ mimalloc**` marked).
  - All other fields populate to schema-v1 minimum with sentinel values matching the existing Run 1 / Run 2 shape (cpu_count: 4, memory_total_kb: 4194304, etc.).

Run `jq -e . crates/alloc-bench-aggregator/tests/fixtures/jemalloc-alpine.json` to verify the updated array is valid JSON.

Edit crates/alloc-bench-aggregator/src/html.rs. NO new HtmlContext fields are needed — the mirror table is built client-side from RESULTS (already inlined). The only html.rs change is to ensure the existing pub(crate) fn env_label and pub(crate) fn is_suspect helpers remain exported (Plan 02 Task 1 set their visibility; verify no Plan-03 change is needed). If they are private, broaden to pub(crate).

Edit crates/alloc-bench-aggregator/templates/index.html.tmpl. Two edits:

  - Add a new `<section class="report-mirror">` element AFTER the existing `<section class="diff-picker">` (the A/B picker block) but BEFORE the closing `</main>` tag (or as a top-level section after `</main>` — pick whichever keeps the existing 2×2 grid intact). Inside: a single `<div id="report-mirror-tables"></div>` placeholder. Above that, an `<h2>` with text "Per-scenario allocator comparison" (Heading 20/600 per UI-SPEC §Typography).

  - Add CSS rules to the existing `<style>` block (every opening curly brace must be backslash-escaped per RESEARCH §Pitfall 1):
    - `.report-mirror` → margin-top: var(--space-2xl); padding: var(--space-md); border: 1px solid var(--color-border); border-radius: 6px; background: var(--color-dominant);
    - `.report-mirror table` → border-collapse: collapse; width: 100%; margin-bottom: var(--space-md);
    - `.report-mirror th, .report-mirror td` → border: 1px solid var(--color-border); padding: var(--space-sm); text-align: left; font-size: var(--font-size-body);
    - `.report-mirror th` → background: var(--color-secondary); font-weight: var(--font-weight-semibold);
    - `.report-mirror tr.winner` → background: var(--color-winner-bg); color: var(--color-winner-text);
    - `.report-mirror tr.winner td:first-child` → font-weight: var(--font-weight-semibold);
    - `.report-mirror .suspect-note` → color: var(--color-destructive); font-style: italic; margin-left: var(--space-xs);

  - Add a JS function renderReportMirrorTable() (every opening curly brace in JS body backslash-escaped) inside the existing `<script>` block, somewhere after the existing chart trace builders and before the bootstrap() call. Function logic:
    1. Group RESULTS by r.scenario.name into a Map. Iterate Object.keys(byScenario).sort() for alphabetical scenario order.
    2. For each scenario, find winnerRun = arr.reduce((best, r) => (r.metrics.ticks_per_s > best.metrics.ticks_per_s ? r : best), arr[0]).
    3. Build a `<table>` element with header `<tr><th>allocator</th><th>throughput</th><th>p50</th><th>p95</th><th>p99</th><th>p999</th><th>peak RSS</th></tr>`.
    4. For each run in arr.sort(...) (alphabetical by allocator+env): build a `<tr>` (with class="winner" if r === winnerRun). Cells:
       - allocator cell: textContent set to `(r === winnerRun ? '\u{2713} ' : '') + r.build.allocator` (✓ U+2713 prefix when winner). Use textContent not innerHTML for safe rendering.
       - throughput cell: build a `<td>` with text `${ticks.toFixed(1)} ${unit}`; if isSuspect(r) (the existing helper from Plan 02), append a `<span class="suspect-note">` whose textContent is one of: warning-glyph + space + "low samples", warning-glyph + space + "short warmup", or warning-glyph + space + "low samples, short warmup" — based on which predicate(s) fire (mirror the markdown.rs suspect_reason classifier).
       - latency cells: `<td>${val} ns</td>` for p50/p95/p99/p999.
       - peak RSS: `<td>${val} kB</td>`.
    5. Append a `<h3>` heading with text scenario_name above each table.
    6. Replace document.querySelector('#report-mirror-tables').innerHTML = '' then appendChild the assembled DOM. (Keep DOM-construction explicit; do NOT use innerHTML with concatenated user data — only the heading text uses textContent and the entire DOM is built programmatically with document.createElement.)
  - Wire the call: at the end of bootstrap() (added in Plan 02 Task 1), append `renderReportMirrorTable();` so the table renders once at page load. The mirror does NOT respond to filter changes — it shows ALL runs always, mirroring REPORT.md's static view.

CRITICAL escape verification before commit: run grep for every literal opening-curly in the template, exclude the backslash-escaped opener pattern, exclude the `unescaped` formatter substitutions — every remaining line must be one of the five raw substitution placeholders (run_count, cell_count, timestamp_iso8601, plotly_cdn_url, plotly_sri_hash). Any other unescaped `{` is a missed escape.

Apply `cargo fmt --all` + `cargo clippy --workspace --all-targets -- -D warnings`.
  </action>
  <verify>
    <automated>cargo fmt --all --check &amp;&amp; jq -e . crates/alloc-bench-aggregator/tests/fixtures/jemalloc-alpine.json &gt; /dev/null &amp;&amp; cargo build -p alloc-bench-aggregator --release &amp;&amp; cargo test -p alloc-bench-aggregator --lib -- html::tests::tinytemplate_compiles_index_template &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - JSON validity: `jq -e '. | length' crates/alloc-bench-aggregator/tests/fixtures/jemalloc-alpine.json` returns 3 (the array has three Runs after Task 4).
    - JSON behavior: `jq -e '[.[] | select(.harness.warmup_duration_s &lt; 5.0)] | length' crates/alloc-bench-aggregator/tests/fixtures/jemalloc-alpine.json` returns at least 1 (warmup-suspect run is present).
    - JSON behavior: `jq -e '[.[] | select(.harness.samples_count &lt; 10000)] | length' crates/alloc-bench-aggregator/tests/fixtures/jemalloc-alpine.json` returns at least 1 (low-samples-suspect run is present).
    - Source assertion: `grep -F 'class="report-mirror"' crates/alloc-bench-aggregator/templates/index.html.tmpl` returns at least 1 line.
    - Source assertion: `grep -F "renderReportMirrorTable" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns at least 2 lines (definition + call from bootstrap).
    - Source assertion: `grep -F "tr.winner" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns at least 1 line (winner CSS rule).
    - Source assertion: `grep -F "var(--color-winner-bg)" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns at least 1 line (winner background per UI-SPEC).
    - Source assertion: `grep -F "var(--color-winner-text)" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns at least 1 line.
    - Source assertion: `grep -F "report-mirror-tables" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns at least 2 lines (div placeholder + JS query).
    - Source assertion: `grep -F "u{2713}" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns at least 1 line (✓ U+2713 escape used in JS for cross-format consistency).
    - Source assertion: `grep -F "suspect-note" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns at least 2 lines (CSS rule + JS span class).
    - Test command: `cargo test -p alloc-bench-aggregator --lib -- html::tests::tinytemplate_compiles_index_template` exits 0 (template still compiles after the JS + CSS additions — no missed brace escape).
    - Behavior assertion (full pipeline): `mkdir -p /tmp/p4-plan3-task4 && cargo run --release -p alloc-bench-aggregator -- --input "crates/alloc-bench-aggregator/tests/fixtures/*.json" --output /tmp/p4-plan3-task4 && grep -c "renderReportMirrorTable" /tmp/p4-plan3-task4/index.html` returns at least 2.
    - Behavior assertion: `grep -F "report-mirror" /tmp/p4-plan3-task4/index.html` returns at least 1 line.
    - Behavior assertion (low-samples branch): `grep -F "*(⚠ suspect: low samples)*" /tmp/p4-plan3-task4/REPORT.md` returns at least 1 line.
    - Behavior assertion (short-warmup branch): `grep -F "*(⚠ suspect: short warmup)*" /tmp/p4-plan3-task4/REPORT.md` returns at least 1 line (proves the augmented Run 3 fixture surfaces in REPORT.md).
    - Code-style: `cargo clippy --workspace --all-targets -- -D warnings` passes.
  </acceptance_criteria>
  <done>HTML report has a per-scenario report-mirror table that visually matches REPORT.md's winner-highlight contract (✓ prefix + green tinted row); suspect rows surface the same warning notes inline with the throughput cell. The augmented jemalloc-alpine fixture exercises both suspect predicate branches end-to-end so REPORT.md emits both `*(⚠ suspect: low samples)*` and `*(⚠ suspect: short warmup)*` literals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Append `## How memory allocation works on Linux` section to README.md (AGG-08)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state — single-line H1 `# rust-benchmark-glibc-musl-mimalloc`; Plan 03 inserts new content directly after this line)
    - .planning/phases/04-aggregator-dashboard/04-UI-SPEC.md line 174 (section heading is locked: `## How memory allocation works on Linux`)
    - .planning/phases/04-aggregator-dashboard/04-UI-SPEC.md line 175 (LOCKED VERBATIM ~80-word paragraph — must be copied character-for-character; any rewording violates the contract)
    - .planning/phases/04-aggregator-dashboard/04-RESEARCH.md §"Code Examples §8" lines 826-838 (the 8-node Mermaid flowchart TD source: app[Application code] → std[Rust std::alloc] → ga[#[global_allocator] / jemalloc / mimalloc / system] → libc[libc malloc / ptmalloc on glibc, mallocng on musl] → kernel[Kernel mmap / brk / sbrk] → phys[Physical memory])
    - .planning/phases/04-aggregator-dashboard/04-CONTEXT.md §"D-13" lines 62-66 (the aggregator does NOT mutate README.md automatically — Plan 03 hand-edits the file; the diagram + paragraph are committed as a static change)
  </read_first>
  <behavior>
    - Behavior 1: README.md after Task 5 has at least three sections in order: the existing `# rust-benchmark-glibc-musl-mimalloc` H1, the new `## How memory allocation works on Linux` H2, and (after Plan 03's Mermaid + paragraph) blank space for Phase 5 to insert "Run it yourself" content below.
    - Behavior 2: README.md contains a fenced mermaid block whose body starts with `flowchart TD` and contains all six labels as substrings: `Application code`, `Rust std::alloc`, `#[global_allocator]`, `libc malloc`, `Kernel mmap / brk / sbrk` (or equivalent literal `Kernel mmap`), and `Physical memory`.
    - Behavior 3: README.md contains the LOCKED 80-word paragraph from UI-SPEC line 175 verbatim (character-for-character — including the backtick-wrapped code samples `Vec::new()`, `Box::new(x)`, `std::alloc`, `#[global_allocator]`, `mmap`, `brk`, `sbrk` and the right-arrow Unicode characters `→`).
    - Behavior 4: A grep for the literal sentence opening `When a Rust program calls` returns exactly 1 line (the locked-paragraph anchor).
    - Behavior 5: A grep for `## How memory allocation works on Linux` returns exactly 1 line.
    - Behavior 6: A grep for `flowchart TD` in README.md returns exactly 1 line (only the system diagram; the four allocator-architecture diagrams live in REPORT.md, NOT README.md).
    - Behavior 7: README.md is hand-edited (D-13: aggregator does NOT mutate README from Rust). NO code change in any .rs file is needed for this task — it's a pure documentation edit.
  </behavior>
  <action>
Edit README.md. Currently the file is a single line: `# rust-benchmark-glibc-musl-mimalloc`. Append the following content AFTER that H1 line, separated by one blank line:

  - Section heading: `## How memory allocation works on Linux` (UI-SPEC line 174 LOCKED).

  - One blank line.

  - The 6-node Mermaid `flowchart TD` block from RESEARCH §Code Examples §8 lines 826-838. The block opens with the literal triple-backtick + `mermaid` info-string on its own line, then `flowchart TD` on its own line, then the six node declarations + the chained edge expression on subsequent lines, then the closing triple-backtick on its own line. The node IDs and labels are EXACTLY:
    - app[Application code]
    - std[Rust std::alloc]
    - ga["#[global_allocator]<br/>jemalloc / mimalloc / system"]    (note the double-quoted label because of the `#` and `[` characters — Mermaid requires quoting node labels containing `#` or `[` literals)
    - libc["libc malloc<br/>(ptmalloc on glibc, mallocng on musl)"]
    - kernel["Kernel mmap / brk / sbrk"]
    - phys[Physical memory]
    Then the edge chain on its own line: `app --> std --> ga --> libc --> kernel --> phys`.

  - One blank line after the closing fence.

  - The LOCKED ~80-word paragraph from UI-SPEC line 175 verbatim. Copy character-for-character. Do NOT reword, paraphrase, or substitute. The paragraph contains seven backtick-wrapped code samples (`Vec::new()`, `Box::new(x)`, `std::alloc`, `#[global_allocator]`, `mmap`, `brk`, `sbrk`) and arrow glyphs (right-arrow Unicode U+2192 `→`). The exact text is:

> When a Rust program calls `Vec::new()` or `Box::new(x)`, the request travels through `std::alloc` → the configured `#[global_allocator]` (jemalloc / mimalloc / system) → libc malloc (ptmalloc on glibc, mallocng on musl) → the kernel's `mmap` / `brk` / `sbrk` → physical memory. Each layer can change the cost, fragmentation profile, and tail-latency shape of an allocation. This benchmark measures those differences across four allocators, six libc·env combinations, and eleven workload scenarios.

  - One trailing blank line so Phase 5's "Run it yourself" section can be appended cleanly later without merging into the paragraph.

Verify the file structure with two greps:
  - `grep -F "## How memory allocation works on Linux" README.md` returns exactly 1 line.
  - `grep -F "When a Rust program calls" README.md` returns exactly 1 line.

NO CODE CHANGES — this is a pure markdown edit. Apply `cargo fmt --all --check` (no Rust file modifications, so this should pass without changes). The Rust workspace is unaffected.
  </action>
  <verify>
    <automated>grep -F "## How memory allocation works on Linux" README.md &amp;&amp; grep -F "flowchart TD" README.md &amp;&amp; grep -F "When a Rust program calls" README.md &amp;&amp; cargo fmt --all --check</automated>
  </verify>
  <acceptance_criteria>
    - File assertion: `wc -l README.md` returns at least 12 lines (was 1; now 1 H1 + heading + mermaid block + paragraph + spacing).
    - Source assertion: `grep -c "^# rust-benchmark-glibc-musl-mimalloc$" README.md` returns exactly 1 (existing H1 preserved).
    - Source assertion: `grep -c "^## How memory allocation works on Linux$" README.md` returns exactly 1.
    - Source assertion: `grep -c "flowchart TD" README.md` returns exactly 1 (only the system diagram lives in README; the four allocator diagrams live in REPORT.md).
    - Source assertion: `grep -F "Application code" README.md` returns at least 1 line.
    - Source assertion: `grep -F "Rust std::alloc" README.md` returns at least 1 line.
    - Source assertion: `grep -F "#[global_allocator]" README.md` returns at least 1 line.
    - Source assertion: `grep -F "ptmalloc on glibc, mallocng on musl" README.md` returns at least 1 line.
    - Source assertion: `grep -F "Kernel mmap" README.md` returns at least 1 line (or `Kernel mmap / brk / sbrk`).
    - Source assertion: `grep -F "Physical memory" README.md` returns at least 1 line.
    - Source assertion: `grep -F "When a Rust program calls" README.md` returns exactly 1 line (locked-paragraph anchor; the LOCKED text begins with this phrase).
    - Source assertion: `grep -F "across four allocators, six libc·env combinations, and eleven workload scenarios" README.md` returns exactly 1 line (locked-paragraph closing anchor).
    - Source assertion: `grep -F "Vec::new()" README.md` returns at least 1 line (the locked paragraph contains backtick-wrapped Vec::new()).
    - Source assertion: `grep -F "Box::new" README.md` returns at least 1 line (the locked paragraph contains backtick-wrapped Box::new(x)).
    - Behavior assertion: `cargo fmt --all --check` passes (no Rust files modified by this task).
  </acceptance_criteria>
  <done>README.md begins with the existing H1, then a new `## How memory allocation works on Linux` section containing the 6-node Mermaid flowchart TD diagram (Application code → Rust std::alloc → #[global_allocator] → libc malloc → Kernel mmap/brk/sbrk → Physical memory) and the LOCKED ~80-word paragraph from UI-SPEC line 175 verbatim. Phase 5 will append the "Run it yourself" walkthrough below this section.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Extend smoke tests for AGG-04..08 (REPORT.md richness + README.md system diagram)</name>
  <files>crates/alloc-bench-aggregator/tests/smoke.rs</files>
  <read_first>
    - crates/alloc-bench-aggregator/tests/smoke.rs (post-Plan-02 — confirm 10 existing tests; Plan 03 APPENDS 5 new tests so the final count is 15)
    - crates/alloc-bench-aggregator/tests/fixtures/jemalloc-alpine.json (post-Task-4 — confirm 3 runs, both suspect predicates exercised)
    - .planning/phases/04-aggregator-dashboard/04-PATTERNS.md §"crates/alloc-bench-aggregator/tests/smoke.rs" lines 554-651 (assert_cmd + tempfile + serde_json::Value pattern; predicates::str::contains pattern from Task 1)
    - The post-Task-3 final state of crates/alloc-bench-aggregator/src/markdown.rs (confirm: REPORT.md emits exactly the heading literals `## Docker runtimes`, `## Allocator architectures`, `## Recommendations by workload`)
    - The post-Task-5 final state of README.md (confirm: contains `## How memory allocation works on Linux` and `flowchart TD`)
  </read_first>
  <behavior>
    - Behavior 1: A new smoke test aggregator_report_md_contains_four_mermaid_diagrams runs the aggregator against the fixtures and asserts REPORT.md contains the substring `flowchart TD` at least 4 times (once per allocator architecture diagram). Use `report.matches("flowchart TD").count() >= 4`.
    - Behavior 2: A new smoke test aggregator_report_md_contains_recommendations_section asserts REPORT.md contains `## Recommendations by workload` AND contains `% throughput vs` (the data-derived rationale signature from recommend.rs Task 2) AND contains all six workload class labels (`channel-heavy`, `contention`, `cpu-bound`, `fragmentation-prone`, `memory-bound`, `web-ser-de`).
    - Behavior 3: A new smoke test aggregator_report_md_contains_docker_runtimes_table asserts REPORT.md contains `## Docker runtimes` AND contains the literal `image_size_mb` (column header) AND contains the literal em-dash `—` (U+2014) AND contains the literal `Phase 5 CI via` (footnote anchor).
    - Behavior 4: A new smoke test aggregator_report_md_contains_winner_prefix asserts REPORT.md contains `**✓ ` (the bold-+-✓ winner-prefix marker) at least once. Plan 01 fixtures + Plan 03 logic guarantee a winner row is rendered for every per-scenario table.
    - Behavior 5: A new smoke test aggregator_report_md_contains_suspect_italic_notes asserts REPORT.md contains BOTH `*(⚠ suspect: low samples)*` AND `*(⚠ suspect: short warmup)*` (proves both predicate branches surface end-to-end thanks to Task 4's augmented jemalloc-alpine.json fixture).
    - Behavior 6: A new smoke test readme_md_contains_system_diagram asserts README.md (read via `std::fs::read_to_string("README.md")` from the workspace root resolved via env!("CARGO_MANIFEST_DIR") + "/../../README.md") contains BOTH `## How memory allocation works on Linux` AND `flowchart TD` (proves Task 5 was committed).
    - Behavior 7: All 10 pre-existing smoke tests from Plan 01/02 still pass — additive change only. Note: Plan 02 Task 3's aggregator_emits_html_and_markdown_against_fixtures may have asserted `aggregated 5 runs, skipped 0` literally; if so, Task 6 ALSO updates that assertion to `assert!(stderr.contains("aggregated ") && stderr.contains(" runs, skipped 0"))` so the run count grows transparently as fixtures grow.
  </behavior>
  <action>
Edit crates/alloc-bench-aggregator/tests/smoke.rs. APPEND five new integration tests at the end of the file. Reuse the assert_cmd::Command::cargo_bin + tempfile::tempdir pattern. Each test reads the rendered output and asserts substring presence/absence.

Helper (factor at the top of the file if not already): a small `fn run_aggregator_against_fixtures(out_dir: &Path)` function that performs the cargo-bin invocation against `tests/fixtures/*.json`. The Plan 01/02 tests can use it OR keep their inline setup.

The five new tests:

  1. aggregator_report_md_contains_four_mermaid_diagrams: Invoke aggregator → read REPORT.md → assert `report.matches("flowchart TD").count() >= 4`. Use sequential `assert!(report.contains("flowchart TD"))` first as a sanity check, then the count assertion.

  2. aggregator_report_md_contains_recommendations_section: Invoke aggregator → read REPORT.md → assert seven substrings present:
     - `## Recommendations by workload`
     - `% throughput vs`
     - `channel-heavy`
     - `contention`
     - `cpu-bound`
     - `fragmentation-prone`
     - `memory-bound`
     - `web-ser-de`
     Use sequential `assert!(report.contains("X"), "expected X in REPORT.md")` so failure messages name the missing piece.

  3. aggregator_report_md_contains_docker_runtimes_table: Invoke aggregator → read REPORT.md → assert four substrings present:
     - `## Docker runtimes`
     - `image_size_mb` (column header)
     - The literal em-dash character `—` (U+2014; in Rust use `"\u{2014}"`)
     - `Phase 5 CI via` (footnote anchor)

  4. aggregator_report_md_contains_winner_prefix: Invoke aggregator → read REPORT.md → assert the substring `**✓ ` (3-byte sequence including the bold-marker asterisks + the U+2713 check + a trailing space) is present at least once. Use `assert!(report.contains("**\u{2713} "), "expected bold-and-check winner prefix")`.

  5. aggregator_report_md_contains_suspect_italic_notes: Invoke aggregator → read REPORT.md → assert BOTH the substring `*(⚠ suspect: low samples)*` AND `*(⚠ suspect: short warmup)*` are present. Use `\u{26A0}` for the warning glyph in the Rust string literal so the file stays pure-ASCII-safe.

  6. readme_md_contains_system_diagram: Read README.md via `std::fs::read_to_string` resolved from the workspace root (use `Path::new(env!("CARGO_MANIFEST_DIR")).join("../../README.md")` — the aggregator crate is at `crates/alloc-bench-aggregator/`, so `../../README.md` goes up two directories to the workspace root). Assert BOTH `## How memory allocation works on Linux` AND `flowchart TD` are present.

If Plan 02 Task 3's aggregator_emits_html_and_markdown_against_fixtures contains a literal `aggregated 5 runs` assertion, modify it to assert `stderr.contains("aggregated ") && stderr.contains(" runs, skipped 0")` so the count is forward-stable. (Task 4 grew the fixture from 5 to 6 runs; future fixture growth should not break this assertion.)

Apply `cargo fmt --all` + `cargo clippy --workspace --all-targets -- -D warnings`. Run `cargo test -p alloc-bench-aggregator --test smoke` to verify all 16 (10 pre-existing + 6 new — note Task 6 adds 6 tests, not 5: aggregator_report_md_contains_four_mermaid_diagrams, aggregator_report_md_contains_recommendations_section, aggregator_report_md_contains_docker_runtimes_table, aggregator_report_md_contains_winner_prefix, aggregator_report_md_contains_suspect_italic_notes, readme_md_contains_system_diagram) integration tests pass.

End-of-Plan-03 sanity sweep:
  - `just aggregate-smoke` exits 0 (recipe wraps cargo test --test smoke).
  - `just aggregate` against a fresh `results/` directory (fixtures copied in) → open `report/index.html` in a browser → verify visually that the report-mirror table renders below the chart grid (manual smoke; not gated by the autonomous-mode test suite, but a useful checkpoint for the developer).
  - `cargo test --workspace --lib --release` passes (all unit tests across all crates).
  </action>
  <verify>
    <automated>cargo fmt --all --check &amp;&amp; cargo test -p alloc-bench-aggregator --test smoke &amp;&amp; just aggregate-smoke &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "fn aggregator_" crates/alloc-bench-aggregator/tests/smoke.rs` returns at least 15 (Plan 01: 4, Plan 02: 6, Plan 03: 5 — though one of the Plan-03 tests is named readme_md_*; counting only fn names starting with `aggregator_` returns 14, but the readme_md_contains_system_diagram test brings the total `fn ` count above 14; the precise structural assertion is on `grep -c "^fn " crates/alloc-bench-aggregator/tests/smoke.rs` returning at least 15 — count function definitions, not name-prefix matches).
    - Source assertion: `grep -c "^fn " crates/alloc-bench-aggregator/tests/smoke.rs` returns at least 15.
    - Source assertion: `grep -F "flowchart TD" crates/alloc-bench-aggregator/tests/smoke.rs` returns at least 1 line (Mermaid-count assert).
    - Source assertion: `grep -F "## Recommendations by workload" crates/alloc-bench-aggregator/tests/smoke.rs` returns at least 1 line (Recommendations heading assert).
    - Source assertion: `grep -F "## Docker runtimes" crates/alloc-bench-aggregator/tests/smoke.rs` returns at least 1 line (Docker heading assert).
    - Source assertion: `grep -F "u{2713}" crates/alloc-bench-aggregator/tests/smoke.rs` returns at least 1 line (winner-prefix assert uses Unicode escape).
    - Source assertion: `grep -F "u{26A0}" crates/alloc-bench-aggregator/tests/smoke.rs` returns at least 1 line (suspect-glyph assert uses Unicode escape).
    - Source assertion: `grep -F "How memory allocation works on Linux" crates/alloc-bench-aggregator/tests/smoke.rs` returns at least 1 line (README assert).
    - Source assertion: `grep -F "% throughput vs" crates/alloc-bench-aggregator/tests/smoke.rs` returns at least 1 line (data-derived rationale assert).
    - Source assertion: `grep -F "channel-heavy" crates/alloc-bench-aggregator/tests/smoke.rs` returns at least 1 line.
    - Source assertion: `grep -F "fragmentation-prone" crates/alloc-bench-aggregator/tests/smoke.rs` returns at least 1 line.
    - Source assertion: `grep -F "Phase 5 CI via" crates/alloc-bench-aggregator/tests/smoke.rs` returns at least 1 line.
    - Test command: `cargo test -p alloc-bench-aggregator --test smoke -- --list 2>&1 | grep -c '^aggregator_\\|^readme_'` returns at least 15 (test discovery lists at least 15 integration tests).
    - Test command: `cargo test -p alloc-bench-aggregator --test smoke` exits 0 with all 15+ tests green.
    - CLI behavior: `just aggregate-smoke` exits 0.
    - Code-style: `cargo clippy --workspace --all-targets -- -D warnings` passes.
  </acceptance_criteria>
  <done>The smoke suite gates the AGG-04..08 contract end-to-end: REPORT.md emits four Mermaid diagrams, Recommendations table with data-derived rationale, Docker runtimes table with em-dash + footnote, winner prefix on at least one row, both suspect predicate notes; README.md contains the system-diagram heading + flowchart. All 15+ integration tests pass via `just aggregate-smoke`. Phase 4 is feature-complete; Phase 5 takes over for CI matrix + image-size gate + multi-run aggregation + README walkthrough.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → aggregator | Plan 01 already mitigated. Plan 03 adds NO new filesystem inputs (recommend.rs reads from already-validated Run structs in memory; diagrams.rs is static `&'static str`; markdown.rs writes to user-specified --output). |
| browser → DOM (report-mirror table) | New report-mirror table in HTML reads from already-validated RESULTS array (server-side serde_json output of v1 schema). DOM construction uses textContent + document.createElement, never innerHTML with user data. No XSS surface. |
| README.md (static commit) | Hand-edited markdown; no code path. Not a runtime trust boundary; included for completeness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Information Disclosure | recommend.rs rationale strings | accept | Rationale strings include allocator names + scenario names + measured deltas. All inputs come from validated v1 schema; output is meant for public benchmark publication. The `*(suspect)*` suffix is a TRUTHFUL annotation — informing the reader that the underlying measurement is below the D-07 quality floor — not a leak. |
| T-04-14 | Tampering | Mermaid `&'static str` constants in diagrams.rs | accept | The diagrams are committed source code in the workspace; modification requires editing diagrams.rs and re-running the aggregator. Two unit tests (each_diagram_has_flowchart_td_and_min_nodes, diagrams_in_alphabetical_emission_order) gate against accidental shape regressions. The diagrams render server-side via GitHub's Mermaid renderer; we do NOT bundle a Mermaid runtime. |
| T-04-15 | Tampering | recommend.rs winner-pick logic | mitigate | Tests gate the data-derived contract (winner_picker_emits_data_derived_rationale_two_allocators, ..._three_allocators_picks_top_with_runner_up, ..._single_allocator_fallback, ..._no_runs_for_class_emits_no_measurements, ..._suspect_winner_appends_suspect_suffix, ..._suspect_runner_up_also_appends_suffix, ..._channel_heavy_means_three_scenarios, ..._handles_zero_throughput_runner_up_without_div_by_zero, ..._alphabetical_class_order). Hard-coded prose injection (RESEARCH §Pitfall 7) is forbidden by the contract; the test suite catches it via the data-derived assertion `rationale.contains("+10.0%")`. |
| T-04-16 | Information Disclosure | Docker runtimes table reveals image_size_mb / build_time_s / run_overhead_pct | accept | Phase 4 emits em-dash for these fields (Pitfall §6). Phase 5 will populate them via docker inspect; the values are then PUBLIC benchmark image sizes. No PII; no secret leak. Footnote documents the Phase-5 backfill so readers don't misinterpret em-dash as bad data. |
| T-04-17 | Tampering | README.md system diagram | accept | README.md is hand-edited per D-13. Plan 03 commits the locked diagram + paragraph as a single diff; future drift is a code-review concern, not a runtime threat. |
| T-04-18 | Spoofing | recommend.rs single-allocator fallback string | accept | The "(only X measured)" allocator field and the "insufficient comparative data — only X measured" rationale are deliberate signals to the reader that the recommendation is not data-supported. The string includes the allocator name verbatim; X comes from build.allocator (already validated). No spoofing surface. |
| T-04-SC | Tampering | crates.io / cdn.plot.ly chain | mitigate | Plan 01 covered both. Plans 02 + 03 add NO new deps. |
</threat_model>

<verification>
End-to-end Plan 03 verification (autonomous, run after Task 6 completes):

1. `cargo fmt --all --check` exits 0 (no formatting drift across edited Rust files; README.md has no fmt impact).
2. `cargo build --workspace --release` exits 0 (workspace builds with the new diagrams + recommend modules).
3. `cargo test --workspace --lib` exits 0 (all unit tests across all crates green — 9 new in recommend, 6 new in markdown, 2 new in diagrams; total adds 17 unit tests across the aggregator's lib target).
4. `cargo test -p alloc-bench-aggregator --test smoke` exits 0 (15+ integration tests, includes the 5 new from Task 6).
5. `just aggregate-smoke` exits 0.
6. `cargo clippy --workspace --all-targets -- -D warnings` passes.
7. `mkdir -p /tmp/p4-plan3-out && cargo run --release -p alloc-bench-aggregator -- --input "crates/alloc-bench-aggregator/tests/fixtures/*.json" --output /tmp/p4-plan3-out 2>&1 1>/dev/null | tail -1` outputs `aggregated 6 runs, skipped 0` (5 → 6 because Task 4 added a third Run to jemalloc-alpine.json).
8. `ls /tmp/p4-plan3-out/` lists `REPORT.md` and `index.html`.
9. `grep -c "flowchart TD" /tmp/p4-plan3-out/REPORT.md` returns at least 4 (Mermaid diagrams).
10. `grep -F "## Recommendations by workload" /tmp/p4-plan3-out/REPORT.md` returns 1 line.
11. `grep -F "## Docker runtimes" /tmp/p4-plan3-out/REPORT.md` returns 1 line.
12. `grep -F "**\u{2713} " /tmp/p4-plan3-out/REPORT.md` returns at least 1 line (winner prefix).
13. `grep -F "*(⚠ suspect: low samples)*" /tmp/p4-plan3-out/REPORT.md` returns at least 1 line.
14. `grep -F "*(⚠ suspect: short warmup)*" /tmp/p4-plan3-out/REPORT.md` returns at least 1 line (proves Task-4 fixture surface).
15. `grep -F "## How memory allocation works on Linux" README.md` returns 1 line.
16. `grep -F "flowchart TD" README.md` returns 1 line (only the system diagram lives in README; the four allocator-architecture diagrams are in REPORT.md).
17. Reproducibility (D-09): `bash -c 'mkdir -p /tmp/p4-r1 /tmp/p4-r2 && cargo run --release -q -p alloc-bench-aggregator -- --input "crates/alloc-bench-aggregator/tests/fixtures/*.json" --output /tmp/p4-r1 && cargo run --release -q -p alloc-bench-aggregator -- --input "crates/alloc-bench-aggregator/tests/fixtures/*.json" --output /tmp/p4-r2 && diff <(grep -v "^<!-- schema_version" /tmp/p4-r1/REPORT.md) <(grep -v "^<!-- schema_version" /tmp/p4-r2/REPORT.md)'` exits 0 with no diff.
</verification>

<success_criteria>
- REPORT.md per-scenario tables: alphabetical scenario sections, alphabetical row order, `**✓ {alloc}**` prefix on best-throughput row only, suspect runs annotated with the matching italic note in the throughput cell (low-samples / short-warmup / both branches all observable end-to-end via the augmented jemalloc-alpine fixture).
- REPORT.md `## Docker runtimes`: alphabetical env rows, em-dash cells for image_size_mb / build_time_s / run_overhead_pct, footnote line documenting Phase 5 backfill.
- REPORT.md `## Allocator architectures`: four `### {alloc}` subsections in alphabetical order (jemalloc, mallocng, mimalloc, ptmalloc), each containing a fenced flowchart TD Mermaid block of 10–15 nodes, sources cited in diagrams.rs.
- REPORT.md `## Recommendations by workload`: six rows (alphabetical class order: channel-heavy, contention, cpu-bound, fragmentation-prone, memory-bound, web-ser-de), each rationale data-derived (`+{delta:.1}% throughput vs {runner_up} on {scenario}` OR `insufficient comparative data — only {alloc} measured` OR `no measurements`), suspect runs surface a `*(suspect)*` suffix.
- README.md: `## How memory allocation works on Linux` heading, 6-node Mermaid flowchart TD (Application code → Rust std::alloc → #[global_allocator] → libc malloc → Kernel mmap/brk/sbrk → Physical memory), LOCKED ~80-word paragraph from UI-SPEC line 175 verbatim.
- HTML report: new `<section class="report-mirror">` below the chart grid containing per-scenario tables that visually mirror REPORT.md's bold-and-✓ winner contract via green-tinted rows + ✓ U+2713 prefix; suspect rows show inline italic notes via `<span class="suspect-note">`.
- Smoke suite (15+ tests via cargo test --test smoke / just aggregate-smoke) gates the AGG-04..08 contract end-to-end.
- Two consecutive aggregator runs against the same fixtures produce byte-identical REPORT.md (after stripping the single top-of-file timestamp comment) — D-09 byte-identical-output contract holds.
- All existing tests (alloc-bench-core, alloc-bench-cli, alloc-bench-aggregator Plan 01/02 unit + integration) continue to pass — no regressions.

REQ coverage:
- AGG-04 (REPORT.md side-by-side comparison table with winner highlighted per row, suspect runs flagged): satisfied. Per-scenario tables; bold-and-✓ winner; italic suspect notes; both predicate branches observable via the augmented fixture.
- AGG-05 (REPORT.md Docker runtime comparison table): satisfied. `## Docker runtimes` with em-dash cells + footnote; v1 schema gap documented for Phase 5 CI backfill.
- AGG-06 (REPORT.md Mermaid allocator architecture diagrams for ptmalloc, mallocng, jemalloc, mimalloc): satisfied. Four flowchart TD constants in diagrams.rs, emitted alphabetically, each 10–15 nodes, sources cited.
- AGG-07 (REPORT.md Recommendations section mapping workload-shape → allocator): satisfied. Six-row table with data-derived rationale strings; hard-coded prose forbidden by recommend.rs unit-test contract.
- AGG-08 (README.md system diagram of how memory allocation works on Linux): satisfied. `## How memory allocation works on Linux` heading + 6-node Mermaid flowchart TD + LOCKED 80-word paragraph from UI-SPEC line 175.
</success_criteria>

<output>
Create `.planning/phases/04-aggregator-dashboard/04-03-SUMMARY.md` when done, documenting:
- Files created/modified (with line counts where useful): diagrams.rs (NEW), recommend.rs (NEW), markdown.rs (extended), main.rs (extended), index.html.tmpl (extended), smoke.rs (extended), jemalloc-alpine.json (extended), README.md (extended).
- The five workload classes + scenario mapping locked in recommend.rs.
- The four Mermaid sources cited in diagrams.rs (Wikipedia / upstream READMEs).
- The byte-identical-output contract (BTreeMap iteration, alphabetical sorts, explicit numeric formatting).
- The locked README paragraph wording (lifted verbatim from UI-SPEC line 175).
- Test counts: +9 unit tests in recommend.rs, +6 unit tests in markdown.rs, +2 unit tests in diagrams.rs, +6 integration tests in smoke.rs (final integration suite size 15+).
- Sample stderr output from `just aggregate` against fixtures: `aggregated 6 runs, skipped 0`.
- Pointers to Phase 5: CI matrix + image-size gate (ORCH-04, ORCH-05); image_size_mb / build_time_s / run_overhead_pct backfill via docker inspect; multi-run median + min/max range (REPR-03); README "Run it yourself" walkthrough (REPR-01) appended below the system diagram section.
- Phase-4 closure note: AGG-01..08 + ORCH-03 are all satisfied across Plan 01 / Plan 02 / Plan 03; Phase 4 is feature-complete.
</output>
