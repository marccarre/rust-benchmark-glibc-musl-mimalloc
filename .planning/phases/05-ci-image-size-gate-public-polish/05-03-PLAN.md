---
phase: 05-ci-image-size-gate-public-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - crates/alloc-bench-aggregator/src/main.rs
  - crates/alloc-bench-aggregator/src/loader.rs
  - crates/alloc-bench-aggregator/src/markdown.rs
  - crates/alloc-bench-aggregator/src/recommend.rs
  - crates/alloc-bench-aggregator/templates/index.html.tmpl
  - crates/alloc-bench-aggregator/src/html.rs
  - crates/alloc-bench-aggregator/tests/smoke.rs
  - justfile
autonomous: true
requirements: [REPR-03]
must_haves:
  truths:
    - "User runs the aggregator with `--input results/*-seed*.json --meta meta/*.json --output report/` and REPORT.md per-scenario throughput cells render the multi-run shape `{median} ({min}..{max}, CV {N}%)` (D-11)."
    - "Cells with CV > 10% display `⚠ high variance` next to the throughput value in REPORT.md (D-12)."
    - "Cells that are BOTH suspect AND high-variance render both flags concatenated, matching CONTEXT.md `<specifics>` ¶5 vocabulary."
    - "The HTML dashboard (`report/index.html`) emits a Plotly `error_y` field with asymmetric `array`/`arrayminus` arrays whenever multi-run data is present, and the legend label gains `⚠ high variance` when any contributing run exceeds CV 10% (D-11 / D-12)."
    - "When `--meta meta/*.json` is supplied and at least one sidecar parses, the `## Docker runtimes` table populates `image_size_mb` from the sidecar (closes Phase 4 D-10 deferral via D-13 sidecar approach)."
    - "When `--meta` is empty (default) — the local `just aggregate` invocation — the existing Phase 4 em-dash placeholder behavior is preserved unchanged. All Phase 4 smoke tests remain green (byte-identical-output contract per markdown.rs lines 18-25)."
    - "The recommend.rs winner picker uses `multi_run::aggregate(&samples).map(|s| s.median).unwrap_or_else(|| mean)` as central tendency — falls back to mean when n<2 so existing single-run-per-cell fixtures still pick the same winner."
    - "User runs `just ci-aggregate` and the recipe invokes the aggregator with `--input results/*.json --meta meta/*.json --output report/` (replacing the Plan 02 stub)."
    - "v1 input schema (`crates/alloc-bench-core/src/output.rs`) is NOT modified (Phase 1 D-11/D-12 + Phase 5 D-14/D-20 lock)."
  artifacts:
    - path: "crates/alloc-bench-aggregator/src/main.rs"
      provides: "CLI gains `--meta` flag (default empty); main flow loads metas conditionally and threads them to markdown::write + html::write"
      contains: "meta:"
    - path: "crates/alloc-bench-aggregator/src/loader.rs"
      provides: "New `pub struct CellMeta` + `pub fn load_cell_metas(pattern: &str) -> Result<HashMap<(String,String), CellMeta>>`"
      exports: ["CellMeta", "load_cell_metas"]
    - path: "crates/alloc-bench-aggregator/src/markdown.rs"
      provides: "Per-scenario throughput cells use `format_throughput_cell(MultiRunStats, suspect)` when ≥2 runs share (alloc, env, scenario); Docker runtimes table populates image_size_mb when metas are present"
      contains: "format_throughput_cell"
    - path: "crates/alloc-bench-aggregator/src/recommend.rs"
      provides: "Winner picker uses median (multi_run::aggregate) with mean fallback for n<2"
      contains: "multi_run::aggregate"
    - path: "crates/alloc-bench-aggregator/templates/index.html.tmpl"
      provides: "Throughput trace gains `error_y` field (asymmetric array/arrayminus); legend label appends `⚠ high variance` when applicable"
      contains: "error_y"
    - path: "crates/alloc-bench-aggregator/tests/smoke.rs"
      provides: "Six new integration tests pinning multi-run + sidecar behavior"
      contains: "aggregator_multi_run"
    - path: "justfile"
      provides: "`ci-aggregate` body replaces Plan 02 stub with the real aggregator invocation"
      contains: "--meta"
  key_links:
    - from: "crates/alloc-bench-aggregator/src/markdown.rs"
      to: "crates/alloc-bench-aggregator/src/multi_run.rs (Plan 01)"
      via: "use crate::multi_run::{aggregate, is_high_variance, MultiRunStats}"
      pattern: "use crate::multi_run"
    - from: "crates/alloc-bench-aggregator/src/recommend.rs"
      to: "crate::multi_run::aggregate"
      via: "central-tendency selection (median with mean fallback)"
      pattern: "crate::multi_run::aggregate"
    - from: "crates/alloc-bench-aggregator/src/main.rs"
      to: "crates/alloc-bench-aggregator/src/loader.rs::load_cell_metas"
      via: "conditional invocation when --meta is non-empty"
      pattern: "loader::load_cell_metas"
    - from: "justfile ci-aggregate"
      to: "alloc-bench-aggregator binary"
      via: "cargo run --release -p alloc-bench-aggregator -- --meta meta/*.json"
      pattern: "--meta"
---

<objective>
Wire the multi-run statistics module (Plan 01) and the sidecar `meta.json` files (Plan 02 CI populates them) into the aggregator's REPORT.md emit, recommend.rs winner picker, and HTML dashboard. After this plan ships, the aggregator decorates per-scenario throughput cells with `{median} ({min}..{max}, CV {N}%)` when ≥2 runs share an `(alloc, env, scenario)` tuple, flags `⚠ high variance` when CV>10%, and backfills `image_size_mb` in the Docker runtimes table from the sidecar when `--meta` is supplied. The v1 input schema is NOT modified — every change is aggregator-output decoration only.

Vertical slice: after this plan ships, a developer can `just bench-all` (3 seeded local runs in CI mode using `just ci-bench-cell` for each cell), then `just aggregate` or `just ci-aggregate`, and see the multi-run shape land in REPORT.md and `report/index.html`. The end-to-end CI pipeline (Plan 02 workflow) becomes fully green when this plan's `ci-aggregate` body lands.

Purpose: Close REPR-03 at the rendering layer (median + min/max + CV displayed in user-facing artifacts) and resolve the Phase 4 D-10 image_size_mb deferral via the schema-preserving sidecar path (D-13).

Output: Six modified Rust source files + one HTML template + smoke test extensions + justfile body update. Six new integration tests. Phase 4 byte-identical-output contract preserved when no multi-run / meta data is present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ci-image-size-gate-public-polish/05-CONTEXT.md
@.planning/phases/05-ci-image-size-gate-public-polish/05-RESEARCH.md
@.planning/phases/05-ci-image-size-gate-public-polish/05-PATTERNS.md
@.planning/phases/05-ci-image-size-gate-public-polish/05-01-PLAN.md
@.planning/phases/05-ci-image-size-gate-public-polish/05-02-PLAN.md

@crates/alloc-bench-aggregator/src/main.rs
@crates/alloc-bench-aggregator/src/loader.rs
@crates/alloc-bench-aggregator/src/markdown.rs
@crates/alloc-bench-aggregator/src/recommend.rs
@crates/alloc-bench-aggregator/src/html.rs
@crates/alloc-bench-aggregator/templates/index.html.tmpl
@crates/alloc-bench-aggregator/tests/smoke.rs
@crates/alloc-bench-aggregator/tests/fixtures/multi_run/seed-1.json
@crates/alloc-bench-aggregator/tests/fixtures/multi_run/seed-2.json
@crates/alloc-bench-aggregator/tests/fixtures/multi_run/seed-3.json
@crates/alloc-bench-aggregator/tests/fixtures/multi_run/meta/jemalloc-alpine.json
@justfile

<interfaces>
<!-- Key contracts the executor needs. Plan 01 ships multi_run.rs; Plan 02 ships fixtures + workflow + ci-aggregate STUB. -->

From Plan 01 (`crates/alloc-bench-aggregator/src/multi_run.rs` — already shipped in Wave 1):
- `pub struct MultiRunStats { pub n: usize, pub mean: f64, pub median: f64, pub min: f64, pub max: f64, pub stddev: f64, pub cv_pct: Option<f64> }` with derives `Debug, Clone, Serialize`.
- `pub fn aggregate(samples: &[f64]) -> Option<MultiRunStats>` — returns None for n<2 or non-finite inputs.
- `pub fn is_high_variance(stats: &MultiRunStats) -> bool` — true iff `cv_pct > 10.0`.
- `mod multi_run;` already declared in `main.rs`.

From Plan 02 (`justfile` — ci-aggregate is currently a stub):
- The stub body is `@echo "ci-aggregate: implemented by Plan 03" >&2 && exit 1`.
- Plan 03 replaces the body with the real aggregator invocation (see Task 6 below).

From `crates/alloc-bench-aggregator/src/main.rs:30-43` (existing CLI struct — extend with a third `--meta` field):
```
struct Cli {
    /// Glob pattern for input JSON files (D-05).
    #[arg(long, default_value = "results/*.json")]
    input: String,
    /// Output directory for index.html + REPORT.md (D-05).
    #[arg(long, default_value = "report/")]
    output: String,
}
```

From `crates/alloc-bench-aggregator/src/markdown.rs:42-47` (existing markdown::write signature — add a metas parameter):
```
pub fn write(outcome: &LoadOutcome, out_dir: &Path) -> Result<()>
```
New shape: `pub fn write(outcome: &LoadOutcome, metas: &HashMap<(String, String), CellMeta>, out_dir: &Path) -> Result<()>`.

From `crates/alloc-bench-aggregator/src/markdown.rs:138-161` (the per-scenario row-emit pattern — replace the raw `format!("{:.1} {}", ticks_per_s, unit)` with `format_throughput_cell(stats, suspect)` when multi-run data is available; fall back to existing single-value formatting when n<2):
- Existing throughput cell: `let mut throughput_cell = format!("{:.1} {}", r.metrics.ticks_per_s, unit);` followed by `if let Some(reason) = suspect_reason(&r.harness) { throughput_cell.push(' '); throughput_cell.push_str(suspect_note(&reason)); }`.
- New shape uses `multi_run::aggregate` over the (alloc, env, scenario) group; on Some, uses `format_throughput_cell`; on None, falls back to existing single-value path.

From `crates/alloc-bench-aggregator/src/markdown.rs:170-189` (the docker runtimes table emitter):
- Existing: emits em-dash for every `image_size_mb` cell. Footnote says "populated by Phase 5 CI via docker inspect (REPR-03)."
- New: when `metas` map is non-empty, look up `metas.get(&(alloc, env))` for each `(env)` row and substitute the `image_size_mb` numeric value (1 decimal place); fall back to em-dash when not found. Footnote rewritten to: `*image_size_mb populated when CI is the source (D-13); em-dash for local runs.*`

From `crates/alloc-bench-aggregator/src/recommend.rs:156-180` (the per-scenario mean computation pattern):
- Existing: `let mean: f64 = matching.iter().map(|r| r.metrics.ticks_per_s).sum::<f64>() / matching.len() as f64;` → uses the mean as central tendency.
- New: `let throughputs: Vec<f64> = matching.iter().map(|r| r.metrics.ticks_per_s).collect(); let central_tendency = match crate::multi_run::aggregate(&throughputs) { Some(s) => s.median, None => throughputs.iter().sum::<f64>() / (throughputs.len() as f64).max(1.0) };` — fall back to mean for n<2.

From `crates/alloc-bench-aggregator/templates/index.html.tmpl:415-447` (the existing throughput-trace JS):
- Existing: `const y = scenarios.map(s => { const hit = allocRuns.find(r => r.scenario.name === s); return hit ? hit.metrics.ticks_per_s : null; });` then `traces.push({type: 'bar', name: alloc + (anySuspect ? ' ⚠' : ''), x: scenarios, y: y, marker: { color: ALLOC_COLORS[alloc] || '#888' }});`.
- New: derive `_multiRun = { median, min, max, cv_pct, suspect }` per Run on the JS side from `RESULTS_GROUPED` (which is built in main.rs at HTML emit time) — when `_multiRun` is set, push `error_y` with asymmetric `array` (`max - median`) and `arrayminus` (`median - min`). Legend `name` gets `⚠ high variance` when any contributing run has `cv_pct > 10`.
- Tinytemplate-escape rule (lines 22, 60, 404, 419, 425, 429): every literal `{` in the JS body is `\{`. The new `error_y` block follows the same convention.

From `crates/alloc-bench-aggregator/src/loader.rs:23-36` (existing LoadOutcome shape — add CellMeta type next to it):
```
pub struct LoadOutcome { pub runs: Vec<Run>, pub skipped: Vec<SkippedFile> }
pub struct SkippedFile { pub path: PathBuf, pub reason: String }
```
Add: `#[derive(Debug, Deserialize)] pub struct CellMeta { pub alloc: String, pub env: String, pub image_size_bytes: u64, pub image_size_mb: f64, pub build_time_s: Option<f64>, pub captured_at: Option<String> }` and `pub fn load_cell_metas(pattern: &str) -> Result<HashMap<(String, String), CellMeta>>`. Empty pattern → empty HashMap (no error). Per-file failure → `eprintln!("warn: skipped meta {}: {}", path, e)` and continue (skip-and-continue per loader's existing D-08 pattern).

From `crates/alloc-bench-aggregator/tests/fixtures/multi_run/` (Plan 01 fixtures):
- `seed-1.json` / `seed-2.json` / `seed-3.json` — each a Vec<Run> with two scenarios per file (multithread → CV ≈ 4.76%, cpu-bound → CV ≈ 19.5%).
- `meta/jemalloc-alpine.json` — sidecar with `image_size_mb: 26.55`.

From `crates/alloc-bench-aggregator/src/markdown.rs` byte-identical-output contract (lines 18-25):
- BTreeMap / BTreeSet (alphabetical iteration; never HashMap / HashSet).
- The `(alloc, env, scenario)` grouping for multi-run lookup uses BTreeMap.

From CONTEXT.md `<specifics>` ¶5 — concatenation rule:
- Both flags concatenated when both fire: `*(⚠ suspect: low samples; ⚠ high variance: CV 14%)*` — italic note style matches `suspect_note` in markdown.rs:284-290.

The `format_throughput_cell` helper signature from RESEARCH §"Code Examples — Multi-run aggregator integration" (lines 956-967 of 05-RESEARCH.md):
```
pub fn format_throughput_cell(s: &MultiRunStats, suspect: bool) -> String
```
Output shape: `"{median:.0} ({min:.0}..{max:.0}, {cv_str}{variance_flag}{suspect_flag})"` where `cv_str` is `format!("CV {:.0}%", cv)` or `"CV \u{2014}"` (em-dash).

Test fixtures map to grep gates as follows:
- multithread scenario → CV ≈ 4.76% → REPORT.md row contains `CV 5%` or `CV 4%` (round 4.76 to nearest integer).
- cpu-bound scenario → CV ≈ 19.5% → REPORT.md row contains `CV 19%` or `CV 20%` AND `⚠ high variance`.
- Sidecar `meta/jemalloc-alpine.json` → `image_size_mb: 26.55` → Docker runtimes table cell shows `26.5` (1 decimal place).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend `loader.rs` with `CellMeta` + `load_cell_metas` (sidecar parsing) + add `--meta` clap flag in `main.rs`; thread metas through markdown::write and html::write</name>
  <files>
    crates/alloc-bench-aggregator/src/loader.rs
    crates/alloc-bench-aggregator/src/main.rs
    crates/alloc-bench-aggregator/src/markdown.rs
    crates/alloc-bench-aggregator/src/html.rs
  </files>
  <read_first>
    - crates/alloc-bench-aggregator/src/loader.rs (full — extend in place; preserve existing `discover` + `LoadOutcome` types)
    - crates/alloc-bench-aggregator/src/main.rs (full — extend Cli struct + main flow)
    - crates/alloc-bench-aggregator/src/markdown.rs lines 38-47 (the `write` signature being extended)
    - crates/alloc-bench-aggregator/src/html.rs lines 85-90 (the `write` signature being extended)
    - crates/alloc-bench-aggregator/tests/fixtures/multi_run/meta/jemalloc-alpine.json (Plan 01 fixture — exact field set the new struct must accept)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-RESEARCH.md §"Pattern 4: Per-cell `meta.json` sidecar + aggregator merge" (the canonical CellMeta struct + load_cell_metas signature)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-PATTERNS.md §"crates/alloc-bench-aggregator/src/main.rs" (the pattern for extending the Cli struct and the main() body)
  </read_first>
  <behavior>
    - Test 1 — `loader::load_cell_metas` returns an empty HashMap when the pattern is `""` (no error).
    - Test 2 — `loader::load_cell_metas` returns a HashMap with one entry keyed `("jemalloc".into(), "alpine".into())` when given the multi-run meta fixture path glob.
    - Test 3 — `loader::load_cell_metas` returns Ok with an empty HashMap (and prints a `warn:` line on stderr) when given a path that matches a malformed JSON file.
    - Test 4 — `Cli::parse_from(["bin", "--input", "x.json", "--meta", "meta/*.json", "--output", "out/"])` populates `cli.meta` to `"meta/*.json"`.
    - Test 5 — `Cli::parse_from(["bin"])` populates `cli.meta` to `""` (default).
  </behavior>
  <action>
    Per D-13 / D-14 / D-20 / RESEARCH §Pattern 4 / PATTERNS §main.rs / PATTERNS §loader: extend `crates/alloc-bench-aggregator/src/loader.rs` by importing `serde::Deserialize` and `std::collections::HashMap` (HashMap is fine here — the keys are `(alloc, env)` tuples not used for byte-identical-output ordering). Add `#[derive(Debug, Deserialize)] pub struct CellMeta { pub alloc: String, pub env: String, pub image_size_bytes: u64, pub image_size_mb: f64, pub build_time_s: Option<f64>, pub captured_at: Option<String> }` (build_time_s and captured_at optional so older sidecars without those fields still parse). Add `pub fn load_cell_metas(pattern: &str) -> Result<HashMap<(String, String), CellMeta>>`: when `pattern.is_empty()` return `Ok(HashMap::new())`; otherwise glob the pattern via `glob::glob`, sort results, parse each via `serde_json::from_slice::<CellMeta>(&bytes)?`, log per-file failures via `eprintln!("warn: skipped meta {}: {}", path.display(), e)` (matches the existing per-file failure pattern in `discover` lines 60-66), and insert each successful parse into the map keyed by `(meta.alloc.clone(), meta.env.clone())`. Add 3 unit tests under `#[cfg(test)] mod meta_tests { use super::*; ... }` covering: empty-pattern returns empty map, valid fixture path produces a non-empty map with the expected key, malformed JSON file is skipped (use a tempdir + writes-bad-json helper). Per D-13: extend `crates/alloc-bench-aggregator/src/main.rs` by adding a third Cli field `#[arg(long, default_value = "")] meta: String` between the existing `input` and `output` fields with the doc-comment `/// Glob pattern for per-cell meta sidecars (image_size_mb / build_time_s). Empty = skip meta merge. CI populates via 'docker inspect' (D-13).`. Add `mod multi_run;` is already in place from Plan 01 — verify it stays. Modify `main()` to call `let metas = loader::load_cell_metas(&cli.meta)?;` between the existing `let outcome = loader::discover(&cli.input)?;` line and the `markdown::write` call. Update both `markdown::write(&outcome, &metas, out_dir)?;` and `html::write(&outcome, &metas, out_dir)?;` to take the metas reference. Update the function signatures in `markdown.rs:42` and `html.rs:87` to accept `metas: &HashMap<(String, String), CellMeta>` as the second parameter (after `outcome`, before `out_dir`). Inside `markdown.rs::write`, propagate `metas` to `build_report` (already a free function); update `build_report` to accept the metas parameter. Inside `html.rs::write`, the metas reference is passed to the template-data assembly step (used in Task 4). Add `use crate::loader::CellMeta;` + `use std::collections::HashMap;` to the modified files. Add unit tests for the clap flag default + custom value. The byte-identical-output contract of Phase 4 still holds when metas is empty: every existing unit test in markdown.rs continues to pass byte-identically because `metas.is_empty() → emit em-dash` is the existing path.
  </action>
  <verify>
    <automated>cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings</automated>
    <automated>cargo test -p alloc-bench-aggregator --lib loader::meta_tests</automated>
    <automated>grep -F "pub struct CellMeta" crates/alloc-bench-aggregator/src/loader.rs</automated>
    <automated>grep -F "pub fn load_cell_metas" crates/alloc-bench-aggregator/src/loader.rs</automated>
    <automated>grep -F "meta: String" crates/alloc-bench-aggregator/src/main.rs</automated>
    <automated>grep -F "load_cell_metas(&cli.meta)" crates/alloc-bench-aggregator/src/main.rs</automated>
    <automated>grep -F "metas: &HashMap<(String, String), CellMeta>" crates/alloc-bench-aggregator/src/markdown.rs</automated>
    <automated>grep -F "metas: &HashMap<(String, String), CellMeta>" crates/alloc-bench-aggregator/src/html.rs</automated>
  </verify>
  <done>
    `loader::load_cell_metas` exists with the documented contract. `Cli::meta` flag defaults to empty string. `main()` invokes the loader and threads the result through `markdown::write` + `html::write`. Three new loader unit tests pass. Phase 4 byte-identical-output tests in `tests/smoke.rs` (existing 6 visual-contract tests) still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend `markdown.rs` to render multi-run throughput cells (`{median} ({min}..{max}, CV {N}%)`) with concatenated suspect + high-variance flags + sidecar-driven Docker runtimes table</name>
  <files>
    crates/alloc-bench-aggregator/src/markdown.rs
  </files>
  <read_first>
    - crates/alloc-bench-aggregator/src/markdown.rs full (especially lines 86-189 for the per-scenario tables + Docker runtimes table)
    - crates/alloc-bench-aggregator/src/multi_run.rs (Plan 01) — exports `aggregate`, `is_high_variance`, `MultiRunStats`
    - .planning/phases/05-ci-image-size-gate-public-polish/05-RESEARCH.md §"Code Examples — Multi-run aggregator integration" (the canonical `format_throughput_cell` body — output format `"{:.0} ({:.0}..{:.0}, {}{}{})"`)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-PATTERNS.md §"crates/alloc-bench-aggregator/src/markdown.rs" (per-scenario row-emit + suspect+variance concatenation rule + docker runtimes backfill pattern + footnote rewrite)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-CONTEXT.md `<specifics>` ¶5 (concatenation rule for both-flags case)
  </read_first>
  <behavior>
    - Test 1 — `format_throughput_cell` for `MultiRunStats { median: 100.0, min: 95.0, max: 110.0, cv_pct: Some(4.76), .. }` with `suspect=false` returns `"100 (95..110, CV 5%)"` (rounded — uses `{:.0}` for both throughput and CV%).
    - Test 2 — `format_throughput_cell` with `cv_pct: Some(15.3)` and `suspect=false` returns `"100 (90..130, CV 15% ⚠ high variance)"` (variance flag appended).
    - Test 3 — `format_throughput_cell` with `cv_pct: Some(15.3)` and `suspect=true` returns `"100 (90..130, CV 15% ⚠ high variance ⚠ suspect)"` (both flags concatenated).
    - Test 4 — `format_throughput_cell` with `cv_pct: None` (mean ≈ 0) returns `"0 (0..0, CV \u{2014})"` (em-dash for undefined CV).
    - Test 5 — `emit_per_scenario_tables` against the multi_run fixture set produces a REPORT.md row containing `(95..110, CV` for the multithread scenario AND a row containing `⚠ high variance` for the cpu-bound scenario.
    - Test 6 — `emit_docker_runtimes_table` with a non-empty metas map (containing `("jemalloc", "alpine") → CellMeta{ image_size_mb: 26.55 }`) emits `26.5` in the alpine row's `image_size_mb` cell.
    - Test 7 — `emit_docker_runtimes_table` with an empty metas map emits the existing em-dash placeholder unchanged (Phase 4 byte-identical-output contract).
    - Test 8 — Phase 4's existing test `report_md_two_runs_byte_identical_after_timestamp_strip` (markdown.rs:487-511) still passes (single-run-per-cell input → multi_run::aggregate returns None → falls back to existing single-value formatting).
  </behavior>
  <action>
    Per D-11 / D-12 / D-13 / RESEARCH §Pattern 5 + §"Code Examples — Multi-run aggregator integration" / PATTERNS §markdown.rs: add at the top of `markdown.rs` the new imports `use crate::loader::CellMeta; use crate::multi_run::{aggregate as mr_aggregate, is_high_variance, MultiRunStats}; use std::collections::HashMap;`. Update `pub fn write(outcome: &LoadOutcome, metas: &HashMap<(String, String), CellMeta>, out_dir: &Path) -> Result<()>` signature and propagate metas to `build_report(outcome, metas)`. Update `pub(crate) fn build_report(outcome: &LoadOutcome, metas: &HashMap<(String, String), CellMeta>) -> String` signature. Add `pub(crate) fn format_throughput_cell(s: &MultiRunStats, suspect: bool) -> String` per RESEARCH §"Code Examples — Multi-run aggregator integration" (output: `format!("{:.0} ({:.0}..{:.0}, {}{}{})", s.median, s.min, s.max, cv_str, variance_flag, suspect_flag)`); cv_str is `format!("CV {:.0}%", cv)` when `cv_pct.is_some()` else `"CV \u{2014}"`; variance_flag is `" \u{26A0} high variance"` when `is_high_variance(s)` else `""`; suspect_flag is `" \u{26A0} suspect"` when suspect else `""`. Modify `emit_per_scenario_tables(buf: &mut String, runs: &[Run])` → `emit_per_scenario_tables(buf: &mut String, runs: &[Run])` body: for each scenario, build a BTreeMap<(allocator: String, env: String), Vec<f64>> grouping `metrics.ticks_per_s` for that scenario; for each row, look up the (alloc, env) group in this map; if `mr_aggregate(&samples)` returns Some, use `format_throughput_cell(&stats, suspect_reason(...).is_some())` for the throughput cell; otherwise fall back to the existing `format!("{:.1} {}", ticks_per_s, unit)` + suspect_note path (preserves Phase 4 byte-identical-output for single-run-per-cell inputs). Modify `emit_docker_runtimes_table(buf: &mut String, runs: &[Run])` to `emit_docker_runtimes_table(buf: &mut String, runs: &[Run], metas: &HashMap<(String, String), CellMeta>)`: for each `env` row in the BTreeSet, look up `metas` by `(alloc, env)` for any alloc that has a meta; if found, format `image_size_mb` as `format!("{:.1}", meta.image_size_mb)`; if not found, fall back to em-dash `\u{2014}`. Footnote text becomes (when metas is non-empty): `*image_size_mb populated from CI sidecar (D-13); em-dash for cells without meta sidecars.*` and (when metas is empty — preserves Phase 4 wording byte-identically): `*image_size_mb / build_time_s / run_overhead_pct populated by Phase 5 CI via docker inspect (REPR-03).*`. Add 8 unit tests under `#[cfg(test)] mod tests { ... }` covering the behaviors named above; reuse the existing `synth_run` helper (or copy its shape if private to recommend.rs). The 8 tests are additive; existing tests must still pass.
  </action>
  <verify>
    <automated>cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings</automated>
    <automated>cargo test -p alloc-bench-aggregator --lib markdown</automated>
    <automated>cargo test -p alloc-bench-aggregator --test smoke</automated>
    <automated>grep -F "pub(crate) fn format_throughput_cell" crates/alloc-bench-aggregator/src/markdown.rs</automated>
    <automated>grep -F "use crate::multi_run" crates/alloc-bench-aggregator/src/markdown.rs</automated>
    <automated>grep -F "use crate::loader::CellMeta" crates/alloc-bench-aggregator/src/markdown.rs</automated>
    <automated>grep -F '\u{26A0} high variance' crates/alloc-bench-aggregator/src/markdown.rs || grep -F "high variance" crates/alloc-bench-aggregator/src/markdown.rs</automated>
  </verify>
  <done>
    8 new markdown.rs unit tests pass. All Phase 4 markdown.rs unit tests still pass byte-identically. `format_throughput_cell` emits the documented shape for low/high CV, both-flags concatenated, and undefined-CV (em-dash) cases. Docker runtimes table populates from sidecar when metas is non-empty; falls back to em-dash byte-identically when empty.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update `recommend.rs` to use multi_run median (with mean fallback) as the central tendency for the workload-class winner picker</name>
  <files>
    crates/alloc-bench-aggregator/src/recommend.rs
  </files>
  <read_first>
    - crates/alloc-bench-aggregator/src/recommend.rs full (especially lines 100-200 for the per-scenario mean computation; the existing `winner_picker_*` tests at lines 478-518 must still pass)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-PATTERNS.md §"crates/alloc-bench-aggregator/src/recommend.rs" (median substitution pattern + test-impact analysis showing single-run-per-cell tests stay green via the mean fallback)
  </read_first>
  <behavior>
    - Test 1 — Existing `winner_picker_*` tests (recommend.rs:478-518) still pass byte-identically (single-run-per-(alloc, scenario) → mr_aggregate returns None → mean fallback picks the same winner).
    - Test 2 — New test `winner_picker_uses_median_when_three_seeds_present`: synthesize 3 Runs per (alloc, scenario) with throughputs (10, 100, 110) for jemalloc-cpu-bound and (50, 50, 50) for ptmalloc-cpu-bound. Mean of jemalloc would be 73.3 (loses to ptmalloc 50). Median of jemalloc is 100 (wins vs 50). Assert the recommendation picks jemalloc.
  </behavior>
  <action>
    Per D-11 / RESEARCH §Pattern 5 / PATTERNS §recommend.rs: import `crate::multi_run`. Locate the per-scenario mean computation around lines 156-180 (or wherever `let mean: f64 = matching.iter().map(|r| r.metrics.ticks_per_s).sum::<f64>() / matching.len() as f64;` lives). Replace with: `let throughputs: Vec<f64> = matching.iter().map(|r| r.metrics.ticks_per_s).collect(); let central_tendency = match crate::multi_run::aggregate(&throughputs) { Some(stats) => stats.median, None => throughputs.iter().sum::<f64>() / (throughputs.len() as f64).max(1.0) };` — store `central_tendency` in `per_scenario` instead of `mean`. The fallback path (n<2) uses the same arithmetic as before so existing tests are byte-stable. Comment the line: `// D-11: prefer median across runs (multi_run::aggregate); fall back to mean for n<2.`. Add the new test `winner_picker_uses_median_when_three_seeds_present` under `#[cfg(test)] mod tests { ... }` using the existing `synth_run` builder (lines 289-354). The test synthesizes 3 Runs per (alloc, scenario) tuple and asserts the median-driven winner.
  </action>
  <verify>
    <automated>cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings</automated>
    <automated>cargo test -p alloc-bench-aggregator --lib recommend</automated>
    <automated>grep -F "crate::multi_run::aggregate" crates/alloc-bench-aggregator/src/recommend.rs</automated>
    <automated>grep -F "fn winner_picker_uses_median_when_three_seeds_present" crates/alloc-bench-aggregator/src/recommend.rs</automated>
  </verify>
  <done>
    All existing recommend.rs tests still pass. The new test pinning median behavior passes. The line `crate::multi_run::aggregate` appears in recommend.rs.
  </done>
</task>

<task type="auto">
  <name>Task 4: Extend `templates/index.html.tmpl` throughput trace with Plotly `error_y` (asymmetric min/max range) + legend `⚠ high variance` flag; assemble derived `RESULTS_GROUPED` in html.rs at template-data time</name>
  <files>
    crates/alloc-bench-aggregator/src/html.rs
    crates/alloc-bench-aggregator/templates/index.html.tmpl
  </files>
  <read_first>
    - crates/alloc-bench-aggregator/src/html.rs full (the `write` function being extended with metas + the template-data assembly path)
    - crates/alloc-bench-aggregator/templates/index.html.tmpl lines 1-100 (the head/CSS block — preserve verbatim) + lines 400-450 (the throughput-trace JS — extend with error_y) + lines 22-60 (tinytemplate-escape rules — every literal `{` is `\{`)
    - crates/alloc-bench-aggregator/src/multi_run.rs (Plan 01) — for the `MultiRunStats` shape that gets serialized to JSON for client-side consumption
    - .planning/phases/05-ci-image-size-gate-public-polish/05-PATTERNS.md §"crates/alloc-bench-aggregator/templates/index.html.tmpl" (the canonical extension — `error_y` field with asymmetric `array`/`arrayminus`; legend label appends `⚠ high variance` when applicable)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-RESEARCH.md §"Pattern 5: Multi-run statistics in Rust" (MultiRunStats shape; serde::Serialize already on the struct from Plan 01)
  </read_first>
  <action>
    Per D-11 / D-12 / PATTERNS §index.html.tmpl: in `crates/alloc-bench-aggregator/src/html.rs::write`, before rendering the template, build a derived map `BTreeMap<(allocator: String, env: String, scenario: String), MultiRunStats>` by grouping `outcome.runs` by 3-tuple and calling `multi_run::aggregate` on the throughputs. Inject this map into the template under a new placeholder `{ multi_run_grouped_json | unescaped }` (a JSON serialization of the BTreeMap). Pass `metas` along too — the docker-runtimes table content is rendered server-side in markdown but the HTML template surfaces image_size_mb in chart hover-text; for this plan, just thread `metas` through but the HTML template additions focus on the chart `error_y` extension and the legend `⚠ high variance` flag. In the JS body of `index.html.tmpl::makeThroughputTraces` (around lines 415-447): the existing `const y = scenarios.map(s => { const hit = allocRuns.find(r => r.scenario.name === s); return hit ? hit.metrics.ticks_per_s : null; });` becomes the fallback path. Add a new derivation: `const yMedians = scenarios.map(s => { const hit = allocRuns.find(r => r.scenario.name === s); if (!hit) return null; const key = hit.build.allocator + '|' + envLabel(hit) + '|' + hit.scenario.name; const mr = MULTI_RUN_GROUPED[key]; return mr ? mr.median : hit.metrics.ticks_per_s; });`. Build asymmetric error arrays: `const yMinus = scenarios.map(s => { const hit = allocRuns.find(r => r.scenario.name === s); if (!hit) return 0; const key = hit.build.allocator + '|' + envLabel(hit) + '|' + hit.scenario.name; const mr = MULTI_RUN_GROUPED[key]; return mr ? (mr.median - mr.min) : 0; });` and a parallel `yPlus` using `(mr.max - mr.median)`. Compute `const anyHighVariance = allocRuns.some(r => { const key = r.build.allocator + '|' + envLabel(r) + '|' + r.scenario.name; const mr = MULTI_RUN_GROUPED[key]; return mr && mr.cv_pct !== null && mr.cv_pct > 10; });`. Build the trace: `traces.push({ type: 'bar', name: alloc + (anySuspect ? ' ⚠ suspect' : '') + (anyHighVariance ? ' ⚠ high variance' : ''), x: scenarios, y: yMedians, error_y: { type: 'data', symmetric: false, array: yPlus, arrayminus: yMinus, visible: true, color: '#1F2328', thickness: 1.0, width: 4 }, marker: { color: ALLOC_COLORS[alloc] || '#888' } });`. The MULTI_RUN_GROUPED constant is keyed by `"alloc|env|scenario"` strings so the JS lookup is O(1). Inject it via the template placeholder: `<script>const MULTI_RUN_GROUPED = {multi_run_grouped_json | unescaped};</script>` placed just below the existing `const RESULTS = ...` block. Keys in the JSON map MUST use the same `alloc|env|scenario` shape (build it server-side in html.rs). When `MULTI_RUN_GROUPED[key]` is undefined (single-run-per-cell case), the trace falls back to `hit.metrics.ticks_per_s` and `error_y` arrays of zeros — Plotly renders as no-error-bars (visible:true with all-zero arrays = zero-width whiskers, equivalent to no decoration). This preserves the Phase 4 visual-contract tests in tests/smoke.rs:278-405 byte-identically. Tinytemplate-escape rule: every literal `{` in the JS body of the new error_y block uses `\{`. Verify by checking the existing pattern at lines 419, 425, 429 — all the `{` characters inside JS object literals are `\{`. The new code follows the same convention.
  </action>
  <verify>
    <automated>cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings</automated>
    <automated>cargo build -p alloc-bench-aggregator --release</automated>
    <automated>grep -F "MULTI_RUN_GROUPED" crates/alloc-bench-aggregator/templates/index.html.tmpl</automated>
    <automated>grep -F "error_y" crates/alloc-bench-aggregator/templates/index.html.tmpl</automated>
    <automated>grep -F "arrayminus" crates/alloc-bench-aggregator/templates/index.html.tmpl</automated>
    <automated>grep -F "high variance" crates/alloc-bench-aggregator/templates/index.html.tmpl</automated>
    <automated>grep -F "multi_run_grouped_json" crates/alloc-bench-aggregator/src/html.rs</automated>
    <automated>grep -F "multi_run_grouped_json" crates/alloc-bench-aggregator/templates/index.html.tmpl</automated>
  </verify>
  <done>
    Aggregator binary compiles with the new template placeholder + JS extension. Plotly `error_y` field is present in the rendered HTML when multi-run data is supplied. Legend label includes `⚠ high variance` when applicable. Phase 4 visual-contract smoke tests still pass byte-identically when no multi-run data is loaded.
  </done>
</task>

<task type="auto">
  <name>Task 5: Add 6 new integration tests in `tests/smoke.rs` covering multi-run REPORT.md output, HTML output, and sidecar-driven Docker runtimes table</name>
  <files>
    crates/alloc-bench-aggregator/tests/smoke.rs
  </files>
  <read_first>
    - crates/alloc-bench-aggregator/tests/smoke.rs full (the existing 6 visual-contract tests at lines 278-405 must still pass; new tests follow the same `cargo_bin → assert.success → read output → grep substrings` pattern)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-PATTERNS.md §"crates/alloc-bench-aggregator/tests/smoke.rs" (the canonical 6-new-test table — exact assertions for each)
  </read_first>
  <behavior>
    - Test 1 `aggregator_multi_run_emits_cv_in_throughput_cell`: aggregator on the multi-run fixture set produces REPORT.md containing `, CV ` (the literal "comma-space-CV-space" anchor) on the per-scenario throughput cells.
    - Test 2 `aggregator_multi_run_emits_min_max_range_in_cell`: REPORT.md contains the `(95..` substring (the `({min}..` syntax) for the multithread scenario.
    - Test 3 `aggregator_high_variance_cell_marked_with_warning_glyph`: REPORT.md for the cpu-bound scenario contains `⚠ high variance` (gates D-12).
    - Test 4 `aggregator_html_contains_error_y_field`: `index.html` contains the string `error_y` (the Plotly errorbar field name; pinned by the template extension in Task 4).
    - Test 5 `aggregator_html_high_variance_appears_in_legend`: `index.html` contains the string `high variance` (appears in the JS `name + ' ⚠ high variance'` concatenation).
    - Test 6 `aggregator_meta_sidecar_populates_image_size_mb`: when `--meta` points at `tests/fixtures/multi_run/meta/*.json`, REPORT.md `## Docker runtimes` row for `alpine` contains `26.5` (the meta fixture's image_size_mb to 1 decimal place).
  </behavior>
  <action>
    Per RESEARCH §Validation Architecture / PATTERNS §smoke.rs: append 6 new `#[test]` functions to `crates/alloc-bench-aggregator/tests/smoke.rs`. Add a sister helper `run_aggregator_with_multi_run_fixtures()` (mirrors the existing `run_aggregator_against_fixtures` at smoke.rs:259-271) that points `--input` at `tests/fixtures/multi_run/seed-*.json` and `--meta` at `tests/fixtures/multi_run/meta/*.json`. The helper returns `(tempdir, html_string, md_string)`. Each new `#[test]` reads HTML or REPORT.md from the helper's output and `assert!(text.contains(expected_substring))`. Use the literal anchors in the `<behavior>` block above. For Test 6 specifically: the assertion is `assert!(md.contains("26.5"))` and additionally `assert!(md.contains("alpine"))` (anchor on both the value and the env row label so a future cell-shuffle doesn't false-pass). Do not modify existing tests. Conventional commit prefix: `test(05)`.
  </action>
  <verify>
    <automated>cargo test -p alloc-bench-aggregator --test smoke -- --nocapture aggregator_multi_run_emits_cv_in_throughput_cell aggregator_multi_run_emits_min_max_range_in_cell aggregator_high_variance_cell_marked_with_warning_glyph aggregator_html_contains_error_y_field aggregator_html_high_variance_appears_in_legend aggregator_meta_sidecar_populates_image_size_mb</automated>
    <automated>cargo test -p alloc-bench-aggregator --test smoke</automated>
    <automated>grep -F "aggregator_multi_run_emits_cv_in_throughput_cell" crates/alloc-bench-aggregator/tests/smoke.rs</automated>
    <automated>grep -F "aggregator_meta_sidecar_populates_image_size_mb" crates/alloc-bench-aggregator/tests/smoke.rs</automated>
  </verify>
  <done>
    6 new smoke tests pass alongside all existing tests. The full `cargo test -p alloc-bench-aggregator` runs end-to-end green.
  </done>
</task>

<task type="auto">
  <name>Task 6: Replace Plan 02's `ci-aggregate` stub with the real aggregator invocation including --meta flag</name>
  <files>
    justfile
  </files>
  <read_first>
    - justfile (the `ci-aggregate` stub installed by Plan 02 — replace its body)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-PATTERNS.md §"justfile" §"ci-aggregate" (canonical body)
  </read_first>
  <action>
    Replace the stub body of `ci-aggregate` with the real invocation. New body (single-line cargo invocation + multi-line for readability): `cargo run --release -p alloc-bench-aggregator -- --input "results/*.json" --meta "meta/*.json" --output report/`. Update the recipe doc-comment to match Phase 4's `aggregate` style: `# CI variant of aggregate — also picks up sidecar meta.json files written by ci-bench-cell (Phase 5 D-13). Used by the GHA aggregate job.` Do NOT touch any other recipe. The Phase 4 `just aggregate` recipe stays unchanged (it does not pass `--meta` so meta-merge is opt-in, matching D-13's "default empty so existing local invocations continue to work").
  </action>
  <verify>
    <automated>just --list 2>&1 | grep -F "ci-aggregate"</automated>
    <automated>grep -F '--meta "meta/*.json"' justfile</automated>
    <automated>! grep -F "Plan 03 implements" justfile</automated>
    <automated>just ci-aggregate 2>&1 | grep -F "no results found" || just ci-aggregate 2>&1</automated>
  </verify>
  <done>
    `ci-aggregate` body invokes the aggregator with `--meta` flag. The Plan 02 stub message is gone. When run with no results/ directory present, the recipe fails with the aggregator's `no results found matching pattern` error (proving the binary is reachable; the actual aggregation requires a populated results directory + meta dir).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sidecar meta.json files ↔ aggregator | Sidecars are produced by `just ci-bench-cell` running locally or in CI (Plan 02). Inputs are project-controlled; malformed sidecars are skip-and-continue (warn on stderr). |
| Multi-run JSON `MULTI_RUN_GROUPED` ↔ HTML viewer | Server-side serialization writes the derived map into the HTML; the JS reads it via constant. Same trust model as `RESULTS` (Phase 4). The CSP allows `'unsafe-inline'` for the script block (Phase 4 WR-04). |
| `--meta` flag input ↔ glob expansion | The flag accepts arbitrary glob patterns; the loader rejects malformed JSON files via skip-and-continue. Empty-string default = no expansion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | sidecar JSON parsing | mitigate | `serde_json::from_slice::<CellMeta>` rejects malformed input; per-file failure logged via `eprintln!("warn: skipped meta {}: {}", path, e)` and skip-continued. Aggregator never panics on malformed sidecars. |
| T-05-10 | Information Disclosure | HTML dashboard contents | accept | All data is project-generated benchmark numbers + image-size metadata. No PII. CSP already locks down inline-script attack surface (Phase 4). |
| T-05-11 | Tampering | byte-identical-output contract | mitigate | The empty-metas / single-run-per-cell path is byte-stable: existing 6 visual-contract smoke tests + 2 markdown.rs byte-identical tests still pass. Regressions are caught by `cargo test`. |
| T-05-12 | Spoofing | malicious image_size_mb in sidecar | accept | The sidecar is project-controlled (only `just ci-bench-cell` writes it); impersonation requires repo write access. The numeric value is rendered into REPORT.md for human inspection only. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | This plan adds zero new Cargo crates (RESEARCH §Package Legitimacy Audit confirms). No slopcheck gate required. |
</threat_model>

<verification>
- `cargo fmt --all --check` succeeds.
- `cargo clippy --workspace --all-targets -- -D warnings` succeeds.
- `cargo test --workspace --release` succeeds (full suite — multi_run unit tests + loader meta_tests + 8 new markdown tests + 1 new recommend test + 6 new smoke tests + ALL existing Phase 1-4 tests).
- `cargo build -p alloc-bench-aggregator --release` succeeds.
- `just aggregate` (the existing Phase 4 recipe — does not pass --meta) still produces byte-identical REPORT.md against the Phase-4 fixture set after this plan lands.
- `just ci-aggregate` invokes the binary with `--meta`.
- The aggregator binary on the multi-run fixture set produces a REPORT.md containing `CV `, `(95..` (range), `⚠ high variance` (cpu-bound scenario), and `26.5` (image_size_mb from sidecar).
- The aggregator binary on the multi-run fixture set produces an `index.html` containing `error_y` and `high variance` literal strings.
</verification>

<success_criteria>
- Multi-run REPORT.md cells show `{median} ({min}..{max}, CV {N}%)` with concatenated suspect + high-variance flags when both apply (D-11 / D-12 / CONTEXT.md `<specifics>` ¶5).
- HTML dashboard shows asymmetric Plotly `error_y` whiskers when multi-run data is present; legend label gains `⚠ high variance` for cells with CV>10%.
- Docker runtimes table populates `image_size_mb` from the sidecar when `--meta` is supplied; falls back to em-dash byte-identically when not (closes Phase 4 D-10 deferral).
- recommend.rs winner picker uses median (with mean fallback) — existing tests stay green.
- `just ci-aggregate` is functional (Plan 02 stub replaced).
- v1 input schema is NOT modified.
- All Phase 1-4 tests still pass.
</success_criteria>

<output>
Create `.planning/phases/05-ci-image-size-gate-public-polish/05-03-SUMMARY.md` recording: total new tests added (3 loader meta_tests + 8 markdown tests + 1 recommend test + 6 smoke tests = 18 new tests), every Phase 1-4 test that was preserved byte-identically (especially the 6 visual-contract smoke tests), and any deviation from RESEARCH §Pattern 4 / §"Code Examples" if encountered. Conventional commit prefix: `feat(05)` for the integration; `test(05)` for the test additions.
</output>
