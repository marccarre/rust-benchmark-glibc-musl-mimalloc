---
phase: 05-ci-image-size-gate-public-polish
plan: 04
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - README.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - CLAUDE.md
autonomous: false
requirements: [REPR-01]
must_haves:
  truths:
    - "User reads `README.md` and finds the CI status badge URL `https://github.com/marccarre/rust-benchmark-glibc-musl-mimalloc/actions/workflows/bench.yml/badge.svg` at the top, before the project title (D-15 / D-18, RESEARCH §Pattern 7)."
    - "User reads `README.md` and the existing `## How memory allocation works on Linux` section + Mermaid diagram (Phase 4 AGG-08) is preserved verbatim — the existing tests/smoke.rs:519-531 `readme_md_contains_system_diagram` passes byte-identically."
    - "User reads `README.md` and finds a `## Run it yourself` section with the 5-step recipe — install, clone, smoke OR full bench, aggregate, open report (D-16)."
    - "The 5-step recipe documents BOTH `just bench-all-smoke` (~10 min) and `just bench-all` (~2.5h, ~5GB disk) explicitly, including the trade-off (D-16)."
    - "User reads `README.md` and finds a `## Allocator matrix overview` Markdown table enumerating all 18 (env × alloc) cells with libc family and target triple (D-15 ¶5, RESEARCH §Pattern 1 — copy from justfile:131-150)."
    - "User reads `README.md` and finds a `## Reproducibility` section that cites `rustc 1.91` (NOT 1.83 — Pitfall 5) sourced from `rust-toolchain.toml`, the six pinned Docker base images (Phase 3 D-05), `target-cpu=x86-64-v3` build flag (Phase 3 D-09), GHA hardware notes (4 vCPU / 16 GB RAM ubuntu-24.04 free tier), and a link to `.planning/research/PITFALLS.md` (D-17)."
    - "User reads `README.md` and finds a `## License` section stating dual-license under `Apache-2.0 OR MIT` and citing both `LICENSE-APACHE` and `LICENSE-MIT` (D-15 ¶7 / D-22)."
    - "D-21: README walkthrough is the sufficient public-facing surface for v1; no separate CONTRIBUTING.md is added (benchmark repo, no public PR contributions expected)."
    - "README.md `### Troubleshooting` block under `## Run it yourself` covers: Apple Silicon `--platform linux/amd64` (Phase 3 D-15), hyperthreading `--cpus=4 --cpuset-cpus=0-3` (Phase 3 D-15), NUMA `--cpuset-cpus` (Phase 3 D-16), low-memory mimalloc OOM-kill (`BENCH_MEMORY=8g`)."
    - "README.md uses the LITERAL repo URL `marccarre/rust-benchmark-glibc-musl-mimalloc` (NOT a `{owner}/{repo}` placeholder — Pattern 7 / PATTERNS §Codebase audit fact)."
    - "STATE.md is updated to reflect Phase 5 completion: `progress.completed_phases = 4` (Phase 1 + 2 + 4 + 5 — Phase 3 still has 03-04 + 03-05 pending), `progress.completed_plans` incremented by 4, `current focus` reflects the next milestone-closing checkpoint."
    - "ROADMAP.md Phase 5 entry shows the 4 plans listed with checkboxes — every Plan 1-3 marked `[x]`, Plan 4 marked `[x]` after this task completes; the success criteria block remains unchanged."
    - "CLAUDE.md `## Conventions` section is updated to record any new patterns learned in Phase 5 (e.g., the sidecar `meta.json` aggregator-decoration approach for adding fields without modifying a locked schema)."
  artifacts:
    - path: "README.md"
      provides: "Public-facing reproduction walkthrough — badge, hero, system diagram (preserved), Run it yourself, Allocator matrix overview, Reproducibility, License"
      contains: "Run it yourself"
    - path: ".planning/STATE.md"
      provides: "Updated milestone position reflecting Phase 5 closure"
      contains: "completed_phases"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 plan list updated with all 4 plans marked complete"
      contains: "05-04-PLAN.md"
    - path: "CLAUDE.md"
      provides: "Project conventions updated with Phase-5-learned patterns (sidecar approach, multi-run statistics convention)"
      contains: "sidecar"
  key_links:
    - from: "README.md (CI badge)"
      to: ".github/workflows/bench.yml (Plan 02)"
      via: "Markdown image link to actions/workflows/bench.yml/badge.svg"
      pattern: "actions/workflows/bench.yml/badge.svg"
    - from: "README.md (License section)"
      to: "LICENSE-MIT + LICENSE-APACHE (Plan 02)"
      via: "Citation by relative path"
      pattern: "LICENSE-APACHE"
    - from: "README.md (Reproducibility section)"
      to: "rust-toolchain.toml + docker/*.Dockerfile + .planning/research/PITFALLS.md"
      via: "Cited values + linked path"
      pattern: "rust-toolchain.toml"
    - from: "README.md (Allocator matrix overview)"
      to: "justfile:131-150 _matrix_cells block"
      via: "18 (env, alloc) tuples copied as a Markdown table"
      pattern: "debian-slim"
---

<objective>
Polish the public-facing surface — `README.md` — so a reader who has never seen the repo can reproduce a representative subset of results from scratch (REPR-01). The Phase 4 system diagram is preserved verbatim. New sections are appended in the locked CONTEXT.md D-15 order: badge → title → tagline → existing system diagram → Run it yourself → Allocator matrix overview → Reproducibility → License.

This plan is the milestone-closer: after it lands, the `bench` workflow badge tracks the matrix run state, both LICENSE files exist (Plan 02), the multi-run statistics + sidecar pipeline runs end-to-end (Plans 01 + 03), and the README walks a fresh user through the entire reproduction loop. STATE.md, ROADMAP.md, and CLAUDE.md are updated to reflect the v1.0 milestone state.

A single human-verify checkpoint guards the README quality gate (REPR-01 is the "fresh-user walkthrough" criterion — there is no automated way to test "a reader follows the recipe and reproduces a representative subset of results").

Vertical slice: after this plan ships, the v1.0 milestone closes — README is public-ready, CI is green, dive enforcement is wired, multi-run statistics decorate the report, and the LICENSE files satisfy SPDX-detection.

Purpose: Close REPR-01 (README walkthrough) and end-of-phase project state hygiene.

Output: A polished README; updated STATE.md / ROADMAP.md / CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ci-image-size-gate-public-polish/05-CONTEXT.md
@.planning/phases/05-ci-image-size-gate-public-polish/05-RESEARCH.md
@.planning/phases/05-ci-image-size-gate-public-polish/05-PATTERNS.md
@.planning/phases/05-ci-image-size-gate-public-polish/05-01-PLAN.md
@.planning/phases/05-ci-image-size-gate-public-polish/05-02-PLAN.md
@.planning/phases/05-ci-image-size-gate-public-polish/05-03-PLAN.md

@README.md
@Cargo.toml
@rust-toolchain.toml
@justfile
@CLAUDE.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- The repo state at the time this plan runs (after Plans 01-03 have shipped). -->

From `Cargo.toml:5-10`:
- `license = "MIT OR Apache-2.0"`
- `repository = "https://github.com/marccarre/rust-benchmark-glibc-musl-mimalloc"`
- `authors = ["Marc Carré"]`

From `rust-toolchain.toml:2`:
- `channel = "1.91"` — README MUST cite this value (NOT the stale 1.83 from CONTEXT.md D-17).

From `README.md:1-16` (the existing Phase 4 system diagram + paragraph — PRESERVE VERBATIM; tests/smoke.rs:519-531 asserts the heading + `flowchart TD` are present):
```
# rust-benchmark-glibc-musl-mimalloc

## How memory allocation works on Linux

\`\`\`mermaid
flowchart TD
  app[Application code]
  std[Rust std::alloc]
  ...
\`\`\`
```
The H1 stays at the top of the file BUT the badge precedes the H1 (Pattern 7 — badge appears at the very top of README.md, before the H1).

From `justfile:131-150` (the `_matrix_cells` block — copy verbatim into the Allocator matrix overview Markdown table; same 18 ordered entries):
- 9 glibc cells: debian-slim/distroless-cc/wolfi × ptmalloc/jemalloc/mimalloc.
- 9 musl cells: alpine/distroless-static/scratch × mallocng/jemalloc/mimalloc.

Phase 3 base images (D-05) — README cites these in the Reproducibility section:
- `alpine:3.20`
- `debian:bookworm-slim`
- `gcr.io/distroless/cc-debian12:nonroot`
- `gcr.io/distroless/static-debian12:nonroot`
- `cgr.dev/chainguard/wolfi-base:latest`
- `scratch`

GHA runner hardware (RESEARCH §Architecture Patterns + RESEARCH §"Common Pitfalls — runner CPU shared"):
- ubuntu-24.04 free tier — 4 vCPU / 16 GB RAM.
- Shared-CPU caveat: "GHA runner CPU is shared; absolute numbers are noisy. The CI matrix proves the pipeline runs and catches regressions in *relative ordering*, not absolute throughput."

CI duration target (CONTEXT.md `<specifics>` ¶1):
- ~60 min p95 wall-clock for 18 cells × 11 scenarios × 6s × 3 runs at full parallelism (smoke recipe).
- Local `just bench-all` is the canonical statistical-quality run (~2.5h, ~5 GB disk per CONTEXT.md D-19 + D-20).

Troubleshooting items (D-16):
- Apple Silicon: `--platform linux/amd64` already in Phase 3 build script — no user action required, just a cite.
- Hyperthreading: `--cpus=4 --cpuset-cpus=0-3` already locked Phase 3 D-15 — cite.
- NUMA: Phase 3 D-16 — `--cpuset-cpus` only, no `numactl --membind`.
- Low memory: `BENCH_MEMORY=8g just run alpine jemalloc` — uses the existing `BENCH_MEMORY` override knob from Phase 3 D-17.

Badge URL (Pattern 7; substitute Cargo.toml repo metadata literal):
- `[![CI](https://github.com/marccarre/rust-benchmark-glibc-musl-mimalloc/actions/workflows/bench.yml/badge.svg)](https://github.com/marccarre/rust-benchmark-glibc-musl-mimalloc/actions/workflows/bench.yml)`

The required README final structure (D-15):
1. CI status badge (above H1)
2. H1 `# rust-benchmark-glibc-musl-mimalloc`
3. 1-line tagline
4. 3-4 sentence hero paragraph (link to `report/index.html` once generated)
5. `## How memory allocation works on Linux` — VERBATIM PRESERVED (Phase 4 AGG-08)
6. `## Run it yourself` — NEW
7. `## Allocator matrix overview` — NEW
8. `## Reproducibility` — NEW
9. `## License` — NEW
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the new README sections (Run it yourself, Allocator matrix overview, Reproducibility, License) and prepend the CI badge — preserving the Phase 4 system diagram verbatim</name>
  <files>
    README.md
  </files>
  <read_first>
    - README.md (full — preserve lines 1-16 verbatim; badge prepended above the existing H1; new sections appended after the existing paragraph at line 16)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-PATTERNS.md §"README.md (doc, modify — additive sections only)" (the canonical final-structure list and the preserve-verbatim block)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-CONTEXT.md `<decisions>` D-15 / D-16 / D-17 / D-18 (the locked content + structure)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-RESEARCH.md §"Pattern 7: GitHub-native CI status badge" (the badge URL with literal owner/repo from Cargo.toml)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-RESEARCH.md §"Pitfall 5: rustc version mismatch" (use 1.91, NOT 1.83)
    - rust-toolchain.toml (cite the channel literal `1.91`)
    - justfile lines 131-150 (the 18-cell matrix — copy as the Allocator matrix overview Markdown table)
    - .planning/research/PITFALLS.md (cite as a link target in the Reproducibility section)
    - LICENSE-MIT + LICENSE-APACHE (cite in the License section as relative paths — both committed in Plan 02)
  </read_first>
  <action>
    Per D-15 / D-16 / D-17 / D-18 / D-22 / RESEARCH §Pattern 7 / RESEARCH §Pitfall 5 / PATTERNS §README.md: edit `README.md` so the final structure matches D-15 exactly. (1) PREPEND the CI badge as the very first line: `[![CI](https://github.com/marccarre/rust-benchmark-glibc-musl-mimalloc/actions/workflows/bench.yml/badge.svg)](https://github.com/marccarre/rust-benchmark-glibc-musl-mimalloc/actions/workflows/bench.yml)` followed by a blank line. Use the LITERAL repo URL — do NOT use a `{owner}/{repo}` placeholder (PATTERNS §Codebase audit fact). (2) PRESERVE the existing H1 + `## How memory allocation works on Linux` + Mermaid `flowchart TD` block + paragraph (lines 1-16 of current README) byte-identically. The existing `tests/smoke.rs:519-531` test asserts `## How memory allocation works on Linux` + `flowchart TD` — this plan must not change those substrings. INSERT a 1-line tagline and a 3-4 sentence hero paragraph between the H1 and the `## How memory allocation works on Linux` heading. The tagline: `> Reproducible Rust allocator benchmarks across glibc/ptmalloc, musl/mallocng, jemalloc, and mimalloc — six libc·environment combinations × eleven workload scenarios.` The hero paragraph: 3-4 sentences mentioning the 4 allocators, 6 envs, 11 scenarios, the dashboard at `report/index.html`, and the GHA matrix (with CI-runs-shape-of-curve / local-runs-statistical-quality split per CONTEXT.md `<specifics>` ¶1). (3) APPEND new sections (after the preserved system-diagram paragraph) in the order: `## Run it yourself`, `## Allocator matrix overview`, `## Reproducibility`, `## License`. SECTION `## Run it yourself` per D-16: 5-step ordered list — (1) Install Docker Desktop (macOS: `brew install --cask docker` or colima) and just (`brew install just`); (2) `git clone https://github.com/marccarre/rust-benchmark-glibc-musl-mimalloc.git && cd rust-benchmark-glibc-musl-mimalloc`; (3) **Smoke run (~10 min)**: `just bench-all-smoke` (CONTEXT.md D-19, RESEARCH §"CI bench duration"). **Full run (~2.5h, ~5 GB disk)**: `just bench-all` (Phase 3 D-20). Document the trade-off explicitly: "smoke proves the loop; full run is the canonical statistical-quality measurement"; (4) `just aggregate`; (5) `open report/index.html` (macOS) / `xdg-open report/index.html` (Linux). Include a `### Troubleshooting` subsection with 4 bullets — Apple Silicon (`--platform linux/amd64` already in Phase 3 build script — no user action required), hyperthreading (`--cpus=4 --cpuset-cpus=0-3` already locked Phase 3 D-15), NUMA (Phase 3 D-16 — `--cpuset-cpus` only), low memory (`BENCH_MEMORY=8g just run alpine jemalloc` — Phase 3 D-17 override knob). SECTION `## Allocator matrix overview` per D-15 ¶5 + RESEARCH §Pattern 1: a Markdown table with columns `| env | alloc | libc | target |` and 18 rows copied from `justfile:131-150` in the same order (glibc family first 9 then musl family 9). Add `target` column values from the same `_matrix_cells` mapping (debian-slim/distroless-cc/wolfi → x86_64-unknown-linux-gnu; alpine/distroless-static/scratch → x86_64-unknown-linux-musl). Recommendation per PATTERNS §README.md: use a Markdown table (more accessible than another Mermaid diagram). SECTION `## Reproducibility` per D-17 / Pitfall 5: 5 bullet items — (a) `rustc 1.91` pinned via `rust-toolchain.toml` (NOT 1.83 from CONTEXT.md text — cite the actual `channel = "1.91"` value); (b) Six pinned Docker base images: `alpine:3.20`, `debian:bookworm-slim`, `gcr.io/distroless/cc-debian12:nonroot`, `gcr.io/distroless/static-debian12:nonroot`, `cgr.dev/chainguard/wolfi-base:latest`, `scratch` (Phase 3 D-05); (c) Build flag: `RUSTFLAGS="-C target-cpu=x86-64-v3"` (Phase 3 D-09); (d) GHA hardware: `ubuntu-24.04` free tier — 4 vCPU / 16 GB RAM; absolute numbers vary across runs (shared-CPU); CI proves *relative ordering* and pipeline correctness, not absolute throughput; (e) Link to `.planning/research/PITFALLS.md` (use the relative path) for the things-that-bias-allocator-benchmarks list. Document the CI duration target (~60 min p95 wall-clock at full parallelism) per CONTEXT.md `<specifics>` ¶1. SECTION `## License` per D-15 ¶7 / D-22: single paragraph — "Dual-licensed under [Apache License 2.0](LICENSE-APACHE) or [MIT License](LICENSE-MIT) at your option. The Cargo.toml SPDX expression is `Apache-2.0 OR MIT`." Do NOT modify the existing tests/smoke.rs:519-531 anchor (`## How memory allocation works on Linux` + `flowchart TD`) — the byte-identical-output gate is locked.
  </action>
  <verify>
    <automated>grep -F "actions/workflows/bench.yml/badge.svg" README.md</automated>
    <automated>grep -F "marccarre/rust-benchmark-glibc-musl-mimalloc" README.md</automated>
    <automated>! grep -F "{owner}/{repo}" README.md</automated>
    <automated>grep -F "## How memory allocation works on Linux" README.md</automated>
    <automated>grep -F "flowchart TD" README.md</automated>
    <automated>grep -F "## Run it yourself" README.md</automated>
    <automated>grep -F "## Allocator matrix overview" README.md</automated>
    <automated>grep -F "## Reproducibility" README.md</automated>
    <automated>grep -F "## License" README.md</automated>
    <automated>grep -F "just bench-all-smoke" README.md</automated>
    <automated>grep -F "just bench-all" README.md</automated>
    <automated>grep -F "just aggregate" README.md</automated>
    <automated>grep -F "1.91" README.md</automated>
    <automated>! grep -F "rustc 1.83" README.md</automated>
    <automated>grep -F "alpine:3.20" README.md</automated>
    <automated>grep -F "target-cpu=x86-64-v3" README.md</automated>
    <automated>grep -F "ubuntu-24.04" README.md</automated>
    <automated>grep -F "PITFALLS.md" README.md</automated>
    <automated>grep -F "LICENSE-APACHE" README.md</automated>
    <automated>grep -F "LICENSE-MIT" README.md</automated>
    <automated>grep -F "Apache-2.0 OR MIT" README.md</automated>
    <automated>grep -F "BENCH_MEMORY=8g" README.md</automated>
    <automated>grep -F "platform linux/amd64" README.md</automated>
    <automated>grep -F "cpuset-cpus" README.md</automated>
    <automated>cargo test -p alloc-bench-aggregator --test smoke -- readme_md_contains_system_diagram</automated>
  </verify>
  <done>
    README.md has all 5 sections in the locked D-15 order. CI badge points at the literal repo URL. Existing system-diagram block + heading is preserved byte-identically (the `readme_md_contains_system_diagram` test passes). All grep gates pass. The 18-cell allocator matrix table is present. The Reproducibility section cites rustc 1.91 (NOT 1.83) and the six Phase 3 base images.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verifies README walkthrough quality (REPR-01 fresh-user reproduction gate)</name>
  <what-built>
    Plan 04 Task 1 just polished `README.md`: prepended the CI status badge, preserved the Phase 4 system diagram verbatim, and appended `## Run it yourself`, `## Allocator matrix overview`, `## Reproducibility`, `## License`. The 5-step recipe documents both `just bench-all-smoke` (~10 min) and `just bench-all` (~2.5h). The Troubleshooting block covers Apple Silicon, hyperthreading, NUMA, and low-memory mimalloc OOM. The matrix overview enumerates all 18 cells. The Reproducibility section cites `rustc 1.91` (the actual codebase pin), the six Phase 3 base images, the `target-cpu=x86-64-v3` flag, the GHA ubuntu-24.04 hardware notes, and links to `.planning/research/PITFALLS.md`. The License section cites both LICENSE files with the `Apache-2.0 OR MIT` SPDX expression.
  </what-built>
  <how-to-verify>
    1. Open `README.md` in a Markdown previewer (e.g., GitHub render, VS Code preview, or `glow`/`mdcat` CLI). Verify visual layout matches the locked D-15 order: badge → H1 → tagline → hero → `## How memory allocation works` (with rendered Mermaid) → `## Run it yourself` → `## Allocator matrix overview` → `## Reproducibility` → `## License`.
    2. Verify the badge image renders or shows "no status" (acceptable per RESEARCH §Pattern 7 + Assumptions Log A3 — silent no-status before first workflow run).
    3. Read the `## Run it yourself` 5-step recipe as if you've never seen this repo before. The recipe MUST be self-contained — no out-of-band knowledge required.
    4. Verify the smoke vs full trade-off is explicit ("smoke ~10 min, full ~2.5h, ~5 GB disk").
    5. Verify the Troubleshooting block addresses every documented pitfall: Apple Silicon, hyperthreading, NUMA, low memory.
    6. Verify the `## Allocator matrix overview` table has 18 rows (count them; should be 9 glibc + 9 musl).
    7. Verify the `## Reproducibility` section cites the EXACT rustc value `1.91` (not `1.83`). Cross-check by running `awk -F'"' '/^channel/{print $2}' rust-toolchain.toml` — should print `1.91`.
    8. Verify the `## License` section links to both `LICENSE-APACHE` and `LICENSE-MIT` files (which Plan 02 committed at repo root).
    9. (Optional dry-run, ~10 min) On a fresh dev box (or a clean git clone), follow the 5-step recipe end-to-end and confirm `report/index.html` opens with Plotly charts. This is the "fresh-user walkthrough" criterion (REPR-01).
    Approve when the walkthrough is self-contained, the 18-cell table is correct, and the Reproducibility numbers match the actual codebase.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any README issues (typos, broken links, missing pitfalls, stale rustc citations, etc.) — Plan 04 Task 1 will be revisited on issues.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md to record Phase 5 closure (4 plans complete)</name>
  <files>
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/STATE.md (full — increment counters; flip current focus)
    - .planning/ROADMAP.md (overall progress for cross-validation)
  </read_first>
  <action>
    Edit `.planning/STATE.md`: bump `progress.completed_plans` by 4 (the four Phase 5 plans). Set `progress.percent` to reflect the new fraction. Update `last_updated` to today (2026-05-19). Update `last_activity` to "Phase 5 complete -- v1.0 milestone closing checkpoint". Update `Current Position` block to reflect Phase 5 as complete; note that Phase 3 still has 2 pending plans (03-04, 03-05) so the milestone is not fully done — flag this in `Blockers/Concerns` ("Phase 3 plans 03-04 and 03-05 outstanding before v1.0 milestone closes; ROADMAP table reflects 3/5 complete"). Update the Performance Metrics table by appending a row `| 05 | 4 | - | - |`. Do NOT delete or rewrite existing decisions or completed-phase entries. The `## Decisions` block should append a single line `- Phase 5: Multi-run statistics computed in aggregator (Bessel-corrected sample stddev, CV>10% high-variance flag); image_size_mb populated via sidecar meta.json (D-13) so v1 input schema stays locked.`. Use Edit, not Write, to preserve the existing file structure.
  </action>
  <verify>
    <automated>grep -F "Phase 5" .planning/STATE.md</automated>
    <automated>grep -F "completed_plans: 16" .planning/STATE.md || grep -F "completed_plans: $((12+4))" .planning/STATE.md || awk '/completed_plans:/ {print $2; exit}' .planning/STATE.md</automated>
    <automated>grep -F "Multi-run statistics" .planning/STATE.md</automated>
    <automated>grep -F "sidecar meta.json" .planning/STATE.md</automated>
    <automated>grep -F "2026-05-19" .planning/STATE.md</automated>
  </verify>
  <done>
    STATE.md reflects Phase 5 closure. The completed_plans counter is incremented by 4. The Phase 5 row in the Performance Metrics table exists. The Decisions block notes the multi-run + sidecar strategy.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update ROADMAP.md Phase 5 entry to mark all 4 plans complete</name>
  <files>
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/ROADMAP.md (full — locate the Phase 5 entry and update the Plans block)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md`: locate the `### Phase 5: CI, Image-Size Gate & Public Polish` block. Update the line containing `**Plans:** 4 plans` to `**Plans:** 4/4 plans complete`. The plan list block (4 lines starting `- [ ] 05-01-PLAN.md` ... `- [ ] 05-04-PLAN.md`) was inserted by the planner during update_roadmap; flip every `[ ]` checkbox to `[x]` for those four lines. Each line MUST keep its existing label suffix verbatim (e.g. `05-01-PLAN.md — Multi-run statistics module + fixtures (REPR-03 math layer)`) — only the checkbox state flips from `[ ]` to `[x]`. In the `## Progress` table at the bottom of ROADMAP.md, locate the row whose first column is `5. CI, Image-Size Gate & Public Polish` and update its cells from `0/TBD | Not started | -` to `4/4 | Complete | 2026-05-19`. Do NOT touch other phases' rows. Do NOT modify the Phase 5 success-criteria block (lines 119-124 of ROADMAP.md before this edit) — those are the locked acceptance gates.
  </action>
  <verify>
    <automated>grep -F "05-01-PLAN.md" .planning/ROADMAP.md</automated>
    <automated>grep -F "05-02-PLAN.md" .planning/ROADMAP.md</automated>
    <automated>grep -F "05-03-PLAN.md" .planning/ROADMAP.md</automated>
    <automated>grep -F "05-04-PLAN.md" .planning/ROADMAP.md</automated>
    <automated>grep -F "4/4" .planning/ROADMAP.md</automated>
    <automated>grep -F "Phase 5: CI, Image-Size Gate & Public Polish" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    ROADMAP.md Phase 5 entry shows 4/4 plans complete with all four plan filenames cited. The Progress table reflects Phase 5 as Complete on 2026-05-19. Phase 5 success-criteria block is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 5: Update CLAUDE.md `## Conventions` section with Phase-5-learned patterns</name>
  <files>
    CLAUDE.md
  </files>
  <read_first>
    - CLAUDE.md (full — extend the `## Conventions` section if it exists, or create concise entries)
  </read_first>
  <action>
    Edit `CLAUDE.md` `## Conventions` section. The current section says "Conventions not yet established. Will populate as patterns emerge during development." Replace with concrete bullets capturing patterns established across Phases 1-5:
    - **Conventional-commit prefixes:** `feat(NN)`, `chore(NN)`, `docs(NN)`, `test(NN)`, `ci(NN)` where NN is the zero-padded phase number.
    - **Aggregator decorate-not-rewrite:** the v1 input schema in `crates/alloc-bench-core/src/output.rs` is locked. New fields ride sidecar files (e.g., `meta/{alloc}-{env}.json` for `image_size_mb` per Phase 5 D-13) and the aggregator merges them at REPORT.md / HTML emit time.
    - **Multi-run statistics convention:** Bessel-corrected sample stddev (n-1 denominator); CV > 10% flags `⚠ high variance` in REPORT.md and HTML legends; CV undefined when mean ≈ 0 (renders as em-dash).
    - **Byte-identical-output discipline:** alphabetical iteration via BTreeMap / BTreeSet; numeric formatting `{:.1}` for throughputs in single-run cells, `{:.0}` for medians in multi-run cells, `{}` for ns latencies; the single timestamp comment at the top of REPORT.md is the only non-stable line.
    - **GHA action pinning:** every action pinned to a specific major version (e.g., `actions/checkout@v4`, `Swatinem/rust-cache@v2`); `dtolnay/rust-toolchain@1.91.0` pinned to a patch version. No `@latest` or `@main` references.
    - **rustc pin source-of-truth:** `rust-toolchain.toml` (`channel = "1.91"`); the workspace `Cargo.toml` `rust-version = "1.83"` is the MSRV (minimum supported), not the build-time pin.
    - **Cross-libc rejection:** mallocng-on-glibc and ptmalloc-on-musl are physically impossible; `justfile build` recipe hard-rejects them; `_matrix_cells` block enumerates only 18 valid (env, alloc) tuples.
    - **CI smoke vs local full:** `just bench-all-smoke` (warmup 1s + duration 5s) is the CI-shape recipe (~60 min p95 wall-clock for 18 cells × 11 scenarios × 3 runs); `just bench-all` (warmup 5s + duration 60s) is the canonical local statistical-quality run (~2.5h).
    - **Suspect run flagging:** runs with `samples_count < 10_000 OR warmup_duration_s < 5.0` flagged with `⚠ suspect` in REPORT.md + HTML legends; this is a read-time decoration, not a render filter.
    Do NOT modify the `## Project` section, the `## Technology Stack` section, the `## GSD Workflow Enforcement` section, or the `## Developer Profile` section. Use Edit (not Write) to preserve all other content.
  </action>
  <verify>
    <automated>grep -F "Conventional-commit prefixes" CLAUDE.md</automated>
    <automated>grep -F "decorate-not-rewrite" CLAUDE.md || grep -F "decorate, not rewrite" CLAUDE.md || grep -F "decorate not rewrite" CLAUDE.md || grep -F "Aggregator decorate" CLAUDE.md</automated>
    <automated>grep -F "Bessel-corrected" CLAUDE.md</automated>
    <automated>grep -F "BTreeMap" CLAUDE.md</automated>
    <automated>grep -F "rust-toolchain.toml" CLAUDE.md</automated>
    <automated>grep -F "Cross-libc rejection" CLAUDE.md</automated>
    <automated>grep -F "bench-all-smoke" CLAUDE.md</automated>
    <automated>grep -F "## Project" CLAUDE.md</automated>
    <automated>grep -F "## Technology Stack" CLAUDE.md</automated>
  </verify>
  <done>
    CLAUDE.md `## Conventions` section captures the 8 Phase-5-learned conventions. Other top-level sections are unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md ↔ public reader | Static markdown content; rendered by GitHub / Markdown viewers; no executable. CI badge URL is a public asset hosted by GitHub Actions. |
| Workflow badge URL ↔ GitHub Actions | Badge SVG fetched from `github.com/{owner}/{repo}/actions/workflows/{file}/badge.svg`; if the workflow file is missing, GitHub returns a "no status" SVG (acceptable, RESEARCH §Pattern 7 + Assumptions A3). |
| .planning/ artifacts ↔ documenter | STATE/ROADMAP/CLAUDE updates are project-controlled; commit-time review is the only gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Information Disclosure | README walkthrough | accept | All commands shown are project-public (no secrets, no env-var leakage). |
| T-05-14 | Tampering | rustc version drift | mitigate | Task 1 verify gate `! grep -F "rustc 1.83" README.md` ensures the stale 1.83 reference (CONTEXT.md D-17 carry-over) does not appear in the published README. |
| T-05-15 | Tampering | system-diagram preservation | mitigate | The `cargo test -p alloc-bench-aggregator --test smoke -- readme_md_contains_system_diagram` gate ensures the Phase 4 contract is byte-stable. |
| T-05-16 | Spoofing | badge URL placeholder leak | mitigate | Task 1 verify gate `! grep -F "{owner}/{repo}" README.md` ensures the placeholder text from CONTEXT.md D-18 is not committed; only the literal `marccarre/rust-benchmark-glibc-musl-mimalloc` is allowed. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Plan 04 adds zero new dependencies. |
</threat_model>

<verification>
- All grep gates in Task 1 pass (badge URL, all five sections present, rustc 1.91 cited NOT 1.83, no `{owner}/{repo}` placeholder, six Phase 3 base images cited, `target-cpu=x86-64-v3` cited, ubuntu-24.04 hardware cited, PITFALLS.md linked, both LICENSE files cited, Apache-2.0 OR MIT SPDX expression present).
- `cargo test -p alloc-bench-aggregator --test smoke -- readme_md_contains_system_diagram` exits 0 (the existing Phase 4 byte-identical test on the system diagram + heading).
- Human verifier approves the walkthrough as self-contained (Task 2 gate).
- STATE.md and ROADMAP.md show Phase 5 closed; CLAUDE.md `## Conventions` section captures the established patterns.
- `prek run --all-files` (project's pre-commit hook framework) passes after edits — particularly `end-of-file-fixer`, `mixed-line-ending`, `check-yaml`, `check-json`, `markdownlint` (if configured).
</verification>

<success_criteria>
- README.md follows D-15 final structure exactly; the existing Phase 4 system-diagram block is byte-identical.
- 5-step Run-it-yourself recipe is complete with smoke + full trade-off + Troubleshooting.
- Allocator matrix overview lists exactly 18 cells in the same order as `justfile:131-150`.
- Reproducibility section cites the actual codebase rustc pin (1.91, not stale 1.83).
- License section cites both LICENSE files with the SPDX expression.
- Human verifier approves the README walkthrough.
- STATE.md / ROADMAP.md / CLAUDE.md reflect Phase 5 closure with all 4 plans marked complete.
</success_criteria>

<output>
Create `.planning/phases/05-ci-image-size-gate-public-polish/05-04-SUMMARY.md` recording: README sections added, rustc pin cited (1.91), six base images cited, badge URL committed (literal owner/repo), human-verify outcome, and any deviation from D-15 / D-16 / D-17 / D-18 / D-22 if encountered. Conventional commit prefix: `docs(05)` for the README + STATE/ROADMAP/CLAUDE updates.

After this plan ships, the v1.0 milestone is in its closing checkpoint state pending Phase 3's two outstanding plans (03-04, 03-05). Call this out in the summary so the user can decide whether to wrap Phase 3 next or transition the milestone-close conversation.
</output>
