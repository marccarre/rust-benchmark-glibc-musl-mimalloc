---
phase: 05-ci-image-size-gate-public-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/bench.yml
  - LICENSE-MIT
  - LICENSE-APACHE
  - justfile
autonomous: true
requirements: [ORCH-04, ORCH-05]
must_haves:
  truths:
    - "User pushes a commit and the `bench` workflow at `.github/workflows/bench.yml` triggers on push, pull_request → main, and workflow_dispatch (D-01)."
    - "The matrix strategy enumerates exactly 18 cells via `strategy.matrix.include:` mirroring the `_matrix_cells` block in `justfile:131-150` (D-02 / RESEARCH §Pattern 1)."
    - "Each matrix-cell job runs `just dive-check {env} {alloc}` after the build and the job fails (non-zero exit) when dive --ci threshold breach is introduced — proving ORCH-05 enforcement (D-08, D-09)."
    - "Each matrix-cell job runs `just ci-bench-cell {env} {alloc}` which executes 3 seeded runs (seeds 1/2/3 per D-04) producing `results/{alloc}-{env}-seed{N}.json` per seed, plus a sidecar `meta/{alloc}-{env}.json` (D-13)."
    - "Each matrix-cell job uploads `results-{alloc}-{env}` artifact via `actions/upload-artifact@v4` with `if-no-files-found: error` and `retention-days: 90` (D-05 / RESEARCH §Pattern 2)."
    - "Concurrency group `bench-${{ github.workflow }}-${{ github.ref }}` cancels in-progress runs on non-default branches but never on `main` (D-06 / RESEARCH §Pattern 6)."
    - "GHA workflow uses Swatinem/rust-cache@v2 with `shared-key: bench-${{ matrix.alloc }}-${{ matrix.env }}` for cargo registry / target caching, plus actions/cache@v4 keyed on `hashFiles('**/Cargo.lock')` (D-07 cargo-registry layer per RESEARCH §Pattern 3 + RESEARCH §Pitfall 6) and Docker BuildKit `cache-to: type=gha,mode=max,scope=${{ matrix.alloc }}-${{ matrix.env }}` for image-layer caching (D-07 BuildKit layer)."
    - "Within a matrix-cell job, the 11 scenarios run sequentially via `just ci-bench-cell` -> `just bench-all-smoke` semantics (D-03 — parallel scenarios are forbidden per Phase 3 D-11 spirit; sharing the kernel page cache would multiplex allocators)."
    - "D-19: CI per-cell wall-clock target ~60 min p95 across 18 cells × 11 scenarios × 3 seeds at `bench-all-smoke` recipe rates (warmup 1s + duration 5s per scenario per seed). Full statistical-quality runs continue to live in `just bench-all` for local dev — CI proves pipeline correctness and relative-ordering stability, not absolute numbers."
    - "GHA workflow pins `dtolnay/rust-toolchain@1.91.0` (NOT 1.83.0 — Pitfall 5; the codebase pins 1.91 in rust-toolchain.toml + all six Dockerfiles + justfile)."
    - "GHA workflow build-args include `RUST_VERSION=1.91` (matches justfile:79 + all six Dockerfiles)."
    - "GHA workflow does NOT use `target-cpu=native`; it uses `RUSTFLAGS=\"-C target-cpu=x86-64-v3\"` per Phase 3 D-09."
    - "GHA workflow does NOT use `actions/upload-artifact@v3` (deprecated; v4 required for matrix unique-name-per-job model)."
    - "User can run `just ci-bench-cell debian-slim ptmalloc` locally and get identical outputs to a CI run — the recipe owns the docker invocation and meta.json capture (RESEARCH §Open Questions ¶3 recommendation)."
    - "User can run `just ci-validate` locally and the recipe runs `cargo fmt --all --check` + `cargo clippy --workspace --all-targets -- -D warnings` + `just dce-check system` — mirrors Phase 5 pre-bench job."
    - "Repo root has `LICENSE-MIT` containing the canonical SPDX MIT text (RESEARCH §LICENSE-MIT canonical text) with `Copyright (c) 2026 Marc Carré` (matches Cargo.toml authors)."
    - "Repo root has `LICENSE-APACHE` containing the canonical Apache 2.0 plain-text body from apache.org/licenses/LICENSE-2.0.txt with the official APPENDIX boilerplate appended (D-22 / RESEARCH §LICENSE-APACHE canonical text)."
  artifacts:
    - path: ".github/workflows/bench.yml"
      provides: "GHA matrix workflow with pre-bench, bench-matrix (18 cells), and aggregate jobs"
      contains: "actions/checkout@v4"
    - path: "LICENSE-MIT"
      provides: "Canonical MIT License text (SPDX-detectable)"
      contains: "MIT License"
    - path: "LICENSE-APACHE"
      provides: "Canonical Apache 2.0 text + APPENDIX (SPDX-detectable)"
      contains: "Apache License"
    - path: "justfile"
      provides: "Three new CI recipes — `ci-bench-cell {env} {alloc}`, `ci-validate`, and the dependency hook for `ci-aggregate` (Plan 03 fills the body)"
      contains: "ci-bench-cell"
  key_links:
    - from: ".github/workflows/bench.yml"
      to: "justfile recipes ci-bench-cell + ci-validate"
      via: "run: just ci-bench-cell + run: just ci-validate"
      pattern: "just ci-bench-cell"
    - from: ".github/workflows/bench.yml"
      to: ".dive-ci config (Phase 3)"
      via: "just dive-check {env} {alloc} (which reads .dive-ci)"
      pattern: "just dive-check"
    - from: ".github/workflows/bench.yml"
      to: "extractions/setup-just@v2"
      via: "uses step in every job"
      pattern: "extractions/setup-just@v2"
    - from: "justfile ci-bench-cell"
      to: "docker inspect → meta/{alloc}-{env}.json"
      via: "shell capture + jq -n"
      pattern: "docker image inspect"
---

<objective>
Ship the GitHub Actions matrix workflow (`.github/workflows/bench.yml`), the dual-license `LICENSE-MIT` + `LICENSE-APACHE` files, and the new justfile recipes `ci-bench-cell` + `ci-validate` (plus a stub `ci-aggregate` whose body Plan 03 fills out). After this plan lands, a developer can push to a branch and observe a green 18-cell GHA matrix run, and `dive --ci` enforcement provably fails the build when thresholds are breached. The aggregate job in the workflow calls `just ci-aggregate`, which in Wave 1 is a stub recipe that simply errors out with "Plan 03 implements ci-aggregate" — so Wave 2 (Plan 03) finishes the loop without touching this YAML.

Vertical slice: after this plan, `gh workflow run bench.yml` succeeds end-to-end on the bench-matrix portion (18 cells build + dive-check + 3-seed run + artifact upload). The `aggregate` job will fail until Plan 03 lands `ci-aggregate`, but the matrix portion proves ORCH-04 + ORCH-05 + REPR-03's input-side already.

Purpose: Close ORCH-04 (CI runs matrix on push and uploads artifacts) and ORCH-05 (dive --ci fails on threshold breach) plus seed the input data REPR-03 needs (3 seeded runs per cell + meta.json sidecar).

Output: New YAML at `.github/workflows/bench.yml`, two LICENSE files at repo root, and three new recipes appended to `justfile`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ci-image-size-gate-public-polish/05-CONTEXT.md
@.planning/phases/05-ci-image-size-gate-public-polish/05-RESEARCH.md
@.planning/phases/05-ci-image-size-gate-public-polish/05-PATTERNS.md
@.planning/phases/03-docker-matrix-local-orchestration/03-CONTEXT.md

@justfile
@.dive-ci
@Cargo.toml
@rust-toolchain.toml

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase + RESEARCH.md. -->

From `justfile:43-84` (the `build` recipe — the analog for ci-bench-cell's build invocation):
- Existing `build env alloc` recipe maps env → target triple, computes OCI annotations, calls `docker buildx build --platform linux/amd64 -f docker/{{env}}.Dockerfile --build-arg ALLOC --build-arg TARGET --build-arg RUST_VERSION=1.91 --build-arg OCI_VERSION --build-arg OCI_REVISION --build-arg OCI_CREATED --tag alloc-bench:{{alloc}}-{{env}} --load .`
- The cross-libc rejection pattern (lines 47-56) is already enforced — `ci-bench-cell` reuses `just build` so it inherits the validation.

From `justfile:94-107` (the `run` recipe — the analog for the seeded-run loop):
- `docker run --rm --platform linux/amd64 --cpus=4 --memory=4g --cpuset-cpus=0-3 -v $(pwd)/results:/out alloc-bench:{{alloc}}-{{env}} run-all --output /out/{{alloc}}-{{env}}.json --seed 7`
- `ci-bench-cell` REPLACES `--seed 7` with a 3-iteration loop using seeds 1/2/3, and the output filename pattern is `{{alloc}}-{{env}}-seed${seed}.json` (CONTEXT.md `<specifics>` ¶2 + ¶3).

From `justfile:131-150` (the `_matrix_cells` block — copy this list verbatim into the GHA matrix include block; the order is glibc-family first then musl-family for BuildKit cache locality):
```
debian-slim ptmalloc / debian-slim jemalloc / debian-slim mimalloc /
distroless-cc ptmalloc / distroless-cc jemalloc / distroless-cc mimalloc /
wolfi ptmalloc / wolfi jemalloc / wolfi mimalloc /
alpine mallocng / alpine jemalloc / alpine mimalloc /
distroless-static mallocng / distroless-static jemalloc / distroless-static mimalloc /
scratch mallocng / scratch jemalloc / scratch mimalloc
```

From `justfile:121` (the just-template double-escape pattern for `docker --format`):
- `docker images --filter "reference=alloc-bench:*" --format '{{ "{{" }}.Repository{{ "}}" }}:{{ "{{" }}.Tag{{ "}}" }}'`
- The `{{ "{{" }}` is just-interpolation emitting a literal `{{` to the shell. `ci-bench-cell` uses the same pattern for `docker image inspect --format '{{ "{{" }}.Size{{ "}}" }}'`.

From `justfile:232-244` (the `dive-check` recipe — already wired with dockerized fallback; ci-bench-cell calls `just dive-check {{env}} {{alloc}}` rather than re-implementing).

From `Cargo.toml:5-10` (workspace package metadata — the badge URL + license substitution data):
- `license = "MIT OR Apache-2.0"`
- `repository = "https://github.com/marccarre/rust-benchmark-glibc-musl-mimalloc"`
- `authors = ["Marc Carré"]`
- The badge URL therefore is: `https://github.com/marccarre/rust-benchmark-glibc-musl-mimalloc/actions/workflows/bench.yml/badge.svg` — Plan 04 (README) uses this literal; this plan does not need it but writes a comment in bench.yml documenting that Plan 04 will reference it.

From `rust-toolchain.toml:2`:
- `channel = "1.91"` — RUST_VERSION pin source-of-truth.

From `.dive-ci:11-14` (already exists per Phase 3 D-21):
- `rules: lowestEfficiency: 0.95 / highestUserWastedPercent: 0.05 / highestWastedBytes: 50MB`.
- `bench.yml` does NOT modify `.dive-ci` — it consumes via `just dive-check`.

GHA action versions (RESEARCH §Standard Stack — verified live):
- `actions/checkout@v4`
- `actions/upload-artifact@v4`
- `actions/download-artifact@v4`
- `Swatinem/rust-cache@v2`
- `docker/setup-buildx-action@v3`
- `docker/build-push-action@v6`
- `dtolnay/rust-toolchain@1.91.0`
- `extractions/setup-just@v2`

Workflow skeleton — the verbatim 160-line skeleton lives in RESEARCH §"Code Examples — Full bench.yml skeleton" (lines 766-925 of 05-RESEARCH.md). Copy structurally; the 18-cell include block must be expanded fully (not elided) since RESEARCH shows ellipses.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LICENSE-MIT and LICENSE-APACHE at repo root</name>
  <files>
    LICENSE-MIT
    LICENSE-APACHE
  </files>
  <read_first>
    - .planning/phases/05-ci-image-size-gate-public-polish/05-RESEARCH.md §"Code Examples — LICENSE-MIT canonical text" (the exact MIT text to commit; substitute year 2026 + holder Marc Carré)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-RESEARCH.md §"Code Examples — LICENSE-APACHE canonical text" (instructs to fetch the unmodified ~11 KB Apache 2.0 text from apache.org/licenses/LICENSE-2.0.txt and append the Apache APPENDIX with year 2026 + holder Marc Carré)
    - Cargo.toml lines 8-10 (`license = "MIT OR Apache-2.0"`, `authors = ["Marc Carré"]`) — verifies the SPDX expression is already declared and the holder name is canonical.
    - .planning/phases/05-ci-image-size-gate-public-polish/05-PATTERNS.md §"LICENSE-MIT" + §"LICENSE-APACHE" (greenfield rule — verbatim canonical text only; SPDX-detection requires byte-exact match).
  </read_first>
  <action>
    Per D-22 / RESEARCH §LICENSE-MIT canonical text: write `LICENSE-MIT` with the canonical SPDX MIT body verbatim from RESEARCH §"Code Examples — LICENSE-MIT canonical text" (lines 974-996 of 05-RESEARCH.md). Substitute `Copyright (c) 2026 Marc Carré` (matches Cargo.toml authors literal). Do NOT paraphrase, do NOT reformat line wraps — SPDX-detection (GitHub Linguist, cargo metadata, pkg.go.dev) requires byte-exact match. Per D-22 / RESEARCH §LICENSE-APACHE canonical text: fetch the canonical Apache 2.0 plain-text body from `https://www.apache.org/licenses/LICENSE-2.0.txt` and write it verbatim as the prefix of `LICENSE-APACHE`. Append a single APPENDIX block with the official Apache "How to apply the Apache License to your work" boilerplate, substituting `Copyright 2026 Marc Carré`. The exact APPENDIX text is at RESEARCH lines 1011-1027 (`05-RESEARCH.md`). Both files MUST end with a trailing newline (prek pre-commit `end-of-file-fixer` will reject otherwise). Do NOT modify `Cargo.toml` — the SPDX expression `license = "MIT OR Apache-2.0"` is already declared at line 8.
  </action>
  <verify>
    <automated>test -f LICENSE-MIT && test -f LICENSE-APACHE</automated>
    <automated>grep -F "MIT License" LICENSE-MIT</automated>
    <automated>grep -F "Copyright (c) 2026 Marc Carr" LICENSE-MIT</automated>
    <automated>grep -F "Apache License" LICENSE-APACHE</automated>
    <automated>grep -F "Version 2.0, January 2004" LICENSE-APACHE</automated>
    <automated>grep -F "APPENDIX" LICENSE-APACHE</automated>
    <automated>grep -F "Copyright 2026 Marc Carr" LICENSE-APACHE</automated>
    <automated>tail -c 1 LICENSE-MIT | od -c | grep -F '\n'</automated>
    <automated>tail -c 1 LICENSE-APACHE | od -c | grep -F '\n'</automated>
  </verify>
  <done>
    Both license files exist at repo root with the canonical (byte-exact) SPDX text. The 2026 year + Marc Carré holder are present. Both files end with a newline. Existing Cargo.toml SPDX expression is unmodified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append `ci-bench-cell`, `ci-validate`, and stub `ci-aggregate` recipes to justfile</name>
  <files>
    justfile
  </files>
  <read_first>
    - justfile (full file — extend the existing recipe set; do NOT rewrite or reorder existing recipes; new recipes go at the end of the file under a `# Phase 5: CI recipes` section header)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-PATTERNS.md §"justfile" (canonical body for ci-bench-cell, ci-aggregate-stub, ci-validate)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-RESEARCH.md §"Pattern 4: Per-cell `meta.json` sidecar + aggregator merge" (the exact `docker image inspect --format '{{ "{{" }}.Size{{ "}}" }}'` + `awk` + `jq -n` shape)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-CONTEXT.md `<specifics>` (seed 1/2/3 convention, results filename pattern `{alloc}-{env}-seed{N}.json`, sidecar shape)
  </read_first>
  <action>
    Per D-13 / D-19 / D-22 / RESEARCH §Pattern 4 / PATTERNS §justfile: append three new recipes to `justfile` under a new section header `# ──── Phase 5: CI recipes (D-13, D-19, RESEARCH §Pattern 4) ────`. (a) `ci-bench-cell env alloc` — uses `#!/usr/bin/env bash` + `set -euo pipefail`; calls `just build {{env}} {{alloc}}` then `just dive-check {{env}} {{alloc}}`; pre-creates `results/` and `meta/` directories with `mkdir -p` + `chmod 0777 results` (mirrors the `run` recipe at lines 97-98 — needed for distroless `:nonroot` UID 65532 to write); captures `SIZE_BYTES=$(docker image inspect alloc-bench:{{alloc}}-{{env}} --format '{{ "{{" }}.Size{{ "}}" }}')`; computes `SIZE_MB=$(awk "BEGIN { printf \"%.2f\", $SIZE_BYTES / 1024 / 1024 }")`; writes `meta/{{alloc}}-{{env}}.json` via `jq -n --argjson b "$SIZE_BYTES" --argjson m "$SIZE_MB" '{alloc: "{{alloc}}", env: "{{env}}", image_size_bytes: $b, image_size_mb: $m, captured_at: now | todate}'`; runs a `for seed in 1 2 3; do docker run --rm --platform linux/amd64 --cpus=4 --memory=4g --cpuset-cpus=0-3 -v "$(pwd)/results:/out" alloc-bench:{{alloc}}-{{env}} run-all --output /out/{{alloc}}-{{env}}-seed${seed}.json --seed ${seed}; done` loop. The cgroup/cpuset values are Phase 3 D-15 locked invariants (4 vCPUs, 4 GiB memory, cpuset 0-3). Document the recipe with the same 5-line doc block style used by `bench-cell` (line 109) — cite D-13, D-19, RESEARCH §Pattern 4. (b) `ci-validate` — three-line recipe (no shebang shell needed; just standalone command lines): `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `just dce-check system`. Document as "CI sanity gate — fmt + clippy + dce-check; mirrors the prek hook so CI catches the same regressions". (c) `ci-aggregate` STUB — single-line body that prints to stderr and exits non-zero: `@echo "ci-aggregate: implemented by Plan 03" >&2 && exit 1`. The stub exists so Plan 02's bench.yml workflow has a recipe to call; Plan 03 replaces the body with the real aggregator invocation. Document with `# STUB — Plan 03 implements the body. Phase 5 D-13 / RESEARCH §Pattern 4 (the meta.json merge step uses --meta flag added by Plan 03's main.rs extension).`. Do NOT touch any existing recipe. Do NOT add a new `just check-matrix`-style verification recipe in this plan — Plan 03 / Plan 04 can extend if needed. The just-template double-escape `{{ "{{" }}` for emitting literal `{{` to docker's `--format` flag is documented in `justfile:121` (`clean-images` recipe) — same pattern.
  </action>
  <verify>
    <automated>just --list 2>&1 | grep -F "ci-bench-cell"</automated>
    <automated>just --list 2>&1 | grep -F "ci-validate"</automated>
    <automated>just --list 2>&1 | grep -F "ci-aggregate"</automated>
    <automated>grep -F "for seed in 1 2 3" justfile</automated>
    <automated>grep -F "docker image inspect" justfile</automated>
    <automated>grep -F "jq -n --argjson" justfile</automated>
    <automated>grep -F "cargo fmt --all --check" justfile</automated>
    <automated>grep -F "cargo clippy --workspace --all-targets -- -D warnings" justfile</automated>
    <automated>grep -F "just dce-check system" justfile</automated>
    <automated>grep -F "exit 1" justfile</automated>
    <automated>just ci-validate 2>&1</automated>
    <automated>just ci-aggregate 2>&1 | grep -F "Plan 03"</automated>
  </verify>
  <done>
    `just --list` shows the three new recipes. `just ci-validate` succeeds (fmt + clippy + dce-check pass on a clean tree — pre-existing Phase 3 / Phase 4 gates already green). `just ci-aggregate` exits non-zero with the "Plan 03 implements" stderr message. `ci-bench-cell` recipe body uses the locked D-13 + D-15 + Pattern-4 shape (3-seed loop, docker inspect → meta.json sidecar, cgroup/cpuset/memory invariants).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create `.github/workflows/bench.yml` with pre-bench + 18-cell bench-matrix + aggregate jobs</name>
  <files>
    .github/workflows/bench.yml
  </files>
  <read_first>
    - .planning/phases/05-ci-image-size-gate-public-polish/05-RESEARCH.md §"Code Examples — Full bench.yml skeleton" (the verbatim 160-line workflow skeleton at lines 766-925; expand the "17 more entries elided" comment into the full 18-cell list)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-RESEARCH.md §"Pattern 1: 18-cell explicit matrix via `include:`-only" (the exact 18 include entries with libc + target fields)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-RESEARCH.md §"Pattern 2: Per-cell artifact upload + final glob download" (upload + download steps with pattern: results-* + merge-multiple: true)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-RESEARCH.md §"Pattern 3: GHA Docker BuildKit cache, scoped per cell" (cache-to mode=max + scope per cell)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-RESEARCH.md §"Pattern 6: Concurrency group with main protected" (boolean expression on cancel-in-progress)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-RESEARCH.md §"Pitfall 3: 6-hour GHA per-job timeout" (timeout-minutes: 30 on bench-matrix; 15 on pre-bench + aggregate)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-RESEARCH.md §"Pitfall 5: rustc version mismatch" (use 1.91, NOT 1.83)
    - justfile:131-150 (the `_matrix_cells` block — the GHA include MUST mirror this exactly)
    - .planning/phases/05-ci-image-size-gate-public-polish/05-CONTEXT.md `<deferred>` (confirm out-of-scope: GitHub Pages, multi-platform CI, aarch64, S3 archival)
  </read_first>
  <action>
    Per D-01..D-08 / D-13 / D-19 / D-20 / RESEARCH §"Code Examples — Full bench.yml skeleton": create `.github/workflows/bench.yml` with three jobs (pre-bench, bench-matrix, aggregate). Top-level `name: bench`. Triggers (D-01): `on: push: branches: ['**']`, `pull_request: branches: [main]`, `workflow_dispatch:`. Concurrency block (D-06 / RESEARCH §Pattern 6): `group: bench-${{ github.workflow }}-${{ github.ref }}`, `cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}`. Top-level `permissions: contents: read, actions: write` (RESEARCH §Open Questions ¶2 — explicit belt-and-suspenders for artifact API). Top-level `env`: `CARGO_TERM_COLOR: always`, `RUSTFLAGS: "-C target-cpu=x86-64-v3"` (Phase 3 D-09 locked). Job 1 `pre-bench` (RESEARCH §"Code Examples" lines 791-808): `runs-on: ubuntu-24.04`, `timeout-minutes: 15`. Steps: `actions/checkout@v4`, `dtolnay/rust-toolchain@1.91.0` with `components: rustfmt, clippy`, `Swatinem/rust-cache@v2` with `shared-key: pre-bench`, `extractions/setup-just@v2`, `run: just ci-validate`. Job 2 `bench-matrix` (RESEARCH §"Code Examples" lines 810-885): `name: Bench ${{ matrix.alloc }}-${{ matrix.env }}`, `needs: pre-bench`, `runs-on: ubuntu-24.04`, `timeout-minutes: 30` (Pitfall 3). `strategy: fail-fast: false, matrix: include:` enumerates exactly 18 cells in the order from `justfile:131-150` glibc-first then musl-first. Each include entry has fields `env`, `alloc`, `libc`, `target` per RESEARCH §Pattern 1 (e.g. `{ env: debian-slim, alloc: ptmalloc, libc: glibc, target: x86_64-unknown-linux-gnu }`). Steps: `actions/checkout@v4`, `docker/setup-buildx-action@v3`, `extractions/setup-just@v2`, `actions/cache@v4` for `~/.cargo/registry`, `~/.cargo/git`, `target/` keyed on `${{ matrix.alloc }}-${{ matrix.env }}-${{ hashFiles('**/Cargo.lock') }}` (Pitfall 6). Then a `Build image` step using `docker/build-push-action@v6` with `context: .`, `file: docker/${{ matrix.env }}.Dockerfile`, `platforms: linux/amd64`, `load: true`, `push: false`, `tags: alloc-bench:${{ matrix.alloc }}-${{ matrix.env }}`, `build-args` block listing `ALLOC=${{ matrix.alloc }}`, `TARGET=${{ matrix.target }}`, `RUST_VERSION=1.91` (NOT 1.83 — Pitfall 5), `OCI_REVISION=${{ github.sha }}`, `cache-from: type=gha,scope=${{ matrix.alloc }}-${{ matrix.env }}`, `cache-to: type=gha,mode=max,scope=${{ matrix.alloc }}-${{ matrix.env }}`. Then a `Dive image-size gate` step with `run: just dive-check ${{ matrix.env }} ${{ matrix.alloc }}` (this is the ORCH-05 enforcement — non-zero exit fails the job). Then `Run bench (3 seeds + meta capture)` step: `run: just ci-bench-cell ${{ matrix.env }} ${{ matrix.alloc }}` (the recipe from Task 2 owns the docker invocation + the meta.json capture). Then `Upload per-cell artifact` step using `actions/upload-artifact@v4`: `name: results-${{ matrix.alloc }}-${{ matrix.env }}`, `path: |` block listing `results/${{ matrix.alloc }}-${{ matrix.env }}-seed*.json` and `meta/${{ matrix.alloc }}-${{ matrix.env }}.json`, `if-no-files-found: error`, `retention-days: 90`. Job 3 `aggregate` (RESEARCH §"Code Examples" lines 887-923 + §Open Questions ¶1 `if: always()`): `name: Aggregate report`, `needs: bench-matrix`, `runs-on: ubuntu-24.04`, `timeout-minutes: 15`, `if: always()` (so partial reports still emit when one cell fails). Steps: `actions/checkout@v4`, `dtolnay/rust-toolchain@1.91.0`, `Swatinem/rust-cache@v2` with `shared-key: aggregate`, `extractions/setup-just@v2`, `actions/download-artifact@v4` with `path: ./artifacts`, `pattern: results-*`, `merge-multiple: true`, then a `Reorganize artifacts` step that splits the merged dir into `results/` (the `*-seed*.json` files) and `meta/` (the meta sidecars), then `run: just ci-aggregate` (Plan 03 implements the body), then `actions/upload-artifact@v4` for the final report: `name: bench-report-${{ github.run_id }}`, `path: report/`, `retention-days: 90`. Add a top-of-file YAML comment block citing D-01..D-08, D-13, D-19, and the RESEARCH section indices used. Do NOT use `actions/upload-artifact@v3` (deprecated). Do NOT use `target-cpu=native` anywhere (Phase 3 D-09). Do NOT add steps for GitHub Pages publish, S3 archival, or aarch64 runners (out-of-scope per CONTEXT.md `<deferred>`).
  </action>
  <verify>
    <automated>test -f .github/workflows/bench.yml</automated>
    <automated>command -v actionlint >/dev/null 2>&1 && actionlint .github/workflows/bench.yml || command -v docker >/dev/null 2>&1 && docker run --rm -v "$(pwd):/repo" rhysd/actionlint:latest -color /repo/.github/workflows/bench.yml || python3 -c "import yaml; yaml.safe_load(open('.github/workflows/bench.yml'))"</automated>
    <automated>grep -F "actions/checkout@v4" .github/workflows/bench.yml</automated>
    <automated>grep -F "actions/upload-artifact@v4" .github/workflows/bench.yml</automated>
    <automated>grep -F "actions/download-artifact@v4" .github/workflows/bench.yml</automated>
    <automated>grep -F "Swatinem/rust-cache@v2" .github/workflows/bench.yml</automated>
    <automated>grep -F "docker/setup-buildx-action@v3" .github/workflows/bench.yml</automated>
    <automated>grep -F "docker/build-push-action@v6" .github/workflows/bench.yml</automated>
    <automated>grep -F "dtolnay/rust-toolchain@1.91.0" .github/workflows/bench.yml</automated>
    <automated>grep -F "extractions/setup-just@v2" .github/workflows/bench.yml</automated>
    <automated>grep -F "RUST_VERSION=1.91" .github/workflows/bench.yml</automated>
    <automated>! grep -F "RUST_VERSION=1.83" .github/workflows/bench.yml</automated>
    <automated>! grep -F "target-cpu=native" .github/workflows/bench.yml</automated>
    <automated>! grep -F "actions/upload-artifact@v3" .github/workflows/bench.yml</automated>
    <automated>grep -F "just dive-check" .github/workflows/bench.yml</automated>
    <automated>grep -F "just ci-bench-cell" .github/workflows/bench.yml</automated>
    <automated>grep -F "just ci-aggregate" .github/workflows/bench.yml</automated>
    <automated>grep -F "just ci-validate" .github/workflows/bench.yml</automated>
    <automated>grep -F "if: always()" .github/workflows/bench.yml</automated>
    <automated>grep -F "fail-fast: false" .github/workflows/bench.yml</automated>
    <automated>grep -F "merge-multiple: true" .github/workflows/bench.yml</automated>
    <automated>grep -F "pattern: results-*" .github/workflows/bench.yml</automated>
    <automated>grep -F "if-no-files-found: error" .github/workflows/bench.yml</automated>
    <automated>grep -F "timeout-minutes: 30" .github/workflows/bench.yml</automated>
    <automated>grep -F "cancel-in-progress" .github/workflows/bench.yml</automated>
    <automated>grep -F "refs/heads/main" .github/workflows/bench.yml</automated>
    <automated>grep -cE "alloc:" .github/workflows/bench.yml | awk '{ if ($1 < 18) { print "expected ≥18 alloc: matrix entries"; exit 1 } }'</automated>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/bench.yml')); inc=d['jobs']['bench-matrix']['strategy']['matrix']['include']; print('cells:', len(inc)); sys.exit(0 if len(inc)==18 else 1)"</automated>
  </verify>
  <done>
    `.github/workflows/bench.yml` exists, parses as valid YAML, has exactly 18 matrix include entries (counted by Python `yaml.safe_load`), uses `dtolnay/rust-toolchain@1.91.0` (NOT 1.83), uses upload-artifact@v4 (NOT v3), references `just ci-bench-cell` + `just ci-validate` + `just ci-aggregate` + `just dive-check`, has `fail-fast: false`, uses BuildKit cache scoped per cell, has the `if: always()` aggregate guard, and contains explicit timeout-minutes guards. Concurrency block protects main from cancellation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner ↔ Docker daemon | The runner builds images and runs them; arbitrary code in `docker/*.Dockerfile` runs as root in the build sandbox. Phase 3 already vets the Dockerfiles. |
| GitHub Actions cache ↔ buildx layer cache | `cache-to: type=gha,mode=max` writes to GitHub's Actions cache; per-cell `scope` prevents cross-cell tampering. |
| Public repo ↔ external action consumers | Workflow runs on `push` from any branch; the `permissions` block restricts the GITHUB_TOKEN to `contents: read, actions: write`. |
| `.dive-ci` config ↔ dive --ci | The threshold file is a project-controlled YAML; dive reads it and exits non-zero on breach (this is the gate). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | bench.yml workflow file | mitigate | Workflow file checked into git; PR review required for changes to default-branch workflow. `actionlint` (or dockerized fallback) gates syntax in CI. |
| T-05-05 | Information Disclosure | GHA artifact contents | accept | Artifacts contain only synthetic benchmark numbers + image-size metadata. Retention 90 days (default; not extended). No secrets ever land in artifacts. |
| T-05-06 | Denial of Service | Wedged matrix-cell job | mitigate | `timeout-minutes: 30` on bench-matrix (RESEARCH §Pitfall 3) caps runtime to 30 min vs. GHA 6 h default. `timeout-minutes: 15` on pre-bench + aggregate. |
| T-05-07 | Elevation of Privilege | Pinned action versions | mitigate | Every action is pinned to a specific major version (`@v4`, `@v3`, `@v6`, `@v2`); no `@latest` or `@main` references. `dtolnay/rust-toolchain@1.91.0` is pinned to a patch version. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds zero new Cargo crates and zero new npm/pip packages. The GHA actions used (Swatinem/rust-cache, docker/setup-buildx-action, dtolnay/rust-toolchain, extractions/setup-just, actions/checkout, actions/upload-artifact, actions/download-artifact, docker/build-push-action) are all in RESEARCH §Standard Stack (verified live against github.com listings). No `[ASSUMED]` or `[SUS]` packages — no human verification gate required. |
| T-05-08 | Repudiation | dive --ci threshold breach not enforced | mitigate | The dive-check step calls `just dive-check` which exits non-zero on threshold breach; the matrix-cell job aborts and uploads no artifact (the upload-artifact step has `if-no-files-found: error` so if the run aborts before producing files, the upload itself fails loudly). |
</threat_model>

<verification>
- `actionlint .github/workflows/bench.yml` exits 0 (or `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/bench.yml'))"` exits 0 if actionlint not installed locally — CI itself will catch syntax errors on first push).
- `python3` parses the YAML and confirms exactly 18 entries in `jobs['bench-matrix']['strategy']['matrix']['include']`.
- All grep gates in Task 3 pass (action versions correct, RUST_VERSION=1.91, no v3 artifact action, no target-cpu=native, all three `just ci-*` references present).
- `just ci-validate` succeeds locally on a clean tree (proves fmt + clippy + dce-check are reachable from the workflow definition).
- `just ci-aggregate` exits non-zero with the documented stub message (proves Plan 03 has a clear hand-off point).
- LICENSE files contain the canonical SPDX text and the literal "Marc Carré" + 2026 substitutions.
- The repo's existing pre-commit hooks (`prek run --all-files`) pass against the new files (end-of-file-fixer, check-yaml, mixed-line-ending).
</verification>

<success_criteria>
- `.github/workflows/bench.yml` parses as YAML and has 18 matrix cells.
- Workflow references the correct action versions (v4 / v3 / v6 / v2 / 1.91.0).
- LICENSE-MIT and LICENSE-APACHE exist at repo root with byte-exact canonical SPDX text.
- `just ci-bench-cell debian-slim ptmalloc` is callable (the recipe exists; running it requires Docker + a valid build).
- `just ci-validate` passes on the current tree.
- `just ci-aggregate` exits non-zero with the documented stub error (Plan 03 replaces).
- The workflow's `if: always()` aggregate job ensures partial reports still emit.
- Concurrency block protects main; cancels in-progress on non-default branches.
</success_criteria>

<output>
Create `.planning/phases/05-ci-image-size-gate-public-polish/05-02-SUMMARY.md` recording: workflow file path + 18-cell list, three new justfile recipes (with `ci-aggregate` flagged as STUB awaiting Plan 03), LICENSE files committed at repo root, and any deviation from RESEARCH §Pattern 1-3-6 if encountered. Conventional commit prefix: `ci(05)` for the workflow, `chore(05)` for the LICENSE files, `feat(05)` for the justfile recipes.
</output>
