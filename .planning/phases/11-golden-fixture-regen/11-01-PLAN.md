---
phase: 11-golden-fixture-regen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/phases/11-golden-fixture-regen/11-01-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [TEST-01, TEST-02]
tags: [docs, release-gate, golden-fixture, byte-identical, conventions]

must_haves:
  truths:
    - "User runs `cargo test -p alloc-bench-aggregator` and the command exits 0 with 138 lib + 31 integration tests passing against committed fixtures (TEST-01)"
    - "User reads CLAUDE.md §Conventions block and finds the Standalone golden-fixture-regen PR bullet immediately after the Suspect run flagging bullet (TEST-02)"
    - "User reads .planning/phases/11-golden-fixture-regen/11-01-SUMMARY.md and finds byte counts for report/REPORT.md and report/index.html captured via wc -c, plus the literal `just aggregate` invocation, plus the cargo test pass count"
    - "User reads .planning/REQUIREMENTS.md and finds TEST-01 and TEST-02 marked `[x]` in the Test & Golden-fixture Discipline section, with Coverage Status flipped from Pending to Complete in the Traceability table"
    - "User reads .planning/STATE.md ## Accumulated Context > Decisions block and finds the appended bullet `[Phase 11]: v1.1 release gate held: byte-identical contract preserved across Phases 6-10`"
  artifacts:
    - path: ".planning/phases/11-golden-fixture-regen/11-01-SUMMARY.md"
      provides: "Release-gate report with byte counts, just aggregate invocation, cargo test pass count"
      contains: "report/REPORT.md"
    - path: "CLAUDE.md"
      provides: "Standalone-PR convention bullet in §Conventions block"
      contains: "Standalone golden-fixture-regen PR:"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TEST-01 and TEST-02 flipped to [x] with Coverage Status updated"
      contains: "- [x] **TEST-01**"
    - path: ".planning/STATE.md"
      provides: "Decisions-log entry for Phase 11 release gate"
      contains: "v1.1 release gate held: byte-identical contract preserved across Phases 6-10"
  key_links:
    - from: "CLAUDE.md §Conventions block"
      to: "Standalone-PR convention codification"
      via: "single bullet placed after Suspect run flagging bullet"
      pattern: "Standalone golden-fixture-regen PR:"
    - from: ".planning/REQUIREMENTS.md Traceability table"
      to: "Phase 11 row Status column"
      via: "Pending → Complete flip for TEST-01 + TEST-02 rows"
      pattern: "TEST-01 \\| Phase 11 \\| Complete"
---

<objective>
Phase 11 is the v1.1 release gate. Doc-only standalone PR with no production
code. The byte-changing surface introduced by Phases 6-10 (axes registry,
scoring, per-cell artifacts, spider chart, direction markers) is now
structurally pinned by existing in-source byte-identical tests; this plan
verifies that pin holds, captures the byte counts of the rendered fixtures,
codifies the standalone-PR convention in CLAUDE.md, and flips TEST-01 and
TEST-02 to complete in REQUIREMENTS.md.

Purpose: Lock the byte-identical contract for v1.1 and establish the
standalone-regen PR convention so future milestones (v1.2 spider-chart
additions, v2 allocator-matrix expansion) inherit the rule.

Output: Updated CLAUDE.md (1 bullet added), 11-01-SUMMARY.md (release-gate
report with byte counts), updated REQUIREMENTS.md (TEST-01/TEST-02 flipped),
updated STATE.md (decisions-log entry appended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-golden-fixture-regen/11-CONTEXT.md
@CLAUDE.md
@justfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify TEST-01 — run aggregator test suite against committed fixtures</name>
  <files>(read-only — no source modifications)</files>
  <read_first>
    - justfile (specifically the `aggregate:` recipe at line 441 — confirms invocation form `cargo run --release -p alloc-bench-aggregator -- --input "results/*.json" --output report/`)
    - .planning/phases/11-golden-fixture-regen/11-CONTEXT.md (verification scope section: 138 lib + 31 integration tests must pass)
    - crates/alloc-bench-aggregator/Cargo.toml (confirms test target wiring)
  </read_first>
  <action>
    Run `cargo test -p alloc-bench-aggregator` from the repo root. Capture the
    full stdout and tail of stderr. Confirm the exit status is 0 and that the
    output contains both "138 passed" (lib tests) and "31 passed" (integration
    tests).

    If either count differs OR if any test fails, halt this plan immediately
    and report the failing test name + error. Do NOT proceed to subsequent
    tasks. The failure must be routed through standard verification before
    Phase 11 can declare TEST-01 satisfied. Phases 7-10 have already shipped;
    a divergent count means the byte-identical contract has drifted between
    Phase 10 close and now.

    Save the literal pass-count line(s) (e.g., `test result: ok. 138 passed; 0
    failed; ...`) for use in Task 3 SUMMARY.md. Use a temporary scratch buffer
    in your context — do NOT write a stdout dump file.
  </action>
  <acceptance_criteria>
    - CLI: `cargo test -p alloc-bench-aggregator` exits 0
    - CLI output contains literal substring `138 passed` (lib test result line)
    - CLI output contains literal substring `31 passed` (integration test result line)
    - Pass-count lines captured in context for downstream task (Task 3)
    - Zero source files modified by this task (read-only verification)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p alloc-bench-aggregator 2>&1 | tee /tmp/phase11-test-output.log; test $? -eq 0 && grep -c '138 passed' /tmp/phase11-test-output.log | grep -v '^0$' && grep -c '31 passed' /tmp/phase11-test-output.log | grep -v '^0$'</automated>
  </verify>
  <done>cargo test exits 0, both 138 (lib) and 31 (integration) pass counts confirmed, pass-count lines captured for Task 3 SUMMARY.md</done>
</task>

<task type="auto">
  <name>Task 2: Add Standalone golden-fixture-regen PR bullet to CLAUDE.md §Conventions</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (specifically lines 85-96 — the §Conventions block; the Suspect run flagging bullet is at line 95; the GSD:conventions-end marker is at line 96)
    - .planning/phases/11-golden-fixture-regen/11-CONTEXT.md (CLAUDE.md §Conventions Promotion section: exact wording template, placement rule "immediately after the existing Suspect run flagging bullet")
  </read_first>
  <action>
    Edit CLAUDE.md to insert a single new flat bullet in the §Conventions
    block, immediately after the Suspect run flagging bullet (currently line
    95) and before the `<!-- GSD:conventions-end -->` marker (currently line
    96). No new subsection heading, no markdown blockquote rendering — match
    the existing flat-bullet style of the surrounding list.

    The bullet text MUST contain the literal opening `**Standalone
    golden-fixture-regen PR:**` so downstream verification grep-matches
    succeed. The bullet body explains: byte-changing surface additions
    (column headers, JSON header arrays, template placeholders,
    server-rendered axis labels) ship in a separate Phase-N PR carrying only
    fixture/snapshot regen + verification metadata; Phases A-E that
    introduce the byte-changing emit code carry no fixture-byte changes; the
    Phase-N PR is reviewer-visible so the regen is intentional and gated;
    established by Phase 11 (v1.1 release gate); inherited by all future
    milestones (e.g., v1.2 spider-chart additions, v2 allocator-matrix
    expansion). Match the existing convention bullet voice (terse, fact-led,
    no second-person directives).

    Use the Edit tool with the Suspect run flagging bullet's full line as
    old_string anchor and the same line plus the new bullet as new_string —
    this guarantees idempotent insertion at the correct line. Do NOT alter
    any other CLAUDE.md content.
  </action>
  <acceptance_criteria>
    - Source: CLAUDE.md contains literal substring `**Standalone golden-fixture-regen PR:**`
    - Source: The new bullet appears AFTER the line containing `**Suspect run flagging:**` and BEFORE `<!-- GSD:conventions-end -->`
    - Source: The new bullet starts with `- ` (flat-bullet style consistent with surrounding §Conventions entries)
    - Source: CLAUDE.md line count increases by exactly 1 (single new line added; no whitespace cruft)
    - Behavior: `grep -nA1 'Suspect run flagging' CLAUDE.md` shows the new Standalone golden-fixture-regen PR bullet immediately following the Suspect run flagging bullet
    - Behavior: No content elsewhere in CLAUDE.md is modified (verifiable via `git diff CLAUDE.md` showing exactly one insertion hunk)
  </acceptance_criteria>
  <verify>
    <automated>grep -c '\*\*Standalone golden-fixture-regen PR:\*\*' CLAUDE.md | grep -q '^1$' && grep -B0 -A1 'Suspect run flagging' CLAUDE.md | grep -q 'Standalone golden-fixture-regen PR' && git diff --stat CLAUDE.md | grep -q '1 insertion'</automated>
  </verify>
  <done>CLAUDE.md §Conventions block contains the Standalone golden-fixture-regen PR bullet immediately after the Suspect run flagging bullet; flat-bullet style preserved; only 1 line inserted; no other content touched</done>
</task>

<task type="auto">
  <name>Task 3: Run `just aggregate`, capture byte counts, write 11-01-SUMMARY.md</name>
  <files>.planning/phases/11-golden-fixture-regen/11-01-SUMMARY.md</files>
  <read_first>
    - justfile (lines 441-443 — the `aggregate:` recipe defines the literal `cargo run --release -p alloc-bench-aggregator -- --input "results/*.json" --output report/` invocation; this is what `just aggregate` runs)
    - .planning/phases/10-direction-markers/10-01-SUMMARY.md (exemplar SUMMARY.md format; mirrors the structure / frontmatter / Performance / Accomplishments sections we want here, scoped to release-gate content)
    - .planning/phases/11-golden-fixture-regen/11-CONTEXT.md (Deliverables section — explicit list of what 11-01-SUMMARY.md must contain: byte counts, just aggregate invocation, cargo test pass count, mirrors ROADMAP wording verbatim)
  </read_first>
  <action>
    Execute the byte-count capture procedure:

    1. Confirm the `results/*.json` glob in the repo root has fixtures (the
       committed fixture set under `crates/alloc-bench-aggregator/tests/fixtures/`
       is for the test suite, not the live aggregate; the gitignored
       `results/` at repo root is what `just aggregate` consumes per the
       justfile recipe). If `results/` is empty or absent, fall back to the
       documented capture path: temporarily symlink or copy the committed
       fixture inputs from `crates/alloc-bench-aggregator/tests/fixtures/`
       into `results/` for the duration of this task ONLY, then `rm -rf
       results/ report/` after capturing the byte counts. This keeps the
       working tree clean — `results/` and `report/` are gitignored.
    2. Run `just aggregate` exactly. Verify exit 0.
    3. Run `wc -c report/REPORT.md` and `wc -c report/index.html`. Capture
       the integer byte counts.
    4. Clean up: if you populated `results/` from fixtures, restore the
       repo to its prior state (rm any temp files / symlinks).

    Write `.planning/phases/11-golden-fixture-regen/11-01-SUMMARY.md` with
    the following minimum content:

    - YAML frontmatter mirroring the 10-01-SUMMARY.md style: phase, plan,
      subsystem (release-gate), tags, requirements-completed: [TEST-01,
      TEST-02], duration, completed timestamp.
    - One-paragraph release-gate headline matching the ROADMAP wording
      verbatim: "Standalone PR with no production code. The v1.1 release
      gate. Direction-marker arrows change column-header bytes;
      spider-trace JSON contributes to the byte-identical surface — neither
      can be pinned until Phases 7-10 land."
    - "## Verification" section listing:
      - The literal `cargo test -p alloc-bench-aggregator` invocation used
        and the captured pass counts (138 lib + 31 integration) from Task 1.
    - "## Byte-identical surface" section listing:
      - The literal `just aggregate` invocation used (which expands per the
        justfile recipe to `cargo run --release -p alloc-bench-aggregator
        -- --input "results/*.json" --output report/`).
      - `report/REPORT.md`: <integer> bytes (captured via `wc -c`)
      - `report/index.html`: <integer> bytes (captured via `wc -c`)
    - "## Standalone-PR convention" section: 1-2 sentences pointing to the
      CLAUDE.md §Conventions bullet added by Task 2.
    - "## Files Modified" section listing CLAUDE.md, REQUIREMENTS.md, STATE.md,
      11-01-SUMMARY.md — and explicitly noting "Zero touches under
      crates/**/src/** or crates/**/templates/** (doc-only PR per TEST-02
      convention)".

    Do NOT include the byte counts as bench results / performance numbers —
    they are static byte-counts of rendered fixtures, not throughput
    measurements. Do NOT inline the full `just aggregate` stdout — only the
    invocation line and the wc -c integer outputs.
  </action>
  <acceptance_criteria>
    - Source: .planning/phases/11-golden-fixture-regen/11-01-SUMMARY.md exists
    - Source: SUMMARY.md contains literal substring `report/REPORT.md` followed (within the same line or next line) by a numeric byte count (regex: `report/REPORT\.md.*[0-9]+`)
    - Source: SUMMARY.md contains literal substring `report/index.html` followed by a numeric byte count
    - Source: SUMMARY.md contains literal substring `cargo test -p alloc-bench-aggregator` (the verification invocation)
    - Source: SUMMARY.md contains literal substring `just aggregate` (the regen invocation)
    - Source: SUMMARY.md contains literal substring `138` and `31` (lib + integration pass counts from Task 1)
    - Source: SUMMARY.md frontmatter contains `requirements-completed: [TEST-01, TEST-02]` (or equivalent YAML list form)
    - CLI: `wc -c report/REPORT.md` exits 0 during execution (file exists at the moment of capture)
    - CLI: `wc -c report/index.html` exits 0 during execution
    - Behavior: After cleanup, `git status` shows no untracked `results/` or `report/` directories at repo root (gitignored, but uncommitted files staged for accidental commit are not present)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/11-golden-fixture-regen/11-01-SUMMARY.md && grep -q 'report/REPORT.md' .planning/phases/11-golden-fixture-regen/11-01-SUMMARY.md && grep -q 'report/index.html' .planning/phases/11-golden-fixture-regen/11-01-SUMMARY.md && grep -q 'cargo test -p alloc-bench-aggregator' .planning/phases/11-golden-fixture-regen/11-01-SUMMARY.md && grep -q 'just aggregate' .planning/phases/11-golden-fixture-regen/11-01-SUMMARY.md && grep -q '138' .planning/phases/11-golden-fixture-regen/11-01-SUMMARY.md && grep -q '31' .planning/phases/11-golden-fixture-regen/11-01-SUMMARY.md && grep -E 'TEST-01.*TEST-02|TEST-02.*TEST-01' .planning/phases/11-golden-fixture-regen/11-01-SUMMARY.md</automated>
  </verify>
  <done>11-01-SUMMARY.md exists with frontmatter (requirements-completed: [TEST-01, TEST-02]), captures REPORT.md and index.html byte counts via wc -c, cites the cargo test and just aggregate invocations, and reports 138 + 31 pass counts</done>
</task>

<task type="auto">
  <name>Task 4: Flip TEST-01 and TEST-02 to [x] in REQUIREMENTS.md and update Coverage Status</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (specifically lines 54-55 for the TEST-01/TEST-02 checkboxes in the Test & Golden-fixture Discipline section; lines 124-126 for the Traceability table TEST-01/TEST-02 rows; line 140 for the Coverage by Phase row)
    - .planning/phases/11-golden-fixture-regen/11-CONTEXT.md (Deliverables: explicitly calls for "flip TEST-01 and TEST-02 from [ ] to [x], update the Coverage table Status column from Pending to Complete")
  </read_first>
  <action>
    Make exactly three edits to `.planning/REQUIREMENTS.md`:

    1. **Test & Golden-fixture Discipline checkboxes** (currently lines 54-55):
       Change `- [ ] **TEST-01**:` → `- [x] **TEST-01**:` (preserve all
       trailing prose verbatim). Same for `- [ ] **TEST-02**:` → `- [x]
       **TEST-02**:`.
    2. **Traceability table** (currently lines 124-125):
       Change `| TEST-01 | Phase 11 | Pending |` → `| TEST-01 | Phase 11 |
       Complete |`. Same for the TEST-02 row.
    3. **No Coverage by Phase row change**: The Coverage by Phase table at
       line 140 enumerates requirement counts (already correct: `Phase 11
       — Golden-fixture Regen | 2 | TEST-01..02`); the per-phase Status
       lives in the Traceability table only.

    Use the Edit tool with anchored old_string / new_string pairs to make
    these surgical changes; do NOT rewrite the file. Make exactly 4 edits
    (2 checkbox flips + 2 Status flips). Preserve the rest of the file
    verbatim, including the trailing `*Last updated: 2026-05-26*` line —
    do NOT bump that timestamp; the Phase 11 commit message captures the
    update date.
  </action>
  <acceptance_criteria>
    - Source: REQUIREMENTS.md contains literal substring `- [x] **TEST-01**`
    - Source: REQUIREMENTS.md contains literal substring `- [x] **TEST-02**`
    - Source: REQUIREMENTS.md contains literal substring `| TEST-01 | Phase 11 | Complete |`
    - Source: REQUIREMENTS.md contains literal substring `| TEST-02 | Phase 11 | Complete |`
    - Source: REQUIREMENTS.md does NOT contain `- [ ] **TEST-01**` or `- [ ] **TEST-02**` (old [ ] markers fully replaced)
    - Source: REQUIREMENTS.md does NOT contain `| TEST-01 | Phase 11 | Pending |` or `| TEST-02 | Phase 11 | Pending |` (old Pending rows fully replaced)
    - Behavior: `git diff --stat .planning/REQUIREMENTS.md` shows exactly 4 lines changed (2 checkbox flips + 2 Traceability rows)
    - Behavior: All other requirement IDs (AXES-*, SEC-*, GUARD-*, SCORE-*, REC-*, CELL-*, POLAR-*, DIR-*, TEST-03..05) retain their pre-edit state byte-for-byte
  </acceptance_criteria>
  <verify>
    <automated>grep -c '\- \[x\] \*\*TEST-01\*\*' .planning/REQUIREMENTS.md | grep -q '^1$' && grep -c '\- \[x\] \*\*TEST-02\*\*' .planning/REQUIREMENTS.md | grep -q '^1$' && grep -c '| TEST-01 | Phase 11 | Complete |' .planning/REQUIREMENTS.md | grep -q '^1$' && grep -c '| TEST-02 | Phase 11 | Complete |' .planning/REQUIREMENTS.md | grep -q '^1$' && ! grep -q '\- \[ \] \*\*TEST-01\*\*' .planning/REQUIREMENTS.md && ! grep -q '\- \[ \] \*\*TEST-02\*\*' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>TEST-01 and TEST-02 checkboxes flipped to [x] in the Test & Golden-fixture Discipline section; both rows in the Traceability table show "Complete" instead of "Pending"; no other requirement rows altered</done>
</task>

<task type="auto">
  <name>Task 5: Append decisions-log entry to STATE.md</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (specifically the `## Accumulated Context > Decisions` block starting around line 84; the existing pattern is a flat bullet list with `- [Phase ?]:` or similar prefixes; the most recent v1.1 entries are at lines 100-101)
    - .planning/phases/11-golden-fixture-regen/11-CONTEXT.md (Deliverables: literal entry text `v1.1 release gate held: byte-identical contract preserved across Phases 6-10`)
  </read_first>
  <action>
    Append a single new bullet to the `## Accumulated Context > Decisions`
    block in `.planning/STATE.md`. The bullet text is the literal:

      `- [Phase 11]: v1.1 release gate held: byte-identical contract
      preserved across Phases 6-10.`

    Place it as the last entry in the Decisions list (after the existing
    `[Phase ?]: Phase 10 Plan 02: A/B comparison chart...` bullet at line
    101, before the `### Pending Todos` subheading at line 103).

    Additionally, update the `progress` block in the YAML frontmatter
    (lines 9-14): bump `completed_phases` from 4 to 5 (Phase 11 now
    complete; v1.1 has 6 phases total, of which Phases 6-10 were already
    counted as complete plus this Phase 11) — actually verify the current
    counts against the existing frontmatter and bump `completed_phases` and
    `completed_plans` by 1 (one phase + one plan landed). Recompute
    `percent` as `floor(100 * completed_phases / total_phases)` to two
    significant figures (the existing `percent: 67` was 4/6 = 66.6%
    truncated; with 5/6 = 83.3% the new percent is 83). Bump
    `last_updated` to today's date in ISO-8601 form.

    Use the Edit tool with anchored old_string / new_string for both edits
    (one for the Decisions append, one for the frontmatter progress bump).
    Do NOT mutate any other STATE.md content (Performance Metrics tables,
    Quick Tasks Completed table, Deferred Items table all stay as-is —
    Phase 11 metrics are recorded separately in the project audit, not in
    this plan).
  </action>
  <acceptance_criteria>
    - Source: STATE.md contains literal substring `v1.1 release gate held: byte-identical contract preserved across Phases 6-10`
    - Source: The new bullet starts with the prefix `- [Phase 11]:` (matches the established decision-bullet voice; `[Phase ?]:` is acceptable per existing pattern at lines 97-101)
    - Source: STATE.md frontmatter `completed_phases:` increased by 1 from the pre-edit value (5 if it was 4)
    - Source: STATE.md frontmatter `completed_plans:` increased by 1 from the pre-edit value (14 if it was 13)
    - Source: STATE.md frontmatter `percent:` recomputed (was 67 = 4/6; becomes 83 = 5/6)
    - Source: STATE.md frontmatter `last_updated:` set to today's ISO-8601 date (2026-05-29)
    - Behavior: `git diff --stat .planning/STATE.md` shows insertions at TWO sites (frontmatter progress block + Decisions block append) and no other modifications
    - Behavior: All Performance Metrics tables, Quick Tasks Completed table, and Deferred Items table rows are byte-identical pre/post edit (verifiable via `git diff` review of those line ranges)
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'v1.1 release gate held: byte-identical contract preserved across Phases 6-10' .planning/STATE.md && grep -q 'last_updated: "2026-05-29' .planning/STATE.md && grep -E 'completed_phases: 5' .planning/STATE.md && grep -E 'completed_plans: 14' .planning/STATE.md && grep -E 'percent: 83' .planning/STATE.md</automated>
  </verify>
  <done>STATE.md ## Accumulated Context > Decisions block has the appended Phase 11 release-gate entry; frontmatter progress block updated to 5/6 phases (83%) and 14 plans complete; last_updated bumped to today; no other STATE.md content mutated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed-fixtures → aggregator | Test inputs (`crates/alloc-bench-aggregator/tests/fixtures/**`) cross into the aggregator's parse + render pipeline. Boundary is well-tested by the existing 31-integration-test suite. |
| repo working tree → SUMMARY.md byte-counts | `wc -c` output crosses from filesystem state into the committed planning artifact. Risk: stale `report/` artifacts left over from a previous local run could yield byte counts that don't match a clean regen. Mitigation: Task 3 explicitly cleans up `report/` artifacts before/after capture. |

## STRIDE Threat Register

Phase 11 is doc-only with zero production-code touches; the threat surface is
narrow. Most STRIDE categories are inapplicable (no untrusted network input,
no auth flows, no privileged resources). The threats below cover the
plausible failure modes specific to this release-gate work.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | CLAUDE.md edit (Task 2) accidentally drops or mangles surrounding §Conventions bullets | mitigate | Use Edit tool with anchored old_string covering the Suspect run flagging line + 0 trailing chars; new_string contains old line + new bullet; idempotent at the line level. Acceptance criterion `git diff --stat CLAUDE.md` shows exactly 1 insertion (no deletions, no off-by-one bullet collapse). |
| T-11-02 | Tampering | REQUIREMENTS.md edit (Task 4) accidentally flips wrong checkbox row (e.g., TEST-03/04/05 also pending) | mitigate | Use anchored old_string `- [ ] **TEST-01**` (whitespace-precise) so only TEST-01 matches; same for TEST-02. Acceptance criterion: `! grep -q '- [ ] **TEST-01**'` AND TEST-03..05 retain `[ ]` markers (verifiable via `grep -c '- \[ \] \*\*TEST-' REQUIREMENTS.md` returning 3 — for TEST-03/04/05). |
| T-11-03 | Information Disclosure | SUMMARY.md (Task 3) inadvertently captures full `cargo test` stdout including stack-trace info from any pre-existing failure | accept | Phase 11 doc-only; no PII or secrets in test stdout. Task 1 halts on test failure before Task 3 runs, so the failure path is never reached. SUMMARY.md captures only invocation line + pass count integers + byte counts. |
| T-11-04 | Tampering | STATE.md frontmatter progress bump (Task 5) miscalculates percent or doesn't sync completed_phases/completed_plans | mitigate | Acceptance criterion pins the exact target values: completed_phases: 5, completed_plans: 14, percent: 83. Verification grep matches all three on a single line basis. Off-by-one defended structurally. |
| T-11-05 | Repudiation | byte counts in SUMMARY.md don't match the committed fixtures (developer reads SUMMARY.md and runs `just aggregate`, sees different counts) | mitigate | Task 3 explicitly captures byte counts immediately after a fresh `just aggregate` against the committed fixture set. Cleanup step ensures no stale `report/` artifacts. SUMMARY.md cites the exact `just aggregate` invocation so reviewer can reproduce. |
| T-11-SC | Tampering | npm/pip/cargo installs | n/a (out of scope) | Phase 11 introduces zero new dependencies — no `cargo add`, no `npm install`, no `pip install`. The only tooling invoked is the existing `just aggregate` recipe and `cargo test -p alloc-bench-aggregator`, both already pinned by `rust-toolchain.toml` (`channel = "1.95"`) and `Cargo.lock`. Slopcheck does not apply. |
</threat_model>

<verification>
**Phase-level verification (run after all 5 tasks complete):**

1. `cargo test -p alloc-bench-aggregator` — confirms TEST-01 (138 lib + 31 integration tests pass against committed fixtures); already exercised by Task 1, but rerun once at phase close to confirm no edits to .planning/** broke anything.
2. `grep -A1 'Suspect run flagging' CLAUDE.md` — confirms Task 2 placed the Standalone golden-fixture-regen PR bullet immediately after Suspect run flagging.
3. `test -f .planning/phases/11-golden-fixture-regen/11-01-SUMMARY.md && wc -l .planning/phases/11-golden-fixture-regen/11-01-SUMMARY.md` — confirms SUMMARY.md exists and is non-trivial.
4. `grep -E '(- \[x\] \*\*TEST-01|- \[x\] \*\*TEST-02|TEST-01 \| Phase 11 \| Complete|TEST-02 \| Phase 11 \| Complete)' .planning/REQUIREMENTS.md` — confirms 4 lines flipped.
5. `grep -q 'v1.1 release gate held' .planning/STATE.md && grep -q 'completed_phases: 5' .planning/STATE.md && grep -q 'percent: 83' .planning/STATE.md` — confirms STATE.md updated.
6. `git status` — confirms 4 modified files (CLAUDE.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/phases/11-golden-fixture-regen/11-01-SUMMARY.md) and zero modifications under `crates/**` (the TEST-02 standalone-PR convention enforces this).

**No automated test added in this phase** (per CONTEXT.md "Run existing tests only — no new golden snapshot files"). The existing in-source byte-identical tests are the structural pin.
</verification>

<success_criteria>
Phase 11 complete when:

- [x] Task 1: `cargo test -p alloc-bench-aggregator` passes with 138 lib + 31 integration tests (TEST-01)
- [x] Task 2: CLAUDE.md §Conventions has the Standalone golden-fixture-regen PR bullet (TEST-02)
- [x] Task 3: 11-01-SUMMARY.md captured with REPORT.md + index.html byte counts, just aggregate invocation, and cargo test pass count
- [x] Task 4: REQUIREMENTS.md TEST-01 + TEST-02 flipped to [x]; Coverage Status flipped to Complete
- [x] Task 5: STATE.md decisions-log appended; progress block updated to 5/6 phases (83%)
- [x] Zero touches under `crates/**/src/**` or `crates/**/templates/**` (the TEST-02 convention enforces this — `git diff --stat` over the phase commit set must show only doc/.planning files)
- [x] PR title prefix `docs(11)`; subordinate commits use `docs(11-01)` per the project conventional-commit-prefix convention
</success_criteria>

<output>
On completion, this plan emits:

1. `.planning/phases/11-golden-fixture-regen/11-01-SUMMARY.md` — release-gate
   report with byte counts, just aggregate invocation, cargo test pass count.
2. Updated `CLAUDE.md` — §Conventions block with Standalone golden-fixture-regen
   PR bullet inserted after Suspect run flagging.
3. Updated `.planning/REQUIREMENTS.md` — TEST-01 and TEST-02 flipped from `[ ]`
   to `[x]`; Traceability Status flipped from Pending to Complete.
4. Updated `.planning/STATE.md` — decisions-log entry appended; progress block
   bumped to 5/6 phases (83%) and 14 plans complete.

Phase 11 closes the v1.1 milestone byte-identical contract. After this plan
ships, the standard milestone lifecycle (audit → complete → cleanup) runs.
</output>
