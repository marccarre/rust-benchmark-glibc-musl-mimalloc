---
phase: 06-foundations
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/alloc-bench-core/Cargo.toml
  - crates/alloc-bench-core/tests/smoke.rs
autonomous: true
requirements:
  - GUARD-01
must_haves:
  truths:
    - "Developer reads `crates/alloc-bench-core/Cargo.toml` and finds `sha2 = \"0.10\"` under `[dev-dependencies]` (NOT under `[dependencies]` — Out-of-Scope rule \"no new runtime crate dependencies\")."
    - "Developer runs `cargo test -p alloc-bench-core --test smoke v1_schema_output_rs_is_frozen` and the test passes — SHA-256 of `crates/alloc-bench-core/src/output.rs` matches the pinned hex constant."
    - "Developer manually mutates a single byte in `crates/alloc-bench-core/src/output.rs` (e.g., changes a comment) and re-runs the test; the test fails with a multi-line error message guiding the contributor through scenarios (a) sidecar-only additive change OR (b) v1→v2 schema migration."
    - "Developer reads `crates/alloc-bench-core/tests/smoke.rs` and finds the test function path is `smoke::tests::v1_schema_output_rs_is_frozen` (verbatim per CONTEXT lock — wrapped in `mod tests { ... }`)."
    - "Developer runs `cargo test --workspace` and ALL tests still pass — the new test does not break any existing test in any crate."
  artifacts:
    - path: "crates/alloc-bench-core/Cargo.toml"
      provides: "[dev-dependencies] section with sha2 = \"0.10\""
      contains: "[dev-dependencies]"
    - path: "crates/alloc-bench-core/tests/smoke.rs"
      provides: "v1_schema_output_rs_is_frozen test pinning SHA-256 of output.rs"
      contains: "v1_schema_output_rs_is_frozen"
  key_links:
    - from: "crates/alloc-bench-core/tests/smoke.rs::V1_OUTPUT_RS_SHA256"
      to: "crates/alloc-bench-core/src/output.rs"
      via: "std::fs::read + Sha256::digest at test runtime"
      pattern: "env!\\(\"CARGO_MANIFEST_DIR\"\\).*src/output\\.rs"
    - from: "crates/alloc-bench-core/Cargo.toml [dev-dependencies]"
      to: "crates/alloc-bench-core/tests/smoke.rs"
      via: "use sha2::{Digest, Sha256};"
      pattern: "sha2 = \"0\\.10\""
---

<objective>
Land the frozen-schema CI gate — `crates/alloc-bench-core/tests/smoke.rs::tests::v1_schema_output_rs_is_frozen` pins the SHA-256 hash of `crates/alloc-bench-core/src/output.rs` against a hard-coded hex constant computed at plan-execution time. Adds `sha2 = "0.10"` to `crates/alloc-bench-core/Cargo.toml` as a `[dev-dependencies]` entry only (NOT a runtime dependency — per the Out-of-Scope rule "no new runtime crate dependencies").

Phase 6 Plan 03 is the leaf-and-guard deliverable for GUARD-01. It guards against accidental mutation of the v1 schema (Phase 1 D-11 lock) by failing `cargo test` whenever any byte of `output.rs` changes — forcing a contributor to either prove their change is sidecar-only-additive (and re-pin the hash) or to do an explicit v1→v2 schema migration (bumping `SCHEMA_VERSION: u32` AND the pinned hash AND regenerating goldens, all in one commit).

Purpose: The v1 input schema (`output.rs`) is the immutable contract every aggregator deserialiser deserialises against. Phases 7-11 add features by writing sidecars (Phase 6 Plan 02 / Phase 5 D-13 precedent), never by mutating `output.rs`. The hash pin makes that contract enforced rather than aspirational.

Output:
- `[dev-dependencies]` block added to `crates/alloc-bench-core/Cargo.toml` (currently has none) with a single entry `sha2 = "0.10"`.
- New file `crates/alloc-bench-core/tests/smoke.rs` (~50 LOC including doc-comment) with one `#[test]` function `v1_schema_output_rs_is_frozen` inside `mod tests { ... }`.
- The hex constant `V1_OUTPUT_RS_SHA256` is computed at plan-execution time by running `sha256sum crates/alloc-bench-core/src/output.rs` (or `shasum -a 256` on macOS — they produce identical lowercase hex output).
- One passing `cargo test -p alloc-bench-core --test smoke` invocation.

Note: Plan 03 is INDEPENDENT of Plans 01 and 02 — it touches `crates/alloc-bench-core/Cargo.toml` and `crates/alloc-bench-core/tests/smoke.rs`. Plans 01 and 02 touch `crates/alloc-bench-aggregator/*`. Zero `files_modified` overlap → safe to ship in the same wave.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-foundations/06-CONTEXT.md
@.planning/phases/06-foundations/06-RESEARCH.md
@./CLAUDE.md

<!-- The frozen file -->
@crates/alloc-bench-core/src/output.rs

<!-- The crate manifest gaining a [dev-dependencies] section -->
@crates/alloc-bench-core/Cargo.toml

<interfaces>
<!-- Required test surface (RESEARCH §Frozen-Schema Gate — verbatim) -->

Required Cargo.toml addition (currently lines 8-22 are `[dependencies]`; no `[dev-dependencies]` section exists yet):

```
[dev-dependencies]
sha2 = "0.10"
```

Append at the end of the file (after the existing `[dependencies]` block ending line 22). Keep the trailing newline / formatting consistent with the surrounding lines.

Required test file `crates/alloc-bench-core/tests/smoke.rs` (NEW — directory and file both new; verified in RESEARCH §`tests/smoke.rs` status):

Module-level doc-comment block (top, ~10 lines of `//!`) cites Phase 6 / GUARD-01, the CONTEXT pinning protocol (sidecar-only additive vs v1→v2 migration), and the LF line-ending assumption (RESEARCH §Line-ending handling).

Top-level constant:
```
const V1_OUTPUT_RS_SHA256: &str = "<HEX-COMPUTED-AT-PLAN-EXECUTION-TIME>";
```
With a one-line comment above: `// Lowercase hex; matches both 'sha256sum' coreutils and 'shasum -a 256' macOS output.` (RESEARCH §Risks #6).

Top-level `use` statement:
```
use sha2::{Digest, Sha256};
```

The `mod tests` wrapper is mandatory per CONTEXT verbatim — the test name in Cargo's discovered output must read `smoke::tests::v1_schema_output_rs_is_frozen`. Layout:

```
mod tests {
    use super::*;

    #[test]
    fn v1_schema_output_rs_is_frozen() {
        // body — see RESEARCH §Test body sketch
    }
}
```

Test body:
1. Build the path: `let path = std::path::Path::new(env!("CARGO_MANIFEST_DIR")).join("src/output.rs");`
2. Read bytes: `let bytes = std::fs::read(&path).unwrap_or_else(|e| panic!("reading {} for SHA-256 freeze test: {e}", path.display()));`
3. Hash: `let mut hasher = Sha256::new(); hasher.update(&bytes); let actual = format!("{:x}", hasher.finalize());` — `{:x}` is mandatory (lowercase hex, RESEARCH §Risks #6).
4. Assert: `assert_eq!(actual, V1_OUTPUT_RS_SHA256, "<multi-line failure message — see RESEARCH §Test body sketch>");`. The failure message must:
    - State the file has changed.
    - Enumerate the two intentional-change scenarios (sidecar-only / v1→v2).
    - List the remediation steps for each.
    - Print computed-vs-expected hashes for diagnosability.
    - Print the file path so a contributor can run `sha256sum` directly.

Hash computation protocol (RESEARCH §The hash-pinning order-of-operations):
1. Before running the test, the executor MUST verify `cargo fmt --check -p alloc-bench-core` is clean (RESEARCH §Risks #2). If `cargo fmt` produces a diff against `output.rs`, the executor MUST apply the format in a separate prior commit (`chore(06-03): cargo fmt output.rs` or rolled into Plan 03's commit) BEFORE computing the hash. Plans 01 and 02 do not touch `output.rs`, so the file's contents at plan-execution time should match exactly the contents at plan-write time. Verify with `git diff HEAD -- crates/alloc-bench-core/src/output.rs` before hashing.
2. Compute the hash with: `sha256sum crates/alloc-bench-core/src/output.rs | awk '{print $1}'` on Linux, or `shasum -a 256 crates/alloc-bench-core/src/output.rs | awk '{print $1}'` on macOS. Both produce identical lowercase 64-hex-char output for the same input bytes.
3. Paste the resulting hex into `V1_OUTPUT_RS_SHA256` (do NOT prefix with "0x").
4. Run `cargo test -p alloc-bench-core --test smoke v1_schema_output_rs_is_frozen` to verify.

Verification of dev-dep status (sanity check after `cargo add`):
- `sha2` should appear in `Cargo.lock` (proves `cargo` resolved it).
- `sha2` should NOT appear in any production-binary dependency tree: `cargo tree -p alloc-bench-core --edges normal | grep sha2` returns nothing (only dev-edges include sha2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sha2 = "0.10" to alloc-bench-core [dev-dependencies]</name>
  <files>crates/alloc-bench-core/Cargo.toml</files>
  <read_first>
    - crates/alloc-bench-core/Cargo.toml (full file, 23 lines — currently has only `[package]` and `[dependencies]`; no `[dev-dependencies]` block)
    - .planning/phases/06-foundations/06-RESEARCH.md (§sha2 dev-dep add — locked: 0.10 line, dev-only, never in [dependencies])
    - .planning/phases/06-foundations/06-CONTEXT.md (§Frozen-Schema Gate — locked: sha2 used at test runtime only)
  </read_first>
  <action>
    Append a new `[dev-dependencies]` section to `crates/alloc-bench-core/Cargo.toml` after the existing `[dependencies]` block (which currently ends at line 22 with `rayon = { workspace = true }`). New block content (verbatim, no extra comments):

    ```
    [dev-dependencies]
    sha2 = "0.10"
    ```

    Use a single blank line separator between `[dependencies]` and `[dev-dependencies]` for consistency with the existing `[package]` → `[dependencies]` separation.

    Run `cargo update -p alloc-bench-core` (or rely on the next `cargo build` / `cargo test` to update `Cargo.lock`) so `sha2` and its transitive deps land in `Cargo.lock`. The expected transitive closure is small: `sha2` pulls `cpufeatures`, `digest`, `generic-array`, `block-buffer`, and a couple of others — all are well-established `RustCrypto` crates.

    DO NOT add `sha2` to `[dependencies]` — it must be `[dev-dependencies]` only. The test compiles only under `cargo test`; the production binary (`alloc-bench-core` is a library used by `alloc-bench-cli`, `alloc-bench-aggregator`, and the scenario crates) does NOT import `sha2`.
    DO NOT add `sha2` to the workspace root `Cargo.toml` deps. Per RESEARCH §Codebase Reconnaissance: the workspace currently does not declare `sha2`; declaring it as a workspace dep would make it implicitly available to every crate, defeating the dev-only intent.
    DO NOT use `cargo add` interactively if it surfaces a feature-flag prompt — accept the default features. `sha2`'s default features (`std`) are correct for a test target.

    Package legitimacy check: `sha2` is `[VERIFIED]` (one of the most-downloaded Rust crates, ~250M+ downloads, maintained by RustCrypto org since 2016 — not a slop or assumed package). No blocking checkpoint required.
  </action>
  <verify>
    <automated>
      grep -A1 "^\\[dev-dependencies\\]" crates/alloc-bench-core/Cargo.toml | grep -E "^sha2 = \"0\\.10\"" &amp;&amp; \
      ! grep -E "^sha2" crates/alloc-bench-core/Cargo.toml | head -1 | grep -v "0\\.10" &amp;&amp; \
      cargo build -p alloc-bench-core 2>&amp;1 | tee /tmp/core-build.log &amp;&amp; ! grep -E "error\\[E[0-9]+\\]" /tmp/core-build.log &amp;&amp; \
      cargo tree -p alloc-bench-core --edges normal 2>/dev/null | grep -c "sha2 v" | grep -E "^0$"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\\[dev-dependencies\\]" crates/alloc-bench-core/Cargo.toml` returns `1` (exactly one dev-deps block).
    - `grep -E "^sha2 = \"0\\.10\"" crates/alloc-bench-core/Cargo.toml` returns one line.
    - The line `sha2 = "0.10"` appears AFTER the line `[dev-dependencies]` (verified with `grep -nE "^\\[dev-dependencies\\]|^sha2 = " crates/alloc-bench-core/Cargo.toml` showing dev-deps before sha2).
    - `grep -B1 "^sha2 = \"0\\.10\"" crates/alloc-bench-core/Cargo.toml | head -1` does NOT match `[dependencies]` (proves sha2 is in dev-deps, not regular deps).
    - `cargo build -p alloc-bench-core` succeeds (exit 0) with no `error[E...]` lines.
    - `cargo tree -p alloc-bench-core --edges normal | grep -c "sha2"` returns `0` (sha2 not in production dependency closure).
    - `cargo tree -p alloc-bench-core --edges dev | grep -c "sha2"` returns at least `1` (sha2 IS in dev dependency closure).
    - `Cargo.lock` contains a `[[package]] name = "sha2"` block (verifiable via `grep "name = \"sha2\"" Cargo.lock | wc -l` returning at least `1`).
  </acceptance_criteria>
  <done>
    `sha2 = "0.10"` is the only entry under `[dev-dependencies]` in `crates/alloc-bench-core/Cargo.toml`; production dependency closure does not include sha2; `cargo build` succeeds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create tests/smoke.rs with v1_schema_output_rs_is_frozen test, compute and pin hash</name>
  <files>crates/alloc-bench-core/tests/smoke.rs</files>
  <read_first>
    - crates/alloc-bench-core/src/output.rs (full file — the artifact whose bytes get hashed; 517 lines / 18,839 bytes per RESEARCH §`output.rs` structure)
    - .planning/phases/06-foundations/06-RESEARCH.md (§Frozen-Schema Gate — full sketch including module doc comment, V1_OUTPUT_RS_SHA256 constant, mod tests wrapping, test body, failure message, line-ending handling, hash-pinning order-of-operations)
    - .planning/phases/06-foundations/06-CONTEXT.md (§Frozen-Schema Gate — locked: test name `smoke::tests::v1_schema_output_rs_is_frozen` verbatim, sha2 mechanism, pinning protocol)
  </read_first>
  <behavior>
    - Test `v1_schema_output_rs_is_frozen`: read `crates/alloc-bench-core/src/output.rs` bytes; SHA-256 hash via `sha2::Sha256`; format with `{:x}` (lowercase); assert hex equals the pinned constant `V1_OUTPUT_RS_SHA256`.
    - Failure case (proves the guard works): manually flip a byte in `output.rs` → re-run test → it fails with the multi-line guidance message containing "v1 schema in {} has changed" plus computed-vs-expected hashes plus the two remediation scenarios. (This is a manual sanity check, not an automated test — the executor verifies once, then reverts the byte flip.)
  </behavior>
  <action>
    Create `crates/alloc-bench-core/tests/` directory if it does not exist (RESEARCH §`tests/smoke.rs` status confirms it does not). Create the file `crates/alloc-bench-core/tests/smoke.rs` with the following structure:

    Module-level `//!` doc-comment block at the top (~12-15 lines). Content per RESEARCH §Test body sketch:
      - One-sentence summary: "Phase 6 GUARD-01: pin SHA-256 of `crates/alloc-bench-core/src/output.rs` to its v1.0 freeze. Guards the v1 schema contract (Phase 1 D-11 + CLAUDE.md Conventions: \"Aggregator decorate-not-rewrite\")."
      - Failure protocol section: "When this test fails, the contributor must either: (a) prove the diff to output.rs is sidecar-only / additive-with-skip-serializing-if (and the existing `run_canonical_shape_snapshot` in output.rs's in-file `mod tests` still passes byte-equivalence), AND bump the pinned hash with a one-line commit message explaining the additive change; OR (b) explicitly migrate the schema to v2 (bump SCHEMA_VERSION to 2 in output.rs:3 in the SAME commit, regenerate goldens, and bump the pinned hash). There is no third option; \"I just refactored, please trust me\" is not."

    Top-level use statement (after doc-comment, before constant):
    ```
    use sha2::{Digest, Sha256};
    ```

    Top-level constant (after `use`, before `mod tests`):
    ```
    // Lowercase hex; matches both 'sha256sum' coreutils and 'shasum -a 256' macOS output.
    // Hash assumes LF line endings; verified on macOS/Linux via 'sha256sum'.
    // To recompute (after an intentional, justified change):
    //   sha256sum crates/alloc-bench-core/src/output.rs
    const V1_OUTPUT_RS_SHA256: &str = "<HEX-COMPUTED-AT-PLAN-EXECUTION-TIME>";
    ```

    `mod tests` block (per CONTEXT verbatim — the test name path must be `smoke::tests::v1_schema_output_rs_is_frozen`):

    ```
    mod tests {
        use super::*;

        #[test]
        fn v1_schema_output_rs_is_frozen() {
            // body
        }
    }
    ```

    Test body:
    1. `let path = std::path::Path::new(env!("CARGO_MANIFEST_DIR")).join("src/output.rs");`
    2. `let bytes = std::fs::read(&path).unwrap_or_else(|e| panic!("reading {} for SHA-256 freeze test: {e}", path.display()));`
    3. `let mut hasher = Sha256::new();`
    4. `hasher.update(&bytes);`
    5. `let actual = format!("{:x}", hasher.finalize());` — MUST use `{:x}` (lowercase). `{:X}` (uppercase) is forbidden because `sha256sum` and `shasum` both emit lowercase.
    6. `assert_eq!(actual, V1_OUTPUT_RS_SHA256, "<failure message per RESEARCH §Test body sketch>", path.display(), actual, V1_OUTPUT_RS_SHA256);`. The multi-line message must include: file-path, the two scenario branches (a/b), explicit remediation steps for each (sidecar-only vs v1→v2), and `Computed: {} / Expected: {}` lines.

    Hash computation steps the executor performs:
    1. Verify `cargo fmt --check -p alloc-bench-core` is clean. If dirty, FIRST commit a `chore(06-03): cargo fmt output.rs for hash stability` commit (or roll into the Plan 03 commit), THEN proceed.
    2. Verify Plans 01 and 02 have NOT mutated `crates/alloc-bench-core/src/output.rs` (per `git diff HEAD -- crates/alloc-bench-core/src/output.rs` returning empty). They shouldn't have — Plans 01/02 only touch the aggregator crate. If a diff IS present, stop and report the unexpected mutation as a Plan 01 or Plan 02 regression.
    3. Compute hash: `sha256sum crates/alloc-bench-core/src/output.rs | awk '{print $1}'` (Linux) or `shasum -a 256 crates/alloc-bench-core/src/output.rs | awk '{print $1}'` (macOS). Both yield 64-character lowercase hex.
    4. Paste the hex into `V1_OUTPUT_RS_SHA256` (no `0x` prefix, no quotes inside the string — just 64 hex chars).
    5. Run `cargo test -p alloc-bench-core --test smoke v1_schema_output_rs_is_frozen` and verify it passes.
    6. As a final sanity check, manually flip ONE BYTE of `output.rs` (e.g., add a trailing space to a comment line), re-run the test, verify it fails with the expected multi-line guidance message, then `git checkout -- crates/alloc-bench-core/src/output.rs` to revert. (This sanity check is documented in the SUMMARY but not required as an automated step.)

    DO NOT modify `crates/alloc-bench-core/src/output.rs` in any way during this plan. The whole point of the guard is that `output.rs` is frozen — Plan 03 doing the freezing must not be the plan that breaks the freeze.
    DO NOT add `#[cfg(test)]` to the `mod tests` block — integration-test files (`tests/*.rs`) compile only under `cargo test` already, so `#[cfg(test)]` is redundant.
    DO NOT change the test name from `v1_schema_output_rs_is_frozen`. The path `smoke::tests::v1_schema_output_rs_is_frozen` is a CONTEXT lock.
  </action>
  <verify>
    <automated>
      test -f crates/alloc-bench-core/tests/smoke.rs &amp;&amp; \
      grep -c "^use sha2::{Digest, Sha256};" crates/alloc-bench-core/tests/smoke.rs | grep -E "^1$" &amp;&amp; \
      grep -cE "^const V1_OUTPUT_RS_SHA256: &amp;str = \"[0-9a-f]{64}\";" crates/alloc-bench-core/tests/smoke.rs | grep -E "^1$" &amp;&amp; \
      grep -c "^mod tests {" crates/alloc-bench-core/tests/smoke.rs | grep -E "^1$" &amp;&amp; \
      grep -c "fn v1_schema_output_rs_is_frozen()" crates/alloc-bench-core/tests/smoke.rs | grep -E "^1$" &amp;&amp; \
      cargo test -p alloc-bench-core --test smoke v1_schema_output_rs_is_frozen 2>&amp;1 | tee /tmp/freeze-test.log &amp;&amp; \
      grep -E "test tests::v1_schema_output_rs_is_frozen \\.\\.\\. ok" /tmp/freeze-test.log
    </automated>
  </verify>
  <acceptance_criteria>
    - File `crates/alloc-bench-core/tests/smoke.rs` exists.
    - `grep -c "^use sha2::{Digest, Sha256};" crates/alloc-bench-core/tests/smoke.rs` returns `1`.
    - `grep -cE "^const V1_OUTPUT_RS_SHA256: &str = \"[0-9a-f]{64}\";" crates/alloc-bench-core/tests/smoke.rs` returns `1` (the constant is exactly 64 lowercase hex chars — placeholder text like `<HEX-...>` would not match this regex, providing a hard gate that the hash was actually computed and pasted).
    - `grep -c "^mod tests {" crates/alloc-bench-core/tests/smoke.rs` returns `1` (the CONTEXT-locked wrapping module).
    - `grep -c "fn v1_schema_output_rs_is_frozen()" crates/alloc-bench-core/tests/smoke.rs` returns `1` (CONTEXT-locked test name).
    - `grep -c 'format!("{:x}"' crates/alloc-bench-core/tests/smoke.rs` returns `1` (lowercase hex format — `{:X}` would be wrong).
    - `grep -E "v1 schema in.*has changed" crates/alloc-bench-core/tests/smoke.rs` returns at least one line (the user-facing failure message body).
    - `grep -c "SCHEMA_VERSION" crates/alloc-bench-core/tests/smoke.rs` returns at least `1` (failure message references the v1→v2 migration scenario).
    - `cargo test -p alloc-bench-core --test smoke v1_schema_output_rs_is_frozen` passes (output contains `test tests::v1_schema_output_rs_is_frozen ... ok`).
    - `git diff HEAD -- crates/alloc-bench-core/src/output.rs` returns EMPTY (Plan 03 must not have mutated `output.rs`).
    - `git diff HEAD -- crates/alloc-bench-core/src/lib.rs` returns EMPTY (no need to touch lib.rs — integration tests are auto-discovered via `tests/`).
  </acceptance_criteria>
  <done>
    `tests/smoke.rs` exists; `v1_schema_output_rs_is_frozen` passes; the pinned hash is a real 64-char lowercase hex string (not a placeholder); `output.rs` is byte-identical with HEAD (Plan 03 did not break its own guard).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → test harness | `tests/smoke.rs` reads `src/output.rs` at test runtime via `std::fs::read`. The path is constructed from `env!("CARGO_MANIFEST_DIR")` (compile-time crate root, not user-controlled) — no path-traversal surface. |
| crates.io → workspace | `sha2 = "0.10"` is a dev-dep; pulled at `cargo build`/`cargo test` time from crates.io via Cargo's existing supply chain (registry signing, not addressed in Phase 6). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-T | Tampering | `crates/alloc-bench-core/src/output.rs` (the protected artifact) | mitigate | This entire plan IS the mitigation. SHA-256 pinning detects any byte change; failure message guides contributor through legitimate-change protocol. |
| T-06-03-SC | Tampering (Supply Chain) | `sha2 = "0.10"` dev-dep | mitigate | `sha2` is `[VERIFIED]` per Package Legitimacy Audit — RustCrypto org, ~250M+ downloads, has been the canonical Rust SHA implementation since 2016. No `[ASSUMED]` / `[SUS]` blocking checkpoint required. The `0.10` major version line has been stable since 2021. |
| T-06-03-T-fmt | Tampering (Inadvertent) | `cargo fmt` reformats `output.rs` and breaks the hash | mitigate | RESEARCH §Risks #2: pre-pin verification step in this plan's task action explicitly runs `cargo fmt --check -p alloc-bench-core` BEFORE computing the hash. If fmt is dirty, applies it as a separate prior commit, THEN computes the hash. Documents this in the smoke.rs comment. |
| T-06-03-T-eol | Tampering (Inadvertent) | Windows contributor checks out repo with CRLF, hash mismatches LF baseline | mitigate (best-effort) | RESEARCH §Line-ending handling: comment in smoke.rs constant block documents the LF assumption ("Hash assumes LF line endings"). All current contributors are macOS/Linux per CLAUDE.md (project is `Cargo` + `just` + Docker-Linux). If a Windows contributor surfaces, the fix is a follow-up `.gitattributes` shipping `* text=auto eol=lf`, deferred per RESEARCH explicit statement. |
| T-06-03-D | Denial of Service | (n/a) | accept | Test reads ~19 KB, hashes once. No attack surface. |
| T-06-03-I | Information Disclosure | (n/a) | accept | `output.rs` is open-source; hashing public bytes is information-neutral. |

Package Legitimacy Audit: `sha2 = "0.10"` is `[VERIFIED]` (RustCrypto canonical SHA crate; 250M+ downloads; stable major-version line since 2021). No blocking human-verify checkpoint required for this dev-dep addition.
</threat_model>

<verification>
- `cargo build -p alloc-bench-core` → clean build, no errors.
- `cargo tree -p alloc-bench-core --edges normal | grep sha2` → empty output (sha2 absent from production deps).
- `cargo tree -p alloc-bench-core --edges dev | grep sha2` → at least one line (sha2 present in dev deps).
- `cargo test -p alloc-bench-core --test smoke` → 1 test passes (`v1_schema_output_rs_is_frozen`).
- `cargo test -p alloc-bench-core` → all tests still pass (existing `output::tests::*` tests + the new smoke test).
- `cargo test --workspace` → all tests across all crates still pass (regression sweep — Plans 01, 02, 03 are independent so a workspace-wide test run after all three land must be clean).
- Manual sanity check (executor performs once, documents in SUMMARY): flip a byte of `output.rs`, re-run smoke test, verify it fails with the expected multi-line guidance message, revert.
- `git diff HEAD -- crates/alloc-bench-core/src/output.rs` → empty (Plan 03 must not have mutated the protected file).
- `Cargo.lock` lists exactly one `[[package]] name = "sha2"` block with version starting `0.10.`.
</verification>

<success_criteria>
1. `crates/alloc-bench-core/Cargo.toml` declares `sha2 = "0.10"` under `[dev-dependencies]` (and ONLY there — not in `[dependencies]` or workspace deps).
2. `crates/alloc-bench-core/tests/smoke.rs` exists and contains the test `tests::v1_schema_output_rs_is_frozen`.
3. The pinned constant `V1_OUTPUT_RS_SHA256` is a 64-character lowercase hex string (real hash, not a placeholder), verifiable by re-running `sha256sum crates/alloc-bench-core/src/output.rs`.
4. `cargo test -p alloc-bench-core --test smoke v1_schema_output_rs_is_frozen` passes.
5. `cargo test --workspace` passes (no regression in any other crate).
6. `crates/alloc-bench-core/src/output.rs` is byte-identical with HEAD before this plan started — verifiable via `git diff HEAD -- crates/alloc-bench-core/src/output.rs` returning empty.
7. The failure-mode sanity check (flip a byte, re-run, observe multi-line guidance, revert) was performed once by the executor and documented in `06-03-SUMMARY.md`.
</success_criteria>

<output>
On completion, the executor creates `.planning/phases/06-foundations/06-03-SUMMARY.md` documenting:
- Files created / modified (Cargo.toml, tests/smoke.rs)
- The actual SHA-256 hex pasted into `V1_OUTPUT_RS_SHA256` (so a future contributor can verify with `sha256sum` against the same file)
- Whether `cargo fmt --check -p alloc-bench-core` was clean before hashing (and if not, the prior commit hash that fixed it)
- Confirmation of the failure-mode sanity check (byte-flip → fail → revert)
- Conventional-commit message used (e.g., `feat(06-03): add v1_schema_output_rs_is_frozen guard (GUARD-01)`)
- Confirmation that `output.rs` was NOT modified by this plan (decorate-not-rewrite — Plan 03 must not break its own guard)
</output>
</content>
</invoke>