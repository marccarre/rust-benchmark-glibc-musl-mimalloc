---
phase: 06-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/alloc-bench-aggregator/src/axes.rs
  - crates/alloc-bench-aggregator/src/main.rs
autonomous: true
requirements:
  - AXES-01
  - AXES-02
must_haves:
  truths:
    - "Developer reads `crates/alloc-bench-aggregator/src/axes.rs` and finds `MEASUREMENT_AXES: [AxisSpec; 8]` exported as a `pub const` (not `lazy_static`)."
    - "Developer reads `axes.rs` and finds `Direction::{Higher, Lower}` enum with a `pub const fn arrow(self) -> char` returning `'\\u{2191}'` for `Higher` and `'\\u{2193}'` for `Lower`."
    - "Developer runs `cargo test -p alloc-bench-aggregator axes::` and the unit-test set passes (length=8, alphabetical, unique, heuristic flags, arrow glyphs)."
    - "Developer runs `cargo build -p alloc-bench-aggregator` and the crate compiles cleanly with the new `mod axes;` declaration in `main.rs` (no `unused_imports` warnings)."
  artifacts:
    - path: "crates/alloc-bench-aggregator/src/axes.rs"
      provides: "Compile-time MEASUREMENT_AXES registry + Direction enum + arrow() const fn"
      contains: "pub const MEASUREMENT_AXES: [AxisSpec; 8]"
    - path: "crates/alloc-bench-aggregator/src/main.rs"
      provides: "Module declaration `mod axes;` (alphabetically placed between `mod html;` and `mod loader;`)"
      contains: "mod axes;"
  key_links:
    - from: "crates/alloc-bench-aggregator/src/main.rs"
      to: "crates/alloc-bench-aggregator/src/axes.rs"
      via: "mod axes; declaration"
      pattern: "^mod axes;$"
    - from: "axes.rs::AxisSpec.is_heuristic"
      to: "Phase 9 polar.rs / Phase 10 markdown.rs"
      via: "is_heuristic boolean drives `(heuristic)` suffix + dashed gridline (POLAR-03, DIR-01)"
      pattern: "is_heuristic: true"
---

<objective>
Land `crates/alloc-bench-aggregator/src/axes.rs` — the single source of truth for the 8 measurement axes (`MEASUREMENT_AXES: [AxisSpec; 8]`), the `Direction::{Higher, Lower}` enum, and the `arrow()` glyph helper. Wire the new module into `main.rs` via a single `mod axes;` declaration. No consumers exist in Phase 6 — Phase 7's `score.rs`, Phase 9's `polar.rs`, and Phase 10's `markdown.rs` will consume this registry.

Purpose: Phases 7-10 all depend on a stable, alphabetically keyed axis registry whose `direction` and `is_heuristic` fields drive normalization (Phase 7), spider geometry (Phase 9), and column-header glyphs (Phase 10). Landing it in Phase 6 gives downstream phases a fixed contract to test against.

Output:
- New file `crates/alloc-bench-aggregator/src/axes.rs` (~80 LOC including unit tests)
- One-line edit to `crates/alloc-bench-aggregator/src/main.rs` (alphabetical `mod axes;` insertion)
- Five passing `cargo test -p alloc-bench-aggregator axes::` unit tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-foundations/06-CONTEXT.md
@.planning/phases/06-foundations/06-RESEARCH.md
@./CLAUDE.md

<!-- Existing module ordering / mod declarations to mirror -->
@crates/alloc-bench-aggregator/src/main.rs

<interfaces>
<!-- AxisSpec layout (RESEARCH §axes.rs Design — Claude's Discretion, recommended) -->
<!-- AxisSpec carries four `&'static str` / `Copy` fields; whole struct is `Copy + Clone + Debug`. -->

Required public API surface (see RESEARCH §axes.rs Design for full tables):
- `pub struct AxisSpec` with fields:
    - `pub key: &'static str` — alphabetical sort key, also JSON / template field name
    - `pub label: &'static str` — human-facing label (used by Phase 9/10)
    - `pub direction: Direction` — higher- or lower-is-better
    - `pub is_heuristic: bool` — true → `(heuristic)` suffix + dashed gridline (POLAR-03, DIR-01)
- `pub enum Direction { Higher, Lower }` with derives `Debug, Clone, Copy, PartialEq, Eq`
- `impl Direction { pub const fn arrow(self) -> char { ... } }`
- `pub const MEASUREMENT_AXES: [AxisSpec; 8]` — alphabetical key order

Required key order (alphabetical, RESEARCH §The 8 axes):
1. `"channel_throughput"` — "Channel throughput" — Higher — false
2. `"cpu_bound_throughput"` — "CPU-bound throughput" — Higher — false
3. `"image_size_efficiency"` — "Image-size efficiency" — Higher — true
4. `"memory_fragmentation"` — "Memory / fragmentation" — Lower — false
5. `"multithread_throughput"` — "Multithread throughput" — Higher — false
6. `"resilience"` — "Resilience" — Higher — false
7. `"security_posture"` — "Security posture" — Higher — true
8. `"web_throughput"` — "Web throughput" — Higher — false

Module ordering in `main.rs` (alphabetical, mirror existing pattern at lines 21-26):
- `mod axes;` → inserted between `mod html;` (line 22) and `mod loader;` (line 23)
- `mod diagrams;` → already exists (line 21)
- `mod html;` → already exists (line 22)
- (NEW) `mod axes;`
- `mod loader;` → already exists (line 23)
- `mod markdown;` → already exists (line 24)
- `mod multi_run;` → already exists (line 25)
- `mod recommend;` → already exists (line 26)

Wait — `axes` precedes `diagrams` alphabetically. Correct insertion: place `mod axes;` BEFORE `mod diagrams;` (top of mod block). Verify lexicographic order: a-x-e-s < d-i-a-g-r-a-m-s. Confirmed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create axes.rs with const registry, Direction enum, and 5 unit tests</name>
  <files>crates/alloc-bench-aggregator/src/axes.rs</files>
  <read_first>
    - .planning/phases/06-foundations/06-RESEARCH.md (§axes.rs Design — full table of 8 axes, AxisSpec layout, alphabetical-key static_assert recipe)
    - .planning/phases/06-foundations/06-CONTEXT.md (§Registry Architecture — locked: const not lazy_static, hard-coded Unicode literals, `arrow()` is associated method)
    - crates/alloc-bench-aggregator/src/loader.rs (lines 1-50) — module-level doc comment style to mirror in axes.rs header
    - crates/alloc-bench-aggregator/src/recommend.rs (lines 1-30) — flat-module convention reference for crate root layout
  </read_first>
  <behavior>
    - Test `axes_count_is_exactly_eight`: `assert_eq!(MEASUREMENT_AXES.len(), 8, "the 8-axis spider shape is locked")`
    - Test `axes_keys_are_alphabetically_sorted`: collect `.key` from each spec, compare against `sort_unstable`-cloned vector — equality required (failure message: "MEASUREMENT_AXES must be sorted by `key`")
    - Test `axes_keys_are_unique`: `BTreeSet::<&str>::from_iter(...).len() == MEASUREMENT_AXES.len()` (failure message: "duplicate axis keys")
    - Test `arrow_glyphs_match_unicode_literals`: `Direction::Higher.arrow() == '\u{2191}'` AND `Direction::Lower.arrow() == '\u{2193}'`
    - Test `heuristic_axes_are_image_size_and_security`: filter `.is_heuristic = true`, collect `.key`, equal to `vec!["image_size_efficiency", "security_posture"]` (alphabetical order)
  </behavior>
  <action>
    Create `crates/alloc-bench-aggregator/src/axes.rs` as a new file. Module-level doc comment (top, two `//!` lines) cites Phase 6 / AXES-01 / AXES-02 and notes "Single source of truth for direction-marker glyphs across `score.rs` (Phase 7), `polar.rs` (Phase 9), `markdown.rs` (Phase 10)."

    Public types:
      - `pub enum Direction { Higher, Lower }` — derives `Debug, Clone, Copy, PartialEq, Eq`.
      - `impl Direction { pub const fn arrow(self) -> char }` — match returning `'\u{2191}'` for `Higher` and `'\u{2193}'` for `Lower`. `const fn` is mandatory (RESEARCH §Direction enum + arrow()).
      - `pub struct AxisSpec` — derives `Debug, Clone, Copy`. Fields (all `pub`): `key: &'static str`, `label: &'static str`, `direction: Direction`, `is_heuristic: bool`. NO `weight_hint` field (per RESEARCH §AxisSpec layout — explicit equal weights, deviation deferred to V12-05/V12-07).
      - `pub const MEASUREMENT_AXES: [AxisSpec; 8]` — eight initialiser blocks, alphabetical `key` order, content as listed in `<interfaces>` block above.

    Unit tests inside `#[cfg(test)] mod tests { use super::*; ... }`:
      - Five `#[test]` functions, names verbatim per RESEARCH §Test Strategy Table: `axes_count_is_exactly_eight`, `axes_keys_are_alphabetically_sorted`, `axes_keys_are_unique`, `arrow_glyphs_match_unicode_literals`, `heuristic_axes_are_image_size_and_security`.
      - `axes_keys_are_unique` imports `std::collections::BTreeSet` (NOT `HashSet` — CLAUDE.md byte-identical-iteration discipline; consistent with project conventions even in tests).

    DO NOT depend on `static_assertions` or any other crate. The five `cargo test` assertions are the gate.
    DO NOT add a `weight_hint` field to `AxisSpec` (V12-05/V12-07 deferred).
    DO NOT use `lazy_static!` or `OnceCell` — `pub const` is mandatory per CONTEXT lock.
  </action>
  <verify>
    <automated>cargo test -p alloc-bench-aggregator axes:: --no-fail-fast 2>&1 | tee /tmp/axes-tests.log &amp;&amp; grep -E "test result: ok\\. 5 passed" /tmp/axes-tests.log</automated>
  </verify>
  <acceptance_criteria>
    - File `crates/alloc-bench-aggregator/src/axes.rs` exists.
    - `grep -c "^pub const MEASUREMENT_AXES: \\[AxisSpec; 8\\]" crates/alloc-bench-aggregator/src/axes.rs` returns `1`.
    - `grep -c "pub const fn arrow(self) -> char" crates/alloc-bench-aggregator/src/axes.rs` returns `1`.
    - `grep -c "'\\\\u{2191}'" crates/alloc-bench-aggregator/src/axes.rs` returns at least `1` (Higher arrow).
    - `grep -c "'\\\\u{2193}'" crates/alloc-bench-aggregator/src/axes.rs` returns at least `1` (Lower arrow).
    - `grep -cE "AxisSpec \\{ key: \"" crates/alloc-bench-aggregator/src/axes.rs` returns `8` (eight initialiser entries).
    - `grep -v '^//' crates/alloc-bench-aggregator/src/axes.rs | grep -c "lazy_static\\|OnceCell\\|once_cell"` returns `0` (const-only).
    - `grep -v '^//' crates/alloc-bench-aggregator/src/axes.rs | grep -c "weight_hint"` returns `0` (no weight_hint field per V12 deferral).
    - `cargo test -p alloc-bench-aggregator axes:: 2>&1` reports "5 passed; 0 failed".
  </acceptance_criteria>
  <done>
    Five unit tests pass; file structure, public surface, and Unicode glyphs match the locked decisions. No new dependencies introduced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire `mod axes;` into main.rs and verify crate compiles</name>
  <files>crates/alloc-bench-aggregator/src/main.rs</files>
  <read_first>
    - crates/alloc-bench-aggregator/src/main.rs (lines 21-26 — existing alphabetical `mod` block: diagrams, html, loader, markdown, multi_run, recommend)
    - .planning/phases/06-foundations/06-RESEARCH.md (§Module location and visibility — "`mod axes;` declared in `main.rs` between the existing `mod html;` and `mod loader;` (alphabetical to match existing ordering)" — NOTE: this is incorrect; `axes` precedes `diagrams` alphabetically. Place `mod axes;` BEFORE `mod diagrams;`. The RESEARCH note pre-dates the verification done in this plan's `<interfaces>` block.)
  </read_first>
  <action>
    Insert a single new line `mod axes;` into `crates/alloc-bench-aggregator/src/main.rs` immediately BEFORE `mod diagrams;` (currently line 21). The resulting mod block ordering must be: `axes`, `diagrams`, `html`, `loader`, `markdown`, `multi_run`, `recommend` — strict alphabetical.

    DO NOT add any `pub use axes::...` re-exports in this plan. Phase 7 will reach into `axes::MEASUREMENT_AXES` via `crate::axes::MEASUREMENT_AXES` once it lands its `score.rs`. Reducing surface area now keeps Phase 6's commit minimal.
    DO NOT touch any other lines of `main.rs` in this plan. The `--security` CLI flag and `load_security_metas` wiring are owned by Plan 02 — that plan modifies `main.rs` lines 31-65 and the in-file `tests` module independently of this insertion.

    Coordination note for executor: Plan 02 also modifies `main.rs`. Both plans declare `crates/alloc-bench-aggregator/src/main.rs` in their `files_modified`. The execute-phase orchestrator will assign these plans to different waves IF the orchestrator detects file overlap. If the orchestrator runs them parallel and Plan 02 has merged its `--security` flag changes first, this plan's one-line insertion is conflict-free (different region of the file). If a merge conflict surfaces at execute time, the executor MUST rebase this plan's `mod axes;` insertion on top of Plan 02's CLI changes — the resulting mod block ordering is unchanged.
  </action>
  <verify>
    <automated>cargo build -p alloc-bench-aggregator 2>&amp;1 | tee /tmp/aggregator-build.log &amp;&amp; ! grep -E "error\\[E[0-9]+\\]|warning: unused" /tmp/aggregator-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^mod (axes|diagrams|html|loader|markdown|multi_run|recommend);$" crates/alloc-bench-aggregator/src/main.rs | awk -F: '{print $3}'` returns the seven `mod ...;` lines in this exact order: `mod axes;`, `mod diagrams;`, `mod html;`, `mod loader;`, `mod markdown;`, `mod multi_run;`, `mod recommend;`.
    - `cargo build -p alloc-bench-aggregator` succeeds (exit 0) with no `error[E...]` lines and no `warning: unused import` lines for the new module.
    - `cargo test -p alloc-bench-aggregator --lib --no-run` succeeds (exit 0) — the tests in `axes::tests` are discovered.
    - Existing tests still discoverable: `cargo test -p alloc-bench-aggregator cli_meta_flag_defaults_to_empty_string --no-run` succeeds (proves Plan 01 did not break Plan 02's overlap target).
  </acceptance_criteria>
  <done>
    `mod axes;` line inserted alphabetically; aggregator crate compiles cleanly with no new warnings; all `axes::tests` are discoverable through the workspace test harness.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in Phase 6 Plan 01) | Pure compile-time addition; no I/O, no network, no untrusted input. `axes.rs` exposes only `pub const` data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-T | Tampering | `MEASUREMENT_AXES` const-array semantics | mitigate | Five `cargo test` assertions guard length, alphabetical order, uniqueness, heuristic-flag set, and arrow glyphs. A future contributor reordering or mutating the array breaks at least one of these tests. |
| T-06-01-I | Information Disclosure | (n/a) | accept | No secrets; entirely public static data. |
| T-06-01-D | Denial of Service | (n/a) | accept | Compile-time const, no runtime cost. |

No `[ASSUMED]` / `[SUS]` packages introduced — this plan adds zero dependencies (no new entries to `Cargo.toml` or `Cargo.lock`).
</threat_model>

<verification>
- `cargo test -p alloc-bench-aggregator axes::` → 5 tests pass.
- `cargo build -p alloc-bench-aggregator` → clean build, no warnings introduced by Plan 01.
- `cargo test -p alloc-bench-aggregator` → all pre-existing tests still pass (Plan 01 introduces only additive symbols; nothing depends on `axes` yet).
- `git diff --stat` shows two files touched: `crates/alloc-bench-aggregator/src/axes.rs` (NEW, ~80 LOC) and `crates/alloc-bench-aggregator/src/main.rs` (1 line added).
</verification>

<success_criteria>
1. `crates/alloc-bench-aggregator/src/axes.rs` exists with `pub const MEASUREMENT_AXES: [AxisSpec; 8]` covering the eight required keys in alphabetical order.
2. `Direction::{Higher, Lower}` enum exists; `Direction::Higher.arrow() == '\u{2191}'` and `Direction::Lower.arrow() == '\u{2193}'`.
3. Five `axes::tests::*` tests pass under `cargo test -p alloc-bench-aggregator axes::`.
4. `mod axes;` declared in `main.rs` in strict alphabetical position (before `mod diagrams;`).
5. No new entries in `Cargo.toml` or `Cargo.lock`.
6. Existing aggregator tests (`cargo test -p alloc-bench-aggregator`) all still pass.
</success_criteria>

<output>
On completion, the executor creates `.planning/phases/06-foundations/06-01-SUMMARY.md` documenting:
- Files created / modified (axes.rs, main.rs)
- Test counts (5 new tests passing, all existing tests passing)
- Conventional-commit message used (e.g., `feat(06-01): add MEASUREMENT_AXES registry + Direction enum (AXES-01, AXES-02)`)
- Confirmation that no `output.rs` mutation occurred (decorate-not-rewrite)
</output>
</content>
</invoke>