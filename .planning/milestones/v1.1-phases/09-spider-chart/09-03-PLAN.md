---
phase: 09-spider-chart
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - crates/alloc-bench-aggregator/src/main.rs
  - crates/alloc-bench-aggregator/src/html.rs
  - crates/alloc-bench-aggregator/src/markdown.rs
  - crates/alloc-bench-aggregator/templates/index.html.tmpl
  - crates/alloc-bench-aggregator/templates/recommend-cell.html.tmpl
  - crates/alloc-bench-aggregator/templates/recommend-cell.md.tmpl
  - crates/alloc-bench-aggregator/tests/smoke.rs
autonomous: true
requirements:
  - POLAR-01
  - POLAR-02
  - POLAR-03
  - POLAR-04
  - POLAR-05

user_setup: []

must_haves:
  truths:
    - "Rendered index.html contains a `<div id=\"chart-spider\">` block when at least one CellScore is available, gated by `{{ if has_spider }}` (mirrors `{{ if has_top_n }}`)"
    - "The spider section sits AFTER the Phase 8 `<section class=\"top-n-recommendations\">` block and BEFORE the existing `<script>` block, with verbatim section heading `Top-3 Above the Fold` and verbatim caption"
    - "Plotly trace JSON is server-side rendered into the page via the new `spider_traces_json` and `spider_layout_json` template variables emitted via `{ field | unescaped }`"
    - "html::tests::plotly_sri_hash_unchanged passes (rendered HTML contains the byte-exact `sha384-MqL7Cy3itNqCI1Wlc926K0XhyRKJ/NMqTaytIIEB+QIdInOploxqRIHRKLlhPykM` hash)"
    - "tests/smoke.rs::spider_div_present_when_data_exists passes (rendered HTML contains the literal `<div id=\"chart-spider\"`)"
    - "REPORT.md `## Top 10 cells` summary table gains a final `Pareto` column; cells on the front carry `★` (U+2605); other cells carry empty string — visually blank in the GFM table"
    - "Both per-cell templates (recommend-cell.{md,html}.tmpl) append `{{ if is_pareto }} ★{{ endif }}` after the existing `{{ if suspect_flag }} *(suspect)*{{ endif }}` decoration on the heading line"
    - "The WR-01 sentinel test (`html::tests::cell_templates_both_reference_all_fields`) is updated to include `is_pareto: true` in the synthetic CellRecommendation and to assert `★` appears in BOTH outputs"
    - "main.rs derives `image_sizes: BTreeMap<String, f64>` from `metas: HashMap<(String, String), CellMeta>` keyed by env (max image_size_mb across allocators per env when multiple allocators map to the same env) and threads it through `recommend::top_n_cells` and the html/markdown writers"
  artifacts:
    - path: "crates/alloc-bench-aggregator/src/html.rs"
      provides: "spider context + sentinel-test extension + Plotly SRI test"
      contains: "spider_traces_json"
    - path: "crates/alloc-bench-aggregator/src/markdown.rs"
      provides: "Pareto column on the Top 10 cells summary table"
      contains: "Pareto"
    - path: "crates/alloc-bench-aggregator/templates/index.html.tmpl"
      provides: "<div id=\"chart-spider\"> + <section class=\"spider-chart\"> + Plotly bootstrap"
      contains: "chart-spider"
    - path: "crates/alloc-bench-aggregator/templates/recommend-cell.html.tmpl"
      provides: "{{ if is_pareto }} ★{{ endif }}"
      contains: "is_pareto"
    - path: "crates/alloc-bench-aggregator/templates/recommend-cell.md.tmpl"
      provides: "{{ if is_pareto }} ★{{ endif }}"
      contains: "is_pareto"
    - path: "crates/alloc-bench-aggregator/src/main.rs"
      provides: "image_sizes derivation + plumbing"
      contains: "image_sizes"
    - path: "crates/alloc-bench-aggregator/tests/smoke.rs"
      provides: "plotly_sri_hash_unchanged + spider_div_present_when_data_exists"
      contains: "chart-spider"
  key_links:
    - from: "crates/alloc-bench-aggregator/src/html.rs::render"
      to: "crates/alloc-bench-aggregator/src/polar.rs"
      via: "polar::build_trace + polar::build_reference_trace"
      pattern: "polar::"
    - from: "crates/alloc-bench-aggregator/src/main.rs"
      to: "crates/alloc-bench-aggregator/src/recommend.rs::top_n_cells"
      via: "image_sizes argument"
      pattern: "recommend::top_n_cells\\(.*image_sizes"
    - from: "crates/alloc-bench-aggregator/src/markdown.rs::emit_top_n_cells"
      to: "CellRecommendation::is_pareto"
      via: "summary table column rendering"
      pattern: "is_pareto"
    - from: "crates/alloc-bench-aggregator/templates/index.html.tmpl"
      to: "html.rs::HtmlContext"
      via: "{ spider_traces_json | unescaped }"
      pattern: "spider_traces_json"
---

<objective>
Wire `polar.rs` (Plan 01) and `score::pareto_front` + `is_pareto` (Plan 02) into the rendered HTML/Markdown surfaces. This is the Wave-2 closing plan — it owns every file that imports from polar/score's new APIs.

Five surfaces gain the Phase 9 deliverables:

1. `index.html.tmpl` — new `<section class="spider-chart">` with `<div id="chart-spider">` and inline Plotly bootstrap; gated by `{{ if has_spider }}` (mirrors Phase 8's `{{ if has_top_n }}`).
2. `html.rs` — `HtmlContext` gains `spider_traces_json`, `spider_layout_json`, `has_spider`; `BuiltContext` gains owned-string equivalents; `render` calls `polar::build_trace` per top-3 cell + `polar::build_reference_trace`. The WR-01 sentinel test gains `is_pareto: true` in its synthetic CellRecommendation. `tests/smoke.rs` gains the two new substring assertions for SRI hash + spider div.
3. `markdown.rs::emit_top_n_cells` — the existing `## Top 10 cells` summary table at lines 421-429 gains a final `Pareto` column; cells with `is_pareto == true` render `★` (U+2605), others render empty.
4. `recommend-cell.{md,html}.tmpl` — both per-cell templates append `{{ if is_pareto }} ★{{ endif }}` to the heading line, mirroring the existing `{{ if suspect_flag }} *(suspect)*{{ endif }}` pattern. `CellTemplateContext` (html.rs:154) gains `pub is_pareto: bool`; `build_cell_template_context` copies the field.
5. `main.rs` — derive `image_sizes: BTreeMap<String, f64>` from the existing `metas: HashMap<(String, String), CellMeta>` (project to `env -> image_size_mb` keeping the maximum across allocators when an env has multiple cells with images, matching CONTEXT.md "macOS host excluded" semantics — the host has no image so its env is absent), and thread `image_sizes` through to `recommend::top_n_cells`, `markdown::write` (only via `top_n` since `is_pareto` rides on each cell), and `html::write` (same — `is_pareto` arrives through the `top_n` slice).

Purpose: After Plan 01 + Plan 02 land, the data layer is complete but every consumer is broken at compile time (top_n_cells signature changed; CellRecommendation has a new field unused by templates). Plan 03 closes the loop and lands the visual surface (spider grid + Pareto column) that the milestone judges as Phase 9 success.

Output: A single `cargo test -p alloc-bench-aggregator` invocation passes all tests including the new POLAR-01..05 assertions. `just aggregate` (run against the existing fixtures) produces an `index.html` containing `<div id="chart-spider">` and a `REPORT.md` whose `## Top 10 cells` summary table has a `Pareto` column.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-spider-chart/09-CONTEXT.md
@.planning/phases/09-spider-chart/09-RESEARCH.md
@.planning/phases/09-spider-chart/09-PATTERNS.md
@.planning/phases/09-spider-chart/09-UI-SPEC.md
@.planning/phases/09-spider-chart/09-01-SUMMARY.md
@.planning/phases/09-spider-chart/09-02-SUMMARY.md

<interfaces>
<!-- Post-Plan-01 + Plan-02 surface area available to this plan. -->

From `crates/alloc-bench-aggregator/src/polar.rs` (Plan 01, just shipped):

```rust
pub fn build_trace(score: &CellScore) -> serde_json::Value;
pub fn build_reference_trace(scores: &[CellScore]) -> serde_json::Value;
pub fn axis_label_for_chart(spec: &AxisSpec) -> String;
```

From `crates/alloc-bench-aggregator/src/score.rs` (Plan 02, just shipped):

```rust
pub fn pareto_front(
    cells: &[CellScore],
    image_sizes: &BTreeMap<String, f64>,
) -> BTreeSet<(String, String)>;
```

From `crates/alloc-bench-aggregator/src/recommend.rs` (Plan 02, just shipped):

```rust
pub struct CellRecommendation {
    /* ... existing 11 fields ... */
    pub suspect_flag: bool,
    pub is_pareto: bool,   // <-- Plan 02 added
}

pub fn top_n_cells(
    scores: Vec<CellScore>,
    runs: &[Run],
    image_sizes: &BTreeMap<String, f64>,    // <-- Plan 02 widened
) -> Vec<CellRecommendation>;
```

From the existing `html.rs` (Phase 8, shipped):

```rust
// html.rs:58
pub(crate) const PLOTLY_CDN_URL:  &str = "https://cdn.plot.ly/plotly-2.35.3.min.js";
// html.rs:65-66
pub(crate) const PLOTLY_SRI_HASH: &str = "sha384-MqL7Cy3itNqCI1Wlc926K0XhyRKJ/NMqTaytIIEB+QIdInOploxqRIHRKLlhPykM";

// html.rs:80 — HtmlContext template-binding struct
struct HtmlContext<'a> { /* 12 fields incl. plotly_cdn_url, plotly_sri_hash, top_n_visible, top_n_collapsed, has_top_n */ }

// html.rs:154 — per-cell template context (subset of CellRecommendation)
pub(crate) struct CellTemplateContext {
    pub rank_padded: String,
    pub rank: usize,
    pub alloc: String,
    pub env: String,
    pub tldr: String,
    pub strengths: Vec<&'static str>,
    pub weaknesses: Vec<&'static str>,
    pub recommended_for: Vec<&'static str>,
    pub avoid_for: Vec<&'static str>,
    pub suspect_flag: bool,
    // Phase 9: append `pub is_pareto: bool`
}

// html.rs:177 — build_cell_template_context maps CellRecommendation -> CellTemplateContext
pub(crate) fn build_cell_template_context(cell: &CellRecommendation) -> CellTemplateContext;

// html.rs:215 — entry point
pub fn write(
    outcome: &LoadOutcome,
    _metas: &HashMap<(String, String), CellMeta>,
    top_n: &[CellRecommendation],
    out_dir: &Path,
) -> Result<()>;

// html.rs:346 — internal render fn
fn render(runs: &[Run], top_n: &[CellRecommendation]) -> Result<String>;

// html.rs:553 — WR-01 sentinel test
fn cell_templates_both_reference_all_fields();
```

From the existing `markdown.rs` (Phase 8, shipped):

```rust
// markdown.rs:49
pub fn write(
    outcome: &LoadOutcome,
    metas: &HashMap<(String, String), CellMeta>,
    top_n: &[CellRecommendation],
    out_dir: &Path,
) -> Result<()>;

// markdown.rs:403 — emits the `## Top 10 cells` section incl. summary table at lines 421-429
fn emit_top_n_cells(buf: &mut String, top_n: &[CellRecommendation]) -> Result<()>;
```

Existing index.html.tmpl insertion point (lines 257-266) — the `{{ if has_top_n }}...{{ endif }}` block ends at line 266 with `{{ endif }}</div>`. Plan 03 inserts the new `<section class="spider-chart">` block IMMEDIATELY AFTER the `</section>` close of `top-n-recommendations` and IMMEDIATELY BEFORE the `<script>` tag at line 267.

Existing per-cell template heading lines:
- `recommend-cell.html.tmpl` line 2: `<h3>{rank}. {alloc}/{env}{{ if suspect_flag }} *(suspect)*{{ endif }}</h3>`
- `recommend-cell.md.tmpl` line 1: `### {rank}. {alloc}/{env}{{ if suspect_flag }} *(suspect)*{{ endif }}`

Existing summary-table emit (markdown.rs:421-429):
```rust
let _ = writeln!(buf, "| Rank | Cell | Score |");
let _ = writeln!(buf, "|------|------|-------|");
for cell in top_n.iter() {
    let _ = writeln!(buf, "| {:02} | {} on {} | {:.3} |",
        cell.rank, cell.alloc, cell.env, cell.composite_score);
}
```

Existing tinytemplate literal-`{` escape rule: every `{` inside inline JS/CSS bodies in `index.html.tmpl` must be `\{` (html.rs:21 Pitfall 1). The `tinytemplate_compiles_index_template` test at html.rs:519 catches violations.

Existing main.rs orchestration (lines 56-90):
```rust
fn main() -> Result<()> {
    let cli = Cli::parse();
    let outcome = loader::discover(&cli.input)?;
    let metas = loader::load_cell_metas(&cli.meta)?;
    let security_metas = loader::load_security_metas(&cli.security)?;
    let cell_axes = score::compute_axes(&outcome.runs, &metas, &security_metas);
    let cell_scores = score::score_cells(cell_axes);
    let top_n = recommend::top_n_cells(cell_scores, &outcome.runs);  // <-- needs image_sizes added
    markdown::write(&outcome, &metas, &top_n, out_dir)?;
    html::write(&outcome, &metas, &top_n, out_dir)?;
    /* ... */
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: main.rs image_sizes derivation + recommend::top_n_cells call site fix</name>
  <files>crates/alloc-bench-aggregator/src/main.rs</files>
  <read_first>
    - crates/alloc-bench-aggregator/src/main.rs (full file — main() fn lines 56-97)
    - crates/alloc-bench-aggregator/src/loader.rs (CellMeta struct lines 59-70; load_cell_metas signature line 100; image_size_mb field line 64)
    - .planning/phases/09-spider-chart/09-CONTEXT.md (Pareto-front data flow; macOS host excluded; image_sizes keyed by env)
    - .planning/phases/09-spider-chart/09-RESEARCH.md (§4 Pareto-front Algorithm — image_sizes type)
    - .planning/phases/09-spider-chart/09-PATTERNS.md (main.rs analog; main() orchestration pattern)
    - crates/alloc-bench-aggregator/src/recommend.rs (post-Plan-02 — top_n_cells signature with new image_sizes parameter)
  </read_first>
  <behavior>
    - Test `main::tests::image_sizes_built_from_metas_keyed_by_env`: Build a synthetic `metas: HashMap<(String, String), CellMeta>` with 3 entries: `("ptmalloc","alpine") -> 50.0`, `("jemalloc","alpine") -> 52.0`, `("mimalloc","debian-slim") -> 200.0`. Call the new `build_image_sizes(&metas)` helper. Assert returned `BTreeMap<String, f64>` has exactly 2 entries: `"alpine" -> 52.0` (max across allocators), `"debian-slim" -> 200.0`. Use `(a-b).abs() < 1e-9` for f64 equality.
    - Test `main::tests::image_sizes_excludes_envs_with_no_metas`: With `metas` containing only `("ptmalloc","alpine") -> 50.0`, the returned map has exactly one entry `"alpine"` (no implicit zero entries for any other env).
    - Test `main::tests::image_sizes_empty_metas_returns_empty_map`: `build_image_sizes(&HashMap::new())` returns an empty `BTreeMap`.
  </behavior>
  <action>
    Edit `crates/alloc-bench-aggregator/src/main.rs`:

    1. Add a `use std::collections::BTreeMap;` import at the top (or extend the existing `use std::collections::` if present).
    2. Add a private helper `fn build_image_sizes(metas: &HashMap<(String, String), CellMeta>) -> BTreeMap<String, f64>`. Iterate `metas.iter()`, accumulate by env using the per-env max image_size_mb (per the rationale: when an env appears multiple times across allocators, all those cells share the same Docker image; taking the max is robust to any rounding differences in repeated meta loads). Return the BTreeMap. Add a doc-comment citing `Phase 9 / POLAR-05` and `09-CONTEXT.md §"Pareto-front data flow"` (macOS host has no metas entry → no map entry → cell excluded by `score::pareto_front`).
    3. In `fn main()` (line 56), after `let metas = loader::load_cell_metas(&cli.meta)?;` (line 59), add `let image_sizes = build_image_sizes(&metas);`.
    4. Update the `recommend::top_n_cells` call (currently at line 87) to pass `&image_sizes`: `let top_n = recommend::top_n_cells(cell_scores, &outcome.runs, &image_sizes);`.
    5. Add a `#[cfg(test)] mod tests { ... }` block (or extend the existing one at line 100 — confirm via Read) with the three tests under `<behavior>`. Test helpers can construct `CellMeta` literals directly; only `image_size_mb` matters for the helper output, but the struct requires all fields — set unused fields to zero/empty.

    Run `cargo test -p alloc-bench-aggregator main::tests::image_sizes` to confirm the three new tests pass and the workspace builds.
  </action>
  <verify>
    <automated>cargo test -p alloc-bench-aggregator main::tests::image_sizes && cargo build -p alloc-bench-aggregator</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^fn build_image_sizes' crates/alloc-bench-aggregator/src/main.rs` returns 1.
    - `grep -F 'recommend::top_n_cells(cell_scores, &outcome.runs, &image_sizes)' crates/alloc-bench-aggregator/src/main.rs` returns 1 match.
    - `cargo test -p alloc-bench-aggregator main::tests::image_sizes_built_from_metas_keyed_by_env` passes.
    - `cargo test -p alloc-bench-aggregator main::tests::image_sizes_excludes_envs_with_no_metas` passes.
    - `cargo test -p alloc-bench-aggregator main::tests::image_sizes_empty_metas_returns_empty_map` passes.
    - `cargo build -p alloc-bench-aggregator` succeeds (the only Plan-02-induced compile error in main.rs is now fixed; html.rs/markdown.rs WR-01 sentinel + tests will fail in this task — addressed in Tasks 2/3 below).
  </acceptance_criteria>
  <done>main.rs derives image_sizes from metas and threads it into recommend::top_n_cells; build_image_sizes helper has three unit tests; the workspace builds for the binary path (html.rs and markdown.rs may still fail tests due to is_pareto field on CellRecommendation literals — Tasks 2/3 fix those).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: markdown Pareto column + per-cell template ★ + WR-01 sentinel update</name>
  <files>crates/alloc-bench-aggregator/src/markdown.rs, crates/alloc-bench-aggregator/templates/recommend-cell.md.tmpl, crates/alloc-bench-aggregator/templates/recommend-cell.html.tmpl, crates/alloc-bench-aggregator/src/html.rs</files>
  <read_first>
    - crates/alloc-bench-aggregator/src/markdown.rs (full file — emit_top_n_cells lines 403-470, summary table emit at 421-429)
    - crates/alloc-bench-aggregator/templates/recommend-cell.md.tmpl (current state — heading line 1)
    - crates/alloc-bench-aggregator/templates/recommend-cell.html.tmpl (current state — h3 line 2)
    - crates/alloc-bench-aggregator/src/html.rs (CellTemplateContext lines 154-170, build_cell_template_context lines 177-190, WR-01 sentinel test cell_templates_both_reference_all_fields lines 553-617, cell_template_context_excludes_score_and_axes lines 627-670)
    - .planning/phases/09-spider-chart/09-CONTEXT.md (Pareto column glyph; ★ U+2605)
    - .planning/phases/09-spider-chart/09-PATTERNS.md (per-cell template ★ analog mirroring suspect_flag; markdown.rs::emit_top_n_cells summary-table extension at lines 421-429; WR-01 sentinel-test impact)
    - .planning/phases/09-spider-chart/09-UI-SPEC.md (Pareto column header `Pareto` 6 bytes; ★ glyph U+2605; empty cell renders as single space in GFM)
  </read_first>
  <behavior>
    - Test `markdown::tests::top_n_cells_summary_table_includes_pareto_column_header`: With 3 synthetic CellRecommendations (one with `is_pareto=true`, two with `is_pareto=false`), call `emit_top_n_cells(&mut buf, &top_n)`. Assert `buf.contains("| Rank | Cell | Score | Pareto |")` (4-column header literal).
    - Test `markdown::tests::top_n_cells_summary_table_renders_star_for_pareto_cells`: Same setup. Assert the buf contains a row `| 01 | mimalloc on alpine | 0.900 | ★ |` for the Pareto cell. Assert the buf contains rows like `| 02 | jemalloc on debian-slim | 0.500 |  |` for non-Pareto cells (note the empty cell — single space between the third `|` and fourth `|`).
    - Test `markdown::tests::top_n_cells_summary_table_separator_includes_pareto_column`: Assert `buf.contains("|------|------|-------|--------|")` (4-column separator with the new `--------` segment for the Pareto column).
    - Test `html::tests::cell_templates_both_reference_all_fields_includes_pareto_star`: The WR-01 sentinel test from line 553 — extend the synthetic CellRecommendation to include `is_pareto: true`. Assert BOTH the markdown render and the html render contain the literal `★`. (Modify existing test in place.)
    - Test `html::tests::cell_template_context_excludes_score_and_axes_includes_is_pareto_in_keys`: The existing test from line 627 collects all keys of the serialized JSON of CellTemplateContext. Assert the sorted key list now includes `"is_pareto"` between `"avoid_for"` and `"rank"` (or wherever alphabetical position lands — the test already sorts; just add the new key to the expected list).
  </behavior>
  <action>
    Edit, in this order:

    1. **`crates/alloc-bench-aggregator/templates/recommend-cell.md.tmpl`** (line 1): Append `{{ if is_pareto }} ★{{ endif }}` AFTER the existing `{{ if suspect_flag }} *(suspect)*{{ endif }}` decoration. Final line: `### {rank}. {alloc}/{env}{{ if suspect_flag }} *(suspect)*{{ endif }}{{ if is_pareto }} ★{{ endif }}`. NOTE: the leading space + `★` (3-byte U+2605) glyph is intentional — leading space separates from suspect annotation when both are present; trailing-no-space matches existing suspect_flag idiom.
    2. **`crates/alloc-bench-aggregator/templates/recommend-cell.html.tmpl`** (line 2): Same append: `<h3>{rank}. {alloc}/{env}{{ if suspect_flag }} *(suspect)*{{ endif }}{{ if is_pareto }} ★{{ endif }}</h3>`.
    3. **`crates/alloc-bench-aggregator/src/html.rs`** (CellTemplateContext at line 154): Append `pub is_pareto: bool,` after `pub suspect_flag: bool,` (last field). Update the doc-comment citing `Phase 9 / POLAR-05`.
    4. **`crates/alloc-bench-aggregator/src/html.rs`** (build_cell_template_context at line 177): Append `is_pareto: cell.is_pareto,` after `suspect_flag: cell.suspect_flag,`.
    5. **`crates/alloc-bench-aggregator/src/html.rs`** (WR-01 sentinel test `cell_templates_both_reference_all_fields` at line 553): In the synthetic `CellRecommendation { ... }` literal, append `is_pareto: true,` after `suspect_flag: true,`. In the `expected_in_both` array (around line 590), append the literal `"\u{2605}"` or `"★"` (must match the glyph emitted by the template — UTF-8 byte sequence for U+2605 is `0xE2 0x98 0x85`, but Rust string literals just use `★` directly). Run the test; it must assert ★ appears in BOTH the markdown render and the html render (mirror the existing `*(suspect)*` assertion logic at lines 588-601 — extend, don't replace).
    6. **`crates/alloc-bench-aggregator/src/html.rs`** (`cell_template_context_excludes_score_and_axes` at line 627): In the synthetic CellRecommendation literal (lines 644+), append `is_pareto: false,` after `suspect_flag: false,`. In the expected sorted-keys list (around lines 660-668), insert `"is_pareto",` in the correct alphabetical position relative to the existing keys (verify the existing list — typically between `"env"` and `"rank"` or between some other adjacent pair).
    7. **`crates/alloc-bench-aggregator/src/markdown.rs`** (emit_top_n_cells at lines 421-429): Replace the 3-column header + separator + row format. New header: `let _ = writeln!(buf, "| Rank | Cell | Score | Pareto |");`. New separator: `let _ = writeln!(buf, "|------|------|-------|--------|");`. New row format inside the for-loop: `let pareto_glyph = if cell.is_pareto { "\u{2605}" } else { "" };` then `let _ = writeln!(buf, "| {:02} | {} on {} | {:.3} | {} |", cell.rank, cell.alloc, cell.env, cell.composite_score, pareto_glyph);`. Use the literal `\u{2605}` (Rust unicode escape — 3 bytes UTF-8: `0xE2 0x98 0x85`) for the glyph; do NOT use the `★` literal in source — `\u{2605}` survives editor encoding mishaps and is what CONTEXT.md "Specifics" specifies.
    8. Add the three new `markdown::tests` from `<behavior>` to the existing `mod tests` block in markdown.rs.

    Run `cargo test -p alloc-bench-aggregator markdown::tests:: html::tests::cell_templates_both_reference_all_fields html::tests::cell_template_context_excludes_score_and_axes` to confirm:
    - The 3 new markdown tests pass.
    - The 2 modified html sentinel tests still pass.
    - All 13 pre-existing markdown::tests still pass (regression gate).
  </action>
  <verify>
    <automated>cargo test -p alloc-bench-aggregator markdown::tests:: html::tests::cell_templates html::tests::cell_template_context</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '| Rank | Cell | Score | Pareto |' crates/alloc-bench-aggregator/src/markdown.rs` returns 1 match (header literal).
    - `grep -F '|------|------|-------|--------|' crates/alloc-bench-aggregator/src/markdown.rs` returns 1 match (separator literal).
    - `grep -F '\u{2605}' crates/alloc-bench-aggregator/src/markdown.rs` returns ≥1 match.
    - `grep -F 'is_pareto' crates/alloc-bench-aggregator/templates/recommend-cell.md.tmpl` returns ≥1 match.
    - `grep -F 'is_pareto' crates/alloc-bench-aggregator/templates/recommend-cell.html.tmpl` returns ≥1 match.
    - `grep -F 'pub is_pareto: bool' crates/alloc-bench-aggregator/src/html.rs` returns 1 match (CellTemplateContext field).
    - `grep -F 'is_pareto: cell.is_pareto' crates/alloc-bench-aggregator/src/html.rs` returns 1 match (build_cell_template_context line).
    - `cargo test -p alloc-bench-aggregator markdown::tests::top_n_cells_summary_table_includes_pareto_column_header` passes.
    - `cargo test -p alloc-bench-aggregator markdown::tests::top_n_cells_summary_table_renders_star_for_pareto_cells` passes.
    - `cargo test -p alloc-bench-aggregator markdown::tests::top_n_cells_summary_table_separator_includes_pareto_column` passes.
    - `cargo test -p alloc-bench-aggregator html::tests::cell_templates_both_reference_all_fields` passes (WR-01 sentinel updated for is_pareto).
    - `cargo test -p alloc-bench-aggregator html::tests::cell_template_context_excludes_score_and_axes` passes (sorted keys include is_pareto).
    - All 13 pre-existing markdown::tests pass: `cargo test -p alloc-bench-aggregator markdown::tests::` returns 0 failures.
  </acceptance_criteria>
  <done>The Pareto column is wired through both summary table (markdown.rs) and per-cell card heading (both templates); WR-01 sentinel test confirms ★ surfaces in BOTH outputs; CellTemplateContext carries is_pareto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: html.rs spider context + index.html.tmpl section + Plotly SRI smoke tests</name>
  <files>crates/alloc-bench-aggregator/src/html.rs, crates/alloc-bench-aggregator/templates/index.html.tmpl, crates/alloc-bench-aggregator/tests/smoke.rs</files>
  <read_first>
    - crates/alloc-bench-aggregator/src/html.rs (full file — HtmlContext lines 80-136, BuiltContext lines 242-251, build_context, render lines 346-398, write lines 215+, PLOTLY consts lines 58-66, to_script_safe_json lines 266-272, tinytemplate_compiles_index_template at 519)
    - crates/alloc-bench-aggregator/templates/index.html.tmpl (full file — Plotly script tag lines 17-19, top-n-recommendations section lines 257-266, Plotly bootstrap inline script lines 267+)
    - crates/alloc-bench-aggregator/tests/smoke.rs (existing assertions lines 122-138 for SRI prefix; existing chart-builder substring tests; existing run_aggregator_against_fixtures helper)
    - crates/alloc-bench-aggregator/src/polar.rs (post-Plan-01 — build_trace, build_reference_trace, axis_label_for_chart)
    - .planning/phases/09-spider-chart/09-CONTEXT.md (Layout & Plotly Configuration; Section title and order)
    - .planning/phases/09-spider-chart/09-RESEARCH.md (§5 Module wiring; §6 tests 6/7)
    - .planning/phases/09-spider-chart/09-PATTERNS.md (HtmlContext field-add pattern; index.html.tmpl insertion location; tinytemplate `\{` escape rule)
    - .planning/phases/09-spider-chart/09-UI-SPEC.md (Section structure HTML; Per-chart Plotly layout; CSS skeleton; Cross-Surface Byte-Identity Contract)
  </read_first>
  <behavior>
    - Test `html::tests::plotly_sri_hash_unchanged`: Build the standard test fixture and call `render(...)`. Assert the rendered HTML contains the exact byte sequence `sha384-MqL7Cy3itNqCI1Wlc926K0XhyRKJ/NMqTaytIIEB+QIdInOploxqRIHRKLlhPykM` AND the literal `https://cdn.plot.ly/plotly-2.35.3.min.js`. Comment-cite the curl re-verification command per CONTEXT.md.
    - Test `html::tests::spider_section_present_when_top_n_non_empty`: Render with at least one CellRecommendation. Assert the rendered HTML contains `<section class="spider-chart">`, `<h2>Top-3 Above the Fold</h2>`, the verbatim caption `Spider charts of the top-3 cells across 8 normalized axes (0-1). Grey reference polygon = mean across all 18 cells.`, and `<div id="chart-spider"`.
    - Test `html::tests::spider_section_absent_when_top_n_empty`: Render with empty `top_n: &[]`. Assert the rendered HTML does NOT contain `<section class="spider-chart">` (gated by `{{ if has_spider }}`).
    - Test `html::tests::spider_traces_json_contains_scatterpolar_type`: Render with a top-3 CellRecommendation set. Assert the rendered HTML contains the literal `"type":"scatterpolar"` (serde_json default formatting; escape via to_script_safe_json preserves byte identity for non-bracket characters).
    - Test `html::tests::tinytemplate_compiles_index_template` (existing test at line 519 — must continue to pass; the spider section's inline JS must escape `{` as `\{`).
    - Test `tests::smoke::spider_div_present_when_data_exists` (in `tests/smoke.rs`): Run aggregator against fixtures (existing helper). Assert `html.contains(r#"<div id="chart-spider""#)`.
    - Test `tests::smoke::plotly_sri_hash_unchanged_full_string` (in `tests/smoke.rs`): Run aggregator. Assert `html.contains("sha384-MqL7Cy3itNqCI1Wlc926K0XhyRKJ/NMqTaytIIEB+QIdInOploxqRIHRKLlhPykM")` (full hash literal — extends the existing prefix-only check at smoke.rs:127). Comment-cite the curl re-verification command.
  </behavior>
  <action>
    Edit, in this order:

    1. **`crates/alloc-bench-aggregator/src/html.rs`** (HtmlContext struct at lines 80-136): Append three new fields:
       - `spider_traces_json: &'a str` — pre-serialized JSON array of trace objects (top-3 cell traces + matrix-mean reference trace). Rendered via `{ spider_traces_json | unescaped }`.
       - `spider_layout_json: &'a str` — pre-serialized JSON layout object (radialaxis range [0,1], angularaxis tickfont, etc.). Rendered via `{ spider_layout_json | unescaped }`.
       - `has_spider: bool` — gates the `<section class="spider-chart">` block via `{{ if has_spider }}`. Set to `!top_n.is_empty()` (mirror `has_top_n`).
       Doc-comment each field citing `Phase 9 / POLAR-01..04`.
    2. **`crates/alloc-bench-aggregator/src/html.rs`** (BuiltContext at lines 242-251): Append two owned-string fields: `spider_traces: String`, `spider_layout: String`. Doc-comment as `Phase 9 spider chart pre-serialized JSON`.
    3. **`crates/alloc-bench-aggregator/src/html.rs`** (build_context fn): After existing JSON serializations, derive `image_sizes` (NOT NEEDED here — build_context only consumes `runs`; spider chart needs `top_n` + an upstream `cell_scores` slice; resolve below). Pass through.
    4. **`crates/alloc-bench-aggregator/src/html.rs`** (render fn at line 346): The render fn currently takes `(runs, top_n)`. Widen its signature to `fn render(runs: &[Run], top_n: &[CellRecommendation], cell_scores: &[CellScore]) -> Result<String>`. Inside render: take the first `TOP_N_SPIDER` cells of `cell_scores` (use `recommend::TOP_N_SPIDER` const). Build `spider_traces: Vec<serde_json::Value>` by `polar::build_reference_trace(cell_scores)` first (FIRST in the trace array per CONTEXT.md decision — reference renders BEHIND the cell polygons), THEN one `polar::build_trace(score)` per top-3 cell. Serialize via `to_script_safe_json(&spider_traces)`. Build `spider_layout: serde_json::Value` per UI-SPEC §"Per-chart Plotly layout" — a single layout object reused for all 3 charts: `serde_json::json!({ "polar": { "radialaxis": { "range": [0, 1], "visible": true, "showticklabels": false, "gridcolor": "#E1E4E8", "linecolor": "#E1E4E8" }, "angularaxis": { "tickfont": { "size": 11, "color": "#666" }, "gridcolor": "#E1E4E8", "linecolor": "#E1E4E8" }, "bgcolor": "#ffffff" }, "showlegend": false, "margin": { "l": 30, "r": 30, "t": 40, "b": 30 }, "autosize": true, "paper_bgcolor": "#ffffff", "plot_bgcolor": "#ffffff" })`. Serialize layout via `to_script_safe_json`. Set `has_spider: !top_n.is_empty()`. Pass all into HtmlContext literal.
    5. **`crates/alloc-bench-aggregator/src/html.rs`** (write fn at line 215): The public `pub fn write` currently calls `render(&outcome.runs, top_n)`. Widen it to take `cell_scores: &[CellScore]` as a new parameter. Pass through to render. Also update the per-cell fragment loop (around line 227) — no changes needed there, the per-cell template's `is_pareto` is sourced from each `CellRecommendation`.
    6. **`crates/alloc-bench-aggregator/src/main.rs`** (closing the loop from Task 1): Pass `&cell_scores` to `html::write`. Update the call site `html::write(&outcome, &metas, &top_n, out_dir)?;` to `html::write(&outcome, &metas, &top_n, &cell_scores, out_dir)?;`. NOTE: this Task touches main.rs again — that's expected; Task 1 owned the `image_sizes` derivation, this Task owns the `cell_scores` plumbing. NO conflict because both are sequential edits to the same file in the same plan execution.

       WAIT — `cell_scores` is consumed by `recommend::top_n_cells(cell_scores, &outcome.runs, &image_sizes)` which moves it (signature is `Vec<CellScore>`, takes by value). Either: (a) clone before passing to top_n_cells: `let scores_for_spider = cell_scores.clone(); let top_n = recommend::top_n_cells(cell_scores, ...);` then pass `&scores_for_spider` to html::write. Or (b) widen `top_n_cells` to take `&[CellScore]` (more invasive — touches Plan 02 surface). Resolve via (a) clone-before-move in main.rs — keeps Plan 02's signature unchanged and preserves the semantic that `top_n_cells` needs the full vec for its top-N truncation.

       Final sequence in main.rs:
       ```
       let cell_scores_full = score::score_cells(cell_axes);
       let scores_for_spider: Vec<CellScore> = cell_scores_full.clone();
       let top_n = recommend::top_n_cells(cell_scores_full, &outcome.runs, &image_sizes);
       markdown::write(&outcome, &metas, &top_n, out_dir)?;
       html::write(&outcome, &metas, &top_n, &scores_for_spider, out_dir)?;
       ```
    7. **`crates/alloc-bench-aggregator/templates/index.html.tmpl`**: Insert a new section IMMEDIATELY AFTER line 266 (end of `{{ if has_top_n }}...{{ endif }}</div>`) and BEFORE line 267 (`<script>`). The new section:

       ```
       {{ if has_spider }}  <section class="spider-chart">
           <h2>Top-3 Above the Fold</h2>
           <p>Spider charts of the top-3 cells across 8 normalized axes (0-1). Grey reference polygon = mean across all 18 cells.</p>
           <div id="chart-spider" class="spider-grid"></div>
           <script>
             Plotly.newPlot('chart-spider',
               { spider_traces_json | unescaped },
               { spider_layout_json | unescaped },
               \{ responsive: true });
           </script>
         </section>
       {{ endif }}
       ```

       CRITICAL: the `\{ responsive: true }` literal `{` MUST be escaped as `\{` per html.rs:21 Pitfall 1 — the `tinytemplate_compiles_index_template` test catches violations. The `{ spider_traces_json | unescaped }` and `{ spider_layout_json | unescaped }` are tinytemplate substitutions (NOT JS object literals).

       ALSO: append CSS rules for `.spider-chart`, `.spider-grid`, `.spider-cell` to the `<style>` block per UI-SPEC §"CSS skeleton" — placed after the existing `.report-mirror` rules (around UI-SPEC line 214 reference). Every `{` inside CSS bodies must be `\{`.
    8. **`crates/alloc-bench-aggregator/tests/smoke.rs`**: Append the two new tests under `<behavior>`. Use the existing `run_aggregator_against_fixtures()` helper at lines 259-271. Comment-cite the curl re-verification command in the SRI test body per CONTEXT.md.
    9. **`crates/alloc-bench-aggregator/src/html.rs`** (existing tests `cell_templates_both_reference_all_fields`, `cell_template_context_excludes_score_and_axes`, `tinytemplate_compiles_index_template` at lines 553/627/519): These were updated in Task 2 for `is_pareto`. Verify they all still pass after this task's CSS + section additions — `tinytemplate_compiles_index_template` is the load-bearing gate against unescaped `{` literals.
    10. Add the four new `html::tests` defined in `<behavior>` to mod tests.

    Run `cargo test -p alloc-bench-aggregator` (full crate) to confirm:
    - All polar::tests pass (Plan 01 deliverables).
    - All score::tests + recommend::tests + markdown::tests + html::tests pass (Plans 02 + 03 deliverables).
    - tests/smoke.rs passes (the two new tests + all existing).

    Then run `just aggregate` against the existing fixtures to manually inspect: `report/index.html` contains the spider section; `report/REPORT.md` `## Top 10 cells` summary table has the Pareto column. Open `report/index.html` in a browser and confirm Plotly renders 3 spider charts with reference polygon overlay.
  </action>
  <verify>
    <automated>cargo test -p alloc-bench-aggregator && cargo build -p alloc-bench-aggregator --release</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'spider_traces_json' crates/alloc-bench-aggregator/src/html.rs` returns ≥3 matches (HtmlContext field, BuiltContext owned string, render assignment).
    - `grep -F 'spider_traces_json' crates/alloc-bench-aggregator/templates/index.html.tmpl` returns 1 match (substitution site).
    - `grep -F 'has_spider' crates/alloc-bench-aggregator/src/html.rs` returns ≥2 matches (HtmlContext field + render assignment).
    - `grep -F '{{ if has_spider }}' crates/alloc-bench-aggregator/templates/index.html.tmpl` returns 1 match.
    - `grep -F 'chart-spider' crates/alloc-bench-aggregator/templates/index.html.tmpl` returns 1 match.
    - `grep -F 'Top-3 Above the Fold' crates/alloc-bench-aggregator/templates/index.html.tmpl` returns 1 match (verbatim section heading).
    - `grep -F 'mean across all 18 cells' crates/alloc-bench-aggregator/templates/index.html.tmpl` returns 1 match (verbatim caption byte stability).
    - `grep -F '\{ responsive: true })' crates/alloc-bench-aggregator/templates/index.html.tmpl` returns 1 match (escape verified).
    - `grep -F 'polar::build_trace' crates/alloc-bench-aggregator/src/html.rs` returns ≥1 match.
    - `grep -F 'polar::build_reference_trace' crates/alloc-bench-aggregator/src/html.rs` returns ≥1 match.
    - `cargo test -p alloc-bench-aggregator html::tests::plotly_sri_hash_unchanged` passes.
    - `cargo test -p alloc-bench-aggregator html::tests::spider_section_present_when_top_n_non_empty` passes.
    - `cargo test -p alloc-bench-aggregator html::tests::spider_section_absent_when_top_n_empty` passes.
    - `cargo test -p alloc-bench-aggregator html::tests::spider_traces_json_contains_scatterpolar_type` passes.
    - `cargo test -p alloc-bench-aggregator html::tests::tinytemplate_compiles_index_template` passes (existing test still green — `\{` escape rule honored).
    - `cargo test -p alloc-bench-aggregator --test smoke spider_div_present_when_data_exists` passes.
    - `cargo test -p alloc-bench-aggregator --test smoke plotly_sri_hash_unchanged_full_string` passes.
    - `cargo test -p alloc-bench-aggregator` passes with 0 failures (full crate including all integration tests).
    - `cargo build -p alloc-bench-aggregator --release` succeeds.
  </acceptance_criteria>
  <done>The spider chart section renders in index.html when top_n is non-empty; Plotly trace JSON is server-inlined via the new template variables; the SRI hash is pinned by smoke tests; all polar/score/recommend/markdown/html/smoke tests pass; cargo build --release succeeds.</done>
</task>

</tasks>

<verification>
- `cargo test -p alloc-bench-aggregator` reports 0 failures across all modules + integration tests.
- `cargo build -p alloc-bench-aggregator --release` succeeds (LTO=fat per CLAUDE.md performance build flags).
- `just aggregate` produces `report/index.html` containing the literal `<div id="chart-spider"` AND `report/REPORT.md` containing the literal `| Pareto |` column header on the `## Top 10 cells` summary table.
- The `## Recommendations by workload` table (markdown.rs:374) is UNCHANGED — Pareto column does NOT land there because that table is class-keyed (no `(alloc, env)` field per PATTERNS.md disambiguation).
- The Plotly SRI hash literal `sha384-MqL7Cy3itNqCI1Wlc926K0XhyRKJ/NMqTaytIIEB+QIdInOploxqRIHRKLlhPykM` and CDN URL `https://cdn.plot.ly/plotly-2.35.3.min.js` are pinned by smoke tests.
- The WR-01 sentinel test confirms `★` surfaces in BOTH per-cell markdown card AND per-cell html card — drift-defense gate.
- `image_sizes` is derived once in `main.rs` and threaded through `recommend::top_n_cells`; no second derivation site.
</verification>

<success_criteria>
1. `cargo test polar::tests::trace_closes_polygon_with_9_elements` passes (POLAR-01 / POLAR-02).
2. `cargo test html::tests::plotly_sri_hash_unchanged` AND `cargo test --test smoke plotly_sri_hash_unchanged_full_string` pass (POLAR-04).
3. Opening `report/index.html` shows three small-multiples spider charts above the per-scenario chart blocks, each with a grey 25%-alpha reference polygon overlay (POLAR-02 / POLAR-04).
4. The angular tick labels for `image_size_efficiency` and `security_posture` carry the ` (heuristic)` suffix; the other six axes carry plain labels (POLAR-03).
5. The `## Top 10 cells` summary table in REPORT.md has a final `Pareto` column with `★` glyphs on Pareto-front cells; the per-cell card headings carry `★` after the existing `*(suspect)*` annotation when on the front (POLAR-05).
</success_criteria>

<output>
Create `.planning/phases/09-spider-chart/09-03-SUMMARY.md` when done. Cite: tests added (count + names), the four template/source surfaces touched (html.rs, markdown.rs, index.html.tmpl, recommend-cell.{md,html}.tmpl), the verbatim section heading + caption strings (byte-stability inputs to Phase 11), the SRI hash literal pinned by smoke tests, and confirmation that `just aggregate` produces both the spider div in index.html and the Pareto column in REPORT.md.
</output>
