---
phase: 10-direction-markers
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - crates/alloc-bench-aggregator/src/html.rs
  - crates/alloc-bench-aggregator/templates/index.html.tmpl
autonomous: true
requirements:
  - DIR-03
  - DIR-04
must_haves:
  truths:
    - "User opens report/index.html and finds every Plotly chart's measurement axis label injected from axes.rs via { axis_label_* } template placeholders carrying the same ↑/↓ glyphs as REPORT.md (single source of truth — no hard-coded direction markers in index.html.tmpl). Throughput chart yaxis: 'throughput ↑ (per scenario unit, see scenario.unit)'. Latency heatmap colorbar: 'latency ↓ (ns)'. RSS chart yaxis: 'RSS ↓ (kB)'. A/B comparison chart yaxis is unchanged (bidirectional delta — does not fit a single arrow per CONTEXT D-claude-discretion-2)."
    - "User reads index.html source and finds every direction-marker glyph wrapped in <span aria-label=\"higher is better\">↑</span> (or aria-label=\"lower is better\" for ↓) for WCAG 2.1 SC 1.3.3 screen-reader accessibility. The wrapper exists for both server-side-rendered axis-title strings AND for the dashboard table headers populated client-side from the report_table_headers_json template var."
  artifacts:
    - path: "crates/alloc-bench-aggregator/src/html.rs"
      provides: "HtmlContext + BuiltContext gain four new owned-string fields: axis_label_throughput_yaxis, axis_label_latency_colorbar, axis_label_rss_yaxis, report_table_headers_json. build_context populates them by calling axes::column_header_with_arrow and JSON-encoding the dashboard table-header array. ARIA-label wrapping happens at HtmlContext build time (server-side) for axis-title strings; the JS fallback in index.html.tmpl handles the table headers parsed from JSON."
      contains: "axis_label_throughput_yaxis"
    - path: "crates/alloc-bench-aggregator/templates/index.html.tmpl"
      provides: "Hard-coded chart-axis title literals at lines 576, 605, 677 are replaced with tinytemplate placeholders { axis_label_throughput_yaxis } / { axis_label_latency_colorbar } / { axis_label_rss_yaxis }. Line 742 (A/B yaxis) is intentionally unchanged. Line 851 dashboard table-header literal array is replaced with JSON.parse('{report_table_headers_json}') plus a small JS aria-wrapping pass. Line 742 stays plain per CONTEXT D-claude-discretion-2."
      contains: "axis_label_throughput_yaxis"
  key_links:
    - from: "crates/alloc-bench-aggregator/src/html.rs (build_context)"
      to: "crates/alloc-bench-aggregator/src/axes.rs::column_header_with_arrow"
      via: "use crate::axes::{column_header_with_arrow, Direction}"
      pattern: "column_header_with_arrow\\("
    - from: "crates/alloc-bench-aggregator/templates/index.html.tmpl"
      to: "crates/alloc-bench-aggregator/src/html.rs::HtmlContext"
      via: "tinytemplate placeholder substitution { axis_label_throughput_yaxis } { axis_label_latency_colorbar } { axis_label_rss_yaxis } { report_table_headers_json }"
      pattern: "\\{ axis_label_"
    - from: "rendered index.html ↑/↓ glyphs"
      to: "<span aria-label=\"higher is better\">↑</span> / <span aria-label=\"lower is better\">↓</span> wrappers"
      via: "server-side wrap in build_context for axis-title strings + client-side wrap-on-render for table headers"
      pattern: "aria-label=\"higher is better\"|aria-label=\"lower is better\""
---

<objective>
Wire the HTML surface (Plotly chart axes + dashboard table headers in `report/index.html`) to consume `axes::column_header_with_arrow` from Plan 10-01. Inject the resulting strings via four new tinytemplate placeholders in `index.html.tmpl`, and wrap every `↑`/`↓` glyph in `<span aria-label="…">` for WCAG 2.1 SC 1.3.3 conformance.

Purpose: Mirror the Markdown surface's direction markers on the HTML surface using the same single-source helper — no hard-coded glyphs in the template, no client-side glyph derivation, no parallel format strings. Satisfy a11y at the same time so screen readers convey direction semantics.

Output: Updated `HtmlContext` + `BuiltContext` structs in `html.rs`, modified `build_context` populating four new fields, four substitutions in `index.html.tmpl` (lines 576, 605, 677, 851), and one new test gating the a11y wrapping invariant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-direction-markers/10-CONTEXT.md
@.planning/phases/10-direction-markers/10-UI-SPEC.md
@.planning/phases/10-direction-markers/10-01-PLAN.md
@crates/alloc-bench-aggregator/src/axes.rs
@crates/alloc-bench-aggregator/src/html.rs
@crates/alloc-bench-aggregator/templates/index.html.tmpl

<interfaces>
<!-- Helper from Plan 10-01 (axes.rs) — Wave 1 prerequisite. -->

From crates/alloc-bench-aggregator/src/axes.rs:
```rust
pub fn column_header_with_arrow(label: &str, dir: Direction) -> String;
// Returns "throughput ↑", "p99 ↓", etc.
```

This plan ADDS to `html.rs` (HtmlContext + BuiltContext mirror each other — non-owned `&str` ↔ owned `String`):

```rust
// HtmlContext (borrowed):
pub axis_label_throughput_yaxis: &'a str,
pub axis_label_latency_colorbar: &'a str,
pub axis_label_rss_yaxis: &'a str,
pub report_table_headers_json: &'a str,   // JSON array literal: ["allocator", "throughput ↑", ...]

// BuiltContext (owned):
pub axis_label_throughput_yaxis: String,
pub axis_label_latency_colorbar: String,
pub axis_label_rss_yaxis: String,
pub report_table_headers_json: String,
```

The four new template placeholders in `index.html.tmpl`:
- `{ axis_label_throughput_yaxis }` at line 576 — replaces literal `'throughput (per scenario unit, see scenario.unit)'`
- `{ axis_label_latency_colorbar }` at line 605 — replaces literal `'latency (ns)'`
- `{ axis_label_rss_yaxis }` at line 677 — replaces literal `'RSS (kB)'`
- `{ report_table_headers_json }` at line 851 — replaces literal `['allocator', 'throughput', 'p50', 'p95', 'p99', 'p999', 'peak RSS']` array

Line 742 (A/B comparison `% delta (B vs A)`) is intentionally NOT changed — bidirectional delta does not fit a single arrow direction (per CONTEXT D-claude-discretion-2).
</interfaces>

<conventions>
- Tinytemplate placeholders use `{ name }` with single spaces; literal `{` in CSS/JS bodies must be escaped as `\{` (project convention used throughout `index.html.tmpl`).
- Aggregator decorate-not-rewrite — v1 input schema is locked. This plan only changes the HTML emit path.
- Plotly v2.35.3 is pinned by SRI hash test (Phase 9) — Plotly accepts HTML in axis-title strings, so embedding `<span aria-label="…">↑</span>` inside `axis_label_throughput_yaxis` is supported and verified. Phase 9 already established this pattern via `polar.rs::axis_label_for_chart` carrying the `(heuristic)` HTML suffix.
- WCAG 2.1 SC 1.3.3 (Sensory Characteristics): a non-text identifier (the arrow glyph) cannot be the only conveyance of meaning — `<span aria-label="…">` provides the text alternative for screen readers.
- ARIA-label literals (verbatim — used in tests):
  - `aria-label="higher is better"` for `↑`
  - `aria-label="lower is better"` for `↓`
- Conventional-commit prefix: `feat(10-02)` for the html.rs + template changes, `test(10-02)` for the a11y test commit, or combined `feat(10-02): wire direction markers + a11y on HTML surface`.
</conventions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend HtmlContext + build_context with arrow-decorated axis labels and JSON header array</name>
  <files>crates/alloc-bench-aggregator/src/html.rs</files>
  <read_first>
    - crates/alloc-bench-aggregator/src/html.rs (lines 80-310 — HtmlContext struct definition + BuiltContext struct + how build_context populates them; line 535 onward — render fn)
    - crates/alloc-bench-aggregator/src/axes.rs (verify column_header_with_arrow + Direction are exported and importable from html.rs)
    - crates/alloc-bench-aggregator/templates/index.html.tmpl lines 570-700, 845-860 (target replacement zones)
  </read_first>
  <behavior>
    - Test 1 (extend an existing render test or add a small one): `BuiltContext` fields `axis_label_throughput_yaxis`, `axis_label_latency_colorbar`, `axis_label_rss_yaxis`, `report_table_headers_json` are populated by `build_context` such that:
      - `axis_label_throughput_yaxis` equals exactly `throughput <span aria-label="higher is better">↑</span> (per scenario unit, see scenario.unit)` (glyph-after-noun per UI-SPEC §Layout & Interaction line 213 + §Copywriting Contract row)
      - `axis_label_latency_colorbar` equals exactly `latency <span aria-label="lower is better">↓</span> (ns)`
      - `axis_label_rss_yaxis` equals exactly `RSS <span aria-label="lower is better">↓</span> (kB)`
      - `report_table_headers_json` is a valid JSON array literal: `["allocator","throughput ↑","p50 ↓","p95 ↓","p99 ↓","p999 ↓","peak RSS ↓"]` — note the JSON-encoded list contains the plain glyph; aria-wrapping for table headers happens client-side in the JS at index.html.tmpl line 851 (Task 2)
    - Test 2 (`html::tests::aria_labels_wrap_direction_marker_glyphs`): full rendered `index.html` output is scanned via regex; every `↑` glyph is preceded by `<span aria-label="higher is better">` and followed by `</span>`; every `↓` glyph is preceded by `<span aria-label="lower is better">` and followed by `</span>`. The test fails if any bare `↑`/`↓` exists outside an aria-span wrapper (the only exception is the literals embedded inside the JSON `report_table_headers_json` string — those get wrapped at JS render time, the test must account for this by either (a) excluding the `<script>` block from the scan or (b) asserting the JSON array contains plain glyphs AND the JS code at line 851-onward contains the aria-wrap logic).
  </behavior>
  <action>
    Modify `html.rs` (existing struct definitions and `build_context` function):

    1. **Add import** at top of `html.rs`: `use crate::axes::{column_header_with_arrow, Direction};` (extending existing `use crate::axes::*` if present; alphabetical order).

    2. **Extend `HtmlContext` struct** (lines 84-162) with four borrowed-string fields:

       ```
       pub axis_label_throughput_yaxis: &'a str,
       pub axis_label_latency_colorbar: &'a str,
       pub axis_label_rss_yaxis: &'a str,
       pub report_table_headers_json: &'a str,
       ```

       Place them adjacent to other axis-label / chart-config fields (existing organizational pattern: chart-related fields grouped together).

    3. **Extend `BuiltContext` struct** (lines 301-310) with the owned-string analogs (same names, `String` type). Mirror is required so `HtmlContext` borrows from `BuiltContext`'s strings at `render` time.

    4. **Update `build_context`** to populate the four new fields. Build-order convention: compute the plain `column_header_with_arrow(...)` strings first, then wrap with aria-spans for the axis labels; build the JSON array using `serde_json::to_string` on a `Vec<String>` of plain glyph headers (NOT pre-wrapped — the JS at template line 851 wraps each entry at render time).

       ```
       // Plain glyph strings via the axes::column_header_with_arrow SSoT helper:
       let throughput_plain = column_header_with_arrow("throughput", Direction::Higher);
       let latency_plain    = column_header_with_arrow("latency",    Direction::Lower);
       let rss_plain        = column_header_with_arrow("RSS",        Direction::Lower);
       let p50_plain        = column_header_with_arrow("p50",        Direction::Lower);
       let p95_plain        = column_header_with_arrow("p95",        Direction::Lower);
       let p99_plain        = column_header_with_arrow("p99",        Direction::Lower);
       let p999_plain       = column_header_with_arrow("p999",       Direction::Lower);
       let peak_rss_plain   = column_header_with_arrow("peak RSS",   Direction::Lower);

       // Server-side wrap with aria-span for axis-title strings (Plotly accepts HTML here):
       // Glyph-after-noun ordering per UI-SPEC §Layout & Interaction line 213 example
       // ("throughput <span aria-label=...>↑</span> (per scenario unit)") and §Copywriting
       // Contract row. UI-SPEC was approved 2026-05-29 (6/6 PASS); this plan honors the
       // locked example. Aria-label phrases ("higher is better" / "lower is better") are
       // verbatim from UI-SPEC §Accessibility.
       let axis_label_throughput_yaxis = format!(
           "throughput <span aria-label=\"higher is better\">↑</span> (per scenario unit, see scenario.unit)"
       );
       let axis_label_latency_colorbar = format!(
           "latency <span aria-label=\"lower is better\">↓</span> (ns)"
       );
       let axis_label_rss_yaxis = format!(
           "RSS <span aria-label=\"lower is better\">↓</span> (kB)"
       );

       // Dashboard table-header array — plain glyphs (JS wraps at render time):
       let report_table_headers: Vec<String> = vec![
           "allocator".to_string(),
           throughput_plain.clone(),
           p50_plain.clone(),
           p95_plain.clone(),
           p99_plain.clone(),
           p999_plain.clone(),
           peak_rss_plain.clone(),
       ];
       let report_table_headers_json = serde_json::to_string(&report_table_headers)
           .expect("table-header Vec<String> always serializable");
       ```

       Note: `serde_json` is already a workspace dep — no new crate is added.

    5. **Update the `HtmlContext { ... }` literal** in `render` (around line 535+) to populate all four new fields from the `BuiltContext` owned strings.

    6. **Add the a11y regression test** `html::tests::aria_labels_wrap_direction_marker_glyphs` inside the existing `#[cfg(test)] mod tests` block in `html.rs`:

       ```
       #[test]
       fn aria_labels_wrap_direction_marker_glyphs() {
           let rendered: String = render_test_fixture(); // existing helper that returns full index.html

           // Strip the script block AND the JSON header array — those are wrapped client-side at
           // template line 851. Test focuses on server-side-rendered glyphs.
           let scriptless = strip_script_blocks(&rendered);

           // Every ↑ in the scriptless body is preceded by <span aria-label="higher is better">
           // and followed by </span>:
           let up_re = regex::Regex::new(
               r#"<span aria-label="higher is better">↑</span>"#
           ).unwrap();
           let bare_up_re = regex::Regex::new(r#"(?<!<span aria-label="higher is better">)↑(?!</span>)"#).unwrap();
           assert!(up_re.is_match(&scriptless), "expected wrapped ↑ glyph");
           assert!(!bare_up_re.is_match(&scriptless), "found bare ↑ glyph outside aria-span wrapper");

           let down_re = regex::Regex::new(
               r#"<span aria-label="lower is better">↓</span>"#
           ).unwrap();
           let bare_down_re = regex::Regex::new(r#"(?<!<span aria-label="lower is better">)↓(?!</span>)"#).unwrap();
           assert!(down_re.is_match(&scriptless), "expected wrapped ↓ glyph");
           assert!(!bare_down_re.is_match(&scriptless), "found bare ↓ glyph outside aria-span wrapper");

           // The JSON header array (inside the <script> block) MUST contain plain glyphs —
           // those are wrapped by the JS at template line 851 at render time:
           assert!(rendered.contains(r#""throughput ↑""#), "JSON array missing throughput ↑");
           assert!(rendered.contains(r#""peak RSS ↓""#), "JSON array missing peak RSS ↓");
       }
       ```

       (Implementation note: `regex` lookbehind/lookahead may need the `regex::bytes::Regex` flavor or a simpler approach — the executor adapts to the available regex flavor. Alternative: scan character-by-character, asserting every `↑`/`↓` is bracketed by the expected wrapper substring. The acceptance criterion is the assertion of zero bare glyphs outside aria-spans, not the specific regex flavor.)

    Do NOT mutate any existing struct field name. Do NOT alter the existing `polar.rs::axis_label_for_chart` `Cow<'static, str>` precedent — that helper stays as-is and remains the spider-chart axis-label source (Phase 9 lock); this plan adds a parallel set of HTML chart-axis labels for the throughput / latency / RSS charts.
  </action>
  <verify>
    <automated>cargo test -p alloc-bench-aggregator html::tests::aria_labels_wrap_direction_marker_glyphs -- --exact</automated>
    <automated>cargo test -p alloc-bench-aggregator html:: -- --quiet</automated>
    <automated>cargo build -p alloc-bench-aggregator --release</automated>
  </verify>
  <acceptance_criteria>
    - Source: `html.rs` imports `column_header_with_arrow` and `Direction` from `crate::axes`
    - Source: `HtmlContext` has 4 new `&'a str` fields: `axis_label_throughput_yaxis`, `axis_label_latency_colorbar`, `axis_label_rss_yaxis`, `report_table_headers_json`
    - Source: `BuiltContext` has 4 owned `String` analogs of the same names
    - Source: `build_context` calls `column_header_with_arrow` for every measurement column (8 calls total — throughput, latency, RSS, p50, p95, p99, p999, peak RSS), wraps the three Plotly axis-title strings with `<span aria-label="…">` server-side, and JSON-encodes the dashboard table-header array via `serde_json::to_string`
    - Test: `html::tests::aria_labels_wrap_direction_marker_glyphs` passes — every `↑`/`↓` glyph in the rendered HTML body (outside the JSON-array literals in `<script>` blocks) is wrapped in the expected `<span aria-label="…">`
    - Test: all existing `html::tests` continue to pass (no regression in the WR-01 sentinel test, the SRI-hash pin test, the cell-template tests)
    - Byte-level: the JSON array `report_table_headers_json` contains plain glyphs (`"throughput ↑"`, `"peak RSS ↓"`, etc.) — wrapping happens client-side at template line 851, intentional separation
    - The `polar.rs::axis_label_for_chart` `Cow<'static, str>` helper is untouched (Phase 9 WR-04 lock preserved)
  </acceptance_criteria>
  <done>
    `cargo test -p alloc-bench-aggregator html::` passes with one additional test (`aria_labels_wrap_direction_marker_glyphs`). `cargo build --release` succeeds. The `HtmlContext` carries arrow-decorated, aria-wrapped axis-label strings ready for the template substitution in Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Substitute hard-coded chart-axis literals in index.html.tmpl with new placeholders + add JS aria-wrap for table headers</name>
  <files>crates/alloc-bench-aggregator/templates/index.html.tmpl</files>
  <read_first>
    - crates/alloc-bench-aggregator/templates/index.html.tmpl lines 570-580 (throughput chart yaxis title — line 576), 600-615 (latency heatmap colorbar — line 605), 670-690 (RSS chart yaxis title — line 677), 740-750 (A/B comparison yaxis title — line 742, intentionally STAYS PLAIN), 845-870 (dashboard table-header `labels` array — line 851)
    - crates/alloc-bench-aggregator/src/html.rs (verify the four new HtmlContext fields exist after Task 1 — these are the placeholders that index.html.tmpl will reference)
  </read_first>
  <behavior>
    - Test 1 (already added in Task 1): `html::tests::aria_labels_wrap_direction_marker_glyphs` continues to pass after the template substitutions, because the server-side wrapping in `build_context` produces the expected HTML AND the JS at line 851 wraps each table-header entry at render time.
    - Behavioral assertion (covered by the existing test from Task 1): the rendered `index.html` contains `<span aria-label="higher is better">↑</span> throughput (per scenario unit, see scenario.unit)` exactly once (in the throughput-chart yaxis title), `<span aria-label="lower is better">↓</span> latency (ns)` exactly once (in the latency-heatmap colorbar), `<span aria-label="lower is better">↓</span> RSS (kB)` exactly once (in the RSS-chart yaxis title), and the dashboard table headers are populated via `JSON.parse('{report_table_headers_json}')` with each `↑`/`↓` glyph wrapped by client-side JS in `<span aria-label="…">`.
  </behavior>
  <action>
    Modify `crates/alloc-bench-aggregator/templates/index.html.tmpl`:

    1. **Line 576** — replace the throughput chart yaxis title literal:

       ```
       BEFORE:  yaxis: \{ title: \{ text: 'throughput (per scenario unit, see scenario.unit)' } }
       AFTER:   yaxis: \{ title: \{ text: '{ axis_label_throughput_yaxis }' } }
       ```

       (The `\{` escapes are existing tinytemplate convention for literal `{` inside JS object bodies — preserved verbatim.)

    2. **Line 605** — replace the latency heatmap colorbar title literal:

       ```
       BEFORE:  colorbar: \{ title: \{ text: 'latency (ns)' } }
       AFTER:   colorbar: \{ title: \{ text: '{ axis_label_latency_colorbar }' } }
       ```

    3. **Line 677** — replace the RSS chart yaxis title literal:

       ```
       BEFORE:  yaxis: \{ title: \{ text: 'RSS (kB)' } }
       AFTER:   yaxis: \{ title: \{ text: '{ axis_label_rss_yaxis }' } }
       ```

    4. **Line 742** — INTENTIONALLY UNCHANGED. The A/B comparison chart's `% delta (B vs A)` is bidirectional (A and B can each be the higher one depending on the run); a single direction arrow does not fit the semantics. CONTEXT D-claude-discretion-2 explicitly defers this to "default to keeping A/B chart unchanged if the semantics don't fit a single arrow". Add a one-line comment in the template above line 742 documenting this decision:

       ```
       // A/B comparison: bidirectional delta — no direction arrow per Phase-10 CONTEXT D-claude-discretion-2.
       ```

    5. **Line 851** — replace the hard-coded dashboard table-header `labels` array with a server-injected JSON.parse + JS aria-wrap pass:

       ```
       BEFORE:
         for (const label of ['allocator', 'throughput', 'p50', 'p95', 'p99', 'p999', 'peak RSS']) \{
           const th = document.createElement('th');
           th.textContent = label;
           tr.appendChild(th);
         }

       AFTER:
         const reportTableHeaders = JSON.parse('{ report_table_headers_json }');
         for (const label of reportTableHeaders) \{
           const th = document.createElement('th');
           // Wrap direction-marker glyphs with aria-span for WCAG 2.1 SC 1.3.3:
           th.innerHTML = label
             .replaceAll('↑', '<span aria-label="higher is better">↑</span>')
             .replaceAll('↓', '<span aria-label="lower is better">↓</span>');
           tr.appendChild(th);
         }
       ```

       Note the switch from `textContent` to `innerHTML`: this is intentional and safe because `report_table_headers_json` is a server-controlled string built from `column_header_with_arrow` (which only emits `'\u{2191}'` or `'\u{2193}'`) plus fixed labels (`"allocator"`, `"throughput"`, `"p50"`, …). No user-controlled input flows into this JSON. The `replaceAll` calls produce a known-safe HTML fragment containing only the literal aria-span wrappers.

       (Threat model T-10-XSS: the JSON array source is the aggregator's owned data, not benchmark output. `innerHTML` here introduces no injection vector — see <threat_model>.)

    Do NOT modify the SRI hash on the Plotly `<script>` tag (Phase 9 lock). Do NOT modify the `polar.rs`-driven spider-chart axis labels (Phase 9 lock — angular ticks already carry `(heuristic)` decoration via `axis_label_for_chart`). Do NOT modify line 742.
  </action>
  <verify>
    <automated>cargo test -p alloc-bench-aggregator html::tests::aria_labels_wrap_direction_marker_glyphs -- --exact</automated>
    <automated>cargo test -p alloc-bench-aggregator html::tests::plotly_sri_hash_unchanged -- --exact</automated>
    <automated>cargo test -p alloc-bench-aggregator html:: -- --quiet</automated>
    <automated>cargo test -p alloc-bench-aggregator -- --quiet</automated>
    <automated>cargo build -p alloc-bench-aggregator --release</automated>
    <automated>grep -c "axis_label_throughput_yaxis\|axis_label_latency_colorbar\|axis_label_rss_yaxis\|report_table_headers_json" crates/alloc-bench-aggregator/templates/index.html.tmpl</automated>
  </verify>
  <acceptance_criteria>
    - Source: `index.html.tmpl` line 576 references `{ axis_label_throughput_yaxis }` (verified by `grep`)
    - Source: `index.html.tmpl` line 605 references `{ axis_label_latency_colorbar }` (verified by `grep`)
    - Source: `index.html.tmpl` line 677 references `{ axis_label_rss_yaxis }` (verified by `grep`)
    - Source: `index.html.tmpl` line 851 references `JSON.parse('{ report_table_headers_json }')` with the aria-wrap `replaceAll` pass following
    - Source: `index.html.tmpl` line 742 is unchanged from v1.0 (`% delta (B vs A)` literal preserved); a comment above it documents the deferral decision
    - Source: the four template placeholders all match the four new `HtmlContext` field names (no typos — tinytemplate fails loudly on unmatched keys, but the test below catches it before deploy)
    - Test: `html::tests::aria_labels_wrap_direction_marker_glyphs` passes against the FULL rendered HTML (not just the build_context output) — confirms the template substitution fired correctly
    - Test: `html::tests::plotly_sri_hash_unchanged` passes (Phase 9 SRI pin preserved)
    - Test: all 4 (Plan 10-01) + 1 (Plan 10-02) new tests pass; all pre-existing `axes::`, `markdown::`, `html::` tests pass
    - Byte-level: tinytemplate escape sequence `\{` for literal `{` in JS object bodies preserved everywhere (no template-rendering crashes)
    - Manual sanity check (executor runs once and confirms): `just aggregate` produces a `report/index.html` whose Plotly throughput chart yaxis title displays `throughput ↑ (per scenario unit, see scenario.unit)` and the dashboard table column headers display `throughput ↑`, `p50 ↓`, etc. with screen-reader-accessible aria-labels.
  </acceptance_criteria>
  <done>
    `cargo test -p alloc-bench-aggregator` passes with all new tests green and no regressions. `cargo build --release` succeeds. `grep` confirms all four template placeholders are in place at the expected lines. The HTML surface now satisfies DIR-03 (axis-label glyphs) and DIR-04 (a11y wrappers). All five Phase-10 requirements (DIR-01..DIR-05) are now satisfied across both surfaces — Phase 11 (Golden-fixture Regen) is unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev → CI | Source-code changes (html.rs + index.html.tmpl) flow through PR; CI runs `cargo test` to gate merges |
| aggregator → rendered HTML | `build_context` strings are interpolated into `index.html.tmpl`; tinytemplate substitutes `{ name }` placeholders |
| server-rendered HTML → browser DOM | The dashboard table-header JS uses `innerHTML` (Task 2 step 5) to apply aria-wrapping — needs explicit XSS analysis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | Cross-surface drift between Markdown (Plan 10-01) and HTML (this plan) — different glyphs or different label strings | mitigate | Both surfaces import `axes::column_header_with_arrow` from the same SSoT module. Helper signature is single. Tests on both surfaces (`markdown::tests::scenario_table_headers_carry_direction_markers` + `html::tests::aria_labels_wrap_direction_marker_glyphs`) gate the invariant from each end. |
| T-10-06 | Tampering | tinytemplate placeholder typo (e.g., `{ axis_lable_throughput }` vs `{ axis_label_throughput_yaxis }`) | mitigate | tinytemplate panics on unknown keys at render time. The existing `html::tests::*` rendering tests force every code path through `render`, surfacing typos. The four new template placeholders all participate in `aria_labels_wrap_direction_marker_glyphs`'s rendered output, so a typo causes that test to fail before merge. |
| T-10-07 | Information Disclosure | WCAG 2.1 SC 1.3.3 violation — screen readers cannot convey direction semantics if `<span aria-label="…">` wrapper is missing | mitigate | `html::tests::aria_labels_wrap_direction_marker_glyphs` asserts zero bare `↑`/`↓` glyphs in the rendered HTML body outside aria-span wrappers (script blocks excluded — those wrap client-side). Failure messages reference WCAG 2.1 SC 1.3.3 directly. |
| T-10-08 | Tampering | XSS via `innerHTML` on the dashboard table-header JS replacement (Task 2 step 5) — a malicious string in `report_table_headers_json` could inject `<script>` tags | accept | The string source is `serde_json::to_string` over a `Vec<String>` built entirely from compile-time-fixed labels (`"allocator"`, `"throughput"`, …) and the deterministic output of `axes::column_header_with_arrow` (which only inserts `'\u{2191}'` or `'\u{2193}'`). No user-controlled input, no benchmark output, no environment data flows into this string. The aggregator is not a multi-tenant service — input is the developer's own benchmark data and committed source code. Risk is structurally absent in this codebase; revisit if a future change introduces user-controlled axis labels. |
| T-10-09 | Tampering | A/B comparison yaxis title accidentally getting an arrow (regression of CONTEXT D-claude-discretion-2 deferral) | accept | Documented via inline template comment at line 742; no test assertion (changing line 742 in the future is a deliberate UX decision worth its own PR + roadmap entry). |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies in this plan. `serde_json` and `regex` (test-only, optional via dev-dep — verify it's already in workspace before invoking; otherwise alternative implementation per Task 1 note) are pre-existing. No package-legitimacy gate triggered. |
</threat_model>

<verification>
- `cargo test -p alloc-bench-aggregator` passes with 5 new tests across both plans (1 in `axes::tests`, 3 in `markdown::tests`, 1 in `html::tests`); zero pre-existing test regressions.
- `cargo clippy -p alloc-bench-aggregator -- -D warnings` passes (no new warnings introduced; in particular, no warning on the `innerHTML` JS replacement — that is template content, not Rust code).
- `cargo build -p alloc-bench-aggregator --release` succeeds.
- `grep -c "{ axis_label_throughput_yaxis\|{ axis_label_latency_colorbar\|{ axis_label_rss_yaxis\|{ report_table_headers_json" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns >= 4 (each placeholder appears at least once at its expected line).
- `grep -c "% delta (B vs A)" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns >= 1 (line 742 unchanged — A/B chart preserves v1.0 byte form).
- Manual sanity: after the executor runs `just aggregate` on a sample fixture set, opening the resulting `report/index.html` in a browser shows: throughput chart yaxis labelled `throughput ↑ (per scenario unit, see scenario.unit)`; latency colorbar labelled `latency ↓ (ns)`; RSS chart yaxis labelled `RSS ↓ (kB)`; A/B chart yaxis still `% delta (B vs A)`; dashboard table headers `allocator`, `throughput ↑`, `p50 ↓`, `p95 ↓`, `p99 ↓`, `p999 ↓`, `peak RSS ↓`; `view-source:` confirms each glyph is bracketed by the expected aria-span. (Optional final check — primary verification is automated.)
</verification>

<success_criteria>
- DIR-03: `report/index.html` chart axis labels (throughput yaxis, latency colorbar, RSS yaxis) carry `↑`/`↓` glyphs injected from `axes.rs` via `{ axis_label_* }` template placeholders. Dashboard table headers carry the same glyphs via `{ report_table_headers_json }`. Source: `axes::column_header_with_arrow` (Plan 10-01) — no parallel implementation, no hard-coded glyphs in `index.html.tmpl`.
- DIR-04: every `↑`/`↓` glyph in the rendered `index.html` is wrapped in `<span aria-label="higher is better">…</span>` or `<span aria-label="lower is better">…</span>` for WCAG 2.1 SC 1.3.3 conformance. Server-side wrap for axis-title strings; client-side wrap (JS at template line 851) for dashboard table headers.
- Single source of truth: `axes::column_header_with_arrow` is the only function that knows the format. `markdown.rs` (Plan 10-01) and `html.rs` (this plan) both call it. The `polar.rs::axis_label_for_chart` `Cow<'static, str>` helper is untouched (Phase 9 lock preserved).
- A/B chart preserved: line 742 of `index.html.tmpl` is byte-identical to v1.0; comment above documents the bidirectional-delta deferral.
- Test gate: `html::tests::aria_labels_wrap_direction_marker_glyphs` guarantees the a11y invariant under future template refactors. SRI hash test (`html::tests::plotly_sri_hash_unchanged`) continues to pass.
- Phase 11 unblocked: with all five DIR-* requirements satisfied across both surfaces, the byte-changing surface for the v1.1 release is now stable. Phase 11's standalone golden-fixture regen PR can proceed.
</success_criteria>

<output>
Create `.planning/phases/10-direction-markers/10-02-SUMMARY.md` when done, capturing:
- New `HtmlContext` / `BuiltContext` fields (4 each) and their builder logic in `build_context`
- Template substitutions: lines 576, 605, 677, 851 of `index.html.tmpl` (before/after diff)
- Line 742 explicitly preserved (A/B chart deferral note)
- Tests added: 1 in `html::tests` (`aria_labels_wrap_direction_marker_glyphs`)
- Bytes changed in `report/index.html` (compare a sample fixture before/after — informs Phase 11's regen accounting)
- Confirmation that DIR-03, DIR-04 are now satisfied, and that DIR-01/DIR-02/DIR-05 (from Plan 10-01) remain satisfied
- Phase 10 closed: all five DIR-* requirements green, ready for Phase 11
</output>
