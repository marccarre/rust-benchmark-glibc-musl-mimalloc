---
context: phase
phase: 07-scoring-top-n
phase_name: Scoring & Top-N
milestone: v1.1
status: paused
last_updated: 2026-05-27T15:06:26.607Z
stopped_at: context warning at 67% during plan checker revision loop (2026-05-27)
branch: main
head_commit: ab46846
---

# Continue Here — Phase 7: Scoring & Top-N

**Paused at:** plan-phase loop, mid-revision after checker returned NEEDS_REVISION. Research + plans + check are written; 4 of ~7 small revisions are applied; plans NOT yet committed.

## Quick Resume

```bash
# 1. Apply the 3 remaining minor warnings to 07-02-PLAN.md (see "Remaining Revisions" below)
# 2. Re-run plan checker:
/gsd:plan-phase 7
# (the workflow detects existing RESEARCH.md + PLANs and re-runs only the checker iteration)

# 3. Once checker returns PASS, commit and execute:
/gsd:execute-phase 7

# Or to resume autonomous mode (executes 7 → 8 → 9 → 10 → 11 → milestone audit):
/gsd-autonomous --from 7
```

## Where We Are

Phase 7 plan-phase is ~85% complete:
- ✅ `07-RESEARCH.md` written (488 lines). Dry-run verdict: **V12-04 TODO NOT needed** — all 8 axes ≥6 distinct scores at N=18.
- ✅ `07-01-PLAN.md` (`score.rs` data-only) and `07-02-PLAN.md` (`recommend.rs` prose-aware) written.
- ✅ `07-PLAN-CHECK.md` written: verdict NEEDS_REVISION, 12/12 substantive questions PASS, 2 blockers + 4 warnings.
- 🔄 4 of 7 small revisions applied to plans (see "Applied Revisions" below).
- ⏳ 3 minor warnings remain (W-01, W-02, W-04 part 2).
- ⏳ Plans not yet committed (all 4 files untracked on `main` at `ab46846`).
- ⏳ execute-phase not started.

Branch: `main` at `ab46846` (last commit: `wip: phase-07 paused at discuss-complete`).

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| Permissive verify regex | Plan acceptance gates with regex like `(6\|7) passed` mask off-by-one test drops | advisory | Use exact counts in `<verify>` blocks; tighten ranges to single integers |
| Mid-plan signature thrash | Helper signature defined one way in Task 1, refactored in Task 2 within the same plan | advisory | Lift the final signature into Task 1 from the start, even if Task 1 doesn't strictly need it yet |

Both patterns surfaced in the checker output; the structural fix is to make Task 1 ship the final-form signature and use exact-count regex ranges.

## Applied Revisions (already in working tree)

- **B-01** — `07-01-PLAN.md` Task 1 verify regex `(6|7) passed` → `7 passed` (exact)
- **B-02** — `07-02-PLAN.md`:
  - `cell_is_suspect` lifted to generic `<'a, I: IntoIterator<Item = &'a Run>>(cell_runs: I) -> bool` in Task 1 (was `&[Run]`)
  - Task 1 suspect-flag tests updated to call `cell_is_suspect(runs.iter())`
  - Task 2 Step 4 (signature-thrash) removed entirely; renumbered subsequent Step 6 → Step 5
- **W-03** — `07-01-PLAN.md` Task 2 Step 1 includes a doc-comment cross-reference for `score.rs::env_short_name` pointing to Plan 02 Step 5 duplicate
- **W-04 part 1** — `07-01-PLAN.md` Task 2 verify regex `(1[2-9]|2[0-9]) passed` → `15 passed` (exact)

## Remaining Revisions (apply on resume)

Reference `07-PLAN-CHECK.md` for full context. All non-blocking, all line-level edits to `07-02-PLAN.md`.

1. **W-01** — Tighten `recommended_for` re-implementation wording at `07-02-PLAN.md` Task 2 `<action>` line ~290. Replace with the suggested text in PLAN-CHECK §W-01:
   > "The CONTEXT decision says `recommended_for` 'Re-uses winner detection (does NOT re-use rationale strings)'. The existing `recommendations()` returns `Recommendation.allocator` as a `String` and loses `(alloc, env)` granularity needed for cell-level recommendation. Therefore `winners_by_class` re-implements the **algorithm** (per-class scenario filtering + per-(alloc, env) mean throughput + max-mean-wins + alphabetical tiebreak) at the (alloc, env) granularity. The CONTEXT contract is satisfied because the *winner-detection logic* is identical to `recommendations()`'s underlying logic; only the output type differs."

2. **W-02** — Promote `losers_by_class` bottom-2 algorithm to acceptance criterion in `07-02-PLAN.md` Task 2 `<acceptance_criteria>` (~line 348 area). Add:
   > `Test cell_recommendation_avoid_for_is_bottom_2_class_rankings constructs a 4-cell synthetic fixture where (ptmalloc, debian-slim) is bottom-2 in two classes; assertion that avoid_for contains exactly those 2 class labels in alphabetical order.`

3. **W-04 part 2** — Tighten `07-02-PLAN.md` verify regexes:
   - Task 1 line 255: `(1[6-9]|2[0-9]) passed` → `16 passed`
   - Task 2 line 343: `(2[1-9]|3[0-9]) passed` → `21 passed`

After applying, re-run the checker (`/gsd:plan-phase 7` will detect existing artifacts and only re-run the verification step).

## Locked Decisions (Phase 7)

| Gray area | Decision | Why |
|-----------|----------|-----|
| Heuristic weights | Equal 1/8 per axis | V12-07 cap deferred to v1.2; defended by `heuristic_axes_cannot_promote_worst_measured_cell_to_top_1` test |
| Winsorization | p10/p90 (NO V12-04 TODO) | Dry-run verified all 8 axes ≥6 distinct scores at N=18 |
| Per-cell prose | All axis-derived; em-dash U+2014 in tldr | No hand-curated tables |
| Suspect flag | Cell-level bool, generic IntoIterator helper | Run is not Clone (v1 schema GUARD-01) |
| Top-N constants | TOP_N_SPIDER=3, TOP_N_TABLE=5, TOP_N_TOTAL=10 | Single source of truth in recommend.rs |
| Module split | score.rs data-only / recommend.rs prose-aware | Phase 9 polar.rs needs data-only; existing 10 winner_picker tests UNTOUCHED |
| Existing test count | 10 (NOT 13 as CONTEXT.md said) | Research corrected by direct grep on recommend.rs source |

## Required Reading (resume order)

1. `.planning/HANDOFF.json` — machine-readable state
2. `.planning/phases/07-scoring-top-n/07-PLAN-CHECK.md` — checker findings (esp. W-01/W-02/W-04 part 2 for remaining edits)
3. `.planning/phases/07-scoring-top-n/07-RESEARCH.md` — dry-run table, test plan, prose-derivation algorithms
4. `.planning/phases/07-scoring-top-n/07-CONTEXT.md` — locked decisions
5. `.planning/phases/07-scoring-top-n/07-01-PLAN.md` and `07-02-PLAN.md` — current plan state with applied revisions
6. `.planning/REQUIREMENTS.md` — SCORE-01..04, REC-01..02, TEST-03..05

## Infrastructure State

- Branch: `main` at `ab46846` (clean except for 4 untracked planning files)
- Workspace tests: 174 passing (Phase 6 baseline)
- `crates/alloc-bench-core/src/output.rs` frozen-schema gate active (SHA-256: `1bcfb91252eddc...`)
- No outstanding worktrees
- No background processes

## Uncommitted Files

```
?? .planning/phases/07-scoring-top-n/07-RESEARCH.md
?? .planning/phases/07-scoring-top-n/07-01-PLAN.md
?? .planning/phases/07-scoring-top-n/07-02-PLAN.md
?? .planning/phases/07-scoring-top-n/07-PLAN-CHECK.md
```

Plans should be committed AFTER the checker returns PASS. Suggested commits (project convention `docs(NN-PP)`):
- `docs(07): research and plan Phase 7 scoring keystone` (batches all 4 files), or
- One commit per file if granularity preferred

## Next Action

Resume by reading `07-PLAN-CHECK.md` §W-01, §W-02, §W-04, then apply the 3 line-level edits to `07-02-PLAN.md` listed in **Remaining Revisions** above. Re-run `/gsd:plan-phase 7` for a clean checker pass, then `/gsd:execute-phase 7` to begin implementation.

## Remaining Work (this milestone)

| Phase | Status | Reqs | Depends on |
|-------|--------|------|------------|
| 7. Scoring & Top-N | plan ~85% (3 small edits + commit + execute) | 9 | Phase 6 ✅ |
| 8. Per-cell Artifacts | not started | 5 | Phase 7 |
| 9. Spider Chart | not started | 5 | Phase 6 ✅ + Phase 7 |
| 10. Direction Markers | not started | 5 | Phase 6 ✅ |
| 11. Golden-fixture Regen | not started | 2 | Phase 10 |

After Phase 11: milestone audit → complete → cleanup.

## Reference Files

- `.planning/STATE.md` — current milestone position
- `.planning/ROADMAP.md` — Phase 6 ✅, Phases 7-11 listed
- `.planning/REQUIREMENTS.md` — SCORE-01..04, REC-01..02, TEST-03..05
- `.planning/phases/07-scoring-top-n/07-CONTEXT.md` — locked decisions
- `.planning/phases/07-scoring-top-n/07-RESEARCH.md` — dry-run + test plan
- `.planning/phases/07-scoring-top-n/07-01-PLAN.md` — score.rs data-only
- `.planning/phases/07-scoring-top-n/07-02-PLAN.md` — recommend.rs prose-aware
- `.planning/phases/07-scoring-top-n/07-PLAN-CHECK.md` — checker findings
- `crates/alloc-bench-aggregator/src/axes.rs` — Phase 6 deliverable Phase 7 consumes
- `crates/alloc-bench-aggregator/src/loader.rs` — SecurityMeta + load_security_metas Phase 7 consumes
- `crates/alloc-bench-aggregator/src/recommend.rs` — to be extended (additive only; 10 existing tests UNTOUCHED)
