---
phase: 04-aggregator-dashboard
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - crates/alloc-bench-aggregator/templates/index.html.tmpl
  - crates/alloc-bench-aggregator/src/html.rs
  - crates/alloc-bench-aggregator/tests/smoke.rs
autonomous: true
requirements:
  - AGG-02
  - AGG-03
must_haves:
  truths:
    - "D-03: Filter sidebar uses vanilla-JS multi-select — three native `<select multiple>` elements (scenarios, envs, allocators) with `change` handlers that filter RESULTS and call `Plotly.react()` to re-render in place; no framework, no bundler, no npm."
    - "D-04: Four charts shipped — (1) grouped throughput bar (allocator-colored, scenario on X), (2) latency-percentile heatmap (allocator·env·scenario rows × p50/p95/p99/p999/max cols), (3) RSS-over-time lines (one per allocator·env·scenario), (4) side-by-side A/B comparison-diff bar (percent delta on throughput / p99 latency / peak RSS). Env-faceting on the throughput bar deferred from v1 with documented rationale (single-panel grouped bar; full Plotly subplot facet remains a v2 stretch)."
    - "User opens report/index.html (after `just aggregate`) and sees four populated Plotly charts: a grouped throughput bar chart (one bar per allocator, scenario on X), a latency-percentile heatmap (rows = allocator·env·scenario, cols = p50/p95/p99/p999/max, color = ns), an RSS-over-time line chart (one line per allocator·env·scenario), and an A/B comparison-diff bar chart (percent delta of throughput / p99 latency / peak RSS between Config A and Config B)."
    - "User changes the multi-select sidebar — deselects all but one allocator, toggles a scenario — and all four charts update IN PLACE via Plotly.react (no flicker)."
    - "User picks Config A and Config B in the A/B picker (two single-select pairs of <allocator, env>) and the diff chart updates to show percent deltas. Default A = (alphabetically-first allocator, alphabetically-first env); default B = (alphabetically-second allocator, same env as A)."
    - "Suspect runs (samples_count < 10000 OR warmup_duration_s < 5.0) are visually marked with a ⚠ prefix in the chart legend label and in the A/B picker option label."
    - "When the user deselects every option in any one of the three multi-selects, all four charts replace their content with the empty-state heading `No data in current filter` and body `Select at least one scenario, environment, and allocator to render charts.` (no Plotly call with empty data)."
  artifacts:
    - path: "crates/alloc-bench-aggregator/templates/index.html.tmpl"
      provides: "Inline <script> block (~200 LOC) implementing readSelections, applyFilters, makeThroughputTraces, makeLatencyHeatmap, makeRssLines, makeDiffBars, onFilterChange, onAbChange, suspect predicate, Viridis allocator color map; bootstraps multi-select option lists from RESULTS at page load; wires `change` listeners on every <select>; calls onFilterChange() once at end."
      contains: "Plotly.react"
    - path: "crates/alloc-bench-aggregator/src/html.rs"
      provides: "Augment HtmlContext with `scenarios_json`, `envs_json`, `allocators_json`, `suspect_pairs_json` (each a serde_json::to_string output); template uses these to seed the <option> lists in the sidebar and the suspect-prefix logic."
      contains: "scenarios_json"
    - path: "crates/alloc-bench-aggregator/tests/smoke.rs"
      provides: "Additional smoke assertions: index.html contains chart trace builder identifiers (`makeThroughputTraces`, `makeLatencyHeatmap`, `makeRssLines`, `makeDiffBars`); contains `Plotly.react`; contains `Viridis` colorscale; the suspect-fixture's allocator name appears with a ⚠ prefix in at least one place in the HTML."
      contains: "Plotly.react"
  key_links:
    - from: "crates/alloc-bench-aggregator/templates/index.html.tmpl"
      to: "browser Plotly 2.35.3 runtime"
      via: "Plotly.react(divId, traces, layout, config)"
      pattern: "Plotly\\.react"
    - from: "crates/alloc-bench-aggregator/templates/index.html.tmpl"
      to: "inlined RESULTS const"
      via: "RESULTS.filter(r => sel.allocs.includes(r.build.allocator) && ...)"
      pattern: "RESULTS\\.filter"
    - from: "crates/alloc-bench-aggregator/src/html.rs"
      to: "crates/alloc-bench-aggregator/templates/index.html.tmpl"
      via: "tinytemplate substitutes scenarios_json/envs_json/allocators_json/suspect_pairs_json as JSON inside <script>"
      pattern: "scenarios_json"
---

<objective>
Phase 4 vertical-slice 2: bring the dashboard to life. Plan 01 shipped a working aggregator with skeleton HTML (four empty `<div id="chart-*">` cards, three empty `<select multiple>` elements, a stub `onFilterChange`). Plan 02 fills in the inline `<script>` block with the four chart trace builders, the filter handlers, the A/B picker logic, the suspect-badge logic, and the empty-state branch. The Rust side gets minimal additions (HtmlContext gains scenarios/envs/allocators/suspect-pairs JSON arrays so the multi-selects bootstrap with all options selected by default).

Purpose: AGG-02 (multi-select filtering + side-by-side comparison) and AGG-03 (four chart types) become real features users can interact with. After Plan 02, a developer running `just aggregate` against committed fixtures gets a fully-functional dashboard openable via `file://` — the visual contract from UI-SPEC §"Layout" + §"Multi-Select Interaction Contract" is in effect.

Output:
- `templates/index.html.tmpl` gains ~200 LOC of vanilla JS inside the existing `<script>` block (every `{` escaped `\{` per RESEARCH §Pitfall 1).
- `html.rs` `HtmlContext` gains four additional `String` fields (sorted scenario/env/allocator label arrays serialized to JSON + suspect-pair set serialized to JSON).
- `tests/smoke.rs` gains assertions that the chart-builder identifiers and `Plotly.react` are present; the suspect ⚠ marker appears at least once when fixtures include a suspect run.
- Charts use exact contract from UI-SPEC + RESEARCH §Code Examples: Viridis allocator palette (ptmalloc=#440154, mallocng=#3B528B, jemalloc=#21908C, mimalloc=#5DC863), system-font override, `displayModeBar: 'hover'`, `displaylogo: false`, `Plotly.react` for in-place updates (NEVER `Plotly.newPlot`).

Out of scope (handled by Plan 03):
- Allocator comparison tables in REPORT.md (per-row winner highlight).
- Mermaid diagrams (allocator architectures + README system diagram).
- Docker runtime comparison table.
- Recommendations section (data-derived workload→allocator picker).
- Modifications to `markdown.rs` — Plan 03 owns it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-aggregator-dashboard/04-CONTEXT.md
@.planning/phases/04-aggregator-dashboard/04-RESEARCH.md
@.planning/phases/04-aggregator-dashboard/04-PATTERNS.md
@.planning/phases/04-aggregator-dashboard/04-UI-SPEC.md
@.planning/phases/04-aggregator-dashboard/04-01-PLAN.md

<interfaces>
<!-- Plotly.js 2.35.3 surface used by Plan 02 (verified in RESEARCH §Code Examples) -->

Plotly.react(divId, traces, layout, config) — re-render in place. NEVER use Plotly.newPlot for re-renders.

Trace shapes used:
- Bar (throughput/diff): { type: 'bar', name, x: [string...], y: [number...], marker: { color }, showlegend }
- Heatmap (latency): { type: 'heatmap', z: [[ns...]], x: ['p50','p95','p99','p999','max'], y: ['alloc·env·scenario'...], colorscale: 'Viridis', reversescale: false, colorbar: { title: { text: 'latency (ns)' } } }
- Scatter (RSS): { type: 'scatter', mode: 'lines', name, x: [t_s...], y: [rss_kb...], line: { color } }

Viridis allocator color map (UI-SPEC line 92-98):
- ptmalloc: #440154
- mallocng: #3B528B
- jemalloc: #21908C
- mimalloc: #5DC863

Suspect predicate (D-07): samples_count < 10000 OR warmup_duration_s < 5.0

envLabel(run): run.env.docker_image || 'host'

Run shape relevant to charts:
- run.build.allocator (string)
- run.env.docker_image (Option<string>)
- run.scenario.name (string), run.scenario.unit (Option<string>)
- run.harness.samples_count (u64), run.harness.warmup_duration_s (f64)
- run.metrics.ticks_per_s (f64)
- run.metrics.tick_latency_ns.{p50,p95,p99,p999,max} (u64)
- run.metrics.peak_rss_kb (u64)
- run.metrics.rss_growth_samples (Vec<{t_s: u64, rss_kb: u64}>)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Augment HtmlContext + sidebar bootstrap (server-side option-list source data)</name>
  <files>crates/alloc-bench-aggregator/src/html.rs, crates/alloc-bench-aggregator/templates/index.html.tmpl</files>
  <read_first>
    - crates/alloc-bench-aggregator/src/html.rs (Plan 01 final state — confirm `HtmlContext` struct, `render` function shape, `count_unique_cells`)
    - crates/alloc-bench-aggregator/templates/index.html.tmpl (Plan 01 final state — confirm three empty `<select multiple id="sel-*">` elements + four `<div id="chart-*">` cards + four A/B `<select>` elements + the `<script>` block containing the placeholder `onFilterChange` stub)
    - crates/alloc-bench-core/src/output.rs (post-Plan-01 — Deserialize derives on Run/Env/Build/ScenarioInfo/HarnessInfo)
    - .planning/phases/04-aggregator-dashboard/04-UI-SPEC.md §"Multi-Select Interaction Contract" lines 234-258 (initial state = ALL options selected; `<select multiple size="6">`; the empty-filter copy)
    - .planning/phases/04-aggregator-dashboard/04-UI-SPEC.md §"A/B picker" lines 251-258 (default A = first alloc + first env; default B = second alloc + same env)
    - .planning/phases/04-aggregator-dashboard/04-UI-SPEC.md §"Color" §"Suspect-run badge" lines 121-133 (the ⚠ U+26A0 glyph; tooltip text format; per-surface placement)
    - .planning/phases/04-aggregator-dashboard/04-RESEARCH.md §"Pitfall 1" lines 522-534 (every literal `{` in inline JS body MUST be escaped `\{`)
  </read_first>
  <behavior>
    - Behavior 1: After Task 1, the rendered `index.html` (against the Plan-01 fixtures) contains pre-populated `<option>` elements inside each multi-select, all `selected` by default per UI-SPEC line 240. Lists are alphabetical (D-09 byte-identical contract).
    - Behavior 2: The A/B picker selects are pre-populated. Default A = (alphabetically-first alloc, alphabetically-first env). Default B = (alphabetically-second alloc, same env as A). Suspect allocators have ⚠ prefix on their option label per UI-SPEC line 128.
    - Behavior 3: HtmlContext has FOUR new `String` fields populated server-side: `scenarios_json`, `envs_json`, `allocators_json`, `suspect_pairs_json`.
    - Behavior 4: Unit test `html::tests::context_extracts_scenarios_envs_allocators` builds three synthetic Runs (`ptmalloc·alloc-bench:ptmalloc-debian-slim·multithread`, `jemalloc·alloc-bench:jemalloc-alpine·multithread`, `mimalloc·alloc-bench:mimalloc-distroless-cc·cpu-bound`) and asserts the rendered HTML contains the literal substrings `["cpu-bound","multithread"]` for scenarios, the alphabetized env list, and `["jemalloc","mimalloc","ptmalloc"]` for allocators.
    - Behavior 5: Unit test `html::tests::context_marks_suspect_pairs` builds two Runs (`jemalloc·alpine·multithread` with samples_count=5000 → suspect; `ptmalloc·debian-slim·multithread` with samples_count=50000 → clean) and asserts the rendered HTML contains a `SUSPECT_PAIRS` const whose JSON value is `["jemalloc·alloc-bench:jemalloc-alpine"]`.
    - Behavior 6: The existing `tinytemplate_compiles_index_template` test still passes — no unescaped `{` regression after the template additions.
    - Behavior 7: Sanity: rendered HTML against the Plan-01 fixtures contains the literal `⚠ jemalloc` (the suspect fixture's alloc·env tuple) at least once in the option-list HTML — proves the suspect-prefix surfaces server-side via the bootstrap code that consumes SUSPECT_PAIRS.
  </behavior>
  <action>
In `crates/alloc-bench-aggregator/src/html.rs`:

  - Extend `HtmlContext<'a>` with FOUR new fields (each `String`):
    - `scenarios_json: String`
    - `envs_json: String`
    - `allocators_json: String`
    - `suspect_pairs_json: String`
  - Add helper `fn env_label(env: &Env) -> &str { env.docker_image.as_deref().unwrap_or("host") }` (pub(crate) so Plan 03 markdown.rs can reuse it).
  - Add helper `pub(crate) fn is_suspect(h: &HarnessInfo) -> bool { h.samples_count < 10_000 || h.warmup_duration_s < 5.0 }`. This is the canonical D-07 predicate. Plan 03's recommend.rs will REUSE this exact function via `use crate::html::is_suspect;` (or move both helpers to a tiny `mod util;` if cleaner).
  - In `build_context` (extract from current `render` if not already factored — see Plan 01 Task 2 done state): derive four sorted-deduped Vecs from `runs`:
    - `scenarios: Vec<String>` from `runs.iter().map(|r| r.scenario.name.clone()).collect::<BTreeSet<_>>().into_iter().collect()`. Use `BTreeSet` (PATTERNS §"Sorted-output / byte-identical-output pattern" — never HashMap/HashSet for byte-identical output).
    - `envs: Vec<String>` from `env_label(&r.env).to_string()` for each run.
    - `allocators: Vec<String>` from `r.build.allocator.clone()` for each run.
    - `suspect_pairs: Vec<String>` of `format!("{}·{}", r.build.allocator, env_label(&r.env))` for each `r` where `is_suspect(&r.harness)`. De-duped via BTreeSet → alphabetical by construction. The separator is `·` (U+00B7 MIDDLE DOT) per UI-SPEC line 128.
  - Each Vec serializes to its `*_json` field via `serde_json::to_string(&vec).context("serializing X to JSON")?`.
  - Add two new unit tests in `mod tests`. Use a small `make_test_run(alloc, env_image, scenario, samples) -> Run` helper at the top of `mod tests`. Use `build_context` to assert per-context arrays directly (preferred over scraping rendered HTML for unit tests; that's smoke-test territory).

In `crates/alloc-bench-aggregator/templates/index.html.tmpl`:

  - Inside the existing `<aside class="sidebar">` block, leave the three empty `<select multiple id="sel-*">` elements as-is (they get populated by JS at page load). Same for the four A/B `<select>` elements.
  - Add to the inline `<script>` block — directly after `const RESULTS = { results_json | unescaped };` from Plan 01 — these four NEW const declarations (every literal `{` in JS bodies escaped `\{`; the four substitutions stay non-escaped because they ARE substitutions):
    - `const SCENARIOS = { scenarios_json | unescaped };`
    - `const ENVS = { envs_json | unescaped };`
    - `const ALLOCATORS = { allocators_json | unescaped };`
    - `const SUSPECT_PAIRS = new Set({ suspect_pairs_json | unescaped });`
  - Add helper functions (every `{` escaped):
    - `function envLabel(run) \{ return run.env.docker_image || 'host'; }`
    - `function isSuspect(run) \{ return run.harness.samples_count < 10000 || run.harness.warmup_duration_s < 5.0; }`
    - `function suspectKey(alloc, env) \{ return alloc + '·' + env; }` — uses Unicode escape so the file remains pure ASCII even after copy/paste through tools that mangle U+00B7. Equivalent string at runtime.
  - Add a `bootstrap()` function (every `{` escaped). For each multi-select id, populate `<option value="X" selected>X</option>` rows from the matching const array. For each A/B select id, populate `<option value="X">prefix + X</option>` where `prefix = SUSPECT_PAIRS contains-any matching key ? '⚠ ' : ''`. After populating, set the default A/B values:
    - `#ab-a-alloc.value = ALLOCATORS[0]`
    - `#ab-a-env.value = ENVS[0]`
    - `#ab-b-alloc.value = ALLOCATORS.length > 1 ? ALLOCATORS[1] : ALLOCATORS[0]`
    - `#ab-b-env.value = ENVS[0]` (same env as A so first impression is allocator-only diff per UI-SPEC line 256)
  - At the very end of the script block: replace the Plan-01 stub `onFilterChange();` with: `bootstrap();` followed by the (still-stub) `onFilterChange();`. Task 2 replaces the stub onFilterChange.

CRITICAL escape check: `grep -nF '{' crates/alloc-bench-aggregator/templates/index.html.tmpl | grep -v '\\{' | grep -v 'unescaped' | grep -v '{ run_count' | grep -v '{ cell_count' | grep -v '{ timestamp_iso8601' | grep -v '{ plotly_cdn_url' | grep -v '{ plotly_sri_hash' | wc -l` should return 0 lines. Any line counted is a missed brace escape.

Apply `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`. Run `cargo test -p alloc-bench-aggregator --lib` to verify the existing `tinytemplate_compiles_index_template` and the two new context tests pass.
  </action>
  <verify>
    <automated>cargo fmt --all --check && cargo build -p alloc-bench-aggregator --release && cargo test -p alloc-bench-aggregator --lib && cargo clippy --workspace --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "scenarios_json" crates/alloc-bench-aggregator/src/html.rs` returns at least 2 (struct field + assignment).
    - Source assertion: `grep -c "suspect_pairs_json" crates/alloc-bench-aggregator/src/html.rs` returns at least 2.
    - Source assertion: `grep -F "is_suspect" crates/alloc-bench-aggregator/src/html.rs` returns at least 1 line.
    - Source assertion: `grep -F "{ scenarios_json | unescaped }" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns exactly 1 line.
    - Source assertion: `grep -F "SUSPECT_PAIRS" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns at least 2 lines (declaration + at least one usage in bootstrap).
    - Source assertion: `grep -F "function bootstrap" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns exactly 1 line.
    - Source assertion: `grep -F "samples_count < 10000" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns exactly 1 line (D-07 threshold).
    - Source assertion: `grep -F "warmup_duration_s < 5.0" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns exactly 1 line.
    - Test command: `cargo test -p alloc-bench-aggregator --lib -- html::tests::tinytemplate_compiles_index_template html::tests::render_inlines_results_json_unescaped html::tests::context_extracts_scenarios_envs_allocators html::tests::context_marks_suspect_pairs` exits 0 with all four tests green.
    - Behavior assertion: `mkdir -p /tmp/p4-plan2-task1 && cargo run --release -p alloc-bench-aggregator -- --input "crates/alloc-bench-aggregator/tests/fixtures/*.json" --output /tmp/p4-plan2-task1 && grep -F "const SCENARIOS = " /tmp/p4-plan2-task1/index.html` returns 1 line; `grep -F "const SUSPECT_PAIRS" /tmp/p4-plan2-task1/index.html` returns 1 line.
    - Behavior assertion: `grep -F '"jemalloc' /tmp/p4-plan2-task1/index.html` returns at least 1 line (jemalloc appears in the SUSPECT_PAIRS array because the jemalloc-alpine fixture has a samples_count=5000 run).
    - Code-style: `cargo clippy --workspace --all-targets -- -D warnings` passes.
  </acceptance_criteria>
  <done>HtmlContext is augmented with four new JSON-string fields; the template embeds them as JS consts; the bootstrap() function populates the multi-select and A/B-picker options dynamically; the suspect prefix surfaces correctly in the A/B picker option labels. Two new unit tests gate the context derivation; the template-compile test still passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inline JS chart trace builders + filter handler + A/B diff + empty-filter state</name>
  <files>crates/alloc-bench-aggregator/templates/index.html.tmpl</files>
  <read_first>
    - crates/alloc-bench-aggregator/templates/index.html.tmpl (post-Task-1: SCENARIOS/ENVS/ALLOCATORS/SUSPECT_PAIRS consts, bootstrap function, stub onFilterChange — Task 2 replaces the stub)
    - .planning/phases/04-aggregator-dashboard/04-RESEARCH.md §"Code Examples §1" lines 663-689 (Plotly heatmap with Viridis, reversescale=false, system font override)
    - .planning/phases/04-aggregator-dashboard/04-RESEARCH.md §"Code Examples §2" lines 691-725 (grouped bar chart, ALLOC_COLORS map, throughputLayout, barmode='group')
    - .planning/phases/04-aggregator-dashboard/04-RESEARCH.md §"Code Examples §3" lines 727-755 (faceted subplots — for Phase 4 we keep ONE bar chart, not faceted; faceting deferred to v2 or to Plan 03 if budget permits)
    - .planning/phases/04-aggregator-dashboard/04-RESEARCH.md §"Code Examples §4" lines 757-769 (Plotly.react re-render in place — DO NOT use newPlot)
    - .planning/phases/04-aggregator-dashboard/04-RESEARCH.md §"Pattern 3: Vanilla JS sidebar + Plotly.react" lines 461-493 (full handler shape)
    - .planning/phases/04-aggregator-dashboard/04-RESEARCH.md §"Pitfall 2" lines 537-548 (heatmap cap ~90 cells; RSS line chart ~12000 points fine on SVG)
    - .planning/phases/04-aggregator-dashboard/04-UI-SPEC.md §"Color" §"Chart palette" lines 87-108 (Viridis colorscale rationale, allocator hex codes, heatmap reversescale=false)
    - .planning/phases/04-aggregator-dashboard/04-UI-SPEC.md §"A/B picker" lines 251-258 (Default A/B; identical-AB inline note; suspect-config banner)
    - .planning/phases/04-aggregator-dashboard/04-UI-SPEC.md §"Copywriting Contract" lines 154-165 (chart titles + subtitles; empty-filter copy)
    - .planning/phases/04-aggregator-dashboard/04-UI-SPEC.md §"Print and Screenshot Friendliness" line 291 (`displayModeBar: 'hover'`, `displaylogo: false`, modeBarButtonsToRemove: ['sendDataToCloud'])
    - .planning/phases/04-aggregator-dashboard/04-RESEARCH.md §"Anti-Patterns to Avoid" lines 495-503 (NEVER newPlot for re-renders; NEVER hand-roll JSON serialization; NEVER hard-code allocator winners; NEVER let glob iteration order leak)
  </read_first>
  <behavior>
    - Behavior 1: Opening `report/index.html` (after `just aggregate` against committed fixtures) renders four populated charts on first paint: throughput bar (3 allocators across 2 scenarios with 5 total runs), latency heatmap (5 rows × 5 cols), RSS over time (5 lines), A/B comparison (default A=jemalloc·alpine, default B=mimalloc·alpine — alphabetically-first two allocators against alphabetically-first env).
    - Behavior 2: Deselecting all options in `#sel-allocs` replaces every chart card's content with the empty-state heading + body. No JS console errors. No `Plotly.react` call with empty data.
    - Behavior 3: Toggling one allocator back on triggers `onFilterChange`, which calls `Plotly.react` for each of the four chart divs. Re-render is in-place (no flicker per RESEARCH §"Anti-Patterns to Avoid").
    - Behavior 4: Changing the A/B picker (e.g., A from jemalloc → ptmalloc) triggers `onAbChange`, which recomputes the diff bars: `pct_delta = ((B_value - A_value) / A_value) * 100` for each of (throughput, p99 latency, peak RSS). Positive bars colored `#1A7F37` (winner-text green), negative `#CF222E` (destructive red). Bar value labels show `+12.3%` / `-8.0%` per UI-SPEC line 161.
    - Behavior 5: When A and B are identical (same allocator + same env), the diff chart's banner div shows `Config A and Config B are identical — pick differing values to see a delta.` per UI-SPEC line 257.
    - Behavior 6: When either A or B references a suspect run, the diff chart's banner shows `⚠ One or both selected configs are flagged suspect.` per UI-SPEC line 258.
    - Behavior 7: All four charts use `font: { family: '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif' }` in their layout config (overrides Plotly's Open Sans default per UI-SPEC line 66).
    - Behavior 8: All `Plotly.react` calls pass a config object `{ displayModeBar: 'hover', displaylogo: false, modeBarButtonsToRemove: ['sendDataToCloud'] }` per UI-SPEC line 291.
    - Behavior 9: Every literal `{` in the new JS body is escaped `\{` per RESEARCH §Pitfall 1; the `tinytemplate_compiles_index_template` test still passes.
  </behavior>
  <action>
Edit `crates/alloc-bench-aggregator/templates/index.html.tmpl`. Replace the Plan-01 stub `function onFilterChange() \{ }` with a full implementation. Add the chart trace builders, the A/B handler, the diff-banner element, and the wire-up of all listeners.

The structure of the augmented `<script>` block (after Task 1's const declarations + bootstrap function). Every `{` opening a JS block, object literal, function body, or arrow body is `\{`. Closing `}` is unescaped. The 5 substitution placeholders (`{ run_count }`, `{ cell_count }`, `{ timestamp_iso8601 }`, `{ plotly_cdn_url }`, `{ plotly_sri_hash }`) and the 5 unescaped JSON injectors (`{ results_json | unescaped }`, `{ scenarios_json | unescaped }`, `{ envs_json | unescaped }`, `{ allocators_json | unescaped }`, `{ suspect_pairs_json | unescaped }`) stay as-is.

Add these JS sections in order (every `{` opening a JS construct is escaped `\{`):

  - Constants directly after Task 1's const block:
    - `const ALLOC_COLORS` object literal mapping ptmalloc/mallocng/jemalloc/mimalloc to Viridis hexes (UI-SPEC line 92).
    - `const PLOTLY_CONFIG` object literal: `displayModeBar: 'hover'`, `displaylogo: false`, `modeBarButtonsToRemove: ['sendDataToCloud']`.
    - `const SHARED_FONT` object literal: `family: '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif'`.

  - `function readSelections()` returns an object `{ scenarios, envs, allocs }` each as `Array.from(document.querySelector('#sel-X').selectedOptions).map(o => o.value)`.

  - `function applyFilters(runs, sel)` filters runs by all three axes: `r.scenario.name in sel.scenarios && envLabel(r) in sel.envs && r.build.allocator in sel.allocs`. Use `Array.includes` not Set lookups for simplicity (small option counts).

  - `function isEmptySelection(sel)` returns true iff any of the three arrays is empty (UI-SPEC line 242).

  - `function setEmptyState(divIds)` iterates divIds; for each, sets `card.innerHTML = '<h3>No data in current filter</h3><p>Select at least one scenario, environment, and allocator to render charts.</p>'` (UI-SPEC line 155 copy).

  - `function legendName(run)` returns `(SUSPECT_PAIRS.has(suspectKey(run.build.allocator, envLabel(run))) ? '⚠ ' : '') + run.build.allocator + '·' + envLabel(run)`. The U+00B7 separator stays consistent with SUSPECT_PAIRS keys.

  - **Chart 1 — throughput bar** (RESEARCH §Code Examples §2):
    - `function makeThroughputTraces(filtered)`:
      - allocs = unique sorted allocators in filtered
      - scenarios = unique sorted scenario names in filtered
      - For each alloc: build a trace `{ type: 'bar', name: alloc + (any-suspect ? ' ⚠' : ''), x: scenarios, y: scenarios.map(s => find first run matching alloc+s, return ticks_per_s or null), marker: { color: ALLOC_COLORS[alloc] || '#888' } }`.
      - Faceting by env is deferred (UI-SPEC §Layout shows ONE bar chart cell, not faceted; faceting was a stretch goal in D-04 but D-04 says "faceted by env" so we use Plotly's `facet_col` via subplot grid). For Plan 02 we ship a single grouped bar (no facet) to keep the implementation tight; the chart card title and y-axis label make the lack of faceting clear. If a later pass needs faceting, RESEARCH §Code Examples §3 provides the pattern. Document this scope choice in the JS comment block above `makeThroughputTraces`.
    - `const throughputLayout = { barmode: 'group', font: SHARED_FONT, yaxis: { title: { text: 'throughput (per scenario unit, see scenario.unit)' } }, xaxis: { title: { text: 'scenario' } }, margin: { t: 40, r: 16, b: 48, l: 64 }, title: { text: 'Throughput by scenario' } }`.

  - **Chart 2 — latency-percentile heatmap** (RESEARCH §Code Examples §1):
    - `function makeLatencyHeatmap(filtered)`:
      - Sort filtered by `(allocator, envLabel, scenario.name)` lexicographically.
      - y = sorted.map(legendName + '·' + scenario.name)
      - x = ['p50', 'p95', 'p99', 'p999', 'max']
      - z = sorted.map(r => [latency.p50, p95, p99, p999, max])
      - Return `[{ type: 'heatmap', z, x, y, colorscale: 'Viridis', reversescale: false, colorbar: { title: { text: 'latency (ns)' } } }]`.
    - `const latencyLayout = { font: SHARED_FONT, margin: { t: 40, r: 16, b: 32, l: 220 }, title: { text: 'Latency percentiles (heatmap)' } }`.

  - **Chart 3 — RSS over time**:
    - `function makeRssLines(filtered)`:
      - Sort filtered by `(allocator, envLabel, scenario.name)`.
      - For each run: build a `{ type: 'scatter', mode: 'lines', name: legendName(r) + '·' + scenario.name, x: rss_growth_samples.map(t_s), y: rss_growth_samples.map(rss_kb), line: { color: ALLOC_COLORS[r.build.allocator] || '#888' } }`.
      - Skip runs with empty rss_growth_samples (return null and filter out).
    - `const rssLayout = { font: SHARED_FONT, yaxis: { title: { text: 'RSS (kB)' } }, xaxis: { title: { text: 'elapsed (s)' } }, margin: { t: 40, r: 16, b: 48, l: 80 }, title: { text: 'RSS over time' } }`.

  - **Chart 4 — A/B comparison-diff**:
    - `function readAbSelections()` returns `{ a: { alloc, env }, b: { alloc, env } }` from the four A/B selects.
    - `function findCellRun(filtered, alloc, env, scenarioPreference)` returns the first matching run; if multiple scenarios match the cell, prefer 'multithread' scenario (the anchor scenario from Phase 1). If no match, return null.
    - `function pctDelta(a, b)` returns 0 if a === 0 else `((b - a) / a) * 100`.
    - `function makeDiffBars(filtered)`:
      - Read A and B; find matching runs.
      - If either is null, return a single trace `{ type: 'bar', x: ['throughput', 'p99 latency', 'peak RSS'], y: [0, 0, 0], marker: { color: '#888' } }` (graceful "no matching cell").
      - Else compute three deltas: throughput = pctDelta(A.metrics.ticks_per_s, B.metrics.ticks_per_s); p99 = pctDelta(A.metrics.tick_latency_ns.p99, B.metrics.tick_latency_ns.p99); rss = pctDelta(A.metrics.peak_rss_kb, B.metrics.peak_rss_kb).
      - Return `[{ type: 'bar', x: ['throughput', 'p99 latency', 'peak RSS'], y: [throughput, p99, rss], marker: { color: ys.map(v => v >= 0 ? '#1A7F37' : '#CF222E') }, text: ys.map(v => (v >= 0 ? '+' : '') + v.toFixed(1) + '%'), textposition: 'outside' }]`.
    - `const diffLayout = { font: SHARED_FONT, yaxis: { title: { text: '% delta (B vs A)' } }, margin: { t: 40, r: 16, b: 48, l: 80 }, title: { text: 'A/B comparison' }, shapes: [{ type: 'line', x0: -0.5, x1: 2.5, y0: 0, y1: 0, line: { color: '#888', width: 1, dash: 'dot' } }] }`.

  - `function maybeDiffBanner(filtered)`:
    - Locate `<div id="diff-banner">` (Plan 01 added the wrapper section but NOT this element — Task 2 ALSO adds a `<div id="diff-banner" style="display:none; color: var(--color-destructive); margin-top: var(--space-sm);"></div>` inside the existing `<section class="diff-picker">`, just below the four `<select>` elements).
    - Read A/B selections.
    - If `ab.a.alloc === ab.b.alloc && ab.a.env === ab.b.env`: banner.textContent = 'Config A and Config B are identical — pick differing values to see a delta.'
    - Else if A is suspect or B is suspect: banner.textContent = '⚠ One or both selected configs are flagged suspect.'
    - Else: banner.textContent = ''.
    - banner.style.display = (msg ? 'block' : 'none').

  - **Replace the stub `onFilterChange()` with**:
    - Read sel.
    - If `isEmptySelection(sel)`: `setEmptyState(['chart-throughput', 'chart-latency', 'chart-rss', 'chart-diff'])`; `maybeDiffBanner([])`; return.
    - filtered = applyFilters(RESULTS, sel).
    - Plotly.react('chart-throughput', makeThroughputTraces(filtered), throughputLayout, PLOTLY_CONFIG).
    - Plotly.react('chart-latency', makeLatencyHeatmap(filtered), latencyLayout, PLOTLY_CONFIG).
    - Plotly.react('chart-rss', makeRssLines(filtered), rssLayout, PLOTLY_CONFIG).
    - Plotly.react('chart-diff', makeDiffBars(filtered), diffLayout, PLOTLY_CONFIG).
    - maybeDiffBanner(filtered).

  - `function onAbChange()`: just calls `onFilterChange()` (the diff chart depends on A/B selections; recomputing all four is cheap and keeps the handler set small).

  - **Wire up listeners** (every `{` escaped):
    - `document.querySelectorAll('select[multiple]').forEach(el => \{ el.addEventListener('change', onFilterChange); });`
    - `['ab-a-alloc', 'ab-a-env', 'ab-b-alloc', 'ab-b-env'].forEach(id => \{ document.querySelector('#' + id).addEventListener('change', onAbChange); });`
    - `bootstrap();`
    - `onFilterChange();` (initial render).

CRITICAL escape verification before commit:
  1. Run `grep -nF '{' crates/alloc-bench-aggregator/templates/index.html.tmpl | grep -v '\\{'` — every line in the output should be one of the 10 substitution placeholders listed at the top of this action section. Any other line is a missed escape.
  2. Run `cargo test -p alloc-bench-aggregator --lib -- html::tests::tinytemplate_compiles_index_template` — must pass; this is the canary for unescaped `{` regressions.

Apply `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`.
  </action>
  <verify>
    <automated>cargo build -p alloc-bench-aggregator --release && cargo test -p alloc-bench-aggregator --lib -- html::tests::tinytemplate_compiles_index_template && cargo clippy --workspace --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "Plotly.react" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns at least 4 (one per chart div).
    - Source assertion: `grep -c "Plotly.newPlot" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns 0 (NEVER use newPlot for re-renders per RESEARCH §Anti-Patterns).
    - Source assertion: `grep -F "makeThroughputTraces" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns at least 2 lines (definition + invocation).
    - Source assertion: `grep -F "makeLatencyHeatmap" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns at least 2 lines.
    - Source assertion: `grep -F "makeRssLines" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns at least 2 lines.
    - Source assertion: `grep -F "makeDiffBars" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns at least 2 lines.
    - Source assertion: `grep -F "Viridis" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns at least 1 line (heatmap colorscale).
    - Source assertion: `grep -F "ALLOC_COLORS" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns at least 5 lines (declaration + 4 lookups in trace builders).
    - Source assertion: `grep -F "#440154" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns at least 1 line (Viridis ptmalloc hex from UI-SPEC).
    - Source assertion: `grep -F "displayModeBar: 'hover'" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns 1 line.
    - Source assertion: `grep -F "displaylogo: false" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns 1 line.
    - Source assertion: `grep -F "diff-banner" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns at least 2 lines (the div + the JS handler).
    - Source assertion: `grep -F "Config A and Config B are identical" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns 1 line (UI-SPEC line 257 copy).
    - Source assertion: `grep -F "One or both selected configs are flagged suspect" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns 1 line (UI-SPEC line 258 copy).
    - Source assertion: `grep -F "No data in current filter" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns 1 line (empty-filter heading).
    - Source assertion: `grep -F "newPlot" crates/alloc-bench-aggregator/templates/index.html.tmpl` returns 0 lines.
    - Test command: `cargo test -p alloc-bench-aggregator --lib -- html::tests::tinytemplate_compiles_index_template` exits 0 (template still compiles after the JS additions — no missed brace escape).
    - Behavior assertion: `mkdir -p /tmp/p4-plan2-task2 && cargo run --release -p alloc-bench-aggregator -- --input "crates/alloc-bench-aggregator/tests/fixtures/*.json" --output /tmp/p4-plan2-task2 && grep -c "Plotly.react" /tmp/p4-plan2-task2/index.html` returns at least 4.
    - Behavior assertion: `grep -F 'colorscale: "Viridis"' /tmp/p4-plan2-task2/index.html || grep -F "colorscale: 'Viridis'" /tmp/p4-plan2-task2/index.html` returns at least 1 line (Viridis is present in the rendered output).
    - Code-style: `cargo clippy --workspace --all-targets -- -D warnings` passes.
  </acceptance_criteria>
  <done>The dashboard is interactive: four chart trace builders implemented (throughput bar, latency heatmap, RSS lines, A/B diff), filter handler wired across all multi-selects + A/B picker, empty-state branch respects UI-SPEC copy, suspect predicate surfaces in legend names + diff banner, identical-AB note shows when configs match. The template still compiles cleanly via tinytemplate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Smoke test assertions for chart wiring + visual contract verification</name>
  <files>crates/alloc-bench-aggregator/tests/smoke.rs</files>
  <read_first>
    - crates/alloc-bench-aggregator/tests/smoke.rs (Plan 01 final state — confirm the four existing tests + the assert_cmd + tempfile pattern)
    - crates/alloc-bench-aggregator/tests/fixtures/jemalloc-alpine.json (post-Plan-01 — confirm the suspect run fixture has samples_count=5000)
    - crates/alloc-bench-aggregator/templates/index.html.tmpl (post-Task-2 final state — confirm what literal substrings are guaranteed to appear in rendered HTML)
    - .planning/phases/04-aggregator-dashboard/04-PATTERNS.md §"Mutual-exclusion shape assertions pattern" lines 638-650 (the test pattern from run_all_smoke that translates to "either render charts or render empty-state")
  </read_first>
  <behavior>
    - Behavior 1: A new smoke test `aggregator_html_contains_four_chart_builders` runs the aggregator against fixtures, reads `index.html`, and asserts it contains all four trace-builder identifiers (`makeThroughputTraces`, `makeLatencyHeatmap`, `makeRssLines`, `makeDiffBars`) — proving Task 2's chart wiring is shipped.
    - Behavior 2: A new smoke test `aggregator_html_uses_plotly_react_not_newplot` asserts the rendered HTML contains `Plotly.react` at least 4 times AND contains 0 instances of `Plotly.newPlot` — gates against the anti-pattern from RESEARCH §Anti-Patterns.
    - Behavior 3: A new smoke test `aggregator_html_marks_suspect_allocator_with_warning_glyph` runs against the fixtures (which include a suspect jemalloc-alpine run with samples_count=5000) and asserts the rendered HTML contains `⚠` (U+26A0 in UTF-8 → `\xE2\x9A\xA0`; in Rust `'\u{26A0}'`).
    - Behavior 4: A new smoke test `aggregator_html_uses_viridis_palette_per_ui_spec` asserts the rendered HTML contains the four Viridis hex codes from UI-SPEC line 92: `#440154`, `#3B528B`, `#21908C`, `#5DC863`.
    - Behavior 5: A new smoke test `aggregator_html_includes_empty_filter_copy` asserts the rendered HTML contains the exact copy `No data in current filter` and `Select at least one scenario, environment, and allocator to render charts.` — lockdown the UI-SPEC §Copywriting Contract line 155 wording.
    - Behavior 6: A new smoke test `aggregator_html_bootstraps_default_ab_pickers` asserts the rendered HTML's bootstrap function (or the resulting page state) has the default A/B values set per UI-SPEC line 256: A = first alphabetical alloc, B = second alphabetical alloc, both env = first alphabetical env. Asserted via the JS source: the bootstrap function literal contains `ALLOCATORS[0]` AND `ALLOCATORS.length > 1 ? ALLOCATORS[1] : ALLOCATORS[0]` AND `ENVS[0]`.
    - Behavior 7: All four pre-existing smoke tests from Plan 01 (`aggregator_emits_html_and_markdown_against_fixtures`, `aggregator_zero_glob_matches_exits_nonzero`, the partial-failure test, the all-files-fail test) still pass — additive change, no regressions.
  </behavior>
  <action>
Edit `crates/alloc-bench-aggregator/tests/smoke.rs`. Append SIX new integration tests after the four from Plan 01. Use the same `assert_cmd::Command::cargo_bin("alloc-bench-aggregator")` + `tempfile::tempdir()` pattern. Each test reads the rendered `index.html` and asserts substring presence/absence via `&str::contains` or `predicates::str::contains`.

Helper: factor a small `run_aggregator_against_fixtures(out_dir: &Path)` function that performs the cargo-bin invocation. The four Plan-01 tests can be refactored to use it OR left as-is — minimize churn; if refactoring risks breaking line counts, just inline the same setup four more times.

The six new tests:

  1. `aggregator_html_contains_four_chart_builders`: After running the aggregator, read `out_dir.join("index.html")`. Assert all four substrings present:
     - `makeThroughputTraces`
     - `makeLatencyHeatmap`
     - `makeRssLines`
     - `makeDiffBars`
     Use sequential `assert!(html.contains("X"), "expected X in index.html")` calls so failure messages name which one is missing.

  2. `aggregator_html_uses_plotly_react_not_newplot`: Assert `html.matches("Plotly.react").count() >= 4` AND `!html.contains("Plotly.newPlot")`. Use `str::matches(...).count()` to count occurrences. The 4-or-more count covers per-chart `Plotly.react` calls.

  3. `aggregator_html_marks_suspect_allocator_with_warning_glyph`: Assert `html.contains('\u{26A0}')` (the ⚠ U+26A0 WARNING SIGN). Plan-01 fixtures include `jemalloc-alpine` with samples_count=5000 → suspect → server-side bootstrap embeds `⚠ jemalloc·alloc-bench:jemalloc-alpine` in the A/B option labels per Task 1. Test asserts the glyph is present.

  4. `aggregator_html_uses_viridis_palette_per_ui_spec`: Assert all four Viridis hex codes from UI-SPEC line 92 appear: `#440154`, `#3B528B`, `#21908C`, `#5DC863`. Use four `assert!(html.contains("X"))` calls.

  5. `aggregator_html_includes_empty_filter_copy`: Assert two literal substrings:
     - `No data in current filter`
     - `Select at least one scenario, environment, and allocator to render charts.`
     (UI-SPEC line 155.)

  6. `aggregator_html_bootstraps_default_ab_pickers`: Assert the JS source contains all three substrings:
     - `ALLOCATORS[0]` (default A allocator)
     - `ALLOCATORS[1]` (default B allocator — note: presence of this string, not the conditional ternary, is what we assert; the ternary may use slightly different syntax but the index access `[1]` will appear)
     - `ENVS[0]` (default A and B env)

Apply `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`. Run `cargo test -p alloc-bench-aggregator --test smoke` to verify all 10 integration tests (4 from Plan 01 + 6 new) pass.

End-of-Plan-02 sanity sweep: run `just aggregate-smoke` → all tests green. Run `just aggregate` against a fresh `results/` (you can copy fixtures into `results/`) → open `report/index.html` in a browser → verify visually that all four charts render with data (manual smoke). The quality gate doesn't require the manual browser check (autonomous mode), but the test suite gates the visual contract via substring presence.
  </action>
  <verify>
    <automated>cargo fmt --all --check && cargo test -p alloc-bench-aggregator --test smoke && cargo clippy --workspace --all-targets -- -D warnings && just aggregate-smoke</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "fn aggregator_" crates/alloc-bench-aggregator/tests/smoke.rs` returns exactly 10 (4 from Plan 01 + 6 from Plan 02 Task 3).
    - Source assertion: `grep -F "makeThroughputTraces" crates/alloc-bench-aggregator/tests/smoke.rs` returns at least 1 line.
    - Source assertion: `grep -F "Plotly.newPlot" crates/alloc-bench-aggregator/tests/smoke.rs` returns at least 1 line (the negative-assertion test references it).
    - Source assertion: `grep -F "u{26A0}" crates/alloc-bench-aggregator/tests/smoke.rs` returns at least 1 line (the suspect-glyph test uses Rust's Unicode escape).
    - Source assertion: `grep -F "#440154" crates/alloc-bench-aggregator/tests/smoke.rs` returns at least 1 line (Viridis test references the canonical hex).
    - Source assertion: `grep -F "No data in current filter" crates/alloc-bench-aggregator/tests/smoke.rs` returns at least 1 line.
    - Source assertion: `grep -F "ALLOCATORS[0]" crates/alloc-bench-aggregator/tests/smoke.rs` returns at least 1 line (the bootstrap-default test).
    - Test command: `cargo test -p alloc-bench-aggregator --test smoke` exits 0 with all 10 tests green.
    - CLI behavior: `just aggregate-smoke` exits 0.
    - Code-style: `cargo clippy --workspace --all-targets -- -D warnings` passes.
  </acceptance_criteria>
  <done>The smoke suite gates the visual contract from UI-SPEC: four chart builders shipped, Viridis palette in use, empty-filter copy present, suspect glyph surfaces, A/B defaults wired. All 10 integration tests pass via `just aggregate-smoke`. AGG-02 + AGG-03 fully realized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → CDN (cdn.plot.ly) | Plan 01 already mitigated via SRI integrity hash; Plan 02 only adds JS that uses Plotly globals — no new CDN deps. |
| browser → DOM (filter selections) | Three multi-selects + four A/B selects accept user-controlled selections. No XSS surface because: (a) all `option.value` strings come from server-side serde_json::to_string of strings already validated by the aggregator's deserialize step; (b) JS handlers use DOM property access (`.textContent`, `.innerHTML` only with our literal copy), never `eval`. |
| browser → inlined RESULTS | Inline JS reads `RESULTS.filter(...)` etc. RESULTS is server-side serde_json::to_string output of validated v1 schema records. No untrusted input crosses into the dashboard at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | inline JS chart builders | accept | The JS lives inside our committed template file, not user-supplied. Any modification requires editing `index.html.tmpl` and re-running the aggregator. The `tinytemplate_compiles_index_template` test from Plan 01 gates against accidental brace-escape regressions. |
| T-04-10 | Information Disclosure | suspect predicate exposes `samples_count` and `warmup_duration_s` to client-side JS | accept | These are diagnostic harness metrics (not secrets); they're already in the inlined RESULTS array. Plan 02 only adds the predicate logic. |
| T-04-11 | Spoofing | A/B picker labels include allocator+env strings | accept | These come from server-side validated schema (Build.allocator, Env.docker_image). Both are already-public benchmark identifiers. No spoofing surface. |
| T-04-12 | Tampering | inline JS reads/writes DOM via .innerHTML | mitigate | `.innerHTML` is used ONLY with literal string templates that contain no user data — `<h3>No data in current filter</h3><p>Select at least one...</p>`. All other DOM updates use `.textContent` (which auto-escapes) or Plotly.react (which validates trace shape). No path from RESULTS → unescaped innerHTML. |
| T-04-SC | Tampering | crates.io / cdn.plot.ly chain | mitigate | Plan 01 covered both. Plan 02 adds NO new deps. |
</threat_model>

<verification>
End-to-end Plan 02 verification (autonomous):

1. `cargo fmt --all --check` exits 0.
2. `cargo build --workspace --release` exits 0.
3. `cargo test --workspace --lib` exits 0 (all unit tests including the new Task-1 context tests).
4. `cargo test -p alloc-bench-aggregator --test smoke` exits 0 (all 10 integration tests).
5. `just aggregate-smoke` exits 0.
6. `cargo clippy --workspace --all-targets -- -D warnings` passes.
7. `mkdir -p /tmp/p4-plan2-out && cargo run --release -p alloc-bench-aggregator -- --input "crates/alloc-bench-aggregator/tests/fixtures/*.json" --output /tmp/p4-plan2-out` exits 0.
8. `grep -c "Plotly.react" /tmp/p4-plan2-out/index.html` returns at least 4.
9. `grep -F "makeThroughputTraces" /tmp/p4-plan2-out/index.html` returns at least 1 line.
10. `grep -F "#440154" /tmp/p4-plan2-out/index.html` returns at least 1 line (Viridis present).
11. Reproducibility (D-09): two consecutive aggregator runs against the same fixtures produce byte-identical index.html (no per-run randomness in chart logic; the only non-determinism is the timestamp in the page title, which is in the rendered HTML — assertion: `diff <(grep -v 'timestamp_iso8601\|generated' run1/index.html) <(grep -v 'timestamp_iso8601\|generated' run2/index.html)` is empty).
</verification>

<success_criteria>
- The dashboard renders four populated Plotly charts on first paint against committed fixtures (throughput bar, latency heatmap, RSS lines, A/B diff).
- Multi-select sidebar filters the data and re-renders all four charts via `Plotly.react` (NOT `Plotly.newPlot`).
- A/B picker drives the diff chart with percent deltas; identical-AB shows the inline note; suspect-config shows the warning banner.
- Empty-filter selection (any of the three axes empty) replaces all chart cards with the empty-state copy.
- Suspect runs (samples_count<10000 OR warmup_duration_s<5.0) get a ⚠ prefix in legend names AND in A/B picker option labels.
- Viridis allocator palette in use (#440154, #3B528B, #21908C, #5DC863).
- Plotly config: `displayModeBar: 'hover'`, `displaylogo: false`, `modeBarButtonsToRemove: ['sendDataToCloud']`.
- All four chart layouts override `font.family` to the system stack (no Open Sans webfont fetch).
- All 10 integration tests pass via `cargo test -p alloc-bench-aggregator --test smoke`.
- Template still compiles via tinytemplate (`tinytemplate_compiles_index_template` test green) — no missed brace escape.

REQ coverage:
- AGG-02 (HTML supports filtering by scenario/env/allocator + side-by-side comparison): satisfied. Multi-select sidebar + A/B picker both functional.
- AGG-03 (four charts: throughput bar, latency heatmap, RSS lines, comparison-diff bar): satisfied. All four traced via Plotly.react with Viridis palette and system font.
</success_criteria>

<output>
Create `.planning/phases/04-aggregator-dashboard/04-02-SUMMARY.md` when done, documenting:
- Lines added to the template (chart builders, filter handlers)
- HtmlContext field additions
- The four chart trace shapes used
- The Viridis hex map verbatim
- Number of `Plotly.react` invocations
- Smoke test count delta (4 → 10)
- Brace-escape grep hit count (must be 0 unexplained)
- Pointers to Plan 03 (REPORT.md richness, Mermaid diagrams, README system diagram, recommend.rs)
</output>
</content>
