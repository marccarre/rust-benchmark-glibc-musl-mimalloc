---
phase: 03-docker-matrix-local-orchestration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/debian-slim.Dockerfile
  - docker/distroless-cc.Dockerfile
  - docker/wolfi.Dockerfile
  - .dockerignore
  - .dive-ci
autonomous: true
requirements: [DOCK-02, DOCK-03, DOCK-06, DOCK-07, DOCK-08]
tags: [docker, glibc, cargo-chef, oci-annotations, dive, dockerignore]

must_haves:
  truths:
    - "User can `docker buildx build -f docker/debian-slim.Dockerfile --platform linux/amd64 --build-arg ALLOC=ptmalloc ...` and the image builds."
    - "User can `docker buildx build -f docker/distroless-cc.Dockerfile --platform linux/amd64 --build-arg ALLOC=mimalloc ...` and the image builds (UID 65532 nonroot)."
    - "User can `docker buildx build -f docker/wolfi.Dockerfile --platform linux/amd64 --build-arg ALLOC=jemalloc ...` and the image builds (digest-pinned wolfi-base)."
    - "Every glibc image carries the eight org.opencontainers.image.{title,description,source,version,revision,licenses,created,authors} LABELs populated from --build-arg."
    - "`.dockerignore` excludes target/ + .planning/ so build context stays small."
    - "`.dive-ci` carries the three thresholds locked in D-21 and is consumed by `dive --ci --ci-config .dive-ci`."
  artifacts:
    - path: "docker/debian-slim.Dockerfile"
      provides: "glibc dynamic runtime on debian:bookworm-slim with cargo-chef multi-stage build"
      contains: "FROM debian:bookworm-slim"
    - path: "docker/distroless-cc.Dockerfile"
      provides: "glibc minimal runtime on gcr.io/distroless/cc-debian12:nonroot (UID 65532)"
      contains: "FROM gcr.io/distroless/cc-debian12:nonroot"
    - path: "docker/wolfi.Dockerfile"
      provides: "glibc Wolfi runtime on cgr.dev/chainguard/wolfi-base pinned by digest"
      contains: "FROM cgr.dev/chainguard/wolfi-base@sha256:"
    - path: ".dockerignore"
      provides: "Excludes target/, .planning/, results/ from Docker build context"
      contains: "target/"
    - path: ".dive-ci"
      provides: "dive --ci threshold config (lowestEfficiency, highestUserWastedPercent, highestWastedBytes)"
      contains: "lowestEfficiency"
  key_links:
    - from: "docker/debian-slim.Dockerfile"
      to: "rust:1.91-bookworm builder + debian:bookworm-slim runtime"
      via: "FROM-as-builder + FROM debian: runtime stage"
      pattern: "FROM rust:.*-bookworm AS chef"
    - from: "docker/distroless-cc.Dockerfile"
      to: "gcr.io/distroless/cc-debian12:nonroot runtime"
      via: "COPY --from=builder /app/target/x86_64-unknown-linux-gnu/release/alloc-bench-cli /alloc-bench-cli"
      pattern: "FROM gcr.io/distroless/cc-debian12:nonroot"
    - from: "docker/wolfi.Dockerfile"
      to: "cgr.dev/chainguard/wolfi-base@sha256:<digest>"
      via: "FROM with digest pin (Pitfall §6)"
      pattern: "@sha256:"
    - from: "All three Dockerfiles"
      to: "cargo-chef recipe.json caching"
      via: "FROM chef AS planner; cargo chef prepare; FROM chef AS builder; cargo chef cook"
      pattern: "cargo chef (prepare|cook)"
    - from: "All three Dockerfiles"
      to: "OCI annotation LABEL block"
      via: "ARG OCI_VERSION/REVISION/CREATED + LABEL org.opencontainers.image.*"
      pattern: "LABEL org.opencontainers.image.title"
---

<objective>
Create the three glibc-family Dockerfiles (debian-slim, distroless-cc, wolfi), the `.dockerignore`, and the `.dive-ci` config so any glibc cell of the Phase 3 matrix can be built end-to-end via `docker buildx build` with cargo-chef caching, OCI-annotated, and audited via `dive --ci`.

Purpose: Three of the six runtime environments live in the glibc family. They share the rust:1.91-bookworm builder, the x86_64-unknown-linux-gnu target triple, and a single set of cargo-chef stages — only the runtime base differs. Phase-3 cells `{ptmalloc,jemalloc,mimalloc} × {debian-slim, distroless-cc, wolfi}` (9 cells) all depend on this plan.

Output: 3 Dockerfiles, 1 `.dockerignore`, 1 `.dive-ci` — plus a manual sanity check that each Dockerfile lints with `docker buildx build --check` before committing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03-docker-matrix-local-orchestration/03-CONTEXT.md
@.planning/phases/03-docker-matrix-local-orchestration/03-RESEARCH.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@Cargo.toml
@rust-toolchain.toml
@.cargo/config.toml
@crates/alloc-bench-cli/Cargo.toml

<interfaces>
<!-- Repository facts the executor must respect. Confirmed live 2026-05-19. -->

From rust-toolchain.toml:
  channel = "1.91" — the project's pinned rustc. RUST_VERSION must match.

From Cargo.toml [features] in crates/alloc-bench-cli:
  default = []
  alloc-jemalloc = ["dep:tikv-jemallocator", "dep:tikv-jemalloc-ctl"]
  alloc-mimalloc = ["dep:mimalloc", "dep:libmimalloc-sys"]
  ALLOC=ptmalloc → no feature flag (default = system allocator on glibc)
  ALLOC=mallocng → no feature flag (default = system allocator on musl) — N/A in this glibc plan
  ALLOC=jemalloc → --no-default-features --features alloc-jemalloc
  ALLOC=mimalloc → --no-default-features --features alloc-mimalloc

From .cargo/config.toml:
  [build] rustflags = ["-C", "target-cpu=native"] — host-only; Dockerfile MUST override via ENV RUSTFLAGS.

From CONTEXT.md D-08 — the eight required OCI annotation keys:
  org.opencontainers.image.title
  org.opencontainers.image.description
  org.opencontainers.image.source
  org.opencontainers.image.version
  org.opencontainers.image.revision
  org.opencontainers.image.licenses
  org.opencontainers.image.created
  org.opencontainers.image.authors

From CONTEXT.md D-21 — .dive-ci thresholds:
  lowestEfficiency: 0.95
  highestUserWastedPercent: 0.05
  highestWastedBytes: 50MB

From RESEARCH.md §"Standard Stack" — verified base images (live `docker buildx imagetools inspect` 2026-05-19):
  rust:1.91-bookworm — exists (replaces D-06's "1.83" because rust-toolchain.toml says 1.91)
  debian:bookworm-slim — 12.13, glibc
  gcr.io/distroless/cc-debian12:nonroot — UID 65532, WorkingDir=/home/nonroot, glibc + libgcc1
  cgr.dev/chainguard/wolfi-base — :latest is mutable (RESEARCH §Pitfall 6); MUST pin by digest.

From CONTEXT.md D-09 + RESEARCH.md §Pitfall 2:
  Builder MUST set ENV RUSTFLAGS="-C target-cpu=x86-64-v3" — overrides .cargo/config.toml's target-cpu=native.

From CONTEXT.md D-15 — Phase-3 runtime context:
  docker run --cpus=4 --memory=4g --cpuset-cpus=0-3 --platform linux/amd64
  -v $(pwd)/results:/out
  alloc-bench:{alloc}-{env} run-all --output /out/{alloc}-{env}.json

From RESEARCH.md §Pitfall 4 — distroless-cc nonroot UID 65532:
  Binary lives at /alloc-bench-cli (root of FS), NOT /usr/local/bin
  USER nonroot
  Output volume mount: host MUST `mkdir -p ./results && chmod 0777 results` (Plan 03 handles)

From CONTEXT.md "code_context" — env block must report DOCKER_IMAGE:
  Each runtime stage MUST set ENV DOCKER_IMAGE="<base>:<tag>" so metrics::env::read_env picks it up
  e.g. ENV DOCKER_IMAGE=debian:bookworm-slim
</interfaces>

<glibc_dockerfile_template>
<!-- Reference shape for all three glibc Dockerfiles. Copy and adjust ONLY the
     runtime stage and DOCKER_IMAGE env var per file. cargo-chef + builder is
     identical across all three. -->

ARG RUST_VERSION=1.91

# ─── Stage 1: chef base ────────────────────────────────────────────
FROM rust:${RUST_VERSION}-bookworm AS chef
RUN cargo install --locked cargo-chef@0.1.77
WORKDIR /app

# ─── Stage 2: planner — compute recipe.json ────────────────────────
FROM chef AS planner
COPY . .
RUN cargo chef prepare --recipe-path recipe.json

# ─── Stage 3: builder ──────────────────────────────────────────────
FROM chef AS builder
ARG ALLOC=ptmalloc
ARG TARGET=x86_64-unknown-linux-gnu
ENV RUSTFLAGS="-C target-cpu=x86-64-v3"
RUN rustup target add ${TARGET}
COPY --from=planner /app/recipe.json recipe.json
RUN if [ "$ALLOC" = "jemalloc" ]; then \
        FEATURES="--no-default-features --features alloc-jemalloc"; \
    elif [ "$ALLOC" = "mimalloc" ]; then \
        FEATURES="--no-default-features --features alloc-mimalloc"; \
    elif [ "$ALLOC" = "ptmalloc" ]; then \
        FEATURES=""; \
    else \
        echo "ERROR: glibc env only supports ALLOC in {ptmalloc, jemalloc, mimalloc}; got '$ALLOC'" >&2; exit 1; \
    fi && \
    cargo chef cook --release --target ${TARGET} ${FEATURES} \
        -p alloc-bench-cli --recipe-path recipe.json
COPY . .
RUN if [ "$ALLOC" = "jemalloc" ]; then \
        FEATURES="--no-default-features --features alloc-jemalloc"; \
    elif [ "$ALLOC" = "mimalloc" ]; then \
        FEATURES="--no-default-features --features alloc-mimalloc"; \
    else \
        FEATURES=""; \
    fi && \
    cargo build --release --target ${TARGET} ${FEATURES} \
        -p alloc-bench-cli

# ─── Stage 4: runtime — per-env. Differs across the three Dockerfiles. ──
# Variables: <RUNTIME_FROM>, <DOCKER_IMAGE_VALUE>, <DESCRIPTION_SUFFIX>, <BIN_PATH>, <USER_LINE>
FROM <RUNTIME_FROM> AS runtime
ARG OCI_VERSION
ARG OCI_REVISION
ARG OCI_CREATED
LABEL org.opencontainers.image.title="alloc-bench" \
      org.opencontainers.image.description="Memory allocator benchmark — <DESCRIPTION_SUFFIX>" \
      org.opencontainers.image.source="https://github.com/marccarre/rust-benchmark-glibc-musl-mimalloc" \
      org.opencontainers.image.version="${OCI_VERSION}" \
      org.opencontainers.image.revision="${OCI_REVISION}" \
      org.opencontainers.image.licenses="MIT OR Apache-2.0" \
      org.opencontainers.image.created="${OCI_CREATED}" \
      org.opencontainers.image.authors="Marc Carré"
ENV DOCKER_IMAGE=<DOCKER_IMAGE_VALUE>
COPY --from=builder /app/target/x86_64-unknown-linux-gnu/release/alloc-bench-cli <BIN_PATH>
<USER_LINE>
ENTRYPOINT ["<BIN_PATH>"]

# Per-Dockerfile substitutions:
# debian-slim.Dockerfile:
#   <RUNTIME_FROM>        = debian:bookworm-slim
#   <DOCKER_IMAGE_VALUE>  = debian:bookworm-slim
#   <DESCRIPTION_SUFFIX>  = debian-slim (glibc dynamic; ptmalloc/jemalloc/mimalloc)
#   <BIN_PATH>            = /usr/local/bin/alloc-bench-cli
#   <USER_LINE>           = (omit; runs as root, fine for benchmark)
#
# distroless-cc.Dockerfile:
#   <RUNTIME_FROM>        = gcr.io/distroless/cc-debian12:nonroot
#   <DOCKER_IMAGE_VALUE>  = gcr.io/distroless/cc-debian12:nonroot
#   <DESCRIPTION_SUFFIX>  = distroless-cc (glibc minimal nonroot; ptmalloc/jemalloc/mimalloc)
#   <BIN_PATH>            = /alloc-bench-cli  (FS root — Pitfall §4 says distroless :nonroot does NOT guarantee /usr/local/bin in PATH)
#   <USER_LINE>           = USER nonroot
#                           WORKDIR /home/nonroot
#
# wolfi.Dockerfile:
#   <RUNTIME_FROM>        = cgr.dev/chainguard/wolfi-base@sha256:<DIGEST>  (capture via:
#                             docker buildx imagetools inspect cgr.dev/chainguard/wolfi-base:latest --raw \
#                               | jq -r '.manifests[] | select(.platform.architecture=="amd64") | .digest' )
#   <DOCKER_IMAGE_VALUE>  = cgr.dev/chainguard/wolfi-base@sha256:<DIGEST>  (same string for traceability)
#   <DESCRIPTION_SUFFIX>  = wolfi (glibc Chainguard; ptmalloc/jemalloc/mimalloc)
#   <BIN_PATH>            = /usr/local/bin/alloc-bench-cli
#   <USER_LINE>           = (omit; wolfi-base is UID 0 by default per RESEARCH §Standard Stack)
</glibc_dockerfile_template>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .dockerignore + .dive-ci</name>

  <files>
    - .dockerignore
    - .dive-ci
  </files>

  <read_first>
    - .planning/phases/03-docker-matrix-local-orchestration/03-CONTEXT.md (D-21 thresholds)
    - .planning/phases/03-docker-matrix-local-orchestration/03-RESEARCH.md §"Code Examples §3, §7"
    - .gitignore (so .dockerignore aligns where appropriate)
  </read_first>

  <action>
    Create `.dockerignore` in repo root excluding target/, **/target/, .git/, .planning/, results/, report/, .DS_Store, *.swp, .cargo-ok, .idea/, .vscode/, **/*.rs.bk, docs/, plus a comment block listing what MUST NOT be excluded (Cargo.toml, Cargo.lock, crates/, rust-toolchain.toml, .cargo/config.toml, build.rs files inside crates/). Do NOT exclude *.md (the workspace has a CLAUDE.md and root README.md whose presence inside the build context is harmless and BuildKit's dep cache invalidation behaves better when the context is consistent).

    Create `.dive-ci` in repo root with three rules under top-level `rules:` mapping per CONTEXT D-21: `lowestEfficiency: 0.95`, `highestUserWastedPercent: 0.05`, `highestWastedBytes: 50MB`. Add a brief comment header citing the dive 0.13.1 README and CONTEXT D-21 as the source of these thresholds.

    Both files are pure text; no code execution.
  </action>

  <verify>
    <automated>
      test -f .dockerignore && \
      test -f .dive-ci && \
      grep -q '^target/' .dockerignore && \
      grep -q 'lowestEfficiency:[[:space:]]*0\.95' .dive-ci && \
      grep -q 'highestUserWastedPercent:[[:space:]]*0\.05' .dive-ci && \
      grep -q 'highestWastedBytes:[[:space:]]*50MB' .dive-ci && \
      echo OK
    </automated>
  </verify>

  <acceptance_criteria>
    - Source: `.dockerignore` exists at repo root and contains a line `target/` (top-level, not commented).
    - Source: `.dockerignore` does NOT contain `Cargo.toml`, `Cargo.lock`, `crates/`, `rust-toolchain.toml` (negative assertion: `grep -E '^(Cargo\.toml|Cargo\.lock|crates/|rust-toolchain\.toml)$' .dockerignore` returns nothing).
    - Source: `.dive-ci` exists at repo root with `rules:` section containing exactly the three keys `lowestEfficiency`, `highestUserWastedPercent`, `highestWastedBytes` matching the D-21 values.
    - Build: `cargo build --workspace --release` (host) still succeeds with `.dockerignore` in place — proves we didn't accidentally hide a tracked file from git or the build (fmt/clippy unaffected since neither file is Rust code).
  </acceptance_criteria>

  <done>
    `.dockerignore` and `.dive-ci` exist at repo root with the values locked by D-21 and the file lists from RESEARCH.md §7. The verify command exits 0 and prints `OK`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create docker/debian-slim.Dockerfile and docker/distroless-cc.Dockerfile</name>

  <files>
    - docker/debian-slim.Dockerfile
    - docker/distroless-cc.Dockerfile
  </files>

  <read_first>
    - .planning/phases/03-docker-matrix-local-orchestration/03-CONTEXT.md (D-05 base images, D-06 builder version, D-07 dockerfile shape, D-08 OCI annotations, D-09 RUSTFLAGS)
    - .planning/phases/03-docker-matrix-local-orchestration/03-RESEARCH.md §"Pattern 1", §"Code Examples §1", §"Pitfall 4 — distroless nonroot"
    - rust-toolchain.toml (confirms RUST_VERSION must be 1.91, NOT D-06's 1.83 — rust-toolchain.toml overrides at runtime)
    - Cargo.toml (allocator features, profile.release flags)
    - crates/alloc-bench-cli/Cargo.toml (alloc-jemalloc, alloc-mimalloc feature definitions)
    - .cargo/config.toml (host-only target-cpu=native; ENV RUSTFLAGS in Dockerfile MUST override)
  </read_first>

  <action>
    Create `docker/debian-slim.Dockerfile` per the `<glibc_dockerfile_template>` above with these substitutions: `<RUNTIME_FROM>=debian:bookworm-slim`; `<DOCKER_IMAGE_VALUE>=debian:bookworm-slim`; `<DESCRIPTION_SUFFIX>=debian-slim (glibc dynamic; ptmalloc/jemalloc/mimalloc)`; `<BIN_PATH>=/usr/local/bin/alloc-bench-cli`; `<USER_LINE>` omitted (runs as root). Builder uses `rust:${RUST_VERSION}-bookworm` with `RUST_VERSION=1.91`. Target triple `x86_64-unknown-linux-gnu`. Do NOT install `numactl` (CONTEXT D-16). Total file ≤ 80 lines (D-07 cap).

    Create `docker/distroless-cc.Dockerfile` per the same template with: `<RUNTIME_FROM>=gcr.io/distroless/cc-debian12:nonroot`; `<DOCKER_IMAGE_VALUE>=gcr.io/distroless/cc-debian12:nonroot`; `<DESCRIPTION_SUFFIX>=distroless-cc (glibc minimal nonroot; ptmalloc/jemalloc/mimalloc)`; `<BIN_PATH>=/alloc-bench-cli` (FS root per RESEARCH §Pitfall 4 — distroless :nonroot has no guarantee on /usr/local/bin PATH for nonroot users); `<USER_LINE>=USER nonroot` followed by `WORKDIR /home/nonroot`. Builder identical to debian-slim. Total file ≤ 80 lines.

    Both Dockerfiles must:
    - Pin `ARG RUST_VERSION=1.91` (deviation from CONTEXT D-06's literal "1.83" because rust-toolchain.toml=1.91 — rustup downloads it anyway during cargo-chef cook; matching the toolchain saves one redundant download. Document the rationale as a single comment at the ARG line referencing rust-toolchain.toml.).
    - Reject ALLOC values outside {ptmalloc, jemalloc, mimalloc} with a non-zero exit and clear error message in BOTH the `chef cook` and the `cargo build` step's `if/elif/else` chain (D-04 cross-libc rejection — mallocng is musl-only and must not silently fall through to "no features").
    - Set `ENV RUSTFLAGS="-C target-cpu=x86-64-v3"` in the builder stage to override `.cargo/config.toml`'s `target-cpu=native` (RESEARCH §Pitfall 2). Do NOT add `+crt-static` here — glibc cells are dynamic.
    - Emit all eight `org.opencontainers.image.*` LABELs from the eight ARG names listed in `<interfaces>`.
    - Set `ENV DOCKER_IMAGE` in the runtime stage so `metrics::env::read_env` populates the JSON env block (RESEARCH §"Runtime State Inventory").

    Do NOT add a numactl install, do NOT add a `--user $(id -u):$(id -g)` workaround in the Dockerfile (Plan 03 handles host-side `chmod 0777 results` per Pitfall 4).

    Do NOT execute `docker buildx build` in this task — Plan 04 runs the smoke matrix. This task only writes files.
  </action>

  <verify>
    <automated>
      test -f docker/debian-slim.Dockerfile && \
      test -f docker/distroless-cc.Dockerfile && \
      grep -q '^ARG RUST_VERSION=1.91' docker/debian-slim.Dockerfile && \
      grep -q '^ARG RUST_VERSION=1.91' docker/distroless-cc.Dockerfile && \
      grep -q 'FROM debian:bookworm-slim AS runtime' docker/debian-slim.Dockerfile && \
      grep -q 'FROM gcr.io/distroless/cc-debian12:nonroot AS runtime' docker/distroless-cc.Dockerfile && \
      grep -q 'cargo chef cook' docker/debian-slim.Dockerfile && \
      grep -q 'cargo chef cook' docker/distroless-cc.Dockerfile && \
      grep -q 'target-cpu=x86-64-v3' docker/debian-slim.Dockerfile && \
      grep -q 'target-cpu=x86-64-v3' docker/distroless-cc.Dockerfile && \
      grep -q 'ENV DOCKER_IMAGE=debian:bookworm-slim' docker/debian-slim.Dockerfile && \
      grep -q 'ENV DOCKER_IMAGE=gcr.io/distroless/cc-debian12:nonroot' docker/distroless-cc.Dockerfile && \
      grep -q 'USER nonroot' docker/distroless-cc.Dockerfile && \
      [ "$(grep -c 'org.opencontainers.image\.' docker/debian-slim.Dockerfile)" -ge 8 ] && \
      [ "$(grep -c 'org.opencontainers.image\.' docker/distroless-cc.Dockerfile)" -ge 8 ] && \
      [ "$(wc -l < docker/debian-slim.Dockerfile | tr -d ' ')" -le 80 ] && \
      [ "$(wc -l < docker/distroless-cc.Dockerfile | tr -d ' ')" -le 80 ] && \
      echo OK
    </automated>
  </verify>

  <acceptance_criteria>
    - Source: Both files exist at `docker/debian-slim.Dockerfile` and `docker/distroless-cc.Dockerfile`.
    - Source: Both contain `ARG RUST_VERSION=1.91` (matches rust-toolchain.toml).
    - Source: `debian-slim.Dockerfile` contains `FROM debian:bookworm-slim AS runtime` and `ENV DOCKER_IMAGE=debian:bookworm-slim`; `distroless-cc.Dockerfile` contains `FROM gcr.io/distroless/cc-debian12:nonroot AS runtime` and `ENV DOCKER_IMAGE=gcr.io/distroless/cc-debian12:nonroot`.
    - Source: Both contain `cargo chef cook --release --target ${TARGET}`.
    - Source: Both set `RUSTFLAGS=...target-cpu=x86-64-v3` (D-09).
    - Source: Both contain at least 8 `org.opencontainers.image.` LABEL lines (D-08 — eight required keys).
    - Source: `distroless-cc.Dockerfile` contains `USER nonroot` (Pitfall 4) and the binary at `/alloc-bench-cli` not `/usr/local/bin/`.
    - Source: Neither file installs `numactl` (`grep -E 'apt.*numactl|apk.*numactl' docker/*.Dockerfile` returns nothing).
    - Source: Neither file is longer than 80 lines (D-07 cap).
    - Lint: `docker buildx build --check -f docker/debian-slim.Dockerfile --build-arg ALLOC=ptmalloc --build-arg TARGET=x86_64-unknown-linux-gnu --build-arg OCI_VERSION=0.1.0 --build-arg OCI_REVISION=test --build-arg OCI_CREATED=2026-05-19T00:00:00Z .` exits 0 (proves syntactic correctness without doing a full build). Same for `distroless-cc.Dockerfile`.
  </acceptance_criteria>

  <done>
    `docker/debian-slim.Dockerfile` and `docker/distroless-cc.Dockerfile` exist, both ≤ 80 lines, both pass `docker buildx build --check`. The verify command exits 0 with `OK`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Capture wolfi-base digest and create docker/wolfi.Dockerfile</name>

  <files>
    - docker/wolfi.Dockerfile
  </files>

  <read_first>
    - .planning/phases/03-docker-matrix-local-orchestration/03-CONTEXT.md (D-05 wolfi-base, D-06 builder version)
    - .planning/phases/03-docker-matrix-local-orchestration/03-RESEARCH.md §"Pitfall 6 — wolfi-base :latest mutability"
    - docker/debian-slim.Dockerfile (Task 2 output — same template applies)
  </read_first>

  <action>
    Capture the current `cgr.dev/chainguard/wolfi-base:latest` amd64 manifest digest using `docker buildx imagetools inspect cgr.dev/chainguard/wolfi-base:latest --raw | jq -r '.manifests[] | select(.platform.architecture=="amd64") | .digest'` (jq is in `Environment Availability`). Store the resulting `sha256:<hex>` string.

    Create `docker/wolfi.Dockerfile` per the `<glibc_dockerfile_template>` with: `<RUNTIME_FROM>=cgr.dev/chainguard/wolfi-base@sha256:<DIGEST>` (the captured digest); `<DOCKER_IMAGE_VALUE>=cgr.dev/chainguard/wolfi-base@sha256:<DIGEST>` (same — keeps the JSON env block traceable to a specific image); `<DESCRIPTION_SUFFIX>=wolfi (glibc Chainguard; ptmalloc/jemalloc/mimalloc)`; `<BIN_PATH>=/usr/local/bin/alloc-bench-cli`; `<USER_LINE>` omitted (wolfi-base runs as UID 0 per RESEARCH §"Standard Stack"). Builder identical to debian-slim/distroless-cc (`rust:1.91-bookworm`, `x86_64-unknown-linux-gnu`).

    Add a comment header to the Dockerfile recording: (a) the digest captured, (b) the date captured, (c) a one-line note "Per RESEARCH §Pitfall 6: cgr.dev/chainguard/wolfi-base:latest is rebuilt daily; pinning to digest preserves reproducibility. Refresh this digest if rebuilding from scratch and the wolfi runtime stage stops linking — but never replace with `:latest`." File ≤ 80 lines.

    Reject ALLOC ∉ {ptmalloc, jemalloc, mimalloc} (same case as Task 2). Wolfi is glibc — `mallocng` is invalid here.
  </action>

  <verify>
    <automated>
      test -f docker/wolfi.Dockerfile && \
      grep -qE '^FROM cgr\.dev/chainguard/wolfi-base@sha256:[0-9a-f]{64} AS runtime' docker/wolfi.Dockerfile && \
      grep -qE '^ENV DOCKER_IMAGE=cgr\.dev/chainguard/wolfi-base@sha256:[0-9a-f]{64}' docker/wolfi.Dockerfile && \
      grep -q '^ARG RUST_VERSION=1.91' docker/wolfi.Dockerfile && \
      grep -q 'cargo chef cook' docker/wolfi.Dockerfile && \
      grep -q 'target-cpu=x86-64-v3' docker/wolfi.Dockerfile && \
      [ "$(grep -c 'org.opencontainers.image\.' docker/wolfi.Dockerfile)" -ge 8 ] && \
      [ "$(wc -l < docker/wolfi.Dockerfile | tr -d ' ')" -le 80 ] && \
      ! grep -q ':latest' docker/wolfi.Dockerfile && \
      echo OK
    </automated>
  </verify>

  <acceptance_criteria>
    - Source: `docker/wolfi.Dockerfile` exists at the path shown.
    - Source: Contains EXACTLY ONE `FROM cgr.dev/chainguard/wolfi-base@sha256:<64-hex-chars>` line (digest pin) and zero references to `:latest`.
    - Source: Contains `ENV DOCKER_IMAGE=cgr.dev/chainguard/wolfi-base@sha256:<same digest>`.
    - Source: Builder stage matches debian-slim's pattern (`rust:${RUST_VERSION}-bookworm`, `RUST_VERSION=1.91`, target `x86_64-unknown-linux-gnu`).
    - Source: At least 8 `org.opencontainers.image.` LABEL lines.
    - Source: ≤ 80 lines.
    - Source: Comment header records the digest source date (e.g., "captured 2026-05-19 via docker buildx imagetools inspect").
    - Lint: `docker buildx build --check -f docker/wolfi.Dockerfile --build-arg ALLOC=ptmalloc --build-arg TARGET=x86_64-unknown-linux-gnu --build-arg OCI_VERSION=0.1.0 --build-arg OCI_REVISION=test --build-arg OCI_CREATED=2026-05-19T00:00:00Z .` exits 0.
  </acceptance_criteria>

  <done>
    `docker/wolfi.Dockerfile` exists, digest-pinned, ≤ 80 lines, passes `docker buildx build --check`. Verify command exits 0 with `OK`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker registry → builder stage | rust:1.91-bookworm, debian:bookworm-slim, gcr.io/distroless/cc-debian12, cgr.dev/chainguard/wolfi-base — third-party images |
| Builder stage → runtime stage | binary copy via `COPY --from=builder` |
| crates.io → cargo-chef install | `cargo install --locked cargo-chef@0.1.77` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | rust:1.91-bookworm tag drift | mitigate | Pin via `ARG RUST_VERSION=1.91`; matches rust-toolchain.toml. Pinning by digest is deferred until Phase 5 (RESEARCH A1). |
| T-03-02 | Tampering | debian:bookworm-slim tag drift | accept | `bookworm-slim` is the Debian-Stable slim variant; mutates only on Debian point-releases (rare). Phase 3 informational only; Phase 5 may pin by digest. |
| T-03-03 | Tampering | wolfi-base :latest mutability | mitigate | Pin by `@sha256:<digest>` captured at plan-phase time per RESEARCH §Pitfall 6 (Task 3). |
| T-03-04 | Tampering | distroless-cc-debian12:nonroot rebuilds | accept | Google rebuilds these; `:nonroot` floats the base layer. Phase-3 informational; pinning by digest deferred to Phase 5. |
| T-03-05 | Tampering | cargo-chef@0.1.77 install in builder | mitigate | `cargo install --locked` enforces Cargo.lock-pinned transitive deps; T-03-SC covers the slopcheck audit (already verified [OK] in RESEARCH.md "Package Legitimacy Audit"). |
| T-03-06 | Information Disclosure | OCI annotations leak | accept | All eight ARG values are public metadata: `OCI_VERSION` = published Cargo crate version, `OCI_REVISION` = public git SHA, `OCI_CREATED` = build timestamp, others are repo metadata. No secret ingress paths. |
| T-03-SC | Tampering | npm/pip/cargo install supply-chain | mitigate | RESEARCH §"Package Legitimacy Audit" has all 5 Rust crates marked `[OK]`. cargo-chef is the only `cargo install` and uses `--locked`. No new packages added in this plan. |
</threat_model>

<verification>
End-of-plan automated verification:
- `test -f docker/debian-slim.Dockerfile docker/distroless-cc.Dockerfile docker/wolfi.Dockerfile .dockerignore .dive-ci`
- `docker buildx build --check -f docker/debian-slim.Dockerfile --build-arg ALLOC=ptmalloc --build-arg TARGET=x86_64-unknown-linux-gnu --build-arg OCI_VERSION=0.1.0 --build-arg OCI_REVISION=test --build-arg OCI_CREATED=2026-05-19T00:00:00Z .`
- Same `--check` for `distroless-cc.Dockerfile` and `wolfi.Dockerfile`.
- `cargo build --workspace --release` (host) still exits 0 — proves `.dockerignore` did not exclude any tracked file the host build still needs (target/ exclusion only affects Docker build context, not host).
- Pre-commit gate: `prek run --all-files` passes (shellcheck, yamllint, trailing-whitespace, etc., per prek.toml).

Note: Actual `docker buildx build` (not `--check`) for the 6 anchor cells happens in Plan 04. This plan ships the file artifacts only.
</verification>

<success_criteria>
This plan is complete when:
- [ ] `.dockerignore` exists with `target/` excluded and Cargo.toml/Cargo.lock NOT excluded.
- [ ] `.dive-ci` exists with the three CONTEXT D-21 thresholds.
- [ ] `docker/debian-slim.Dockerfile`, `docker/distroless-cc.Dockerfile`, `docker/wolfi.Dockerfile` all exist, ≤ 80 lines each.
- [ ] All three Dockerfiles use `ARG RUST_VERSION=1.91` (matches rust-toolchain.toml).
- [ ] All three Dockerfiles use cargo-chef (chef → planner → builder → runtime).
- [ ] All three Dockerfiles emit all 8 OCI annotation LABELs from --build-arg (D-08).
- [ ] All three Dockerfiles set `RUSTFLAGS=...target-cpu=x86-64-v3` (D-09).
- [ ] All three Dockerfiles set `ENV DOCKER_IMAGE=...` so `metrics::env::read_env` populates the JSON env block.
- [ ] `wolfi.Dockerfile` pins by digest (no `:latest` reference).
- [ ] `distroless-cc.Dockerfile` runs as `USER nonroot`, binary at `/alloc-bench-cli`.
- [ ] `docker buildx build --check` passes for all three Dockerfiles.
- [ ] Pre-commit hooks pass (`prek run --all-files`).
</success_criteria>

<output>
Create `.planning/phases/03-docker-matrix-local-orchestration/03-01-SUMMARY.md` documenting:
1. The wolfi-base digest captured (sha256 + date).
2. Confirmation that `RUST_VERSION=1.91` matches `rust-toolchain.toml` (rationale for deviating from CONTEXT D-06's literal "1.83").
3. The DOCKER_IMAGE env values for each runtime stage.
4. Output of `docker buildx build --check` for each Dockerfile (passing).
5. Reference to Plan 04 for the actual smoke build of these images.
</output>
</content>
</invoke>