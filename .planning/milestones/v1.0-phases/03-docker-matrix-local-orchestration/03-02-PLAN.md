---
phase: 03-docker-matrix-local-orchestration
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docker/alpine.Dockerfile
  - docker/distroless-static.Dockerfile
  - docker/scratch.Dockerfile
autonomous: true
requirements: [DOCK-01, DOCK-04, DOCK-05, DOCK-08]
tags: [docker, musl, cargo-chef, oci-annotations, crt-static, scratch, distroless-static]

must_haves:
  truths:
    - "User can `docker buildx build -f docker/alpine.Dockerfile --platform linux/amd64 --build-arg ALLOC=mallocng ...` and the image builds (musl dynamic)."
    - "User can `docker buildx build -f docker/distroless-static.Dockerfile --platform linux/amd64 --build-arg ALLOC=mallocng ...` and the image builds (UID 65532 nonroot, +crt-static)."
    - "User can `docker buildx build -f docker/scratch.Dockerfile --platform linux/amd64 --build-arg ALLOC=mallocng ...` and the image builds (FROM scratch, fully static)."
    - "Every musl image carries the eight org.opencontainers.image.{title,description,source,version,revision,licenses,created,authors} LABELs populated from --build-arg."
    - "alpine + jemalloc and alpine + mimalloc are smoke-build candidates in Plan 04 — RESEARCH §Pitfall 1 escape hatch already drops cells if static-link fails."
  artifacts:
    - path: "docker/alpine.Dockerfile"
      provides: "musl dynamic runtime on alpine:3.20 with cargo-chef multi-stage build"
      contains: "FROM alpine:3.20"
    - path: "docker/distroless-static.Dockerfile"
      provides: "musl static runtime on gcr.io/distroless/static-debian12:nonroot (UID 65532, +crt-static)"
      contains: "FROM gcr.io/distroless/static-debian12:nonroot"
    - path: "docker/scratch.Dockerfile"
      provides: "musl fully-static runtime FROM scratch (+crt-static)"
      contains: "FROM scratch"
  key_links:
    - from: "All three Dockerfiles"
      to: "rust:1.91-alpine builder"
      via: "FROM rust:${RUST_VERSION}-alpine AS chef"
      pattern: "FROM rust:.*-alpine AS chef"
    - from: "distroless-static.Dockerfile + scratch.Dockerfile"
      to: "+crt-static linker flag"
      via: "ENV RUSTFLAGS=...+crt-static"
      pattern: "target-feature=\\+crt-static"
    - from: "alpine.Dockerfile"
      to: "musl dynamic linkage (NO +crt-static)"
      via: "ENV RUSTFLAGS without +crt-static"
      pattern: "target-cpu=x86-64-v3"
    - from: "All three Dockerfiles"
      to: "OCI annotation LABEL block"
      via: "ARG OCI_VERSION/REVISION/CREATED + LABEL org.opencontainers.image.*"
      pattern: "LABEL org.opencontainers.image.title"
---

<objective>
Create the three musl-family Dockerfiles (alpine, distroless-static, scratch) so any musl cell of the Phase 3 matrix can be built end-to-end via `docker buildx build` with cargo-chef caching, OCI-annotated, and audited via `dive --ci`.

Purpose: Three of the six runtime environments live in the musl family. They share the rust:1.91-alpine builder, the x86_64-unknown-linux-musl target triple, and a single set of cargo-chef stages. Two of them (distroless-static, scratch) require `+crt-static`. Phase-3 cells `{mallocng, jemalloc, mimalloc} × {alpine, distroless-static, scratch}` (9 nominal cells; D-01 escape hatch drops `jemalloc-distroless-static` and/or `mimalloc-scratch` if static-link smoke fails) all depend on this plan.

Output: 3 Dockerfiles. Plan 04 runs the actual smoke matrix and exercises Pitfall §1's escape hatch for any musl-static jemalloc/mimalloc cells that fail to link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03-docker-matrix-local-orchestration/03-CONTEXT.md
@.planning/phases/03-docker-matrix-local-orchestration/03-RESEARCH.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@Cargo.toml
@rust-toolchain.toml
@.cargo/config.toml
@crates/alloc-bench-cli/Cargo.toml

<interfaces>
<!-- Repository facts the executor must respect. Confirmed live 2026-05-19. -->

From rust-toolchain.toml:
  channel = "1.91" — RUST_VERSION must match.

From Cargo.toml [features] in crates/alloc-bench-cli (musl-relevant only):
  default = []
  alloc-jemalloc = ["dep:tikv-jemallocator", "dep:tikv-jemalloc-ctl"]
  alloc-mimalloc = ["dep:mimalloc", "dep:libmimalloc-sys"]
  ALLOC=mallocng → no feature flag (default = system allocator on musl IS mallocng)
  ALLOC=jemalloc → --no-default-features --features alloc-jemalloc
  ALLOC=mimalloc → --no-default-features --features alloc-mimalloc

From Cargo.toml workspace:
  mimalloc = { version = "0.1", default-features = false, features = ["extended"] }
  tikv-jemallocator = "0.6"
  Both already use the musl-safe defaults per RESEARCH §Pitfall 1, §Pitfall 2.

From CONTEXT.md D-08 — eight required OCI annotation keys (same eight as glibc plan):
  org.opencontainers.image.{title, description, source, version, revision, licenses, created, authors}

From RESEARCH.md §"Standard Stack" — verified base images (live `docker buildx imagetools inspect` 2026-05-19):
  rust:1.91-alpine — exists (replaces D-06's "1.83" because rust-toolchain.toml=1.91)
  alpine:3.20 — 3.20.10, musl libc (matches success criterion 2 verbatim: docker_image="alpine:3.20")
  gcr.io/distroless/static-debian12:nonroot — UID 65532, WorkingDir=/home/nonroot, NO libc (requires fully-static binary)
  scratch — empty, no /etc, no resolver, no shell

From CONTEXT.md D-09 + RESEARCH.md §Pitfall 2 (target-cpu) and §Pitfall 1 (musl static-link):
  alpine.Dockerfile: ENV RUSTFLAGS="-C target-cpu=x86-64-v3" (NO +crt-static — alpine is musl dynamic)
  distroless-static.Dockerfile + scratch.Dockerfile: ENV RUSTFLAGS="-C target-cpu=x86-64-v3 -C target-feature=+crt-static"

From CONTEXT.md D-01 — 18-cell matrix; D-01 escape hatch:
  If jemalloc-distroless-static or mimalloc-scratch (or any musl-static + alt-allocator combo) fails to link in Plan 04's smoke pass, the Justfile cell drops to `status: skipped` and SUMMARY.md documents the reason. THIS plan's Dockerfiles must still ATTEMPT the build for {mallocng, jemalloc, mimalloc} — the static-link decision lives in the build invocation, not the Dockerfile.

From RESEARCH.md §"Pattern 1" (canonical musl Dockerfile shape):
  Builder: FROM rust:1.91-alpine + apk add --no-cache musl-dev pkgconfig + cargo install --locked cargo-chef@0.1.77
  Planner: cargo chef prepare --recipe-path recipe.json
  Builder: rustup target add x86_64-unknown-linux-musl + cargo chef cook + cargo build
  Runtime: FROM <env-base> + LABELs + ENV DOCKER_IMAGE + COPY binary + ENTRYPOINT

From RESEARCH.md §Pitfall 4 (distroless-static nonroot):
  Same as distroless-cc: binary at /alloc-bench-cli (NOT /usr/local/bin), USER nonroot, WORKDIR /home/nonroot.

From RESEARCH.md §Pitfall 5 (scratch image):
  No /etc, no terminfo. The bench's `eprintln!` works; chrono with default-features=false works; no TZ data needed.
  Binary path: /alloc-bench-cli (no /usr/local/bin in scratch).
  No USER directive needed (scratch has no /etc/passwd; binary runs as root by default which is fine for benchmark).

From CONTEXT.md "code_context" — DOCKER_IMAGE env for JSON env block:
  alpine: ENV DOCKER_IMAGE=alpine:3.20  (matches success criterion 2 literal)
  distroless-static: ENV DOCKER_IMAGE=gcr.io/distroless/static-debian12:nonroot
  scratch: ENV DOCKER_IMAGE=scratch
</interfaces>

<musl_dockerfile_template>
<!-- Reference shape for all three musl Dockerfiles. The builder stage and
     cargo-chef invocations are identical; only the runtime stage and
     RUSTFLAGS (with/without +crt-static) differ. Note: alpine's runtime
     uses musl dynamic linkage so it does NOT add +crt-static; the other
     two REQUIRE it. -->

ARG RUST_VERSION=1.91

# ─── Stage 1: chef base ────────────────────────────────────────────
FROM rust:${RUST_VERSION}-alpine AS chef
RUN apk add --no-cache musl-dev pkgconfig
RUN cargo install --locked cargo-chef@0.1.77
WORKDIR /app

# ─── Stage 2: planner — compute recipe.json ────────────────────────
FROM chef AS planner
COPY . .
RUN cargo chef prepare --recipe-path recipe.json

# ─── Stage 3: builder ──────────────────────────────────────────────
FROM chef AS builder
ARG ALLOC=mallocng
ARG TARGET=x86_64-unknown-linux-musl
ENV RUSTFLAGS="<RUSTFLAGS_LINE>"
RUN rustup target add ${TARGET}
COPY --from=planner /app/recipe.json recipe.json
RUN if [ "$ALLOC" = "jemalloc" ]; then \
        FEATURES="--no-default-features --features alloc-jemalloc"; \
    elif [ "$ALLOC" = "mimalloc" ]; then \
        FEATURES="--no-default-features --features alloc-mimalloc"; \
    elif [ "$ALLOC" = "mallocng" ]; then \
        FEATURES=""; \
    else \
        echo "ERROR: musl env only supports ALLOC in {mallocng, jemalloc, mimalloc}; got '$ALLOC'" >&2; exit 1; \
    fi && \
    cargo chef cook --release --target ${TARGET} ${FEATURES} \
        -p alloc-bench-cli --recipe-path recipe.json
COPY . .
RUN if [ "$ALLOC" = "jemalloc" ]; then \
        FEATURES="--no-default-features --features alloc-jemalloc"; \
    elif [ "$ALLOC" = "mimalloc" ]; then \
        FEATURES="--no-default-features --features alloc-mimalloc"; \
    else \
        FEATURES=""; \
    fi && \
    cargo build --release --target ${TARGET} ${FEATURES} \
        -p alloc-bench-cli

# ─── Stage 4: runtime — per-env. Differs across the three Dockerfiles. ──
FROM <RUNTIME_FROM> AS runtime
ARG OCI_VERSION
ARG OCI_REVISION
ARG OCI_CREATED
LABEL org.opencontainers.image.title="alloc-bench" \
      org.opencontainers.image.description="Memory allocator benchmark — <DESCRIPTION_SUFFIX>" \
      org.opencontainers.image.source="https://github.com/marccarre/rust-benchmark-glibc-musl-mimalloc" \
      org.opencontainers.image.version="${OCI_VERSION}" \
      org.opencontainers.image.revision="${OCI_REVISION}" \
      org.opencontainers.image.licenses="MIT OR Apache-2.0" \
      org.opencontainers.image.created="${OCI_CREATED}" \
      org.opencontainers.image.authors="Marc Carré"
ENV DOCKER_IMAGE=<DOCKER_IMAGE_VALUE>
COPY --from=builder /app/target/x86_64-unknown-linux-musl/release/alloc-bench-cli <BIN_PATH>
<USER_LINE>
ENTRYPOINT ["<BIN_PATH>"]

# Per-Dockerfile substitutions:
# alpine.Dockerfile:
#   <RUSTFLAGS_LINE>      = -C target-cpu=x86-64-v3
#   <RUNTIME_FROM>        = alpine:3.20
#   <DOCKER_IMAGE_VALUE>  = alpine:3.20
#   <DESCRIPTION_SUFFIX>  = alpine (musl dynamic; mallocng/jemalloc/mimalloc)
#   <BIN_PATH>            = /usr/local/bin/alloc-bench-cli
#   <USER_LINE>           = (omit)
#
# distroless-static.Dockerfile:
#   <RUSTFLAGS_LINE>      = -C target-cpu=x86-64-v3 -C target-feature=+crt-static
#   <RUNTIME_FROM>        = gcr.io/distroless/static-debian12:nonroot
#   <DOCKER_IMAGE_VALUE>  = gcr.io/distroless/static-debian12:nonroot
#   <DESCRIPTION_SUFFIX>  = distroless-static (musl static nonroot; mallocng/jemalloc/mimalloc)
#   <BIN_PATH>            = /alloc-bench-cli   (Pitfall §4: no /usr/local/bin guarantee for nonroot)
#   <USER_LINE>           = USER nonroot
#                           WORKDIR /home/nonroot
#
# scratch.Dockerfile:
#   <RUSTFLAGS_LINE>      = -C target-cpu=x86-64-v3 -C target-feature=+crt-static
#   <RUNTIME_FROM>        = scratch
#   <DOCKER_IMAGE_VALUE>  = scratch
#   <DESCRIPTION_SUFFIX>  = scratch (musl fully-static; mallocng/jemalloc/mimalloc)
#   <BIN_PATH>            = /alloc-bench-cli   (no /usr/local/bin exists in scratch)
#   <USER_LINE>           = (omit; scratch has no /etc/passwd; binary runs as root)
</musl_dockerfile_template>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docker/alpine.Dockerfile (musl dynamic)</name>

  <files>
    - docker/alpine.Dockerfile
  </files>

  <read_first>
    - .planning/phases/03-docker-matrix-local-orchestration/03-CONTEXT.md (D-01 success criterion 2 — alpine MUST report `docker_image: "alpine:3.20"` literally; D-05 base images; D-06 builder version; D-09 RUSTFLAGS)
    - .planning/phases/03-docker-matrix-local-orchestration/03-RESEARCH.md §"Pattern 1", §"Code Examples §1"
    - rust-toolchain.toml (channel=1.91 — RUST_VERSION must match)
    - .cargo/config.toml (host target-cpu=native; ENV RUSTFLAGS in Dockerfile MUST override)
    - Cargo.toml (mimalloc default-features=false already set per RESEARCH §Pitfall 2)
  </read_first>

  <action>
    Create `docker/alpine.Dockerfile` per the `<musl_dockerfile_template>` with these substitutions: `<RUSTFLAGS_LINE>=-C target-cpu=x86-64-v3` (NO +crt-static — alpine is dynamic musl); `<RUNTIME_FROM>=alpine:3.20` (matches success criterion 2 literal); `<DOCKER_IMAGE_VALUE>=alpine:3.20`; `<DESCRIPTION_SUFFIX>=alpine (musl dynamic; mallocng/jemalloc/mimalloc)`; `<BIN_PATH>=/usr/local/bin/alloc-bench-cli`; `<USER_LINE>` omitted.

    Builder uses `rust:${RUST_VERSION}-alpine` with `RUST_VERSION=1.91` (matches rust-toolchain.toml; deviation from CONTEXT D-06 noted in comment). Builder MUST `apk add --no-cache musl-dev pkgconfig` so cargo-chef can compile the workspace's transitive deps that link to musl headers (axum, tokio, reqwest's rustls). Target triple `x86_64-unknown-linux-musl`. Total file ≤ 80 lines (D-07 cap).

    Reject ALLOC ∉ {mallocng, jemalloc, mimalloc} with a non-zero exit and clear error message in BOTH the `chef cook` and `cargo build` step (D-04 cross-libc rejection — ptmalloc is glibc-only and must not silently compile here).

    Do NOT add `+crt-static` to RUSTFLAGS — alpine has musl as a runtime dependency; static linking would defeat the purpose of testing musl dynamic. Static linking is for distroless-static and scratch only.
  </action>

  <verify>
    <automated>
      test -f docker/alpine.Dockerfile && \
      grep -q '^ARG RUST_VERSION=1.91' docker/alpine.Dockerfile && \
      grep -q '^FROM rust:\${RUST_VERSION}-alpine AS chef' docker/alpine.Dockerfile && \
      grep -q 'apk add --no-cache musl-dev pkgconfig' docker/alpine.Dockerfile && \
      grep -q 'cargo install --locked cargo-chef' docker/alpine.Dockerfile && \
      grep -q 'rustup target add' docker/alpine.Dockerfile && \
      grep -q 'cargo chef cook --release --target ' docker/alpine.Dockerfile && \
      grep -q 'target-cpu=x86-64-v3' docker/alpine.Dockerfile && \
      ! grep -q 'crt-static' docker/alpine.Dockerfile && \
      grep -q '^FROM alpine:3.20 AS runtime' docker/alpine.Dockerfile && \
      grep -q '^ENV DOCKER_IMAGE=alpine:3.20' docker/alpine.Dockerfile && \
      [ "$(grep -c 'org.opencontainers.image\.' docker/alpine.Dockerfile)" -ge 8 ] && \
      [ "$(wc -l < docker/alpine.Dockerfile | tr -d ' ')" -le 80 ] && \
      echo OK
    </automated>
  </verify>

  <acceptance_criteria>
    - Source: `docker/alpine.Dockerfile` exists.
    - Source: Contains `ARG RUST_VERSION=1.91` and uses `rust:${RUST_VERSION}-alpine` builder.
    - Source: Builder stage installs `musl-dev pkgconfig` via apk.
    - Source: Builder stage installs `cargo-chef@0.1.77` with `--locked`.
    - Source: Runtime stage is exactly `FROM alpine:3.20 AS runtime` (matches success criterion 2 literal).
    - Source: `ENV DOCKER_IMAGE=alpine:3.20` literal (success criterion 2 expects this exact string).
    - Source: `RUSTFLAGS` contains `-C target-cpu=x86-64-v3`.
    - Source: NO occurrence of `+crt-static` (alpine is musl dynamic — verify via `! grep crt-static docker/alpine.Dockerfile`).
    - Source: At least 8 `org.opencontainers.image.` LABELs.
    - Source: ≤ 80 lines.
    - Lint: `docker buildx build --check -f docker/alpine.Dockerfile --build-arg ALLOC=mallocng --build-arg TARGET=x86_64-unknown-linux-musl --build-arg OCI_VERSION=0.1.0 --build-arg OCI_REVISION=test --build-arg OCI_CREATED=2026-05-19T00:00:00Z .` exits 0.
  </acceptance_criteria>

  <done>
    `docker/alpine.Dockerfile` exists, ≤ 80 lines, dynamic musl (no `+crt-static`), `ENV DOCKER_IMAGE=alpine:3.20` matches success criterion 2 literal, passes `docker buildx build --check`. Verify command exits 0 with `OK`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create docker/distroless-static.Dockerfile (musl static, nonroot)</name>

  <files>
    - docker/distroless-static.Dockerfile
  </files>

  <read_first>
    - .planning/phases/03-docker-matrix-local-orchestration/03-CONTEXT.md (D-01 escape hatch for jemalloc/mimalloc-on-static-musl, D-04 cross-libc rejection, D-05 base images)
    - .planning/phases/03-docker-matrix-local-orchestration/03-RESEARCH.md §"Pattern 1" (canonical musl-static Dockerfile is the reference; copy and rename), §"Pitfall 1" (jemalloc/mimalloc static-musl smoke), §"Pitfall 4" (distroless nonroot UID 65532 + binary at /alloc-bench-cli)
    - docker/alpine.Dockerfile (Task 1 output — same builder stage; only runtime + +crt-static differ)
  </read_first>

  <action>
    Create `docker/distroless-static.Dockerfile` per the `<musl_dockerfile_template>` with these substitutions: `<RUSTFLAGS_LINE>=-C target-cpu=x86-64-v3 -C target-feature=+crt-static` (REQUIRED for distroless-static — image has no libc); `<RUNTIME_FROM>=gcr.io/distroless/static-debian12:nonroot`; `<DOCKER_IMAGE_VALUE>=gcr.io/distroless/static-debian12:nonroot`; `<DESCRIPTION_SUFFIX>=distroless-static (musl static nonroot; mallocng/jemalloc/mimalloc)`; `<BIN_PATH>=/alloc-bench-cli` (FS root — Pitfall §4); `<USER_LINE>=USER nonroot` followed by `WORKDIR /home/nonroot`.

    Builder identical to alpine (rust:1.91-alpine + musl-dev + pkgconfig + cargo-chef). Reject ALLOC ∉ {mallocng, jemalloc, mimalloc} (D-04). Total file ≤ 80 lines.

    Add a comment block above the runtime stage citing RESEARCH §Pitfall 1: "If `--build-arg ALLOC=jemalloc` or `--build-arg ALLOC=mimalloc` fails to link due to static-musl + crt-static incompatibility, Plan 04's smoke pass drops the cell from the matrix per CONTEXT D-01 escape hatch and documents the reason in 03-04-SUMMARY.md. The Dockerfile itself remains valid — the failure is at link time, not parse time."

    The binary at `/alloc-bench-cli` must be world-readable AND executable by UID 65532. By default `COPY --from=builder` preserves UID 0 ownership but `0755` mode bits on a binary in `/` are sufficient for nonroot to execute (Linux read+execute on `/`). Do NOT chown — that adds an extra layer.
  </action>

  <verify>
    <automated>
      test -f docker/distroless-static.Dockerfile && \
      grep -q '^ARG RUST_VERSION=1.91' docker/distroless-static.Dockerfile && \
      grep -q '^FROM rust:\${RUST_VERSION}-alpine AS chef' docker/distroless-static.Dockerfile && \
      grep -q 'cargo chef cook --release --target ' docker/distroless-static.Dockerfile && \
      grep -q 'target-cpu=x86-64-v3' docker/distroless-static.Dockerfile && \
      grep -q 'target-feature=+crt-static' docker/distroless-static.Dockerfile && \
      grep -q '^FROM gcr.io/distroless/static-debian12:nonroot AS runtime' docker/distroless-static.Dockerfile && \
      grep -q '^ENV DOCKER_IMAGE=gcr.io/distroless/static-debian12:nonroot' docker/distroless-static.Dockerfile && \
      grep -q 'USER nonroot' docker/distroless-static.Dockerfile && \
      grep -q 'WORKDIR /home/nonroot' docker/distroless-static.Dockerfile && \
      grep -q 'COPY --from=builder /app/target/x86_64-unknown-linux-musl/release/alloc-bench-cli /alloc-bench-cli' docker/distroless-static.Dockerfile && \
      [ "$(grep -c 'org.opencontainers.image\.' docker/distroless-static.Dockerfile)" -ge 8 ] && \
      [ "$(wc -l < docker/distroless-static.Dockerfile | tr -d ' ')" -le 80 ] && \
      echo OK
    </automated>
  </verify>

  <acceptance_criteria>
    - Source: `docker/distroless-static.Dockerfile` exists.
    - Source: `RUSTFLAGS` contains BOTH `-C target-cpu=x86-64-v3` AND `-C target-feature=+crt-static` (verify both substrings present).
    - Source: Runtime stage is exactly `FROM gcr.io/distroless/static-debian12:nonroot AS runtime`.
    - Source: Contains `USER nonroot` and `WORKDIR /home/nonroot`.
    - Source: Binary copied to `/alloc-bench-cli` (root of FS), NOT `/usr/local/bin/alloc-bench-cli` (Pitfall §4).
    - Source: ENTRYPOINT references `/alloc-bench-cli`.
    - Source: At least 8 `org.opencontainers.image.` LABELs.
    - Source: ≤ 80 lines.
    - Lint: `docker buildx build --check -f docker/distroless-static.Dockerfile --build-arg ALLOC=mallocng --build-arg TARGET=x86_64-unknown-linux-musl --build-arg OCI_VERSION=0.1.0 --build-arg OCI_REVISION=test --build-arg OCI_CREATED=2026-05-19T00:00:00Z .` exits 0.
  </acceptance_criteria>

  <done>
    `docker/distroless-static.Dockerfile` exists, ≤ 80 lines, sets +crt-static, USER nonroot, binary at `/alloc-bench-cli`, passes `docker buildx build --check`. Verify command exits 0 with `OK`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create docker/scratch.Dockerfile (musl fully-static, FROM scratch)</name>

  <files>
    - docker/scratch.Dockerfile
  </files>

  <read_first>
    - .planning/phases/03-docker-matrix-local-orchestration/03-CONTEXT.md (D-01 escape hatch for mimalloc-on-scratch; D-04, D-05)
    - .planning/phases/03-docker-matrix-local-orchestration/03-RESEARCH.md §"Code Examples §2" (scratch.Dockerfile reference), §"Pitfall 5" (scratch image: no /etc, no terminfo; eprintln OK; UTC OK)
    - docker/distroless-static.Dockerfile (Task 2 output — same +crt-static config; only runtime base differs)
  </read_first>

  <action>
    Create `docker/scratch.Dockerfile` per the `<musl_dockerfile_template>` with: `<RUSTFLAGS_LINE>=-C target-cpu=x86-64-v3 -C target-feature=+crt-static` (REQUIRED — scratch has nothing); `<RUNTIME_FROM>=scratch`; `<DOCKER_IMAGE_VALUE>=scratch`; `<DESCRIPTION_SUFFIX>=scratch (musl fully-static; mallocng/jemalloc/mimalloc)`; `<BIN_PATH>=/alloc-bench-cli`; `<USER_LINE>` omitted (scratch has no /etc/passwd; binary runs as root by default — fine for benchmark).

    Builder identical to distroless-static (rust:1.91-alpine + musl-dev + pkgconfig + cargo-chef + +crt-static). Reject ALLOC ∉ {mallocng, jemalloc, mimalloc}. Total file ≤ 80 lines.

    Add a comment header citing RESEARCH §Pitfall 5: "scratch contains nothing — no /etc, no shell, no resolver, no terminfo. The bench's `eprintln!` works (writes to fd 2, no library lookup); chrono is configured with default-features=false so no /etc/localtime read; bench writes to UTC only. No /etc/passwd or /etc/ssl/certs needed (HTTP-only on 127.0.0.1 per PITFALLS §2.3)."

    Do NOT COPY any /etc/* or /etc/ssl/* from the builder — keeps image at the smallest possible size (target ≤ 15 MB per CONTEXT D-22). Do NOT add a USER directive (no /etc/passwd in scratch).
  </action>

  <verify>
    <automated>
      test -f docker/scratch.Dockerfile && \
      grep -q '^ARG RUST_VERSION=1.91' docker/scratch.Dockerfile && \
      grep -q '^FROM rust:\${RUST_VERSION}-alpine AS chef' docker/scratch.Dockerfile && \
      grep -q 'target-cpu=x86-64-v3' docker/scratch.Dockerfile && \
      grep -q 'target-feature=+crt-static' docker/scratch.Dockerfile && \
      grep -q '^FROM scratch AS runtime' docker/scratch.Dockerfile && \
      grep -q '^ENV DOCKER_IMAGE=scratch' docker/scratch.Dockerfile && \
      ! grep -qE '^USER ' docker/scratch.Dockerfile && \
      ! grep -q 'COPY.*--from=builder.*/etc/' docker/scratch.Dockerfile && \
      grep -q 'COPY --from=builder /app/target/x86_64-unknown-linux-musl/release/alloc-bench-cli /alloc-bench-cli' docker/scratch.Dockerfile && \
      grep -q '^ENTRYPOINT \["/alloc-bench-cli"\]' docker/scratch.Dockerfile && \
      [ "$(grep -c 'org.opencontainers.image\.' docker/scratch.Dockerfile)" -ge 8 ] && \
      [ "$(wc -l < docker/scratch.Dockerfile | tr -d ' ')" -le 80 ] && \
      echo OK
    </automated>
  </verify>

  <acceptance_criteria>
    - Source: `docker/scratch.Dockerfile` exists.
    - Source: `RUSTFLAGS` contains both `-C target-cpu=x86-64-v3` and `-C target-feature=+crt-static`.
    - Source: Runtime stage is exactly `FROM scratch AS runtime`.
    - Source: `ENV DOCKER_IMAGE=scratch`.
    - Source: NO `USER` directive (scratch has no /etc/passwd — verify via `! grep -E '^USER ' docker/scratch.Dockerfile`).
    - Source: NO COPY of /etc/* from builder (verify via `! grep 'COPY.*--from=builder.*/etc/' docker/scratch.Dockerfile`).
    - Source: Binary copied to `/alloc-bench-cli` (FS root).
    - Source: `ENTRYPOINT ["/alloc-bench-cli"]` exact line.
    - Source: At least 8 `org.opencontainers.image.` LABELs.
    - Source: ≤ 80 lines.
    - Lint: `docker buildx build --check -f docker/scratch.Dockerfile --build-arg ALLOC=mallocng --build-arg TARGET=x86_64-unknown-linux-musl --build-arg OCI_VERSION=0.1.0 --build-arg OCI_REVISION=test --build-arg OCI_CREATED=2026-05-19T00:00:00Z .` exits 0.
  </acceptance_criteria>

  <done>
    `docker/scratch.Dockerfile` exists, ≤ 80 lines, sets +crt-static, FROM scratch, no USER directive, no /etc COPY, passes `docker buildx build --check`. Verify command exits 0 with `OK`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker registry → builder stage | rust:1.91-alpine, alpine:3.20, gcr.io/distroless/static-debian12, scratch (built-in) |
| Builder stage → runtime stage | binary copy via `COPY --from=builder` |
| crates.io → cargo-chef install | `cargo install --locked cargo-chef@0.1.77` (same as Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | rust:1.91-alpine tag drift | mitigate | `ARG RUST_VERSION=1.91` matches rust-toolchain.toml. Pin by digest deferred to Phase 5. |
| T-03-08 | Tampering | alpine:3.20 tag drift | accept | Patch-level only (3.20.x); 3.20 is the literal in CONTEXT success criterion 2. |
| T-03-09 | Tampering | distroless/static-debian12:nonroot rebuild | accept | Google rebuilds for CVE; `:nonroot` floats. Phase-3 informational; pinning by digest deferred to Phase 5. |
| T-03-10 | Tampering | scratch (built-in) | accept | Empty image; no surface. Tampering not applicable. |
| T-03-11 | Information Disclosure | static binary leaks symbols | accept | `[profile.release] strip = "symbols"` already locks this in Cargo.toml. Verified in Phase 1. |
| T-03-12 | Denial of Service | scratch image fails silently if static-link is incomplete | mitigate | Plan 04 smoke-builds scratch first to confirm linkage; Pitfall 5's "empty docker logs" warning sign covered there. |
| T-03-SC | Tampering | npm/pip/cargo install supply-chain | mitigate | Same as Plan 01 — cargo-chef@0.1.77 with --locked. |
</threat_model>

<verification>
End-of-plan automated verification:
- `test -f docker/alpine.Dockerfile docker/distroless-static.Dockerfile docker/scratch.Dockerfile`
- `docker buildx build --check -f docker/alpine.Dockerfile --build-arg ALLOC=mallocng --build-arg TARGET=x86_64-unknown-linux-musl --build-arg OCI_VERSION=0.1.0 --build-arg OCI_REVISION=test --build-arg OCI_CREATED=2026-05-19T00:00:00Z .`
- Same `--check` for `distroless-static.Dockerfile` and `scratch.Dockerfile` with appropriate ALLOC values.
- Pre-commit gate: `prek run --all-files` passes.

Note: Actual `docker buildx build` (no `--check`) for the 6 anchor cells (including `mallocng-alpine`, `mallocng-scratch`, `mallocng-distroless-static`) happens in Plan 04. The static-link smoke for jemalloc/mimalloc on musl-static lives in Plan 04 and may invoke the D-01 escape hatch (drop cells, document reason).
</verification>

<success_criteria>
This plan is complete when:
- [ ] `docker/alpine.Dockerfile`, `docker/distroless-static.Dockerfile`, `docker/scratch.Dockerfile` all exist, ≤ 80 lines each.
- [ ] All three use `ARG RUST_VERSION=1.91` and `rust:${RUST_VERSION}-alpine` builder.
- [ ] All three install `musl-dev pkgconfig` + cargo-chef@0.1.77 in builder stage.
- [ ] `alpine.Dockerfile` does NOT add `+crt-static` (musl dynamic).
- [ ] `distroless-static.Dockerfile` AND `scratch.Dockerfile` add `-C target-feature=+crt-static`.
- [ ] All three emit all 8 OCI annotation LABELs (D-08).
- [ ] All three set `ENV DOCKER_IMAGE=...` so the bench JSON env block reports the runtime base.
- [ ] `alpine.Dockerfile` has `ENV DOCKER_IMAGE=alpine:3.20` literally (success criterion 2).
- [ ] `distroless-static.Dockerfile` runs as `USER nonroot`, binary at `/alloc-bench-cli`.
- [ ] `scratch.Dockerfile` has no USER directive, no /etc COPY, binary at `/alloc-bench-cli`.
- [ ] All three pass `docker buildx build --check`.
- [ ] Pre-commit hooks pass (`prek run --all-files`).
</success_criteria>

<output>
Create `.planning/phases/03-docker-matrix-local-orchestration/03-02-SUMMARY.md` documenting:
1. The three musl Dockerfiles created with their RUSTFLAGS values (alpine: dynamic; the other two: +crt-static).
2. Confirmation that the alpine `ENV DOCKER_IMAGE=alpine:3.20` matches success criterion 2 literal.
3. Reference to Plan 04 for actual smoke build of musl + alt-allocator combos and any cells dropped via D-01 escape hatch.
</output>
</content>
</invoke>