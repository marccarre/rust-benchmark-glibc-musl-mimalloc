//! REPORT.md emitter (D-09/D-10/D-11/D-12).
//!
//! Plan 01 shipped a deliberately minimal emitter (header + a `## Runs`
//! bullet list + `## Skipped inputs`). Plan 03 makes the report readable:
//!
//!   - Per-scenario allocator comparison tables with bold-and-✓ winner
//!     prefix and italic suspect notes (D-09 / AGG-04).
//!   - `## Docker runtimes` table with em-dash cells + Phase-5 backfill
//!     footnote (D-10 / AGG-05).
//!   - `## Allocator architectures` section embedding the four Mermaid
//!     constants in alphabetical order (D-11 / AGG-06).
//!   - `## Recommendations by workload` with data-derived rationale
//!     strings (D-12 / AGG-07) — calls `recommend::recommendations`.
//!
//! The Plan-01 `## Runs` bullet section is REMOVED — the per-scenario
//! tables subsume it.
//!
//! Byte-identical-output contract (D-09 / RESEARCH §Pitfall 5):
//!   - Sort everything alphabetically (BTreeMap / BTreeSet, never
//!     HashMap / HashSet).
//!   - Format throughput as `{:.1}` with the unit appended.
//!   - Format integers as `{}`, latency cells as `{} ns`, RSS as `{} kB`.
//!   - The single timestamp comment at the top is the only non-stable
//!     line — strippable in tests via first-line removal.

use std::collections::{BTreeMap, BTreeSet, HashMap};
use std::fmt::Write as _;
use std::path::Path;

use alloc_bench_core::output::{Env, HarnessInfo, Run};
use anyhow::{Context, Result};
use tinytemplate::TinyTemplate;

use crate::axes::{column_header_with_arrow, Direction};
use crate::diagrams::ALL_DIAGRAMS;
use crate::html::{build_cell_template_context, is_suspect, RECOMMEND_CELL_MD};
use crate::loader::{CellMeta, LoadOutcome};
use crate::multi_run::{aggregate as mr_aggregate, is_high_variance, MultiRunStats};
use crate::recommend::{recommendations, CellRecommendation, TOP_N_TABLE};

/// Public entry point — write `report/REPORT.md` against the loaded
/// outcome. Builds the buffer end-to-end via `build_report()` then
/// commits to disk. The two-step shape lets the byte-identical test
/// (Behavior 13) compare two builds without touching the filesystem.
///
/// Phase 8 / Plan 02 (CELL-03): after `REPORT.md`, also write 10
/// standalone single-card `.md` fragments to
/// `report/recommend-{rank:02d}-{alloc}-{env}.md` (one per top-N cell)
/// for future drilldown-link surfaces (V12-04 deferred).
pub fn write(
    outcome: &LoadOutcome,
    metas: &HashMap<(String, String), CellMeta>,
    top_n: &[CellRecommendation],
    out_dir: &Path,
) -> Result<()> {
    let buf = build_report(outcome, metas, top_n)?;
    let out_path = out_dir.join("REPORT.md");
    std::fs::write(&out_path, &buf).with_context(|| format!("writing {}", out_path.display()))?;

    // Phase 8 / Plan 02 / CELL-03 — per-cell standalone Markdown fragments.
    // Filename pattern locked: `recommend-{rank:02d}-{alloc}-{env}.md`
    // (CONTEXT.md "Filenames & Per-cell Artifacts"; rank zero-padded for
    // natural filename sort; cell.env is already short-form per Phase 7
    // plumbing, recommend.rs:659).
    for cell in top_n.iter() {
        let frag = render_cell_md(cell)?;
        let frag_path = out_dir.join(format!(
            "recommend-{:02}-{}-{}.md",
            cell.rank, cell.alloc, cell.env
        ));
        std::fs::write(&frag_path, &frag)
            .with_context(|| format!("writing {}", frag_path.display()))?;
    }
    Ok(())
}

/// Build the complete REPORT.md as a String. Factored from `write()` so
/// the byte-identical-output test can call it twice without disk I/O.
///
/// Phase 8 / Plan 02: returns `Result<String>` (was `String`) so render
/// errors from `emit_top_n_cells` (tinytemplate compile/render failures)
/// propagate via `?`. Existing single-run-per-cell golden fixtures are
/// preserved via the early-return-on-empty-top_n discipline in
/// `emit_top_n_cells` (Q3 RESOLVED — see 08-RESEARCH §Section 6).
pub(crate) fn build_report(
    outcome: &LoadOutcome,
    metas: &HashMap<(String, String), CellMeta>,
    top_n: &[CellRecommendation],
) -> Result<String> {
    let mut buf = String::new();
    emit_header(&mut buf, outcome);
    emit_per_scenario_tables(&mut buf, &outcome.runs);
    emit_docker_runtimes_table(&mut buf, &outcome.runs, metas);
    emit_allocator_diagrams(&mut buf);
    emit_recommendations(&mut buf, &outcome.runs);
    // Phase 8 / Plan 02 / CELL-04 — Top 10 cells AFTER recommendations
    // (Q1 lock — see 08-RESEARCH §Section 6). Empty top_n early-returns
    // from `emit_top_n_cells` so v1.0 byte-identical golden fixtures
    // continue to pass for synthetic outcomes that carry zero scoring data.
    emit_top_n_cells(&mut buf, top_n)?;
    if !outcome.skipped.is_empty() {
        emit_skipped(&mut buf, &outcome.skipped);
    }
    Ok(buf)
}

fn emit_header(buf: &mut String, outcome: &LoadOutcome) {
    let timestamp = chrono::Utc::now().to_rfc3339();
    let _ = writeln!(
        buf,
        "<!-- schema_version: 1 · generated by alloc-bench-aggregator at {timestamp} -->"
    );
    let _ = writeln!(buf, "# alloc-bench REPORT");
    let _ = writeln!(buf);
    let cell_count = count_unique_cells(&outcome.runs);
    let _ = writeln!(
        buf,
        "**{} runs across {} cells.**",
        outcome.runs.len(),
        cell_count
    );
    let _ = writeln!(buf);
}

/// Per-scenario allocator comparison tables (D-09 / AGG-04 / D-11 / D-12).
///
/// One section per scenario in alphabetical order; rows alphabetical by
/// (allocator, env_label); winner row prefixed `**✓ {alloc}**`.
///
/// Multi-run decoration (D-11): when ≥2 runs share an `(alloc, env, scenario)`
/// triple, the throughput cell renders as `{median} ({min}..{max}, CV {N}%)`
/// with optional `⚠ high variance` (CV > 10%) and `⚠ suspect` flags
/// concatenated. When the cell has fewer than 2 runs, falls back to the
/// existing single-value `{:.1} {unit}` format with the suspect-note
/// italic — preserving the Phase-4 byte-identical-output contract for
/// every existing single-run-per-cell fixture.
fn emit_per_scenario_tables(buf: &mut String, runs: &[Run]) {
    // BTreeMap → alphabetical scenario iteration.
    let mut by_scenario: BTreeMap<&str, Vec<&Run>> = BTreeMap::new();
    for r in runs {
        by_scenario
            .entry(r.scenario.name.as_str())
            .or_default()
            .push(r);
    }

    for (scenario_name, scenario_runs) in by_scenario.iter() {
        // D-11 grouping pre-pass: build a BTreeMap<(alloc, env), Vec<f64>>
        // of throughput samples for this scenario. Used to decide whether
        // to emit the multi-run cell shape vs the single-value fallback.
        // BTreeMap is mandatory for alphabetical-iteration / byte-identical
        // output (RESEARCH §Pitfall 5).
        let mut by_cell: BTreeMap<(String, String), Vec<f64>> = BTreeMap::new();
        for r in scenario_runs.iter() {
            by_cell
                .entry((r.build.allocator.clone(), env_label(&r.env).to_string()))
                .or_default()
                .push(r.metrics.ticks_per_s);
        }

        let _ = writeln!(buf, "## {scenario_name}");
        let _ = writeln!(buf);
        // Phase 10 / DIR-02: one-line legend above every per-scenario table,
        // between the `## {scenario}` heading and the table header. Layout
        // is `heading\n\n{legend}\n\nheader\n` — gives one blank line
        // above the legend and one blank line below. Interpunct is U+00B7
        // (NOT U+2022 BULLET, NOT U+002E FULL STOP); pinned by
        // `legend_row_above_each_scenario_table` test.
        let _ = writeln!(
            buf,
            "\u{2191} higher is better \u{00b7} \u{2193} lower is better \u{00b7} \u{26a0} suspect run"
        );
        let _ = writeln!(buf);
        // Phase 10 / DIR-01: arrow-decorated header line built via
        // `column_header_with_arrow` (axes.rs SSoT helper). The
        // `allocator` column stays plain — it's a label, not a measurement.
        let _ = writeln!(
            buf,
            "| allocator | {} | {} | {} | {} | {} | {} |",
            column_header_with_arrow("throughput", Direction::Higher),
            column_header_with_arrow("p50", Direction::Lower),
            column_header_with_arrow("p95", Direction::Lower),
            column_header_with_arrow("p99", Direction::Lower),
            column_header_with_arrow("p999", Direction::Lower),
            column_header_with_arrow("peak RSS", Direction::Lower),
        );
        let _ = writeln!(buf, "|---|---|---|---|---|---|---|");

        // Build a per-cell BTreeMap<(alloc, env), &Run> for emission rows.
        // Pick the FIRST run per (alloc, env) tuple (deterministic via
        // BTreeMap ordering once scenario_runs is sorted alpha) so we
        // don't emit duplicate rows when a fixture has multiple seeds.
        let mut row_keys: BTreeMap<(String, String), &Run> = BTreeMap::new();
        for r in scenario_runs.iter() {
            let key = (r.build.allocator.clone(), env_label(&r.env).to_string());
            row_keys.entry(key).or_insert(r);
        }

        // Winner index in iteration order: maximum CENTRAL TENDENCY (median
        // when ≥2 runs, mean fallback when n<2). The strict `>` keeps the
        // FIRST-seen winner on a tie, matching `recommend::pick_rationale_scenario`
        // and the dashboard's `renderReportMirrorTable`.
        let row_vec: Vec<((String, String), &Run)> =
            row_keys.iter().map(|(k, v)| (k.clone(), *v)).collect();
        let mut winner_idx: Option<usize> = None;
        for (i, (key, _)) in row_vec.iter().enumerate() {
            let tps = central_tendency(by_cell.get(key));
            match winner_idx {
                Some(j) => {
                    let prev = central_tendency(by_cell.get(&row_vec[j].0));
                    if tps > prev {
                        winner_idx = Some(i);
                    }
                }
                None => winner_idx = Some(i),
            }
        }

        for (idx, (key, r)) in row_vec.iter().enumerate() {
            let unit = r.scenario.unit.as_deref().unwrap_or("ticks/s");
            let alloc_cell = if Some(idx) == winner_idx {
                format!("**\u{2713} {}**", r.build.allocator)
            } else {
                r.build.allocator.clone()
            };
            let suspect = suspect_reason(&r.harness).is_some();
            // D-11 / D-12: multi-run shape when ≥2 runs in this cell.
            let throughput_cell = if let Some(samples) = by_cell.get(key) {
                if let Some(stats) = mr_aggregate(samples) {
                    format_throughput_cell(&stats, suspect)
                } else {
                    let mut s = format!("{:.1} {}", r.metrics.ticks_per_s, unit);
                    if let Some(reason) = suspect_reason(&r.harness) {
                        s.push(' ');
                        s.push_str(suspect_note(&reason));
                    }
                    s
                }
            } else {
                let mut s = format!("{:.1} {}", r.metrics.ticks_per_s, unit);
                if let Some(reason) = suspect_reason(&r.harness) {
                    s.push(' ');
                    s.push_str(suspect_note(&reason));
                }
                s
            };
            let _ = writeln!(
                buf,
                "| {alloc} | {tps} | {p50} ns | {p95} ns | {p99} ns | {p999} ns | {rss} kB |",
                alloc = alloc_cell,
                tps = throughput_cell,
                p50 = r.metrics.tick_latency_ns.p50,
                p95 = r.metrics.tick_latency_ns.p95,
                p99 = r.metrics.tick_latency_ns.p99,
                p999 = r.metrics.tick_latency_ns.p999,
                rss = r.metrics.peak_rss_kb,
            );
        }
        let _ = writeln!(buf);
    }
}

/// Central tendency for a set of throughput samples: median when ≥2 runs
/// (matching D-11), mean fallback for n<2 so winner-picking on existing
/// single-run-per-cell fixtures is byte-stable. Returns `0.0` when the
/// samples slice is empty (only possible if the caller passed `None`).
fn central_tendency(samples: Option<&Vec<f64>>) -> f64 {
    let Some(samples) = samples else { return 0.0 };
    if samples.is_empty() {
        return 0.0;
    }
    if let Some(s) = mr_aggregate(samples) {
        s.median
    } else {
        samples.iter().sum::<f64>() / samples.len() as f64
    }
}

/// Format a single throughput cell with multi-run decoration:
///   "100 (95..110, CV 5%)"
/// or with high-variance flag (CV > 10%):
///   "100 (90..130, CV 15% ⚠ high variance)"
/// or both flags concatenated (per CONTEXT.md `<specifics>` ¶5):
///   "100 (90..130, CV 15% ⚠ high variance ⚠ suspect)"
/// or with undefined CV (mean ≈ 0):
///   "0 (0..0, CV —)"
///
/// Output shape is canonical per RESEARCH §"Code Examples — Multi-run
/// aggregator integration" (lines 956-967 of 05-RESEARCH.md): all numbers
/// `{:.0}` rounded; CV `{:.0}%`; em-dash `\u{2014}` for undefined CV;
/// variance flag `\u{26A0} high variance`; suspect flag `\u{26A0} suspect`.
pub(crate) fn format_throughput_cell(s: &MultiRunStats, suspect: bool) -> String {
    let cv_str = match s.cv_pct {
        Some(cv) => format!("CV {cv:.0}%"),
        None => "CV \u{2014}".to_string(),
    };
    let variance_flag = if is_high_variance(s) {
        " \u{26A0} high variance"
    } else {
        ""
    };
    let suspect_flag = if suspect { " \u{26A0} suspect" } else { "" };
    format!(
        "{:.0} ({:.0}..{:.0}, {}{}{})",
        s.median, s.min, s.max, cv_str, variance_flag, suspect_flag
    )
}

/// Docker runtimes table (D-10 / AGG-05 / D-13).
///
/// When `metas` is empty (default `--meta` flag), every value cell renders
/// as em-dash — preserving Phase-4 byte-identical-output. When `metas` is
/// non-empty (CI populated it via `docker image inspect --format '{{.Size}}'`),
/// each env row's `image_size_mb` cell is filled from the matching sidecar
/// (1 decimal place); cells without a meta sidecar fall back to em-dash.
///
/// Join semantics: a meta sidecar carries the **short** env name
/// (e.g. `"alpine"`), but `env_label` here is the docker_image tag
/// (e.g. `"alloc-bench:jemalloc-alpine"`). We synthesize the expected
/// docker_image as `format!("alloc-bench:{}-{}", meta.alloc, meta.env)`
/// and look that up against the row's env_label. This avoids parsing
/// the docker tag on the read side.
fn emit_docker_runtimes_table(
    buf: &mut String,
    runs: &[Run],
    metas: &HashMap<(String, String), CellMeta>,
) {
    let envs: BTreeSet<String> = runs.iter().map(|r| env_label(&r.env).to_string()).collect();

    // Build a reverse index: docker_image_tag → image_size_mb (alphabetical
    // tiebreak via BTreeMap when multiple metas synthesize the same tag,
    // which shouldn't happen in practice but the order is deterministic).
    let mut by_docker_image: BTreeMap<String, f64> = BTreeMap::new();
    for ((alloc, env_short), meta) in metas.iter() {
        let synth = format!("alloc-bench:{alloc}-{env_short}");
        by_docker_image.insert(synth, meta.image_size_mb);
    }

    let _ = writeln!(buf, "## Docker runtimes");
    let _ = writeln!(buf);
    let _ = writeln!(
        buf,
        "| env | image_size_mb | build_time_s | run_overhead_pct |"
    );
    let _ = writeln!(buf, "|---|---|---|---|");
    for env in envs.iter() {
        let size_cell = match by_docker_image.get(env) {
            Some(mb) => format!("{mb:.1}"),
            None => "\u{2014}".to_string(),
        };
        let _ = writeln!(buf, "| {env} | {size_cell} | \u{2014} | \u{2014} |");
    }
    let _ = writeln!(buf);
    if metas.is_empty() {
        let _ = writeln!(
            buf,
            "*image_size_mb / build_time_s / run_overhead_pct populated by Phase 5 CI via docker inspect (REPR-03).*"
        );
    } else {
        let _ = writeln!(
            buf,
            "*image_size_mb populated from CI sidecar (D-13); em-dash for cells without meta sidecars.*"
        );
    }
    let _ = writeln!(buf);
}

/// Allocator architectures section (D-11 / AGG-06). Iterates
/// `diagrams::ALL_DIAGRAMS` in alphabetical order (jemalloc, mallocng,
/// mimalloc, ptmalloc) and emits each Mermaid constant verbatim under
/// its own `### {name}` subsection.
fn emit_allocator_diagrams(buf: &mut String) {
    let _ = writeln!(buf, "## Allocator architectures");
    let _ = writeln!(buf);
    for (name, body) in ALL_DIAGRAMS.iter() {
        let _ = writeln!(buf, "### {name}");
        let _ = writeln!(buf);
        let _ = writeln!(
            buf,
            "Source: see crates/alloc-bench-aggregator/src/diagrams.rs for upstream reference."
        );
        // The constant body opens with a leading newline + fenced mermaid
        // block (Task 1 baked the leading newline in deliberately).
        buf.push_str(body);
        let _ = writeln!(buf);
    }
}

/// Recommendations by workload (D-12 / AGG-07). Calls
/// `recommend::recommendations` and renders one row per Recommendation.
/// The rationale cell is emitted verbatim — including the data-derived
/// `+{delta:.1}%` substring and the optional `*(suspect)*` annotation.
/// Hardcoded prose is forbidden — the recommend module is the SINGLE
/// authority for these strings (RESEARCH §Pitfall 7).
fn emit_recommendations(buf: &mut String, runs: &[Run]) {
    let recs = recommendations(runs);
    let _ = writeln!(buf, "## Recommendations by workload");
    let _ = writeln!(buf);
    let _ = writeln!(buf, "| Workload | Recommended | Rationale |");
    let _ = writeln!(buf, "|---|---|---|");
    for r in recs.iter() {
        let _ = writeln!(buf, "| {} | {} | {} |", r.class, r.allocator, r.rationale);
    }
    let _ = writeln!(buf);
}

/// Phase 8 / Plan 02 / CELL-04 — `## Top 10 cells` section emit.
///
/// Inserts AFTER `emit_recommendations` (Q1 lock — see 08-RESEARCH §Section 6).
/// Composition (per UI-SPEC §Layout & Interaction):
///   - section heading + caption
///   - leading `| Rank | Cell | Score |` summary table (Q3 RESOLVED in-scope)
///   - top-`TOP_N_TABLE` (5) cards always visible, separated by `---` rules
///   - cards 6-10 inside a single `<details><summary>Show ranks 6–10</summary>`
///     block (en-dash U+2013 between 6 and 10)
///
/// Empty-`top_n` early-return preserves v1.0 byte-identical golden fixtures
/// where synthetic outcomes have zero scoring data — no bytes appended.
/// Symmetrical with the HTML side's `{{if has_top_n}}` wrapper (Plan 02 Task 2).
///
/// Returns `Result<()>` to propagate `tinytemplate` render errors from
/// `render_cell_md` via `?`. Cascades up through `build_report`'s
/// `Result<String>` return type to `pub fn write`.
fn emit_top_n_cells(buf: &mut String, top_n: &[CellRecommendation]) -> Result<()> {
    if top_n.is_empty() {
        return Ok(());
    }

    let _ = writeln!(buf, "## Top 10 cells");
    let _ = writeln!(buf);
    let _ = writeln!(
        buf,
        "Ranked 1-10 by composite score (equal-weighted across 8 axes). Cards 6-10 collapsed by default."
    );
    let _ = writeln!(buf);

    // Leading `| Rank | Cell | Score |` summary table (Q3 RESOLVED in-scope —
    // see 08-RESEARCH §Section 6 / 08-CONTEXT.md "Specifics"). The table is
    // the only surface where `composite_score` is visible (per CELL-05 the
    // cards themselves don't show it). `{:.3}` precision matches the
    // CONTEXT.md sentinel test value `0.789`.
    // Phase 9 / POLAR-05 — Pareto column. `\u{2605}` (★) glyph renders for
    // cells on the front (composite_score↑ × image_size_mb↓ strict-dominance
    // sweep, computed by `score::pareto_front` and decorated onto each
    // `CellRecommendation` by `recommend::top_n_cells` per Plan 09-02). The
    // column matches the per-cell template trailing-★ in
    // `recommend-cell.md.tmpl` so the summary table and the detailed cards
    // agree byte-for-byte on Pareto-front membership (WR-01 cross-surface
    // drift defense). Empty image_sizes (CI-without-meta path) yields all
    // cells `is_pareto: false` → all rows render an empty cell, preserving
    // v1.0 byte-identical output until the meta sidecar is wired.
    let _ = writeln!(buf, "| Rank | Cell | Score | Pareto |");
    let _ = writeln!(buf, "|------|------|-------|--------|");
    for cell in top_n.iter() {
        let pareto_glyph = if cell.is_pareto { "\u{2605}" } else { "" };
        let _ = writeln!(
            buf,
            "| {:02} | {} on {} | {:.3} | {} |",
            cell.rank, cell.alloc, cell.env, cell.composite_score, pareto_glyph
        );
    }
    let _ = writeln!(buf);

    // Visible/collapsed split — handles `top_n.len() < TOP_N_TABLE` gracefully
    // by capping the visible count at the actual length.
    let split = top_n.len().min(TOP_N_TABLE);

    // Top-`TOP_N_TABLE` (5) cards always visible. Separated by `---` between
    // consecutive cards (NOT after the final visible card).
    let visible = &top_n[..split];
    for (i, cell) in visible.iter().enumerate() {
        buf.push_str(&render_cell_md(cell)?);
        if i + 1 < visible.len() {
            let _ = writeln!(buf, "---");
            let _ = writeln!(buf);
        }
    }

    // Cards `TOP_N_TABLE+1`..=`TOP_N_TOTAL` inside `<details>` block (only if
    // any collapsed cards exist).
    if top_n.len() > TOP_N_TABLE {
        let _ = writeln!(buf);
        let _ = writeln!(buf, "<details>");
        // En-dash U+2013 between 6 and 10 per UI-SPEC §Top-10-cells
        // composition. The summary text is content for `<details>`'s GFM
        // disclosure widget.
        let _ = writeln!(buf, "<summary>Show ranks 6\u{2013}10</summary>");
        let _ = writeln!(buf);
        let collapsed = &top_n[TOP_N_TABLE..];
        for (i, cell) in collapsed.iter().enumerate() {
            buf.push_str(&render_cell_md(cell)?);
            if i + 1 < collapsed.len() {
                let _ = writeln!(buf, "---");
                let _ = writeln!(buf);
            }
        }
        let _ = writeln!(buf, "</details>");
        let _ = writeln!(buf);
    }

    Ok(())
}

/// Phase 8 / Plan 02 / CELL-03 — render a single CellRecommendation
/// through the per-cell Markdown template. Used by `emit_top_n_cells`
/// (in-document section) and by `pub fn write`'s per-cell fragment
/// loop.
///
/// Trailing `\n` on every fragment per CONTEXT.md "Specifics" ¶6
/// (POSIX file convention; concatenable / cat-friendly).
fn render_cell_md(cell: &CellRecommendation) -> Result<String> {
    let mut tt = TinyTemplate::new();
    tt.add_template("recommend-cell-md", RECOMMEND_CELL_MD)
        .context("compiling recommend-cell.md.tmpl")?;
    let ctx = build_cell_template_context(cell);
    let mut s = tt
        .render("recommend-cell-md", &ctx)
        .context("rendering recommend-cell.md")?;
    s.push('\n');
    Ok(s)
}

fn emit_skipped(buf: &mut String, skipped: &[crate::loader::SkippedFile]) {
    let _ = writeln!(buf, "## Skipped inputs");
    let _ = writeln!(buf);
    let mut sorted: Vec<&crate::loader::SkippedFile> = skipped.iter().collect();
    sorted.sort_by(|a, b| a.path.cmp(&b.path));
    for s in sorted {
        let _ = writeln!(buf, "- {}: {}", s.path.display(), s.reason);
    }
    let _ = writeln!(buf);
}

/// Stable label for an env (Docker image tag if present, else "host").
/// Used both for cell-counting and for sorted bullet emission. Matches
/// the html.rs unique-cell counter so the two stay coherent.
pub(crate) fn env_label(env: &Env) -> &str {
    env.docker_image.as_deref().unwrap_or("host")
}

/// Count of unique (allocator, env_label) cells across all runs.
fn count_unique_cells(runs: &[Run]) -> usize {
    let mut set = BTreeSet::new();
    for r in runs {
        set.insert((r.build.allocator.as_str(), env_label(&r.env)));
    }
    set.len()
}

/// Which D-07 suspect predicate(s) fire for this harness. Return values
/// match `is_suspect(h) == reason.is_some()` exactly — keep them in
/// lockstep so REPORT.md's italic notes can't drift from the HTML
/// dashboard's badges.
enum SuspectReason {
    LowSamples,
    ShortWarmup,
    Both,
}

fn suspect_reason(h: &HarnessInfo) -> Option<SuspectReason> {
    // SOURCE-OF-TRUTH lockstep with `crate::html::is_suspect` —
    // threshold lowered from 10_000 in quick task 260524-5nc.
    let low = h.samples_count < 1_000;
    let short = h.warmup_duration_s < 5.0;
    let reason = match (low, short) {
        (true, true) => Some(SuspectReason::Both),
        (true, false) => Some(SuspectReason::LowSamples),
        (false, true) => Some(SuspectReason::ShortWarmup),
        (false, false) => None,
    };
    // Single-source-of-truth sanity check: this classifier and the
    // canonical D-07 predicate `crate::html::is_suspect` must agree.
    // If they ever drift, REPORT.md italic notes and the dashboard ⚠
    // badges would split-brain on which runs are flagged.
    debug_assert_eq!(reason.is_some(), is_suspect(h));
    reason
}

fn suspect_note(reason: &SuspectReason) -> &'static str {
    match reason {
        SuspectReason::LowSamples => "*(\u{26A0} suspect: low samples)*",
        SuspectReason::ShortWarmup => "*(\u{26A0} suspect: short warmup)*",
        SuspectReason::Both => "*(\u{26A0} suspect: low samples, short warmup)*",
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use alloc_bench_core::output::{
        Build, Env, HarnessInfo, LatencyNs, Metrics, Rusage, ScenarioInfo,
    };
    use alloc_bench_core::SCHEMA_VERSION;

    fn make_run(
        alloc: &str,
        scenario: &str,
        ticks: f64,
        samples: u64,
        warmup_s: f64,
        docker_image: Option<&str>,
    ) -> Run {
        Run {
            schema_version: SCHEMA_VERSION,
            run_id: format!("test-{alloc}-{scenario}"),
            env: Env {
                os: "linux".into(),
                os_version: "test".into(),
                docker_image: docker_image.map(|s| s.to_string()),
                cpu_model: "test-cpu".into(),
                cpu_count: 1,
                memory_total_kb: 1,
            },
            build: Build {
                allocator: alloc.into(),
                rustc_version: "1.83.0".into(),
                target_triple: "x86_64-unknown-linux-gnu".into(),
                host_triple: "x86_64-unknown-linux-gnu".into(),
                profile: "release".into(),
                git_sha: "0".repeat(40),
                git_dirty: false,
                build_timestamp: "2026-05-19T00:00:00Z".into(),
                rustflags: "".into(),
            },
            scenario: ScenarioInfo {
                name: scenario.into(),
                config: serde_json::json!({}),
                unit: None,
            },
            harness: HarnessInfo {
                warmup_duration_s: warmup_s,
                measurement_duration_s: 5.0,
                samples_count: samples,
            },
            metrics: Metrics {
                ticks_per_s: ticks,
                allocations_per_tick: 100,
                tick_latency_ns: LatencyNs {
                    p50: 1000,
                    p95: 2000,
                    p99: 3000,
                    p999: 5000,
                    max: 10000,
                },
                peak_rss_kb: 1000,
                rss_growth_samples: vec![],
                rusage: Rusage {
                    user_time_s: 0.0,
                    sys_time_s: 0.0,
                    minor_faults: 0,
                    major_faults: 0,
                    voluntary_csw: 0,
                    involuntary_csw: 0,
                    peak_rss_kb: 1000,
                },
                allocator_stats: serde_json::json!({}),
            },
            status: Some("success".into()),
            error: None,
        }
    }

    #[test]
    fn per_scenario_table_marks_winner_with_check_prefix() {
        let runs = vec![
            make_run("jemalloc", "cpu-bound", 100.0, 50_000, 5.0, None),
            make_run("mimalloc", "cpu-bound", 110.0, 50_000, 5.0, None),
            make_run("ptmalloc", "cpu-bound", 80.0, 50_000, 5.0, None),
        ];
        let mut buf = String::new();
        emit_per_scenario_tables(&mut buf, &runs);
        assert!(
            buf.contains("**\u{2713} mimalloc**"),
            "expected `**✓ mimalloc**` in output:\n{buf}"
        );
        assert!(
            !buf.contains("**\u{2713} jemalloc**"),
            "jemalloc should not be marked winner"
        );
        assert!(
            !buf.contains("**\u{2713} ptmalloc**"),
            "ptmalloc should not be marked winner"
        );
    }

    /// WR-01 regression: when two or more allocators record IDENTICAL
    /// throughput on a scenario, the per-scenario table must mark the
    /// alphabetically-FIRST allocator as the winner (matching
    /// `recommend.rs::pick_rationale_scenario` and the dashboard's
    /// `renderReportMirrorTable`). Previously `Iterator::max_by` returned
    /// the LAST equal element, which split-brained the markdown output
    /// against the recommendations table and the HTML dashboard.
    #[test]
    fn per_scenario_table_winner_picks_alphabetically_first_on_throughput_tie() {
        let runs = vec![
            // jemalloc and mimalloc tie at 100.0; alphabetically jemalloc < mimalloc.
            make_run("jemalloc", "cpu-bound", 100.0, 50_000, 5.0, None),
            make_run("mimalloc", "cpu-bound", 100.0, 50_000, 5.0, None),
        ];
        let mut buf = String::new();
        emit_per_scenario_tables(&mut buf, &runs);
        assert!(
            buf.contains("**\u{2713} jemalloc**"),
            "expected alphabetically-first `**✓ jemalloc**` on tie:\n{buf}"
        );
        assert!(
            !buf.contains("**\u{2713} mimalloc**"),
            "mimalloc should NOT win on tie (alphabetical tiebreak):\n{buf}"
        );
    }

    #[test]
    fn per_scenario_table_appends_suspect_note_to_low_samples_run() {
        let runs = vec![
            make_run("jemalloc", "cpu-bound", 100.0, 500, 5.0, None), // low samples (< 1_000)
            make_run("ptmalloc", "cpu-bound", 80.0, 50_000, 5.0, None),
        ];
        let mut buf = String::new();
        emit_per_scenario_tables(&mut buf, &runs);
        let count = buf.matches("*(\u{26A0} suspect: low samples)*").count();
        assert_eq!(count, 1, "expected exactly one low-samples note in:\n{buf}");
        assert!(
            !buf.contains("short warmup"),
            "did not expect short-warmup note"
        );
    }

    #[test]
    fn per_scenario_table_marks_warmup_suspect() {
        let runs = vec![make_run("jemalloc", "cpu-bound", 100.0, 50_000, 2.0, None)];
        let mut buf = String::new();
        emit_per_scenario_tables(&mut buf, &runs);
        assert!(
            buf.contains("*(\u{26A0} suspect: short warmup)*"),
            "expected short-warmup note in:\n{buf}"
        );
    }

    #[test]
    fn per_scenario_table_marks_both_suspect_predicates() {
        let runs = vec![make_run("jemalloc", "cpu-bound", 100.0, 500, 2.0, None)];
        let mut buf = String::new();
        emit_per_scenario_tables(&mut buf, &runs);
        assert!(
            buf.contains("*(\u{26A0} suspect: low samples, short warmup)*"),
            "expected combined note in:\n{buf}"
        );
    }

    #[test]
    fn docker_runtimes_table_emits_em_dash_for_missing_fields() {
        let runs = vec![
            make_run("jemalloc", "cpu-bound", 100.0, 50_000, 5.0, Some("img-a")),
            make_run("ptmalloc", "cpu-bound", 80.0, 50_000, 5.0, Some("img-b")),
        ];
        let mut buf = String::new();
        let metas: HashMap<(String, String), CellMeta> = HashMap::new();
        emit_docker_runtimes_table(&mut buf, &runs, &metas);
        // Two unique env labels → two non-header rows.
        let row_count = buf
            .lines()
            .filter(|l| l.starts_with("| ") && !l.contains("env |") && !l.starts_with("|---"))
            .count();
        assert_eq!(row_count, 2, "expected exactly 2 data rows in:\n{buf}");
        // Every value cell is em-dash U+2014.
        assert!(
            buf.contains("| img-a | \u{2014} | \u{2014} | \u{2014} |"),
            "img-a row missing em-dash cells:\n{buf}"
        );
        assert!(
            buf.contains("| img-b | \u{2014} | \u{2014} | \u{2014} |"),
            "img-b row missing em-dash cells:\n{buf}"
        );
        assert!(
            buf.contains("Phase 5 CI via docker inspect"),
            "missing Phase-5 backfill footnote in:\n{buf}"
        );
    }

    /// D-09 byte-identical-output contract: two builds against the same
    /// outcome differ only in the leading schema_version timestamp line.
    ///
    /// Phase 8 / Plan 02 update: `build_report` now returns `Result<String>`
    /// (was `String`) — call `.expect("byte-identity test")` on each. Empty
    /// `top_n` is passed so `emit_top_n_cells` early-returns and adds zero
    /// bytes (preserves v1.0 byte-identity for synthetic-no-scores fixtures).
    #[test]
    fn report_md_two_runs_byte_identical_after_timestamp_strip() {
        let runs = vec![
            make_run("jemalloc", "cpu-bound", 100.0, 50_000, 5.0, Some("img-a")),
            make_run("ptmalloc", "cpu-bound", 80.0, 50_000, 5.0, Some("img-b")),
            make_run("mimalloc", "multithread", 120.0, 50_000, 5.0, Some("img-c")),
        ];
        let outcome = LoadOutcome {
            runs,
            skipped: vec![],
        };
        let metas: HashMap<(String, String), CellMeta> = HashMap::new();
        let top_n: Vec<CellRecommendation> = Vec::new();
        let a = build_report(&outcome, &metas, &top_n).expect("byte-identity test");
        let b = build_report(&outcome, &metas, &top_n).expect("byte-identity test");
        // Strip the FIRST line (schema_version comment) from each.
        let strip_first = |s: &str| -> String {
            let mut lines = s.splitn(2, '\n');
            lines.next();
            lines.next().unwrap_or("").to_string()
        };
        let a_body = strip_first(&a);
        let b_body = strip_first(&b);
        assert_eq!(
            a_body, b_body,
            "REPORT.md bodies differ after stripping timestamp"
        );
    }

    // ---------------------------------------------------------------------
    // D-11 / D-12 — multi-run rendering (Plan 03 Task 2 additions).
    //
    // Each test pins one row of the contract table from CONTEXT.md
    // `<specifics>` ¶5 + RESEARCH §"Code Examples — Multi-run aggregator
    // integration" lines 956-967. The fixture set is synthetic (built via
    // `make_run`) so the assertions don't depend on the multi_run/seed-*
    // fixture files (covered by the integration tests in tests/smoke.rs).
    // ---------------------------------------------------------------------

    /// D-11 / RESEARCH §"Code Examples": format_throughput_cell pin —
    /// canonical low-CV shape `{median:.0} ({min:.0}..{max:.0}, CV {N}%)`.
    #[test]
    fn format_throughput_cell_low_cv_no_flags() {
        let stats = MultiRunStats {
            n: 3,
            mean: 105.0,
            median: 100.0,
            min: 95.0,
            max: 110.0,
            stddev: 4.76,
            cv_pct: Some(4.76),
        };
        let cell = format_throughput_cell(&stats, false);
        assert_eq!(cell, "100 (95..110, CV 5%)", "got {cell:?}");
    }

    /// D-12: when `cv_pct > 10.0`, the variance flag `⚠ high variance`
    /// is appended after the CV percentage. No suspect flag here.
    #[test]
    fn format_throughput_cell_high_variance_flag_appended() {
        let stats = MultiRunStats {
            n: 3,
            mean: 106.67,
            median: 100.0,
            min: 90.0,
            max: 130.0,
            stddev: 20.82,
            cv_pct: Some(15.3),
        };
        let cell = format_throughput_cell(&stats, false);
        assert_eq!(
            cell, "100 (90..130, CV 15% \u{26A0} high variance)",
            "got {cell:?}"
        );
    }

    /// CONTEXT.md `<specifics>` ¶5: when both flags fire (suspect AND
    /// high-variance), they are concatenated in this exact order:
    /// `... CV {N}% ⚠ high variance ⚠ suspect`.
    #[test]
    fn format_throughput_cell_both_flags_concatenated() {
        let stats = MultiRunStats {
            n: 3,
            mean: 106.67,
            median: 100.0,
            min: 90.0,
            max: 130.0,
            stddev: 20.82,
            cv_pct: Some(15.3),
        };
        let cell = format_throughput_cell(&stats, true);
        assert_eq!(
            cell, "100 (90..130, CV 15% \u{26A0} high variance \u{26A0} suspect)",
            "got {cell:?}"
        );
    }

    /// D-11 + Wikipedia near-zero edge case: when `cv_pct` is `None`
    /// (mean ≈ 0), the CV cell renders as em-dash `\u{2014}`.
    #[test]
    fn format_throughput_cell_undefined_cv_em_dash() {
        let stats = MultiRunStats {
            n: 3,
            mean: 0.0,
            median: 0.0,
            min: 0.0,
            max: 0.0,
            stddev: 0.0,
            cv_pct: None,
        };
        let cell = format_throughput_cell(&stats, false);
        assert_eq!(cell, "0 (0..0, CV \u{2014})", "got {cell:?}");
    }

    /// D-11: per-scenario table emits the multi-run cell shape (with the
    /// `(min..max, CV N%)` substring) when ≥2 runs share an
    /// `(alloc, env, scenario)` triple. Multithread fixture: seed values
    /// 100/110/105 → median=105, min=95... wait, this synthesis uses
    /// 100/110/105 → min=100, max=110, CV ≈ 4.76%. Anchor `(100..110, CV`.
    #[test]
    fn per_scenario_table_emits_multi_run_cell_when_three_runs_share_cell() {
        let runs = vec![
            make_run(
                "jemalloc",
                "multithread",
                100.0,
                50_000,
                5.0,
                Some("alpine"),
            ),
            make_run(
                "jemalloc",
                "multithread",
                110.0,
                50_000,
                5.0,
                Some("alpine"),
            ),
            make_run(
                "jemalloc",
                "multithread",
                105.0,
                50_000,
                5.0,
                Some("alpine"),
            ),
        ];
        let mut buf = String::new();
        emit_per_scenario_tables(&mut buf, &runs);
        assert!(
            buf.contains("(100..110, CV"),
            "expected multi-run shape `(100..110, CV` in:\n{buf}"
        );
    }

    /// D-12: per-scenario table flags `⚠ high variance` for cells with
    /// CV > 10%. Cpu-bound fixture: 100/130/90 → mean 106.67, CV ≈ 19.5%.
    #[test]
    fn per_scenario_table_emits_high_variance_flag_for_volatile_cell() {
        let runs = vec![
            make_run("jemalloc", "cpu-bound", 100.0, 50_000, 5.0, Some("alpine")),
            make_run("jemalloc", "cpu-bound", 130.0, 50_000, 5.0, Some("alpine")),
            make_run("jemalloc", "cpu-bound", 90.0, 50_000, 5.0, Some("alpine")),
        ];
        let mut buf = String::new();
        emit_per_scenario_tables(&mut buf, &runs);
        assert!(
            buf.contains("\u{26A0} high variance"),
            "expected `⚠ high variance` glyph in:\n{buf}"
        );
    }

    /// D-13: `emit_docker_runtimes_table` populates `image_size_mb` from
    /// the meta sidecar when one matches the row's docker_image label.
    /// Sidecar carries `image_size_mb: 26.55` → cell shows `26.6` (Rust's
    /// `{:.1}` rounds the IEEE-754 representation of `26.55` half-up;
    /// see deviation note in 05-03-SUMMARY.md). We anchor on the
    /// invariant `26.` prefix so the test is stable across reasonable
    /// formatting choices, and add an exact-match assertion against the
    /// canonical Rust output for regression visibility.
    #[test]
    fn docker_runtimes_table_populates_image_size_mb_from_meta() {
        let runs = vec![make_run(
            "jemalloc",
            "cpu-bound",
            100.0,
            50_000,
            5.0,
            Some("alloc-bench:jemalloc-alpine"),
        )];
        let mut metas: HashMap<(String, String), CellMeta> = HashMap::new();
        metas.insert(
            ("jemalloc".to_string(), "alpine".to_string()),
            CellMeta {
                alloc: "jemalloc".to_string(),
                env: "alpine".to_string(),
                image_size_bytes: 27_845_632,
                image_size_mb: 26.55,
                build_time_s: Some(142.3),
                captured_at: Some("2026-05-19T15:30:42Z".to_string()),
            },
        );
        let mut buf = String::new();
        emit_docker_runtimes_table(&mut buf, &runs, &metas);
        // Stable anchor: `26.` — independent of the `{:.1}` rounding
        // direction (5→6 vs 5→5 across language runtimes).
        assert!(
            buf.contains("26."),
            "expected `26.` (image_size_mb formatted) in:\n{buf}"
        );
        // Exact-match for Rust's canonical `{:.1}` of `26.55_f64`.
        assert!(
            buf.contains("| alloc-bench:jemalloc-alpine | 26.6 |"),
            "expected canonical Rust formatting `| ... | 26.6 |` in:\n{buf}"
        );
        // Footnote should switch to the D-13 wording when metas non-empty.
        assert!(
            buf.contains("populated from CI sidecar"),
            "expected D-13 footnote wording in:\n{buf}"
        );
    }

    /// D-13 byte-identical-output contract preservation: when `metas` is
    /// empty (default `--meta=""` invocation), the existing Phase-4
    /// em-dash output and footnote are produced verbatim.
    #[test]
    fn docker_runtimes_table_byte_stable_when_metas_empty() {
        let runs = vec![make_run(
            "jemalloc",
            "cpu-bound",
            100.0,
            50_000,
            5.0,
            Some("img-a"),
        )];
        let metas: HashMap<(String, String), CellMeta> = HashMap::new();
        let mut buf = String::new();
        emit_docker_runtimes_table(&mut buf, &runs, &metas);
        assert!(
            buf.contains("| img-a | \u{2014} | \u{2014} | \u{2014} |"),
            "expected Phase-4 em-dash row in:\n{buf}"
        );
        assert!(
            buf.contains("Phase 5 CI via docker inspect"),
            "expected original Phase-5 backfill footnote when metas empty in:\n{buf}"
        );
        assert!(
            !buf.contains("populated from CI sidecar"),
            "did not expect D-13 footnote when metas empty:\n{buf}"
        );
    }

    // ---------------------------------------------------------------------
    // Phase 8 / Plan 02 / CELL-04 — `## Top 10 cells` section emit tests.
    //
    // Synthesizes minimal `CellRecommendation` instances directly (the
    // per-cell template only references rank, alloc, env, tldr,
    // strengths/weaknesses/recommended_for/avoid_for, suspect_flag — empty
    // axes BTreeMap is OK).
    // ---------------------------------------------------------------------

    /// Build a synthetic `CellRecommendation` with the given rank/alloc/env.
    /// `composite_score` matches the CONTEXT.md sentinel pattern (0.789 for
    /// rank 1, descending) so Test 5 can assert `{:.3}` formatting.
    fn make_cell(rank: usize, alloc: &str, env: &str) -> CellRecommendation {
        CellRecommendation {
            rank,
            alloc: alloc.to_string(),
            env: env.to_string(),
            // Composite-score sentinel: rank 1 → 0.789; descending by 0.033
            // per rank so `{:.3}` formatting renders three distinct decimals.
            composite_score: 0.789 - 0.033 * (rank as f64 - 1.0),
            axes: BTreeMap::new(),
            tldr: format!("synthetic-tldr-rank-{rank}"),
            strengths: vec!["throughput", "latency"],
            weaknesses: vec!["memory", "image-size"],
            recommended_for: vec!["cpu-bound"],
            avoid_for: vec!["memory-bound"],
            suspect_flag: false,
            // Phase 9 / POLAR-05: render-rendering tests don't exercise the
            // Pareto column yet — the markdown emitter row-formatter gains
            // its `★` glyph in Plan 09-03. Default `false` is byte-identical
            // with the v1.0 baseline.
            is_pareto: false,
        }
    }

    /// Build a top_n vec of length `n` with deterministic alloc/env labels.
    /// Uses the four canonical allocators × short env names so cards have
    /// non-empty, byte-stable rendered output.
    fn make_top_n(n: usize) -> Vec<CellRecommendation> {
        let allocs = ["mimalloc", "jemalloc", "mallocng", "ptmalloc"];
        let envs = ["alpine", "debian-slim", "wolfi"];
        (1..=n)
            .map(|rank| {
                let alloc = allocs[(rank - 1) % allocs.len()];
                let env = envs[(rank - 1) % envs.len()];
                make_cell(rank, alloc, env)
            })
            .collect()
    }

    /// Test 1: 10-cell top_n splits visible/collapsed at TOP_N_TABLE=5.
    #[test]
    fn emit_top_n_cells_section_splits_at_top_n_table() {
        let top_n = make_top_n(10);
        let mut buf = String::new();
        emit_top_n_cells(&mut buf, &top_n).expect("emit");

        // Section heading + caption verbatim.
        assert!(
            buf.contains("## Top 10 cells"),
            "missing section heading in:\n{buf}"
        );
        assert!(
            buf.contains("Ranked 1-10 by composite score (equal-weighted across 8 axes). Cards 6-10 collapsed by default."),
            "missing section caption verbatim in:\n{buf}"
        );

        // `<details>` / `<summary>` with en-dash.
        let details_idx = buf
            .find("<details>")
            .expect(&format!("missing <details> in:\n{buf}"));
        let summary_count = buf
            .matches("<summary>Show ranks 6\u{2013}10</summary>")
            .count();
        assert_eq!(
            summary_count, 1,
            "expected exactly one summary line with en-dash 6–10 in:\n{buf}"
        );
        let close_idx = buf
            .find("</details>")
            .expect(&format!("missing </details> in:\n{buf}"));
        assert!(
            close_idx > details_idx,
            "</details> must come after <details> in:\n{buf}"
        );

        // Visible cards (ranks 1..=5) appear BEFORE <details>; collapsed
        // cards (ranks 6..=10) appear AFTER it.
        for rank in 1..=5 {
            let needle = format!("synthetic-tldr-rank-{rank}");
            let idx = buf
                .find(&needle)
                .expect(&format!("missing visible rank-{rank} tldr in:\n{buf}"));
            assert!(
                idx < details_idx,
                "rank {rank} tldr must appear BEFORE <details> in:\n{buf}"
            );
        }
        for rank in 6..=10 {
            let needle = format!("synthetic-tldr-rank-{rank}");
            let idx = buf
                .find(&needle)
                .expect(&format!("missing collapsed rank-{rank} tldr in:\n{buf}"));
            assert!(
                idx > details_idx,
                "rank {rank} tldr must appear AFTER <details> in:\n{buf}"
            );
            assert!(
                idx < close_idx,
                "rank {rank} tldr must appear BEFORE </details> in:\n{buf}"
            );
        }

        // Exactly four `---` separators between visible cards (1-2, 2-3,
        // 3-4, 4-5) and exactly four between collapsed cards (6-7, 7-8,
        // 8-9, 9-10) → 8 total inside the section.
        let visible_section = &buf[..details_idx];
        let visible_section_start = visible_section
            .find("## Top 10 cells")
            .expect("section start");
        // Strip everything before the first card to count only intra-card
        // separators (NOT the leading-summary-table separator `|------|...`).
        let first_card_idx = visible_section
            .find("synthetic-tldr-rank-1")
            .expect("first visible card");
        let _ = visible_section_start; // anchor sanity, not used in the count
        let between_visible = &visible_section[first_card_idx..];
        let visible_seps = between_visible.matches("\n---\n").count();
        assert_eq!(
            visible_seps, 4,
            "expected 4 `---` separators between visible cards (1-2, 2-3, 3-4, 4-5) in:\n{between_visible}"
        );
        // Between collapsed cards (after <details>, before </details>).
        let collapsed_section = &buf[details_idx..close_idx];
        let collapsed_seps = collapsed_section.matches("\n---\n").count();
        assert_eq!(
            collapsed_seps, 4,
            "expected 4 `---` separators between collapsed cards (6-7..9-10) in:\n{collapsed_section}"
        );
    }

    /// Test 2: top_n length 3 → no `<details>` block, three cards with two
    /// `---` separators, leading summary table has exactly 3 data rows.
    #[test]
    fn emit_top_n_cells_handles_fewer_than_ten_cells() {
        let top_n = make_top_n(3);
        let mut buf = String::new();
        emit_top_n_cells(&mut buf, &top_n).expect("emit");

        assert!(
            !buf.contains("<details>"),
            "expected NO <details> block when top_n.len() < TOP_N_TABLE+1, got:\n{buf}"
        );
        assert!(
            !buf.contains("<summary>"),
            "expected NO <summary> when top_n.len() < TOP_N_TABLE+1, got:\n{buf}"
        );

        // Three cards (ranks 1, 2, 3).
        for rank in 1..=3 {
            let needle = format!("synthetic-tldr-rank-{rank}");
            assert!(
                buf.contains(&needle),
                "missing rank-{rank} tldr in:\n{buf}"
            );
        }

        // Two `---` separators (between 1-2 and 2-3).
        let sep_count = buf.matches("\n---\n").count();
        assert_eq!(
            sep_count, 2,
            "expected exactly 2 `---` separators for top_n.len()=3 in:\n{buf}"
        );

        // Leading summary table has exactly 3 data rows (in addition to
        // header row `| Rank | Cell | Score |` and separator row).
        // Each data row matches the pattern `| 0X | <alloc> on <env> | 0.YYY |`.
        let table_start = buf
            .find("| Rank | Cell | Score |")
            .expect("missing summary header");
        // The table extends until the first card (`### 1. ...`).
        let first_card = buf.find("### 1. ").expect("missing first card heading");
        let table_section = &buf[table_start..first_card];
        // Count rows starting with `| 0` (matching `| 01 ` / `| 02 ` / `| 03 `).
        let data_rows = table_section
            .lines()
            .filter(|l| {
                l.starts_with("| 0")
                    && !l.starts_with("|----")
                    && !l.starts_with("| Rank")
            })
            .count();
        assert_eq!(
            data_rows, 3,
            "expected 3 data rows in summary table for top_n.len()=3 in:\n{table_section}"
        );
    }

    /// Test 3: a cell with `suspect_flag = true` emits the literal
    /// six-byte `*(suspect)*` substring on its rank line.
    #[test]
    fn emit_top_n_cells_emits_suspect_suffix_for_flagged_cells() {
        let mut cell = make_cell(1, "mimalloc", "alpine");
        cell.suspect_flag = true;
        let top_n = vec![cell];
        let mut buf = String::new();
        emit_top_n_cells(&mut buf, &top_n).expect("emit");

        assert!(
            buf.contains("*(suspect)*"),
            "expected literal `*(suspect)*` substring for suspect-flagged cell in:\n{buf}"
        );
    }

    /// Test 4: build_report inserts ## Top 10 cells AFTER ## Recommendations
    /// by workload AND BEFORE ## Skipped inputs (when present).
    #[test]
    fn build_report_inserts_top_n_after_recommendations_before_skipped() {
        let runs = vec![
            make_run("jemalloc", "cpu-bound", 100.0, 50_000, 5.0, Some("img-a")),
            make_run("ptmalloc", "cpu-bound", 80.0, 50_000, 5.0, Some("img-b")),
        ];
        let skipped = vec![crate::loader::SkippedFile {
            path: std::path::PathBuf::from("test.json"),
            reason: "synthetic skip".to_string(),
        }];
        let outcome = LoadOutcome { runs, skipped };
        let metas: HashMap<(String, String), CellMeta> = HashMap::new();
        let top_n = make_top_n(10);
        let buf =
            build_report(&outcome, &metas, &top_n).expect("build_report should succeed");

        let recommendations_idx = buf
            .find("## Recommendations by workload")
            .expect(&format!("missing ## Recommendations by workload:\n{buf}"));
        let top_n_idx = buf
            .find("## Top 10 cells")
            .expect(&format!("missing ## Top 10 cells:\n{buf}"));
        let skipped_idx = buf
            .find("## Skipped inputs")
            .expect(&format!("missing ## Skipped inputs:\n{buf}"));

        assert!(
            recommendations_idx < top_n_idx,
            "recommendations ({recommendations_idx}) must precede top_n ({top_n_idx}) in:\n{buf}"
        );
        assert!(
            top_n_idx < skipped_idx,
            "top_n ({top_n_idx}) must precede skipped ({skipped_idx}) in:\n{buf}"
        );

        // No reorder: per-scenario tables must still come before recommendations.
        let cpu_bound_idx = buf
            .find("## cpu-bound")
            .expect(&format!("missing ## cpu-bound section:\n{buf}"));
        assert!(
            cpu_bound_idx < recommendations_idx,
            "per-scenario tables must precede recommendations (no reorder) in:\n{buf}"
        );
    }

    // ---------------------------------------------------------------------
    // Phase 9 / Plan 09-03 / Task 2 — POLAR-05 Pareto column.
    //
    // Three tests gate the 4-column summary table extension:
    //   - top_n_cells_summary_table_includes_pareto_column_header
    //   - top_n_cells_summary_table_renders_star_for_pareto_cells
    //   - top_n_cells_summary_table_separator_includes_pareto_column
    // Star glyph U+2605 (3 bytes UTF-8: `0xE2 0x98 0x85`).
    // ---------------------------------------------------------------------

    /// POLAR-05: the leading `## Top 10 cells` summary table header is
    /// widened to 4 columns: `| Rank | Cell | Score | Pareto |`. Exact
    /// literal substring match — no whitespace drift permitted.
    #[test]
    fn top_n_cells_summary_table_includes_pareto_column_header() {
        let mut top_n = make_top_n(3);
        // Spike one cell on the front so the table renders both the
        // glyph and an empty cell.
        top_n[0].is_pareto = true;
        let mut buf = String::new();
        emit_top_n_cells(&mut buf, &top_n).expect("emit");

        assert!(
            buf.contains("| Rank | Cell | Score | Pareto |"),
            "expected literal `| Rank | Cell | Score | Pareto |` 4-column header in:\n{buf}"
        );
    }

    /// POLAR-05: rows render `\u{2605}` (★) for Pareto-front cells and
    /// empty (single space) for non-Pareto cells. The 4-column shape
    /// `| 01 | mimalloc on alpine | 0.789 | ★ |` for is_pareto=true;
    /// `| 02 | jemalloc on debian-slim | 0.756 |  |` for is_pareto=false
    /// (note the empty cell — single space between the third `|` and
    /// fourth `|`).
    #[test]
    fn top_n_cells_summary_table_renders_star_for_pareto_cells() {
        let mut top_n = make_top_n(3);
        top_n[0].is_pareto = true; // rank 1 — should render ★
        // top_n[1] / top_n[2] keep is_pareto=false from make_cell default

        let mut buf = String::new();
        emit_top_n_cells(&mut buf, &top_n).expect("emit");

        // make_top_n cycles through allocs[0..4]=[mimalloc,jemalloc,mallocng,ptmalloc]
        // and envs[0..3]=[alpine,debian-slim,wolfi]; rank 1 → mimalloc/alpine,
        // rank 2 → jemalloc/debian-slim. Score values from make_cell:
        //   rank 1: 0.789 - 0.033*0 = 0.789 → "0.789"
        //   rank 2: 0.789 - 0.033*1 = 0.756 → "0.756"
        assert!(
            buf.contains("| 01 | mimalloc on alpine | 0.789 | \u{2605} |"),
            "expected Pareto row literal `| 01 | mimalloc on alpine | 0.789 | \u{2605} |` in:\n{buf}"
        );
        assert!(
            buf.contains("| 02 | jemalloc on debian-slim | 0.756 |  |"),
            "expected non-Pareto row with empty cell `|  |` in:\n{buf}"
        );
    }

    /// POLAR-05: the 4-column separator row matches the 4-column header.
    /// `|------|------|-------|--------|` — the new `--------` segment
    /// (8 dashes) widens the line by exactly one column.
    #[test]
    fn top_n_cells_summary_table_separator_includes_pareto_column() {
        let top_n = make_top_n(3);
        let mut buf = String::new();
        emit_top_n_cells(&mut buf, &top_n).expect("emit");

        assert!(
            buf.contains("|------|------|-------|--------|"),
            "expected literal 4-column separator `|------|------|-------|--------|` in:\n{buf}"
        );
    }

    // ---------------------------------------------------------------------
    // Phase 10 / Plan 10-01 Task 2 — DIR-01 / DIR-02 / DIR-05 tests.
    //
    // Three tests gate the per-scenario table arrow decoration + legend:
    //   - scenario_table_headers_carry_direction_markers   (DIR-01)
    //   - legend_row_above_each_scenario_table             (DIR-02)
    //   - data_cells_contain_no_direction_markers          (DIR-05)
    //
    // U+2191 / U+2193 / U+00B7 / U+26A0 literals used in expected strings
    // to guard against look-alike glyph substitution (T-10-01 / T-10-02
    // mitigations).
    // ---------------------------------------------------------------------

    /// DIR-01: every per-scenario table header line must equal exactly
    /// `| allocator | throughput ↑ | p50 ↓ | p95 ↓ | p99 ↓ | p999 ↓ | peak RSS ↓ |`
    /// — the `allocator` column stays plain, every measurement column
    /// carries its arrow glyph from `column_header_with_arrow`. Uses
    /// `\u{2191}` / `\u{2193}` literals to pin the exact U+2191 /
    /// U+2193 bytes (T-10-01 mitigation).
    #[test]
    fn scenario_table_headers_carry_direction_markers() {
        let runs = vec![
            make_run("jemalloc", "cpu-bound", 100.0, 50_000, 5.0, None),
            make_run("mimalloc", "cpu-bound", 110.0, 50_000, 5.0, None),
        ];
        let mut buf = String::new();
        emit_per_scenario_tables(&mut buf, &runs);

        let expected_header = "| allocator | throughput \u{2191} | p50 \u{2193} | p95 \u{2193} | p99 \u{2193} | p999 \u{2193} | peak RSS \u{2193} |";
        assert!(
            buf.contains(expected_header),
            "expected literal arrow-decorated header line in:\n{buf}\n\nexpected:\n{expected_header}"
        );

        // Sanity: the `allocator` column stays plain (no arrow attached).
        assert!(
            !buf.contains("allocator \u{2191}") && !buf.contains("allocator \u{2193}"),
            "the allocator column must stay plain (no arrow) in:\n{buf}"
        );
    }

    /// DIR-02: a one-line legend `↑ higher is better · ↓ lower is better
    /// · ⚠ suspect run` appears once per scenario, on its own line, with
    /// one blank line on each side, between the `## {scenario}` heading
    /// and the table header. Uses interpunct U+00B7 (NOT U+2022 BULLET,
    /// NOT U+002E FULL STOP) — pinned by literal escape (T-10-02).
    #[test]
    fn legend_row_above_each_scenario_table() {
        // Two distinct scenarios → two legend occurrences.
        let runs = vec![
            make_run("jemalloc", "cpu-bound", 100.0, 50_000, 5.0, None),
            make_run("jemalloc", "multithread", 120.0, 50_000, 5.0, None),
        ];
        let mut buf = String::new();
        emit_per_scenario_tables(&mut buf, &runs);

        // Legend literal — U+2191, U+2193, U+00B7, U+26A0.
        let legend = "\u{2191} higher is better \u{00b7} \u{2193} lower is better \u{00b7} \u{26a0} suspect run";
        let occurrences = buf.matches(legend).count();
        assert_eq!(
            occurrences, 2,
            "expected legend to appear once per scenario (2 total) in:\n{buf}"
        );

        // Interpunct discipline: must contain U+00B7, must NOT contain
        // U+2022 BULLET in the legend region. Scan all lines.
        for line in buf.lines() {
            if line.contains("higher is better") {
                assert!(
                    line.contains('\u{00b7}'),
                    "legend line must contain U+00B7 MIDDLE DOT, got: {line:?}"
                );
                assert!(
                    !line.contains('\u{2022}'),
                    "legend must NOT contain U+2022 BULLET, got: {line:?}"
                );
            }
        }

        // Layout pin: for each scenario, find `## {name}` then assert
        // the next non-blank line is the legend, and the line after the
        // legend (skipping one blank) is the table header.
        for scenario in &["cpu-bound", "multithread"] {
            let heading = format!("## {scenario}");
            let lines: Vec<&str> = buf.lines().collect();
            let heading_idx = lines
                .iter()
                .position(|l| *l == heading)
                .unwrap_or_else(|| panic!("missing heading `{heading}` in:\n{buf}"));
            // Expect: heading, blank, legend, blank, header.
            assert_eq!(
                lines[heading_idx + 1], "",
                "expected blank line below `{heading}`, got: {:?}",
                lines[heading_idx + 1]
            );
            assert_eq!(
                lines[heading_idx + 2], legend,
                "expected legend on the line after the blank below `{heading}`, got: {:?}",
                lines[heading_idx + 2]
            );
            assert_eq!(
                lines[heading_idx + 3], "",
                "expected blank line above the table for `{heading}`, got: {:?}",
                lines[heading_idx + 3]
            );
            assert!(
                lines[heading_idx + 4].starts_with("| allocator |"),
                "expected table header on the line after the legend's trailing blank for `{heading}`, got: {:?}",
                lines[heading_idx + 4]
            );
        }
    }

    /// DIR-05: data cells contain ZERO `↑` / `↓` glyphs across the
    /// per-scenario tables surface (the contract this test enforces).
    /// Arrows live in column headers and the legend ONLY; cells stay
    /// numeric (preserves byte-stable `{:.0}` / `{:.1}` / `{}` formatting).
    /// Implementation: iterate every line in the per-scenario-tables
    /// output; skip heading lines (`## `), the legend, the table-header
    /// line (matches `| allocator |`), and the separator (`|---`). For
    /// the remaining table data rows, count `↑` / `↓` occurrences — must
    /// be zero.
    ///
    /// Scope note (WR-05): this test calls `emit_per_scenario_tables`
    /// directly, so it gates ONLY the per-scenario-tables surface — NOT
    /// the wider REPORT.md (which also contains `### {scenario}` mermaid
    /// diagrams, per-cell `<details>` cards from `recommend-cell.md.tmpl`,
    /// etc.). Future work (deferred): broaden the fixture to call
    /// `build_report` end-to-end against a non-empty `top_n` and
    /// `outcome.skipped`, then run the same line-by-line filter to gate
    /// the full REPORT.md surface.
    #[test]
    fn data_cells_contain_no_direction_markers() {
        // Multi-scenario fixture that exercises winner-marker, suspect
        // notes, and the multi-run cell shape — none of those decorations
        // should leak `↑` / `↓` glyphs into data cells.
        let runs = vec![
            make_run("jemalloc", "cpu-bound", 100.0, 50_000, 5.0, Some("alpine")),
            make_run("jemalloc", "cpu-bound", 110.0, 50_000, 5.0, Some("alpine")),
            make_run("jemalloc", "cpu-bound", 105.0, 50_000, 5.0, Some("alpine")),
            make_run("mimalloc", "cpu-bound", 95.0, 500, 5.0, Some("alpine")), // suspect
            make_run(
                "ptmalloc",
                "multithread",
                80.0,
                50_000,
                5.0,
                Some("debian-slim"),
            ),
        ];
        let mut buf = String::new();
        emit_per_scenario_tables(&mut buf, &runs);

        for line in buf.lines() {
            // Skip non-data lines:
            // - section headings (`## scenario`)
            // - the legend (contains `higher is better`)
            // - the table-header line (contains `allocator |` and the arrows belong here)
            // - the separator (`|---`)
            // - blank lines
            if line.starts_with("## ")
                || line.contains("higher is better")
                || line.starts_with("| allocator |")
                || line.starts_with("|---")
                || line.is_empty()
            {
                continue;
            }
            // Remaining lines are table data rows — must NOT contain
            // U+2191 or U+2193.
            assert!(
                !line.contains('\u{2191}'),
                "data row contains forbidden U+2191 glyph (DIR-05):\n  {line:?}"
            );
            assert!(
                !line.contains('\u{2193}'),
                "data row contains forbidden U+2193 glyph (DIR-05):\n  {line:?}"
            );
        }
    }

    /// Test 5: leading `| Rank | Cell | Score |` summary table sits between
    /// the section caption and the first card body. Exactly 10 data rows
    /// for top_n.len()=10, matching `\| 0X | <alloc> on <env> | 0.YYY |`
    /// formatting.
    #[test]
    fn top_n_section_starts_with_summary_table() {
        let top_n = make_top_n(10);
        let mut buf = String::new();
        emit_top_n_cells(&mut buf, &top_n).expect("emit");

        let section_start = buf
            .find("## Top 10 cells")
            .expect(&format!("missing section heading:\n{buf}"));
        let first_card = buf.find("### 1. ").expect(&format!(
            "missing first-card heading `### 1. `:\n{buf}"
        ));
        assert!(
            section_start < first_card,
            "section heading must come before first card in:\n{buf}"
        );

        let between = &buf[section_start..first_card];
        // Header + separator rows.
        assert!(
            between.contains("| Rank | Cell | Score |"),
            "missing summary table header in:\n{between}"
        );
        assert!(
            between.contains("|------|------|-------|"),
            "missing summary table separator in:\n{between}"
        );

        // 10 data rows matching `| 0X | <alloc> on <env> | 0.YYY |`.
        // Anchor the regex via simple substring checks over each rank.
        for rank in 1..=10 {
            let prefix = format!("| {:02} | ", rank);
            assert!(
                between.contains(&prefix),
                "missing rank-{rank} row prefix `{prefix}` in:\n{between}"
            );
        }

        // {:.3} score precision — at least the rank-1 score `0.789` should
        // appear verbatim (matches CONTEXT.md sentinel and the Test fixture).
        assert!(
            between.contains("| 0.789 |"),
            "expected rank-1 score `| 0.789 |` ({{:.3}}) in:\n{between}"
        );
    }
}
